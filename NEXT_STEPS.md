# NEXT_STEPS.md — что делаем дальше

## Текущий фокус

**Docs-заход + PR фичи `5.8.11-e4-voice-v2` в `main`.**
Серия `e4` полностью закрыта в фиче. Осталось зафиксировать в документации
и влить фичу в main.

## Ближайшие действия

| # | Действие | Где |
|---|---|---|
| 1 | Обновить 7 docs-файлов + `AI_RULES.md` | дома |
| 2 | PR фичи `e4` в `main` | работа (github.com) |
| 3 | Догон фичи от `main` (если PR фичи ещё не влит) | дома |

## Очередь заходов (следующая серия)

Все — **в новой фиче** `feature/5.8.11-e4-voice-v3` (или аналогичной),
ответвление от `main` после merge.

| # | Заход | Что делает | Файлов | Где |
|---|---|---|---|---|
| 1 | `e4-dicts` | Динамические словари Vosk для 5 узких состояний | 3 | дома |
| 2 | `e4-search-context` | Три параллельные проверки в выбранном наряде | 2 | дома |
| 3 | `e4d` | Динамическая грамматика Vosk + пересоздание `Recognizer` во время TTS | 3–4 | дома |

**После `e4d`** — закрыть И-35 (Vosk «четвёртая» ↔ «четырнадцатая»)
и И-24 (обрыв длинных номеров).

## Ближайший заход — `e4-dicts`

### Что делать

Сужение словаря Vosk в узких состояниях ГП. Полный словарь ~350 слов —
маленькая модель путается. В `AWAITING_*` нужен ограниченный набор.

### Логика

| Состояние | Словарь |
|---|---|
| `LISTENING` | полный (~350) — **не сужать** |
| `FOUND_PINNED` | полный — **не сужать** |
| `AWAITING_WEIGHT` | ~50 |
| `AWAITING_WEIGHT_QUEUE` | ~50 (как вес) |
| `AWAITING_CHOICE` | 7 |
| `AWAITING_CONTINUE` | ~30 |
| `AWAITING_MARK` | ~10 (порядковые) |
| `AWAITING_CLEAR` | ~10 |
| `AWAITING_POSTPONE` | ~10 |
| `AWAITING_CONFIRM` | 4 (подтверждаю / отменяю / стоп) |
| `PAUSED` | 3 |

### Файлы

- `VoiceGrammar.kt` — параметризация по состоянию.
- `VoiceController.kt` — пересоздание `Recognizer` при смене состояния.
- `ReconciliationViewModel.kt` — дёргать смену словаря.

### После `e4-dicts`

`e4-search-context` (приоритеты поиска), потом `e4d` (динамика грамматики).

## Отложенные задачи

- **D — команды выбора наряда/участка голосом.** Ждёт `e4d`.
- **Confidence threshold Vosk.** Нужно проверить `build.gradle`.
- **И-24** — Vosk endpoint. После `e4d`.
- **И-35** — Vosk «четвёртая»/«четырнадцатая». После `e4d`.
- **Визуальная пометка pin в UI.** Панель ГП.
- **`Postpone` голосом** — не реализовано (есть в `e4-markers`, работает).

## Device-check — незакрытые пункты

### Серия `5.8.6`

- **5f:** отмена голосом/кнопкой мгновенно снимает отметку.
- **5g:** «пять четвертых» → Unknown; «четвёртая» → `MarkOrdinal(4)`.

### Серия `5.8.10`

- **a (И-10):** свернуть «Характеристика» → перезапуск → скрыто.
- **b (И-9):** снести → свежий APK → онбординг появился.
- **c:** «первая вторая третья» → «Отмечено: первая, вторая, третья».

## Готово к мерджу

**В фиче `feature/5.8.11-e4-voice-v2` (готова к PR в main):**
- Вся серия `e4` — 15 пачек.

**В `main`:**
- Серия `5.8.11-e4`: `a`, `b`, `g`, `g3`, `fix-1`, `fix-2`.
- Серия `5.8.11-a…d2`: закладки единого поиска.
- Серия `5.8.10`: `a`…`g2`.
- Серия `5.8.9`, `5.8.6`.
- Docs: `rules-fix-merge`, `git-rules`, `tests-rules`, `e4-pin-close`.
- CI: `ci-automation`.

## Заморозка (не трогать)

- Схема БД (version = 2).
- `AI_RULES.md` — актуален.