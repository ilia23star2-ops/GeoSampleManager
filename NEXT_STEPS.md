# План следующих заходов

> Обновляется после каждого завершённого захода.
> **Этап 5.5 закрыт. Следующий — доводка 5.9 или переход к 5.8.**

---

## Что можно делать сейчас

Этап 5.5 (заметки и фото) полностью закрыт. Осталось три задачи
в **этапе 5.9 (доводка Room)**, они небольшие и не блокируют:

1. **SQL-статистика** — если UI-подсчёт начнёт тормозить на 300+ пробах.
   Пока тормозов нет — можно отложить.
2. **Diff-persist после undo/redo** — сейчас `persistAll` сохраняет все
   строки всех групп. Если групп много — это долго. Можно diff-подход.
3. **Проверка производительности** на больших нарядах.

После 5.9 — **этап 5.8 (голосовой помощник)**.

---

## Открытые вопросы (решаются по ходу)

1. **`number_in_well`** — вычислять в UI (сейчас) или хранить в БД?
   Отложено. При следующей миграции можно добавить.
2. **Сохранение настроек холостых в БД** — сейчас живут в
   `ReconciliationState`. Нужно ли переживать перезапуск?
3. **Логика «Ошибка импорта»** — сейчас эвристика
   `sample_number == well_number`. Стоит добавить флаг `is_import_error`
   в `SampleEntity` (ТД-4).
4. **Автоматическая простановка ВК** — при импорте, каждая N-я рядовая
   проба (холостые пропускаются). Настройка N — на наряд. Отдельный этап
   после 5.8 (ТД-7).

---

## Готовые заходы (справка)

### ✅ 5.5.1 — Инфраструктура заметок и фото

Сделано:
- FileProvider, `file_paths.xml`, CAMERA.
- `PhotoStorage`.
- `SampleImageEntity`, `SampleImageDao`.
- Миграция 1 → 2.
- Методы репозитория для фото и заметок.
- Очистка файлов при удалении.

### ✅ 5.5.2 — Диалог заметки и фото

Сделано:
- `NotePhotoDialog`.
- Лаунчеры `TakePicture` / `PickVisualMedia`.
- Защита от закрытия без сохранения.
- Декодирование миниатюр в фоне.
- Все операции с I/O — в `Dispatchers.IO`.

### ✅ 5.5.3 — Отображение и синхронизация

Сделано:
- `hasPhoto` берётся из `SampleEntity` в маппере.
- Иконки заметки и фото отображаются после перезапуска.
- Флаги синхронизируются.

---

## Следующие этапы (кратко)

### 5.9 — доводка Room (1 заход)

Задачи:
- SQL-статистика (по необходимости).
- Diff-persist после undo/redo.

### 5.8 — Голосовой помощник (4–5 заходов)

**Вариант А** — микрофон на экране сверки. Компоненты:
`SpeechRecognizer`, `TextToSpeech`, Bluetooth-роутинг.
Разрешения: `RECORD_AUDIO`, `BLUETOOTH_CONNECT`.

Сценарии:
- «Скважина 123» → TTS: «Скважина 123, наряд 5, N проб».
- «Первая» / «Один» → отметка первой пробы.
- «Взвесьте» → диалог веса ВК.
- Отложенная → TTS: «Отложена. Заметка?».
- Все отмечены → TTS: «Всё отмечено».

### 5.10 — Полировка (3 захода)

- Тёмная тема (системная + ручное переключение).
- Адаптивность под телефон.
- Haptic feedback, крупные элементы под перчатки, сохранение скролла,
  обработка отсутствия прав, анимации.

---

## Технический долг

См. таблицу в `ROADMAP.md` → «Технический долг».