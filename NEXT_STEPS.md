# NEXT_STEPS.md — что делаем дальше

## Текущий фокус

**PR фичи `5.8.11-e4-voice-v2` в `main` + пачка И-24 (Vosk обрыв).**
Серия `sort-fix` полностью закрыта в фиче. Два варианта дальше —
или сразу PR в main, или сначала закрыть И-24 в той же фиче.

## Ближайшие действия

| # | Действие | Где |
|---|---|---|
| 1 | Решить: PR фичи сейчас или сначала И-24 | — |
| 2 | Если И-24 — пачка 2–3 файла: `VoiceController`, `VoiceDialog` | дома |
| 3 | PR фичи `e4` в `main` (после И-24 или сразу) | работа (github.com) |
| 4 | Догон фичи от `main` (если PR фичи ещё не влит) | дома |

## Очередь заходов (следующая серия)

Все — **в новой фиче** `feature/5.8.11-e4-voice-v3` (или аналогичной),
ответвление от `main` после merge фичи `e4`.

| # | Заход | Что делает | Файлов | Где |
|---|---|---|---|---|
| 1 | И-24 | Vosk endpoint tuning — debounce `onResult` | 2–3 | дома |
| 2 | `e4-dicts` | Динамические словари Vosk для 5 узких состояний | 3 | дома |
| 3 | `e4-search-context` | Три параллельные проверки в выбранном наряде | 2 | дома |
| 4 | `e4d` | Динамическая грамматика Vosk + пересоздание `Recognizer` во время TTS | 3–4 | дома |

**После `e4d`** — закрыть И-35 (Vosk «четвёртая» ↔ «четырнадцатая»).

## Ближайший заход — И-24

### Симптом

Пользователь начинает говорить длинную фразу: «эн вэ тысяча триста
шестьдесят шесть и ка пэ дэ сто девять ноль ноль тридцать один».
Vosk срабатывает по короткой паузе между словами и **отдаёт
промежуточный `onResult`** до конца фразы. Команда исполняется на
неполной фразе.

### Что нужно

1. Разобрать `VoiceController.listener.onResult` — какие результаты
   приходят, когда срабатывает endpoint.
2. Возможно, увеличить `setWords` / endpoint timeout через
   `vosk-android` API (проверить доступность).
3. Если API нет — пост-обработка: debounce в `VoiceDialog` —
   игнорировать `onResult`, если с последнего `onPartialResult`
   прошло < N мс.

### Файлы

- `data/util/VoiceController.kt`.
- `ui/screens/VoiceDialog.kt`.
- Возможно, `data/voice/VoiceSettings.kt` — настройка таймаута.

### После И-24

`e4-dicts`, `e4-search-context`, `e4d` — в новой фиче.

## Отложенные задачи

- **D — команды выбора наряда/участка голосом.** Ждёт `e4d`.
- **Confidence threshold Vosk.** Нужно проверить `build.gradle`.
- **И-35** — Vosk «четвёртая»/«четырнадцатая». После `e4d`.
- **Визуальная пометка pin в UI.** Панель ГП.

## Device-check — незакрытые пункты

### Серия `5.8.6`

- **5f:** отмена голосом/кнопкой мгновенно снимает отметку.
- **5g:** «пять четвертых» → Unknown; «четвёртая» → `MarkOrdinal(4)`.

### Серия `5.8.10`

- **a (И-10):** свернуть «Характеристика» → перезапуск → скрыто.
- **b (И-9):** снести → свежий APK → онбординг появился.
- **c:** «первая вторая третья» → «Отмечено: первая, вторая, третья».

### Серия `5.8.11-sort-fix` — закрыто ✅

Все 5 сценариев device-check пройдены:
очередь + «далее», «следующая» без очереди, синонимы, SORT + Result,
SORT + 2 номера.

## Готово к мерджу

**В фиче `feature/5.8.11-e4-voice-v2` (готова к PR в main):**
- Серия `e4` — 15 пачек.
- Серия `sort-fix` — 5 коммитов.
- Docs-пачка `docs/5.8.11-sort-fix-close`.

**В `main`:**
- Серия `5.8.11-e4`: `a`, `b`, `g`, `g3`, `fix-1`, `fix-2`.
- Серия `5.8.11-a…d2`: закладки единого поиска.
- Серия `5.8.10`: `a`…`g2`.
- Серия `5.8.9`, `5.8.6`.
- Docs: `rules-fix-merge`, `git-rules`, `tests-rules`, `e4-pin-close`,
  `rules-cleanup`.
- CI: `ci-automation`.

## Заморозка (не трогать)

- Схема БД (version = 2).
- `AI_RULES.md` — актуален.