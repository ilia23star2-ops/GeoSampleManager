# План следующих заходов

> Обновляется после каждого захода.
> **Следующий — 5.8.4 (команды отметок + авто-режим + Vosk).**

---

## Текущий этап — 5.8

### Порядок заходов

| # | Заход | Заходов | Статус |
|---|---|---|---|
| 5.8.4 | Команды отметок + авто-режим + Vosk | 1 | ⬜ |
| 5.8.5 | Сессии + сортировка + статус-бар + онбординг | 1 | ⬜ |
| 5.8.6 | Полировка: BT, шумоподавление, лог, настройки | 1 | ⬜ |
| 5.9 | Room — доводка (опционально) | 1 | ⬜ |
| 5.10 | Общая полировка | 3 | ⬜ |
| 6 | Релиз MVP | 2 | ⬜ |

---

## Заход 5.8.4 — Команды отметок + авто-режим + Vosk

**Цель:** подключить реальное распознавание речи, научить ГП понимать
команды отметок и включать авто-режим.

### Что должно заработать

**Интеграция Vosk:**
- Замена заглушки `VoiceController` на Vosk-small-ru.
- Прогон распознанной фразы через `VoiceNumberParser` → `VoicePrefixResolver`
  → `VoiceSearch`.
- Озвучка результата через TTS.

**Авто-режим отметок:**
- Включается автоматически, когда ответ единственный (§9.2 VOICE.md).
- Звук-сигнал «двойной восходящий бип».

**Команды отметок (§9.3):**
- `первая` … `тридцатая` — отметка пробы.
- `<суффикс>` — отметка по суффиксу.
- `вес <значение>` — вес.
- `снять первую` / `снять последнюю` / `снять все`.
- `отложить` / `снять отложенную`.
- `следующая` / `далее` — завершить скважину, очистить запрос.

**Команды вне режима (§9.4):**
- `снять первую`, `отмена` / `верни`, `повтори`, `пауза` / `продолжить`,
  `стоп` / `хватит`.

**Информация (§9.5):**
- `сколько осталось`, `показать отложенные`, `показать найденные`,
  `помощь` / `команды`.

**Сортировка (§9.6):**
- Два и более номера подряд — без отметок, до 5 номеров.

### Файлы (план)

- `data/voice/VoiceCommand.kt` — sealed-класс команд.
- `data/voice/VoiceCommandParser.kt` — распознавание команд из текста.
- `data/voice/VoiceOrdinals.kt` — порядковые (`первая` … `тридцатая`).
- `data/voice/VoiceController.kt` — обновление, интеграция с Vosk.
- `ui/screens/ReconciliationViewModel.kt` — методы `voiceMarkByOrdinal()`,
  `voiceSetWeight()` и др.
- `ui/screens/ReconciliationState.kt` — контекст текущей скважины.
- `ui/screens/VoiceDialog.kt` — переписать под реальную работу.

### Открытые вопросы

1. Где хранить контекст сессии: в `ReconciliationState` или в отдельном
   `VoiceSession`.
2. Как загружать модель Vosk: из assets или из `filesDir`.
3. Порог срабатывания авто-режима: только «ровно 1 совпадение» или
   «одно уникальное совпадение по всем кандидатам».

---

## Заход 5.8.5 — Сессии + сортировка + статус-бар + онбординг

**Цель:** контекст сессии, «следующая», сортировка, статус-бар,
справка, онбординг.

### Файлы (план)
- `data/voice/VoiceSession.kt` — контекст, undo/redo, «стоп».
- `ui/screens/VoiceStatusBar.kt` — статус-бар внизу сверки.
- `ui/screens/VoiceHelpDialog.kt` — справка.
- `ui/screens/VoiceOnboarding.kt` — 5 экранов онбординга.

---

## Заход 5.8.6 — Полировка ГП

**Цель:** Bluetooth, шумоподавление, лог, настройки, тесты на реальных
данных.

### Файлы (план)
- `ui/screens/VoiceSettingsScreen.kt` — UI настроек.
- `data/voice/VoiceLogger.kt` — лог в `filesDir/voice_log.txt`.
- Bluetooth-роутинг.
- `NoiseSuppressor` + `AGC`.

---

## Открытые вопросы (общие)

1. **`number_in_well`** — вычислять в UI или хранить в БД.
2. **Настройки холостых** — в `ReconciliationState` или в `OrderEntity`.
3. **Флаг `is_import_error`** — добавить в `SampleEntity`.
4. **Авто-простановка ВК** — при импорте.

---

## Готовые заходы

- ✅ 1–4 (база)
- ✅ 5.1 (каркас сверки)
- ✅ 5.5.1–5.5.3 (заметки и фото)
- ✅ 5.8.1 (каркас голоса)
- ✅ Спецификация ГП (`VOICE.md`)
- ✅ 5.11 (множественный поиск UI)
- ✅ 5.8.2 (парсер ГП)
- ✅ 5.8.3 (префиксы + поиск)

---

## Заметки по окружению (не удалять)

- **Gradle user home:** `C:\gradle_home` — кириллица в пути ломает
  `GradleWorkerMain`.
- **Gradle JDK:** Oracle OpenJDK 17.0.12.
- **Имена тестов:** только латиница.