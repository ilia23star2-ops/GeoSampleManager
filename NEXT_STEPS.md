# План следующих заходов

> Обновляется после каждого захода.
> **Следующий — 5.8.5 (выполнение команд + авто-режим + сессии).**

---

## Текущий этап — 5.8.8d

### Порядок заходов

| # | Заход | Заходов | Статус |
|---|---|---|---|
| 5.8.8d | Голосовой помощник — финальные фиксы | 1 | 🟡 в работе |
| 5.8.9 | UI без диалога (волны + статус-бар) | 1 | ⬜ |
| 5.8.10 | Piper TTS (после решения по лицензиям) | 1 | ⬜ |
| 5.9 | Room — доводка | 1 | ⬜ |
| 5.10 | Общая полировка | 3 | ⬜ |
| 6 | Релиз MVP | 2 | ⬜ |

### Заход 5.8.8d — что сделано

1. `VoiceSpeaker.spellNumber`: короткие числа — как есть, TTS читает словами.
2. `VoiceSession.awaitingWeight` — состояние «жду вес».
3. `VoiceCommandParser.parseWeightAnswer`: «два с половиной» → 2.5.
4. При ожидании веса `voiceExecute` парсит Search как вес.
5. ГП спрашивает вес и для холостых, и для ВК.
6. `totalSamples` = 1 для пробы; для скважины — все пробы.

### Что делать после проверки

- Если всё ок → заход 5.8.9: убрать блокирующий диалог, добавить оверлей с волнами и строку статуса под кнопками «Отмена / Вперёд».

## Заход 5.8.5 — Выполнение команд + авто-режим + сессии

**Цель:** связать распознанные команды с ViewModel, ввести контекст
сессии и авто-режим отметок.

### Что должно заработать

**Контекст сессии:**
- Текущая скважина (`VoiceSession`).
- Последняя скважина для восстановления.
- 20 шагов undo, «повтори».

**Авто-режим отметок:**
- Включается автоматически, когда ответ единственный (§9.2).
- Звук-сигнал «двойной восходящий бип».

**Выполнение команд:**
- `MarkOrdinal` — отметка пробы по номеру в скважине.
- `SetWeight` — установка веса.
- `ClearOrdinal` / `ClearLast` / `ClearAll` — снятие отметок.
- `Unpostpone` — снять «отложена».
- `Next` — завершить скважину, очистить запрос.
- `Undo` / `Redo` — в ViewModel.
- `Sort` — показать сортировку без отметок.

**Статус-бар внизу сверки:**
- Слушаю / Понял / Ищу / Найдено / Отмечено / Ошибка / Пауза.

**Справка:**
- Диалог с командами (отдельно от легенды).

**Онбординг:**
- 5 экранов при первом запуске.
- «Больше не показывать».

### Файлы (план)

- `data/voice/VoiceSession.kt` — контекст сессии.
- `data/voice/VoiceAutoMode.kt` — определение авто-режима.
- `data/voice/VoiceFeedback.kt` — бип + TTS-сообщения.
- `ui/screens/ReconciliationViewModel.kt` — методы `voiceExecute(cmd)`.
- `ui/screens/VoiceDialog.kt` — выполнение команд.
- `ui/screens/VoiceStatusBar.kt` — статус-бар.
- `ui/screens/VoiceHelpDialog.kt` — справка.
- `ui/screens/VoiceOnboarding.kt` — онбординг.
- `ui/screens/SearchScreen.kt` — врезка статус-бара.

### Открытые вопросы

1. Где хранить контекст: в `ReconciliationState` или в отдельном
   `VoiceSession`?  
   **Рекомендация:** отдельный `VoiceSession`, привязанный к ViewModel.

2. Авто-режим — только для одиночного запроса или и для мультизапроса?  
   **Рекомендация:** только для одиночного (в мультизапросе отметки вручную).

3. Бип-сигнал — системный `ToneGenerator` или ресурс?  
   **Рекомендация:** `ToneGenerator` (без файлов).

---

## Заход 5.8.6 — Vosk + полировка

**Цель:** заменить движок распознавания на Vosk, добавить Bluetooth
и шумоподавление, лог, настройки ГП.

### Файлы (план)
- `data/voice/VoskEngine.kt` — обёртка над Vosk + `Model` из assets.
- `data/voice/VoiceLogger.kt` — лог в `filesDir/voice_log.txt`.
- `ui/screens/VoiceSettingsScreen.kt` — UI настроек.
- `VoiceController.kt` — замена SpeechRecognizer на Vosk.
- Bluetooth-роутинг, `NoiseSuppressor`, `AGC`.

### Подготовка перед заходом
- Модель `vosk-model-small-ru-0.22` (~40 МБ) — в `assets/`.
- Зависимость `com.alphacephei:vosk-android:0.3.47`.
- `StorageService` для распаковки модели.

---

## Открытые вопросы (общие)

1. **`number_in_well`** — вычислять в UI или хранить в БД.
2. **Настройки холостых** — в `ReconciliationState` или в `OrderEntity`.
3. **Флаг `is_import_error`** — добавить в `SampleEntity`.
4. **Авто-простановка ВК** — при импорте.

---

## Готовые заходы

- ✅ 1–4 (база)
- ✅ 5.1 (каркас сверки)
- ✅ 5.5.1–5.5.3 (заметки и фото)
- ✅ 5.8.1 (каркас голоса)
- ✅ Спецификация ГП (`VOICE.md`)
- ✅ 5.11 (множественный поиск UI)
- ✅ 5.8.2 (парсер ГП)
- ✅ 5.8.3 (префиксы + поиск)
- ✅ 5.8.4 (команды отметок — парсер)

---

## Заметки по окружению (не удалять)

- **Gradle user home:** `C:\gradle_home` — кириллица в пути ломает
  `GradleWorkerMain`.
- **Gradle JDK:** Oracle OpenJDK 17.0.12.
- **Имена тестов:** только латиница (backtick-кириллица ломает
  Gradle Test Executor).