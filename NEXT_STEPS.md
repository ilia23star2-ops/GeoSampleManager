# План следующих заходов

> Обновляется после каждого захода.
> **Следующий — 5.8.2 (парсер ГП).**

---

## Текущий этап — 5.8

### Порядок заходов

| # | Заход | Заходов | Статус |
|---|---|---|---|
| 5.8.2 | Парсер + словари + сегментация | 1 | ⬜ |
| 5.8.3 | Префиксы + поиск (5 уровней) | 1 | ⬜ |
| 5.8.4 | Команды отметок + авто-режим | 1 | ⬜ |
| 5.8.5 | Сессии + сортировка + статус-бар + онбординг | 1 | ⬜ |
| 5.8.6 | Полировка: BT, шумоподавление, лог, настройки | 1 | ⬜ |
| 5.9 | Room — доводка (опционально) | 1 | ⬜ |
| 5.10 | Общая полировка | 3 | ⬜ |
| 6 | Релиз MVP | 2 | ⬜ |

---

## Заход 5.8.2 — Парсер + словари + сегментация

**Цель:** чистый Kotlin, без Vosk и UI. Все словари + парсер числа + сегментация.

### Файлы (план)

- `data/voice/VoiceDictionary.kt` — все словари.
- `data/voice/VoiceNumberParser.kt` — парсер.
- `data/voice/VoiceSegmenter.kt` — сегментация.
- `data/voice/VoiceSettings.kt` — настройки ГП.
- `data/voice/VoiceNumberParserTest.kt` — тесты.

### Тесты
См. `VOICE.md` §17.

---

## Заход 5.8.3 — Префиксы + поиск

**Цель:** `VoicePrefixResolver` + `VoiceSearch` (5 уровней + fuzzy) + Vosk.

### Файлы (план)
- `data/voice/VoicePrefixResolver.kt`
- `data/voice/VoiceSearch.kt`
- `VoiceController` — интеграция с Vosk.
- `VoiceDialog` → переписать под реальную работу.

---

## Заход 5.8.4 — Команды отметок + авто-режим

**Цель:** авто-режим отметок, порядковые, вес, снятие, undo.

### Файлы (план)
- `VoiceController` — распознавание команд.
- `ReconciliationViewModel` — методы `voiceMarkFirst()`, `voiceSetWeight()` и др.
- `ReconciliationState` — команды в контексте.

---

## Заход 5.8.5 — Сессии + сортировка + статус-бар + онбординг

**Цель:** контекст сессии, «следующая», сортировка, статус-бар,
справка, онбординг.

### Файлы (план)
- `VoiceSession.kt` — контекст, undo/redo, «стоп».
- `VoiceStatusBar.kt` — статус-бар.
- `VoiceHelpDialog.kt` — справка.
- `VoiceOnboarding.kt` — онбординг.
- `SearchScreen.kt` — голосовая интеграция.

---

## Заход 5.8.6 — Полировка

**Цель:** Bluetooth, шумоподавление, лог, настройки, тесты на реальных
данных.

### Файлы (план)
- `VoiceSettingsScreen.kt` — UI настроек.
- `VoiceLogger.kt` — лог в `filesDir/voice_log.txt`.
- Bluetooth-роутинг.
- `NoiseSuppressor` + `AGC`.

---

## Открытые вопросы

1. **`number_in_well`** — вычислять в UI или хранить в БД.
2. **Настройки холостых** — в `ReconciliationState` или в `OrderEntity`.
3. **Флаг `is_import_error`** — добавить в `SampleEntity`.
4. **Авто-простановка ВК** — при импорте.

---

## Готовые заходы

- ✅ 1–4 (база)
- ✅ 5.1 (каркас сверки)
- ✅ 5.5.1–5.5.3 (заметки и фото)
- ✅ 5.8.1 (каркас голоса)
- ✅ Спецификация ГП (`VOICE.md`)
- ✅ 5.11 (множественный поиск UI)