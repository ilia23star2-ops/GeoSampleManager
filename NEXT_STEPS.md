# NEXT_STEPS.md — что делаем дальше

## Текущий фокус

**`5.8.11-e4-pin-5`** — fallback 400→4 в `voiceMarkOrdinal`.
**Дома.** Ветка `fix/5.8.11-e4-pin-5` от фичи `feature/5.8.11-e4-voice-v2`.

## Очередь заходов

| # | Заход | Что делает | Файлов | Где |
|---|---|---|---|---|
| 1 | `e4-pin-5` | Fallback 400→4 (Vosk путает «четвёртая»/«четыреста») | 1–2 | дома |
| 2 | `e4-markers` | Маркеры намерения: `AWAITING_MARK`, `AWAITING_CLEAR`, `AWAITING_POSTPONE` | 3 | дома |
| 3 | `e4-dicts` | Динамические словари для 5 узких состояний | 3 | дома |
| 4 | `e4-search-context` | Три параллельные проверки в выбранном наряде | 2 | дома |
| 5 | `e4d` | Динамическая грамматика Vosk + пересоздание `Recognizer` во время TTS | 3–4 | дома |

**Порядок обязателен.** После всей серии — PR фичи в `main`.

## Ближайший заход — `e4-pin-5`

### Что делать

В `voiceMarkOrdinal` расширить fallback:
- **14 ↔ 4** — уже есть (`e4-pin-4`).
- **400 → 4, 500 → 5, …, 900 → 9** — новое.
- **Возможно:** 4000 → 4, 5000 → 5 и т.д.

### Логика

Если проба `ordinal` не найдена:
1. Круглые сотни: 400 → 4, 500 → 5, …, 900 → 9.
2. Десятки: 40 → 4, 50 → 5, …, 90 → 9.
3. `ordinal ± 10` для диапазонов 1..9 и 10..19 — уже есть.

Пробуем по очереди. Если что-то нашлось — отметка. Иначе — «Проба №N не найдена».

### Тесты

`VoiceMarkOrdinalFallbackTest.kt` — 6 тестов: 400, 500, 40, 90, 14↔4, всё не найдено.

### Файлы

- `ReconciliationViewModel.kt` — `voiceMarkOrdinal`.
- `VoiceMarkOrdinalFallbackTest.kt` — новый тест.

## Другие задачи — дома

### Device-check серии `5.8.6`

- **5f:** отмена голосом/кнопкой мгновенно снимает отметку.
- **5g:** «пять четвертых» → Unknown; «четвёртая» → `MarkOrdinal(4)`.

### Device-check серии `5.8.10`

- **a (И-10):** свернуть «Характеристика» → перезапуск → скрыто.
- **b (И-9):** снести → свежий APK → онбординг появился.
- **c:** «первая вторая третья» → «Отмечено: первая, вторая, третья».

## Отложенные (не в этом спринте)

- **D — команды выбора наряда/участка голосом.** Ждёт `e4d`.
- **Confidence threshold Vosk.** Нужно проверить `build.gradle`.
- **И-24** — Vosk endpoint. После `e4d`.
- **`Postpone` голосом.** Обсудить.
- **Визуальная пометка pin в UI.** Панель ГП: «Закреплено: Наряд 12 / KPD1090031».

## Готово к мерджу

**В `main`:**
- Серия `5.8.11-e4`: `a`, `b`, `g`, `g3`, `fix-1`, `fix-2`, `e4e-a`, `e4e-bundle`, `e4-tests`.
- Серия `5.8.11-a…d2`: закладки единого поиска.
- Серия `5.8.10`: `a`…`g2`.
- Серия `5.8.9`, `5.8.6`.
- Docs: `rules-fix-merge`, `git-rules`, `tests-rules`.
- CI: `ci-automation`.

**В фиче `feature/5.8.11-e4-voice-v2` (ждут мержа в main после всей серии):**
- `e4-pin-1`, `e4-pin-2`, `e4-pin-3+4`.

## Заморозка (не трогать)

- Схема БД (version = 2).
- `AI_RULES.md`.