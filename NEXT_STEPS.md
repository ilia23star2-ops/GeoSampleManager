# NEXT_STEPS.md — что делаем дальше

## Текущий фокус

**Дома (с устройством):** интеграция `5.8.11-e1/e2/e3` + device-check.
**На github.com:** свободных задач нет — серия 5.8.11-a…d2 закрыта,
всё остальное требует устройства.

## Ближайшие заходы — дома

### `5.8.11-e1` — setQuery на новый путь (1 файл)
- Файл: `ui/screens/ReconciliationViewModel.kt`.
- Заменить прямой вызов `analyzeMatch` + SQL на
  `QueryNormalizer` → `QueryTokenizer` → `SearchService`.
- **Device-check:** ручной ввод ищет так же, как раньше. «1524», «15 24 01»,
  «KPD1090031» — находят то же самое.
- **Откат при поломке:** вернуть старый путь в одном файле.

### `5.8.11-e2` — парсер принимает состояние (1 файл)
- Файл: `data/voice/VoiceCommandParser.kt`.
- Сигнатура `parse(text, state: VoiceState, mode: VoiceSessionMode)`.
- В `AWAITING_WEIGHT` не пытается распознать `MarkOrdinal` — только вес.
- В `AWAITING_CHOICE` — только `ChoiceRemove/Postpone/Skip`.
- **Device-check:** «семь утра было холодно» → молчание; в `AWAITING_WEIGHT`
  «семь» → вес 7.
- **Откат при поломке:** вернуть старый парсер.

### `5.8.11-e3` — VoiceDialog через Response (1 файл)
- Файл: `ui/screens/VoiceDialog.kt`.
- Рядом со старым `describeResult()` добавить `renderFromResponse()`.
- Переключить `VoiceDialog` на новый путь.
- **Device-check:** ГП говорит те же фразы, что и раньше. Список проб,
  отметки, вес, отмена — всё работает.
- **Откат при поломке:** вернуть `describeResult()`.

### Порядок и безопасность

- **Строго по одному.** После каждого — device-check. Если сломалось —
  откат одного файла, не идём дальше.
- Старые методы **не удаляем** до тех пор, пока все три захода не пройдут
  device-check. Только после — отдельный заход на чистку.

## Другие задачи — дома

### Device-check серии `5.8.6`
- **5f:** отмена голосом и кнопкой мгновенно снимает отметку.
- **5g:** «пять четвертых» → Unknown; «четвертая» → MarkOrdinal(4).

### Device-check серии `5.8.10`
- **a (И-10):** свернуть «Характеристика» → перезапуск → скрыто.
- **b (И-9):** снести → свежий APK → онбординг появился.
- **c:** «первая вторая третья» → «Отмечено: первая, вторая, третья».

## Отложенные (не в этом спринте)

- **И-24** — Vosk endpoint tuning. Требует апгрейда Vosk (0.3.47 → 0.3.75)
  или пост-обработки. Решение — после device-check.
- UI без диалога (волны + статус-бар).
- Полная переработка множественной отметки с озвучкой.

## Готово к мерджу

- Серия `5.8.6`: `2`, `2a`, `3`, `4`, `5a`, `5c`, `5f`, `5g`.
- Серия `5.8.9`: `f-2b`, `i-1/2/3`, `d-3c2b1/3c2b2`, `f-1a-fix-1`, `d-2a`, `g-1/3`.
- Серия `5.8.10`: `a`, `b`, `c`, `d`, `e`, `f`, `g1`, `g2`.
- Серия `5.8.11`: `a`, `b`, `c1`, `c2`, `d1`, `d2` — **закладки**, ждут `e1/e2/e3`.

## Заморозка (не трогать)

- Схема БД (version = 2).
- `UnifiedSearch`, `VoiceSampleHit`, `VoiceModels.kt`.
- `AI_RULES.md`.
