# План следующих заходов

> Обновляется после каждого захода.
> **Текущий — 5.8.9 (UI без диалога).**

---

## Порядок заходов

| # | Заход | Заходов | Статус |
|---|---|---|---|
| 5.8.9 | UI без диалога (волны + статус-бар) | 1 | ⬜ |
| 5.8.5 | Выполнение команд + авто-режим + сессии | 1 | ⬜ |
| 5.8.10 | Piper TTS (после решения по лицензиям) | 1 | ⬜ |
| 5.8.6 | Vosk-полировка (BT, шумоподавление, лог, настройки) | 1 | ⬜ |
| 5.9 | Room — доводка | 1 | ⬜ |
| 5.10 | Общая полировка | 3–4 | ⬜ |
| 6 | Релиз MVP | 2 | ⬜ |

*Нумерация не по порядку — исторически сложилось.*

---

## Текущий заход — 5.8.9 (UI без диалога)

**Цель:** заменить блокирующий `VoiceDialog` на неблокирующий оверлей.

### Что делаем

1. **Убрать `AlertDialog`** из `VoiceDialog`. Он закрывает таблицу проб.
2. **Добавить `VoiceOverlay`** — неблокирующий слой поверх таблицы.
    - Волны внизу экрана.
    - Индикатор ответа.
    - Подсказки команд.
3. **Статус-бар** — под кнопками «Отмена / Вперёд».
4. **Звук единственного ответа** — двойной восходящий бип.

### Файлы (план)

- `ui/screens/VoiceOverlay.kt` — новый.
- `ui/screens/VoiceStatusBar.kt` — новый.
- `ui/screens/SearchScreen.kt` — врезка оверлея.
- `ui/screens/VoiceDialog.kt` — упростить или удалить.

### Открытые вопросы

1. Где именно рисовать волны — снизу или в шапке?
2. Индикатор ответа — текст или иконка?
3. Статус-бар — видим постоянно или только когда ГП слушает?

---

## Заход 5.8.5 — Выполнение команд + авто-режим + сессии

**Цель:** связать распознанные команды с ViewModel, ввести контекст сессии
и авто-режим отметок.

### Что должно заработать

**Контекст сессии:**
- Текущая скважина (`VoiceSession`).
- Последняя скважина для восстановления.
- 20 шагов undo, «повтори».

**Авто-режим отметок:**
- Включается, когда ответ единственный.
- Звук-сигнал «двойной восходящий бип».

**Выполнение команд:**
- `MarkOrdinal` — отметка пробы по номеру в скважине.
- `SetWeight` — установка веса.
- `ClearOrdinal` / `ClearLast` / `ClearAll` — снятие отметок.
- `Unpostpone` — снять «отложена».
- `Next` — завершить скважину, очистить запрос.
- `Undo` / `Redo` — в ViewModel.
- `Sort` — показать сортировку без отметок.

**Статус-бар внизу сверки:**
- Слушаю / Понял / Ищу / Найдено / Отмечено / Ошибка / Пауза.

**Справка:**
- Диалог с командами (отдельно от легенды).

**Онбординг:**
- 5 экранов при первом запуске.
- «Больше не показывать».

### Файлы (план)

- `data/voice/VoiceSession.kt` — контекст сессии.
- `data/voice/VoiceAutoMode.kt` — определение авто-режима.
- `data/voice/VoiceFeedback.kt` — бип + TTS-сообщения.
- `ui/screens/ReconciliationViewModel.kt` — методы `voiceExecute(cmd)`.
- `ui/screens/VoiceStatusBar.kt` — статус-бар.
- `ui/screens/VoiceHelpDialog.kt` — справка.
- `ui/screens/VoiceOnboarding.kt` — онбординг.
- `ui/screens/SearchScreen.kt` — врезка.

### Открытые вопросы

1. Где хранить контекст: в `ReconciliationState` или в отдельном
   `VoiceSession`?
   **Рекомендация:** отдельный `VoiceSession`.
2. Авто-режим — только для одиночного запроса или и для мультизапроса?
   **Рекомендация:** только для одиночного.
3. Бип-сигнал — `ToneGenerator` или ресурс?
   **Рекомендация:** `ToneGenerator`.

---

## Заход 5.8.6 — Vosk-полировка

**Цель:** Bluetooth, шумоподавление, лог, настройки ГП.

### Что осталось

- Bluetooth-роутинг.
- `NoiseSuppressor`, `AGC`.
- `VoiceLogger` — лог в `filesDir/voice_log.txt`.
- `VoiceSettingsScreen` — UI настроек.

### Файлы (план)

- `data/voice/VoiceLogger.kt` — новый.
- `ui/screens/VoiceSettingsScreen.kt` — новый.
- `VoiceController.kt` — добавить Bluetooth + шумоподавление.

---

## Открытые вопросы (общие)

1. **`number_in_well`** — вычислять в UI или хранить в БД.
2. **Настройки холостых** — в `ReconciliationState` или в `OrderEntity`.
3. **Флаг `is_import_error`** — добавить в `SampleEntity`.
4. **Авто-простановка ВК** — при импорте.

---

## Готовые заходы

- ✅ 1–4 (база)
- ✅ 5.1 (каркас сверки)
- ✅ 5.5.1–5.5.3 (заметки и фото)
- ✅ 5.8.1 (каркас голоса)
- ✅ Спецификация ГП (`VOICE.md`)
- ✅ 5.11 (множественный поиск UI)
- ✅ 5.8.2 (парсер ГП)
- ✅ 5.8.3 (префиксы + поиск)
- ✅ 5.8.4 (команды отметок — парсер)
- ✅ 5.8.8 (Vosk + грамматика + TTS)
- ✅ 5.8.8d–k (финальные фиксы ГП)

---

## Заметки по окружению (не удалять)

- **Gradle user home:** `C:\gradle_home` — кириллица в пути ломает
  `GradleWorkerMain`.
- **Gradle JDK:** Oracle OpenJDK 17.0.12.
- **Имена тестов:** только латиница (backtick-кириллица ломает
  Gradle Test Executor).