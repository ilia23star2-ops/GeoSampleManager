# NEXT_STEPS.md — что делаем дальше

## Текущий фокус

**Голосовая серия `5.8.6`** — Vosk-полировка. Unit-тесты зелёные, device-check на планшете не пройден.

## Ближайшие заходы (в порядке приоритета)

### 1. Device-check серии `5.8.6` (без кода)
APK + планшет. Проверить по списку из `CONTEXT_BRIEF.md`.
Если что-то падает — фиксить, не продолжать.

### 2. `5.8.6-5e` — Vosk endpoint tuning
- Файл: `data/util/VoiceController.kt`.
- Настройка `Recognizer.setEndpoint` / увеличение окна речи.
- Цель: Vosk не обрывает длинные номера (`KPD1090031`, `15 24 01 2 ноля 5`).
- Обязателен device-check + logcat.

### 3. «Назад» не обновляет UI
- Файлы: `ui/screens/ReconciliationState.kt`, `ui/screens/SearchScreen.kt`.
- Проверить Compose-рекомпозицию после `undo()`.
- Возможно, snapshot не меняется или `ReconciliationState` не `@Stable`.

### 4. «четвертых» → «четыре» в числовом контексте
- Файл: `data/voice/VoiceCommandParser.kt`, возможно `VoiceNumberParser.kt`.
- В поисковом контексте дробные/порядковые формы не должны отмечать пробы.
- Одиночное «четвертая» (без чисел рядом) остаётся `MarkOrdinal(4)`.

## Отложенные (не в этом спринте)

- `5.8.9f-2b` — UI: индикатор режима у микрофона.
- `5.8.9d-3` — переспросы ВК / отложенных (логика есть, UX проверить).
- UI без диалога (волны + статус-бар) — заход 5.8.9, вне текущей серии.
- Множественная отметка — озвучивать список проб или «Отмечено N проб».

## Готово к мерджу (после device-check)

- `5.8.6-2`, `5.8.6-3`, `5.8.6-4`, `5.8.6-5a`, `5.8.6-5c`.

## Заморозка (не трогать)

- Excel-импорт, заметки и фото.
- Схема БД (version = 2).
- `UnifiedSearch`, `VoiceSampleHit`, `VoiceModels.kt`.
