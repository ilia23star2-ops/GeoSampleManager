# NEXT_STEPS.md — что делаем дальше

## Текущий фокус

**`5.8.11-e4-markers`** — маркеры намерения + подтверждение массовых.
**Дома.** Ветка `fix/5.8.11-e4-markers` от фичи `feature/5.8.11-e4-voice-v2`.

## Очередь заходов

| # | Заход | Что делает | Файлов | Где |
|---|---|---|---|---|
| 1 | `e4-markers` | Маркеры намерения + подтверждение массовых | 5–6 | дома |
| 2 | `e4-dicts` | Динамические словари для 5 узких состояний | 3 | дома |
| 3 | `e4-search-context` | Три параллельные проверки в выбранном наряде | 2 | дома |
| 4 | `e4d` | Динамическая грамматика Vosk + пересоздание `Recognizer` во время TTS | 3–4 | дома |

**Порядок обязателен.** После всей серии — PR фичи в `main`.

## Ближайший заход — `e4-markers`

### Что делать

Ввести **маркеры намерения** — состояние, в котором ГП ждёт номер пробы для конкретного действия:

| Маркер | Ведёт в | Словарь |
|---|---|---|
| «отметь» | `AWAITING_MARK` | порядковые 1..N |
| «снять» | `AWAITING_CLEAR` | 1..N |
| «отложить» | `AWAITING_POSTPONE` | 1..N |
| «все» (массовое) | `AWAITING_CONFIRM` | да / нет |

**Решение от 25.09 (см. `DECISIONS.md`):** массовые действия («отметь все», «снять все») требуют подтверждения голосом. Одиночные — нет. Удаление голосом не реализуем — слишком опасно.

### Логика

**Маркеры:**
- «отметь» → `AWAITING_MARK`. ГП: «Какую пробу?» Скажи «четыре» → `MarkOrdinal(4)`.
- «снять» → `AWAITING_CLEAR`. ГП: «Какую снять?» Скажи «четыре» → `ClearOrdinal(4)`.
- «отложить» → `AWAITING_POSTPONE`. ГП: «Какую отложить?»
- Тайм-аут 10 сек → выход из маркера.

**Подтверждение массовых:**
- «отметь все» / «снять все» → `AWAITING_CONFIRM`. ГП: «Отметить все 15 проб? Скажите „да" или „нет".»
- «да» / «подтверждаю» → выполнить.
- «нет» / «отмена» → отменить.
- Молчание 10 сек → автоотмена.

### Файлы

- `VoiceState.kt` — + 4 состояния.
- `VoiceSession.kt` — + `pendingMarkIntent`, `pendingConfirm`.
- `VoiceCommand.kt` — + `MarkIntent`, `ClearIntent`, `PostponeIntent`, `Confirm`, `Decline`.
- `VoiceCommandParser.kt` — распознавать маркеры.
- `ReconciliationViewModel.kt` — обработка маркеров и подтверждения.
- `VoiceDialog.kt` — фразы для маркеров и подтверждения.

## Другие задачи — дома

### Device-check серии `5.8.6`

- **5f:** отмена голосом/кнопкой мгновенно снимает отметку.
- **5g:** «пять четвертых» → Unknown; «четвёртая» → `MarkOrdinal(4)`.

### Device-check серии `5.8.10`

- **a (И-10):** свернуть «Характеристика» → перезапуск → скрыто.
- **b (И-9):** снести → свежий APK → онбординг появился.
- **c:** «первая вторая третья» → «Отмечено: первая, вторая, третья».

## Отложенные (не в этом спринте)

- **D — команды выбора наряда/участка голосом.** Ждёт `e4d`.
- **Confidence threshold Vosk.** Нужно проверить `build.gradle`.
- **И-24** — Vosk endpoint. После `e4d`.
- **И-35** — Vosk путает «четвёртая»/«четырнадцатая». Ждёт `e4d`.
- **Визуальная пометка pin в UI.** Панель ГП.

## Готово к мерджу

**В фиче `feature/5.8.11-e4-voice-v2` (ждут мержа в main после всей серии):**
- `e4e-bundle`, `e4-tests`.
- `e4-pin-1`, `e4-pin-2`, `e4-pin-3`, `e4-pin-4`, `e4-pin-5`, `e4-pin-6`, `e4-pin-7`.

**В `main`:**
- Серия `5.8.11-e4`: `a`, `b`, `g`, `g3`, `fix-1`, `fix-2`, `e4e-a`, `e4e-bundle`, `e4-tests`.
- Docs: `rules-fix-merge`, `git-rules`, `tests-rules`.
- CI: `ci-automation`.

## Заморозка (не трогать)

- Схема БД (version = 2).
- `AI_RULES.md`.
