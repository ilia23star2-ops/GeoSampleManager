# NEXT_STEPS.md — что делаем дальше

## Текущий фокус

**`5.8.11-e4e`** — свести пути поиска (UI и голос) в один через `SearchService`.
**Дома, устройство.** Ветка не создаётся — работаем в `main`.

## Очередь заходов

| # | Заход | Что делает | Файлов | Где |
|---|---|---|---|---|
| 1 | `e4e` | Единый путь поиска + мимикрия в TTS | 3 | дома |
| 2 | `e4-pin-1` | Состояние `FOUND_PINNED`, голое число → отметка | 3–4 | дома |
| 3 | `e4-pin-2` | Очередь мультизапроса + attention-переключение | 2–3 | дома |
| 4 | `e4-pin-3` | Конец очереди + `AWAITING_NOT_FOUND_CHOICE` | 2 | дома |
| 5 | `e4-markers` | Маркеры намерения: `AWAITING_MARK`, `AWAITING_CLEAR`, `AWAITING_POSTPONE` | 3 | дома |
| 6 | `e4-dicts` | Динамические словари для 5 узких состояний | 3 | дома |
| 7 | `e4d` | Динамическая грамматика Vosk + пересоздание `Recognizer` во время TTS | 3–4 | дома |
| 8 | `e4-search-context` | Три параллельные проверки в выбранном наряде | 2 | дома |
| 9 | `e4-pin-4` | `Postpone` + `Help` по состоянию | 2 | дома |

**Порядок обязателен.** `e4e` — база, остальные — расширения.

## Ближайший заход — `e4e`

Разбит на три подзахода:

### `e4e-a` — VoiceSpeaker: мимикрия (1 файл)
- `spellOut(groups)`: `joinToString(" ")`, без запятых.
- `spellLetters` — одно слово («капэдэ»), не по буквам.
- Канонический маппинг буква → звук. **W → «даблю».**
- **Риск:** низкий. Метод пока не вызывается.
- **Проверка:** после `e4e-b/c`.

### `e4e-b` — voiceSearch на новый путь (2 файла)
- `VoiceModels.kt`: поле `groups` в `VoiceExecResult.FoundOne`.
- `ReconciliationViewModel.kt`: `voiceSearch` через `QueryTokenizer` → `DigitGrouper` → `SearchService`.
- **Риск:** высокий. Голосовой поиск — основная функция.
- **Откат:** `git checkout main -- <файлы>`.

### `e4e-c` — VoiceDialog: spellOut(groups) (1 файл)
- `VoiceDialog.kt`: `buildFoundOnePhrase` использует `spellOut(r.groups)`.
- **Риск:** средний. Меняется озвучка.
- **Проверка:** голосом «KPD 109 00 31» → ответ «капэдэ сто девять ноль ноль тридцать один».

## Другие задачи — дома

### Device-check серии `5.8.6`
- **5f:** отмена голосом/кнопкой мгновенно снимает отметку.
- **5g:** «пять четвертых» → Unknown; «четвёртая» → `MarkOrdinal(4)`.

### Device-check серии `5.8.10`
- **a (И-10):** свернуть «Характеристика» → перезапуск → скрыто.
- **b (И-9):** снести → свежий APK → онбординг появился.
- **c:** «первая вторая третья» → «Отмечено: первая, вторая, третья».

## Отложенные (не в этом спринте)

- **D — команды выбора наряда/участка голосом.** Ждёт `e4d`.
- **Confidence threshold Vosk.** Нужно проверить `build.gradle`.
- **И-24** — Vosk endpoint. После `e4d`.

## Готово к мерджу

- Серия `5.8.11-e4`: `a`, `b`, `g`, `g3`, `fix-1`, `fix-2` — ✅ unit.
- Серия `5.8.11-a…d2`: закладки единого поиска.
- Серия `5.8.10`: `a`…`g2`.
- Серия `5.8.9`, `5.8.6`.

## Заморозка (не трогать)

- Схема БД (version = 2).
- `UnifiedSearch`, `VoiceSampleHit`, `VoiceModels.kt`.
- `AI_RULES.md`.
