# GeoSample Manager — статус проекта

> Обновляется после каждого захода.
> Последнее обновление: **после закрытия захода 5.8.3 (префиксы + поиск)**.

---

## Как продолжить работу в новом чате

1. Прикрепи файлы:
    - `AI_RULES.md`
    - `README.md`
    - `PROGRESS.md` (этот)
    - `NEXT_STEPS.md`
    - `DECISIONS.md`
    - `DATABASE.md`
    - `ROADMAP.md`
    - `VOICE.md`
2. Скажи: «Изучи файлы, продолжаем с этапа N, заход N.M».

---

## Статус одной строкой

**Этапы 1–4, 5.1, 5.5, 5.8.1, 5.11, 5.8.2, 5.8.3 — ✅. Спецификация ГП — ✅.
Следующий — 5.8.4 (команды отметок + авто-режим + интеграция с Vosk).**

---

## Что сделано

### ✅ Этапы 1–4 — База проекта
Скелет, Drawer, 7 вкладок, навигация, Room-БД (6 таблиц, version=2),
настройки импорта, Excel-импорт.

### ✅ Этап 5.1 — Каркас сверки
Полный UI-экран, модели, состояние, undo/redo, 11 диалогов.

### ✅ Этап 5.5 — Заметки и фото
`PhotoStorage`, `SampleImageEntity`, миграция 1→2, `NotePhotoDialog`,
флаги `has_note` / `has_photo`.

### ✅ Этап 5.8.1 — Каркас голоса
Разрешения, `VoiceController`, `VoiceDialog`, кнопка микрофона.

### ✅ Спецификация ГП (`VOICE.md`)
Полная спецификация.

### ✅ Заход 5.11 — Множественный поиск (UI)
Группировка, подсветка, дебаунс, режимы поиска, все баги закрыты.

### ✅ Заход 5.8.2 — Парсер ГП
`VoiceDictionary`, `VoiceNumberParser`, `VoiceSegmenter`, `VoiceSettings`.
12 юнит-тестов зелёные.

### ✅ Заход 5.8.3 — Префиксы + поиск

**Новые файлы в `data/voice/`:**
- `VoiceLetterSounds.kt` — произношения латинских букв (эй, би, си,
  капэдэ, энвэ и т.п.).
- `VoicePrefixResolver.kt` — превращает «капэдэ» → `KPD`, «энвэ» → `NV`.
  Учитывает пользовательские произношения из `VoiceSettings`.
- `VoiceSearch.kt` — 5 уровней поиска из §6 + fuzzy из §7.
- `VoiceSearchRepository.kt` — источник данных из БД через JOIN.

**Изменения:**
- `SampleDao.kt` — добавлен `getAllForVoiceSearch()` (JOIN samples ×
  orders × areas) для голосового поиска.

**Тесты:**
- `VoicePrefixResolverTest.kt` — 7 тестов.
- `VoiceSearchTest.kt` — 7 тестов.

**Что работает:**
- «капэдэ» → `KPD` (буквенный парсер).
- Пользовательские произношения перебивают буквенный парсер.
- Неизвестные префиксы отвергаются (`BCD` не в `known` — отказ).
- Уровень 1: точное совпадение `sample_number`.
- Уровень 2: точное совпадение `well_number` — все пробы скважины.
- Уровень 3: нормализация нулей (`10900031` ≈ `1090031`).
- Уровень 5: fuzzy (ненулевые цифры совпадают, длина ±1, Левенштейн ≤ 1).
- Первый найденный кандидат — победитель.

**Технические детали:**
- `Gradle user home`: `C:\gradle_home` (кириллица в пути ломала
  `GradleWorkerMain`).
- Gradle JDK: Oracle OpenJDK 17.0.12.
- Имена тестов только латиница.

---

## Что в работе

**Ничего.** Заход 5.8.3 закрыт.

---

## Что впереди

| Заход | Что | Заходов |
|---|---|---|
| **5.8.4** | Команды отметок + авто-режим + Vosk | 1 |
| **5.8.5** | Сессии + сортировка + статус-бар + онбординг | 1 |
| **5.8.6** | Полировка: BT, шумоподавление, лог, настройки | 1 |
| **5.9** | Room — доводка (опционально) | 1 |
| **5.10** | Общая полировка | 3 |
| **6** | Релиз MVP | 2 |

**Итого: ~8 заходов.**

---

## Полезные ссылки

- `README.md` — где что лежит.
- `DATABASE.md` — схема БД.
- `DECISIONS.md` — договорённости по UI и логике.
- `NEXT_STEPS.md` — план заходов.
- `ROADMAP.md` — общий план.
- `VOICE.md` — спецификация ГП.