# GeoSample Manager — статус проекта

> Обновляется после каждого захода.
> Последнее обновление: **после закрытия захода 5.8.2 (парсер ГП)**.

---

## Как продолжить работу в новом чате

1. Прикрепи файлы:
    - `AI_RULES.md`
    - `README.md`
    - `PROGRESS.md` (этот)
    - `NEXT_STEPS.md`
    - `DECISIONS.md`
    - `DATABASE.md`
    - `ROADMAP.md`
    - `VOICE.md`
2. Скажи: «Изучи файлы, продолжаем с этапа N, заход N.M».

---

## Статус одной строкой

**Этапы 1–4, 5.1, 5.5, 5.8.1, 5.11, 5.8.2 — ✅. Спецификация ГП — ✅.
Следующий — 5.8.3 (префиксы + поиск).**

---

## Что сделано

### ✅ Этапы 1–4 — База проекта
Скелет, Drawer, 7 вкладок, навигация, Room-БД (6 таблиц, version=2),
настройки импорта, Excel-импорт.

### ✅ Этап 5.1 — Каркас сверки
Полный UI-экран, модели, состояние, undo/redo, 11 диалогов.

### ✅ Этап 5.5 — Заметки и фото
`PhotoStorage`, `SampleImageEntity`, миграция 1→2, `NotePhotoDialog`,
флаги `has_note` / `has_photo`.

### ✅ Этап 5.8.1 — Каркас голоса
Разрешения, `VoiceController`, `VoiceDialog`, кнопка микрофона.

### ✅ Спецификация ГП (`VOICE.md`)
Полная спецификация: типы токенов, словари, разбивка на блоки,
приоритеты поиска, fuzzy, сегментация, команды, сценарии, конфликты,
UI статус-бар, сессия, настройки.

### ✅ Заход 5.11 — Множественный поиск (UI)
- Ввод до 5 запросов через пробел. Дебаунс 500 мс.
- Группировка по запросам.
- Подсветка заголовков: зелёный / красный / оранжевый (чужой участок).
- Ненайденные запросы сверху.
- Кнопка «Развернуть/Свернуть все», не исчезает.
- Все группы свёрнуты по умолчанию.
- Режимы поиска: строгий / фильтр.
- Исправлены баги: список участков из БД, наряд без участка заблокирован,
  поиск по скважине с любого участка, скролл легенды, «1» больше не
  подсвечивает все пробы.

### ✅ Заход 5.8.2 — Парсер ГП

**Новые файлы в `data/voice/`:**
- `VoiceDictionary.kt` — все словари (цифры, десятки, сотни, тысячи,
  миллионы, нули, разделители, мусор, команды).
- `VoiceNumberParser.kt` — нормализация, NULLS-детекция, токенизация,
  склейка множителей, разбивка на блоки по §4.4, кандидаты по §5.
- `VoiceSegmenter.kt` — сегментация по паузам (§8).
- `VoiceSettings.kt` — модель + репозиторий JSON (`filesDir/voice_settings.json`).
- `VoiceNumberParserTest.kt` — 12 юнит-тестов по §17 — **все зелёные**.

**Что работает:**
- NULLS: `два нуля` → `00`.
- Многословные множители: `две тысячи пятьсот` → `2500`,
  `сто тысяч двадцать` → `100020`.
- Разрядный парсер: `сто двадцать четыре` → `124`,
  `тысяча пятьсот шестьдесят два` → `1562`.
- Поцифровой блок: `ноль ноль три` → `003`.
- Границы H→H: `тысяча пятьсот сто шестьдесят два` → `1500162`.
- SEP: `пятьдесят и один` → кандидат `50|1`.
- STOP: `сто эээ двадцать` → `120`.
- Нормализация: регистр, ё→е.
- Сегментация по паузам (500/800/1200 мс).
- Настройки ГП: загрузка/сохранение.

**Технические детали:**
- `Gradle user home` переехал из `C:\Users\Илья\.gradle` в `C:\gradle_home`
  — кириллица в пути ломала `GradleWorkerMain`.
- Gradle JDK: Oracle OpenJDK 17.0.12.
- Имена тестов — на латинице (backtick-кириллица глючила с Gradle Test Executor).

---

## Что в работе

**Ничего.** Заход 5.8.2 закрыт.

---

## Что впереди

| Заход | Что | Заходов |
|---|---|---|
| **5.8.3** | Префиксы + поиск (5 уровней) | 1 |
| **5.8.4** | Команды отметок + авто-режим | 1 |
| **5.8.5** | Сессии + сортировка + статус-бар + онбординг | 1 |
| **5.8.6** | Полировка: BT, шумоподавление, лог, настройки | 1 |
| **5.9** | Room — доводка (опционально) | 1 |
| **5.10** | Общая полировка | 3 |
| **6** | Релиз MVP | 2 |

**Итого: ~9 заходов.**

---

## Полезные ссылки

- `README.md` — где что лежит.
- `DATABASE.md` — схема БД.
- `DECISIONS.md` — договорённости по UI и логике.
- `NEXT_STEPS.md` — план заходов.
- `ROADMAP.md` — общий план.
- `VOICE.md` — спецификация ГП.