# GeoSample Manager — статус проекта

> Обновляется после каждого значимого захода.
> Последнее обновление: этап 5.5 (заметки и фото) — **начат, заход 5.5.1**.

---

## Как продолжить работу в новом чате

1. Прикрепи к первому сообщению файлы:
    - `README.md`
    - `PROGRESS.md` (этот файл)
    - `NEXT_STEPS.md`
    - `DECISIONS.md`
    - `DATABASE.md`
    - `ROADMAP.md`
2. Скажи: «Изучи файлы, продолжаем с этапа N, заход N.M».
3. Дождись подтверждения понимания — ИИ перескажет текущее состояние.

**Правила проекта** — см. `README.md`.

---

## Статус одной строкой

**Этапы 1–4 и 5.1 завершены. Этап 5.9 (Room) закрыт частично.
В работе — 5.5.1 (инфраструктура заметок и фото).**

---

## Что сделано

### ✅ Этапы 1–4 — База проекта

1. **Скелет.** Drawer, 7 вкладок, навигация через `AppScaffold`.
2. **Room-БД.** 5 таблиц: `areas`, `orders`, `samples`, `order_wells`,
   `sample_notes`. DAO + Repository. Version = 1.
3. **Настройки импорта.** Дерево категорий, экспорт/импорт JSON,
   пользовательские префиксы участков, словари заголовков и типов.
4. **Excel-импорт.** Свой парсер `.xlsx`, `ExcelAnalyzer` с гибридным
   автодетектом, `AreaResolver`, `OrderNumberExtractor`, `SampleFilter`,
   многослойная очередь, ручной маппинг, обучение словарей, история.

### ✅ Этап 5.1 — Каркас сверки

Полностью готов UI-экран «Сверка и поиск» + модели + состояние.

**Ключевые файлы:**
- `ReconciliationModels.kt` — модели и чистые функции.
- `ReconciliationState.kt` — @Stable, undo/redo (20 шагов).
- `ReconciliationDialogs.kt` — 11 диалогов.
- `ReconciliationViewModel.kt` — AndroidViewModel + БД.
- `ReconciliationMapper.kt` — мапперы.
- `SearchScreen.kt` — плоский `LazyColumn`.

**Реализовано на экране:**
- Селекторы участка и наряда (оба опциональны, наряды зависят от участка).
- Поиск по скважине/пробе, фонарик-индикатор единственного совпадения.
- Группировка: выбранный наряд сверху, чужие — ниже с подсветкой.
- Логика чек-бокса: найдена / ВК / холостая / отложена / ошибка.
- Диалоги веса, ВК, характеристик, редактора, удаления.
- Настройки наряда: холостые (FIXED/AVERAGE/MANUAL) + шаг ВК.
- Массовая отметка с диалогом решений.
- Undo/Redo со статус-баром.
- Динамический заголовок «Скважина / Выработка».
- Легенда символики.

### 🟡 Этап 5.9 — Room (частично закрыт)

**Работает:**
- `ReconciliationViewModel` загружает всё из БД.
- Одиночные действия — атомарные UPDATE без чтения (`SampleDao`).
- Групповые — `saveRows` (батч-`@Update` в транзакции).
- Удаление — `deleteSampleWithRenumber` с пересчётом номеров.
- Undo/redo — `persistAll` через `saveRows`.

**Осталось в 5.9 (не блокирует):**
- ⬜ SQL-статистика для больших нарядов (если UI начнёт тормозить).
- ⬜ Diff-persist после undo/redo (сохранять только изменённые строки).
- ⬜ Дописать `README.md` и `DATABASE.md` под новые методы (частично закрыто
  в текущей ревизии документов).
- ⬜ Синхронизация `has_note` / `has_photo` — после 5.5.

---

## Что в работе

### 🟡 Этап 5.5 — Заметки и фото

**Решения приняты (см. `DECISIONS.md`, раздел 11):**
- Несколько фото на пробу (таблица `sample_images`).
- Заметка и фото независимы.
- Файлы — во внутреннем хранилище `filesDir/sample_photos/`.
- Сжатие до 1024 px, JPEG 80%.
- Съёмка — `TakePicture()` + `FileProvider`.
- Галерея — `PickVisualMedia()`.
- Иконки в строке + пункты меню 3 точки.
- Один диалог: текст + превью фото.
- Флаги `has_note` и `has_photo` синхронизируются.

**План (3 захода):**

| Заход | Что делаем | Статус |
|---|---|---|
| **5.5.1** | FileProvider, `file_paths.xml`, CAMERA, `PhotoStorage`, миграция 1→2, методы репозитория | 🟡 **в работе** |
| 5.5.2 | `NotePhotoDialog`, лаунчеры камеры/галереи, подключение к ViewModel | ⬜ |
| 5.5.3 | Иконки в строке, превью, синхронизация флагов | ⬜ |

**Состояние перед стартом 5.5.1 (что уже есть):**
- `SampleEntity.hasNote: Boolean` — есть.
- `SampleEntity.hasPhoto` — **нет** (миграция).
- `SampleNoteEntity.imagePath` — **есть, но устарело** (по решению 2Б
  убираем, фото — в отдельной таблице).
- `SampleRow.hasPhoto` — уже в UI-модели, в `toRow()` стоит `false` с TODO.
- `NoteDialog` в `ReconciliationDialogs.kt` — заглушка с надписью
  «Прикрепить фото — в разработке».
- `NoteDialog` вызывается из `SearchScreen` (при нажатии на иконку
  заметки или пункт меню «Заметка»), `onSave` показывает снекбар
  «Заметка — в разработке».

---

## Что впереди (кратко)

- **5.5.2–5.5.3** — UI заметок и фото.
- **5.8** — голосовой помощник (4–5 заходов).
- **5.10** — полировка: тёмная тема, адаптивность, haptic.
- **6** — релиз MVP.

Подробно — в `ROADMAP.md` и `NEXT_STEPS.md`.

---

## Полезные ссылки

- `README.md` — где что лежит.
- `DATABASE.md` — схема БД и путь данных.
- `DECISIONS.md` — все договорённости по UI и логике.
- `NEXT_STEPS.md` — детальный план текущего этапа.
- `ROADMAP.md` — общий план.