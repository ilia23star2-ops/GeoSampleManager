# VOICE.md — спецификация голосового помощника

## Текущий статус

ГП работает через Vosk (маленькая модель `vosk-model-small-ru-0.22`) + Android TTS.
Грамматика ограничена (см. `VoiceGrammar.kt`).
TTS-нормализация через `VoiceSpeaker` (склонения, `spokenWeight`, `samples`).

**Unit-тесты:** зелёные.
**Device-check:** 🟡 не пройден для серии `5.8.6`.

## Команды

### Управление сессией

| Фраза | Команда | Озвучка |
|---|---|---|
| `стоп`, `хватит` | `Stop` | (закрывает диалог) |
| `пауза`, `паузу` | `Pause` | «Пауза» |
| `продолжить`, `продолжай` | `Resume` | «Продолжаю» |
| `отмена`, `отменить`, `верни`, `назад` | `Undo` | «Отменено.» |
| `вперёд`, `вперед` | `Redo` | «Повторено.» |
| `следующая`, `далее`, `следующую`, `следующий` | `Next` | «Слушаю следующую скважину.» |
| `помощь`, `команда`, `команды` | `Help` | список команд |
| `сколько осталось` | `HowManyLeft` | список оставшихся |
| `показать отложенные` / `отложенные` | `ShowPostponed` | фильтр |
| `показать найденные` / `найденные` | `ShowFound` | фильтр |

### Режимы

| Фраза | Команда | Озвучка |
|---|---|---|
| `сортировка`, `режим сортировка`, `режим сортировки` | `SetMode(SORT)` | «Сортировка.» |
| `поиск`, `режим поиск` | `SetMode(SEARCH)` | «Поиск.» |

### Отметка

| Фраза | Команда | Озвучка |
|---|---|---|
| `первая`, `вторая`, ..., `тридцатая` | `MarkOrdinal(n)` | «Первая отмечена.» |
| `двадцать первая`, `тридцать первая` | `MarkOrdinal(n)` | «Двадцать первая отмечена.» |
| `первая вторая третья` | `MarkByNumbers([1,2,3])` | «Отмечено три пробы.» |
| `первая и вторая` | `MarkByNumbers([1,2])` | «Отмечено две пробы.» |
| `все`, `отметь все`, `отметить все` | `MarkAll` | «Отмечено N проб. Все пробы скважины.» (блокируется если есть ВК/холостые без веса) |
| `отметь 7`, `отметить седьмую` | `MarkOrdinal(7)` | «Седьмая отмечена.» |
| `отметь`, `отметь эту`, `эту`, `ее`, `её` | `MarkCurrent` | (как обычная отметка найденной) |

### Снятие

| Фраза | Команда | Озвучка |
|---|---|---|
| `снять первую`, `убрать первую` | `ClearOrdinal(1)` | «Снято.» |
| `снять последнюю`, `последнюю снять` | `ClearLast` | «Снято.» |
| `снять все`, `сбросить все`, `очистить все` | `ClearAll` | «Снято отметок: N» |
| `снять отложенную` | `Unpostpone` | «Отложенность снята» |

### Вес (в состоянии `awaitingWeight`)

| Фраза | Значение |
|---|---|
| `два` | 2.0 |
| `два пять` | 25.0 |
| `два и шесть` | 2.6 |
| `2,6` / `2.6` | 2.6 |
| `две целых шесть десятых` | 2.6 |
| `два целых шесть сотых` | 2.06 |
| `шесть десятых` | 0.6 |
| `шесть сотых` | 0.06 |
| `два с половиной` | 2.5 |
| `два с четвертью` | 2.25 |
| `полтора`, `полторы` | 1.5 |
| `полкило` | 0.5 |
| `два кг`, `два кило` | 2.0 |

Озвучка: `Вес два и шесть. Проба отмечена.` (через `VoiceSpeaker.spokenWeight`).

### Выбор действия (в состоянии `pendingMarkChoice`)

После фразы «Уже отмечена. Снять, отложить или пропустить?» или «Отложена. ...»:

| Фраза | Команда |
|---|---|
| `снять`, `сними`, `убрать`, `убери`, `удали`, `удалить` | `ChoiceRemove` |
| `отложить`, `отложи`, `перенести`, `перенеси` | `ChoicePostpone` |
| `пропустить`, `пропусти`, `дальше`, `не надо`, `ничего`, `оставить`, `потом` | `ChoiceSkip` |
| `отметить`, `отметь`, `эту`, `её` (если `pendingMarkChoice.type == POSTPONED`) | `MarkCurrent` → снимает postponement и отмечает |

### Поиск

Всё, что не команда и похоже на номер/код:

- `1524`, `15 24 01`, `15 24`
- `KPD1090031`
- `пятнадцать двадцать четыре ноль один` (через `VoiceNumberParser`)

### Сортировка (2+ номера)

Разделители:
- `и`: `1524 и 1525`
- `запятая`: `1524 запятая 1525`
- `тире`: `1524 тире 1525`
- `;` и `,` — пунктуационные

Озвучка (SEARCH): «Скважина ..., Наряд №...» с возможной неоднозначностью.
Озвучка (SORT): «Проба ... → Наряд №...» без статистики.

## Запрещённые преобразования (строгий шлюз)

- `семья` → НЕ `MarkOrdinal(7)`.
- `это первая проба` → НЕ `MarkOrdinal(1)`.
- `назад один` → НЕ `MarkOrdinal(1)`.
- `два и шесть` → НЕ `Sort(["2", "6"])` (проверка: каждый элемент ≥2 цифр).
- Одиночные количественные числа (`семь`, `двадцать`) → НЕ `MarkOrdinal`.
- Фразы со служебными словами → `Unknown`, НЕ `Search`.

## Anti-echo и debounce

- **Echo suppression:** Vosk глушится во время TTS, возобновляется через 800 мс. Окно подавления = `600 мс + 70 мс × длина_фразы`.
- **Duplicate window:** 600 мс. Одинаковая фраза в этом окне игнорируется, после — принимается (чтобы «отмена» работала несколько раз).

## Озвучка

- Скорость TTS: `1.10`.
- Склонения: `1 проба / 2 пробы / 5 проб`.
- Вес: `два и шесть`, `два с половиной`, `полтора`.
- Номера: парами (`15 24` → «пятнадцать двадцать четыре»).

## Файлы
data/util/
VoiceController.kt Vosk + TTS обёртка
data/voice/
VoiceCommand.kt sealed class команд
VoiceCommandParser.kt текст → VoiceCommand
VoiceNumberParser.kt русские числительные → кандидаты
VoiceGrammar.kt JSON-грамматика для Vosk
VoiceSpeaker.kt TTS-нормализатор
VoiceSession.kt состояние сессии + pendingMarkChoice
VoiceDictionary.kt словари числительных
VoiceOrdinals.kt порядковые числительные
ui/screens/
VoiceDialog.kt Compose-диалог ГП
ReconciliationViewModel.kt выполнение команд

