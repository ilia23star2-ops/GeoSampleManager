# VOICE.md — спецификация голосового помощника

## Текущий статус

ГП работает через Vosk (`vosk-model-small-ru-0.22`) + Android TTS.
Грамматика — одна на все состояния (`VoiceGrammar.kt`, ~400 слов).

**Unit-тесты:** зелёные (~260).
**Device-check:** серия `5.8.11-e4` пройдена полностью.
**Реализовано в фиче `feature/5.8.11-e4-voice-v2`:**
- `e4e-bundle`, `e4-tests`.
- `e4-pin-1…7` — pin, fallback, честная ошибка.
- `e4-markers`, `e4-markers-2` — маркеры и подтверждение.
- `e4-speak-1` — `splitLikeHuman`.
- `e4-prefix-1` — префиксы по буквам.
- `e4-weight-queue` — очередь веса.
- `e4-ui-1` — кнопка «Наверх».
- `e4-fix-voice-1` — 4 фикса.

## Состояния ГП

| Состояние | Что означает | Что принимает |
|---|---|---|
| `IDLE` | ГП выключен | — |
| `LISTENING` | Слушает, полный словарь | Все команды + поиск |
| `FOUND_PINNED` | Скважина закреплена | Число → отметка; «дальше» → очередь; «следующая X» → Find |
| `AWAITING_WEIGHT` | Ждёт вес | Весовые формы + управление |
| `AWAITING_WEIGHT_QUEUE` | Очередь веса после «отметь все» | Вес, «пропустить», управление |
| `AWAITING_CHOICE` | Ждёт выбор для проблемной пробы | 3 команды + управление |
| `AWAITING_CONTINUE` | «Найден в нескольких нарядах» | «продолжить» / «стоп» |
| `AWAITING_MARK` | «отметь» → ждёт номер | Порядковые 1..N |
| `AWAITING_CLEAR` | «снять» → ждёт номер | 1..N |
| `AWAITING_POSTPONE` | «отложить» → ждёт номер | 1..N |
| `AWAITING_CONFIRM` | «отметь все» → подтверждение | «подтверждаю» / «отменяю» |
| `PAUSED` | Пауза | «продолжить» / «стоп» / Undo / Redo |

**`VoiceSessionMode` (SEARCH/SORT)** ортогонален состоянию.

**Тайм-аут ожидания:** 30 сек, предупреждение на 20-й. Работает во всех
`AWAITING_*`, кроме `AWAITING_CONTINUE` (там ждёт выбора на экране).

## Pin скважины

**Правила:**
- После `FoundOne` скважина закрепляется (`VoiceSession.pin`).
- В `FOUND_PINNED` **любое голое число** → `MarkOrdinal`. Search запрещён.
- Фраза из числительных склеивается: «тринадцать шестьдесят семь» →
  `MarkOrdinal(1367)`.
- «Дальше» → `NextInQueue`.
- «Следующая» / «Далее» → `Next` — выход из pin.
- «Следующая X» → `Find(X)`.
- «Отмена» → `Undo` + сброс pin и очереди.
- **Ручной ввод UI** — сбрасывает pin и очередь.

**Честная ошибка вместо fallback (pin-7):**
- Проба с распознанным номером **есть** → отметка.
- Пробы **нет** → `MarkOrdinalNotFound(ordinal, hintOrdinal)`.
- `hintOrdinal` — «спорный» двойник (14 → 4, 40 → 4, 400 → 4,
  4000 → 4), если такой есть. Для 1..3 и чисел типа 20, 25 — null.
- UI и голос: «Пробы №14 нет. Если нужна №4 — произнесите „четыре".»
- Пользователь уточняет числом — количественные Vosk распознаёт чётко.

**Почему без fallback:** Vosk путает «четвёртая» ↔ «четырнадцатая» в
обе стороны. Если юзер сказал «четырнадцатая» и хотел 14, а её нет —
fallback молча отмечал 4. Неправильное действие опаснее честной ошибки.

## Очередь мультизапроса

- `voiceSort` при 2+ запросах: если **все** дали единственное попадание —
  строим очередь.
- Первый — `pinned`, остальные в `queue`.
- `FoundOne.queueSize = N` → VoiceDialog показывает «Найдено N скважин».
- «Дальше» → следующий.
- Конец очереди → «Очередь пуста. Скажите „следующая" для нового запроса.»

## Очередь веса

**Запускается при «отметь все» + подтверждение**, если остались
холостые без веса или ВК без веса.

**Логика:**
1. Отметить всё, что можно (обычные + уже с весом).
2. Собрать очередь `[холостая №5, ВК №9, …]` в порядке списка.
3. ГП: «Отмечено N проб. Холостая, 5-я. Вес?»
4. Юзер: вес → отметка → «Отмечено N+1. Холостая, 7-я. Вес?»
5. «Пропустить» → следующая.
6. Конец: если `skipped == 0` → «Все пробы скважины отмечены.»,
   иначе «Отмечено N. Пропущено M. Очередь веса завершена.»

**Тайм-аут:** 30 сек / предупреждение на 20-й. По тайм-ауту — очередь
сбрасывается.

## Маркеры намерения

| Маркер | Ведёт в | Словарь | Пример |
|---|---|---|---|
| «отметь» | `AWAITING_MARK` | Порядковые 1..N | «отметь» → «Какую?» → «четвёртую» |
| «снять» | `AWAITING_CLEAR` | 1..N | «снять» → «Какую?» → «пятую» |
| «отложить» | `AWAITING_POSTPONE` | 1..N | «отложить» → «Какую?» → «вторую» |
| «все» (массовое) | `AWAITING_CONFIRM` | Подтверждение | «отметь все» → «Отметить N?» |

**Одна фраза работает сразу:** «отметь пятую» → `MarkOrdinal(5)` без
вопроса. «Отметь пять шесть» → `MarkByNumbers([5, 6])`.

## Подтверждение массовых

- «отметь все» / «снять все» → `AWAITING_CONFIRM`.
- ГП: «Отметить все 15 проб? Скажите „подтверждаю" или „отменяю".»
- «подтверждаю» / «подтвердить» → выполнить.
- «отменяю» / «отменить» / «отмена» → отменить.
- «стоп» → стоп всей сессии.
- Молчание 30 сек → автоотмена.
- **Удаление голосом не реализуем** — только UI.

## Команды

### Управление сессией

| Фраза | Команда | Озвучка |
|---|---|---|
| `стоп`, `хватит` | `Stop` | (закрывает) |
| `пауза`, `паузу` | `Pause` | «Пауза» |
| `продолжить`, `продолжай` | `Resume` | «Продолжаю» |
| `отмена`, `отменить`, `верни`, `назад` | `Undo` | «Отменено.» |
| `вперёд`, `вперед` | `Redo` | «Повторено.» |
| `следующая`, `далее` | `Next` | — |
| `следующая X`, `далее X` | `Find(X)` | поиск |
| `дальше` (в pin с очередью) | `NextInQueue` | следующий |

### Поиск

| Фраза | Команда |
|---|---|
| `1524`, `15 24 01`, `KPD1090031` | `Search(query)` |
| `найди`, `найти`, `ищи` | `Find(null)` |
| `найди 1524` | `Find("1524")` |

### Отметка

| Фраза | Команда |
|---|---|
| `первая` … `тридцатая` | `MarkOrdinal(n)` |
| `отметь 7`, `отметить седьмую` | `MarkOrdinal(7)` |
| `отметь пятую` (одной фразой) | `MarkOrdinal(5)` |
| `отметь пять шесть` | `MarkByNumbers([5, 6])` |
| `два три` (в pin) | `MarkByNumbers([2, 3])` |
| `двадцать три` (в pin) | `MarkOrdinal(23)` |
| `первая вторая третья` | `MarkByNumbers([1, 2, 3])` |
| `все`, `отметь все` | `MarkAll` → `AWAITING_CONFIRM` |
| `отметь`, `эту`, `её` | `MarkCurrent` |

### Снятие

| Фраза | Команда |
|---|---|
| `снять первую`, `убрать первую` | `ClearOrdinal(1)` |
| `снять 7` | `ClearOrdinal(7)` |
| `снять последнюю` | `ClearLast` |
| `снять все` | `ClearAll` → `AWAITING_CONFIRM` |
| `снять отложенную` | `Unpostpone` |

### Отложение

| Фраза | Команда |
|---|---|
| `отложить`, `отложи` | `PostponeIntent` → `AWAITING_POSTPONE` |
| `отложить вторую` | `PostponeOrdinal(2)` |
| `отложи 7` | `PostponeOrdinal(7)` |
| `перенести третью` | `PostponeOrdinal(3)` |

### Вес

| Фраза | Значение |
|---|---|
| `два` | 2.0 |
| `два и шесть` | 2.6 |
| `два семьсот` | 2.7 |
| `2,6` / `2.6` | 2.6 |
| `два с половиной` | 2.5 |
| `полтора` | 1.5 |
| `полкило` | 0.5 |

**Мусорный фильтр:** фраза целиком из весовых слов. Иначе → Unknown.

### Выбор действия (в `AWAITING_CHOICE`)

| Фраза | Команда |
|---|---|
| `снять`, `сними`, `убрать` | `ChoiceRemove` |
| `отложить`, `отложи`, `перенести` | `ChoicePostpone` |
| `пропустить`, `пропусти`, `дальше` | `ChoiceSkip` |

## Мимикрия TTS

**Правило:** ГП произносит номера скважин и проб **как человек**.
Единый метод — `VoiceSpeaker.spellMimicry(text)`.

**Алгоритм:**
1. Если строка = «префикс + цифры» (`NV1366`) — разбить.
2. Префикс — по буквам раздельно: `NV` → «эн вэ».
3. Цифры — `splitLikeHuman`:
  - чётная длина → пары: `1366` → `13|66`;
  - нечётная → первая 3, потом пары: `1090031` → `109|00|31`.
4. Каждая группа — число словами.

**Примеры:**
- `NV1366` → «эн вэ тринадцать шестьдесят шесть»
- `NV136601` → «эн вэ тринадцать шестьдесят шесть ноль один»
- `KPD1090031` → «ка пэ дэ сто девять ноль ноль тридцать один»
- `109003101` → «сто девять ноль ноль тридцать один ноль один»

**Симметрия:** VoiceNumberParser собирает «сто девять ноль ноль
тридцать один» → `1090031`. `splitLikeHuman` делает обратное —
`1090031` → `109|00|31`. ГП отвечает той же формой, что услышал.

**Поле «Распознано» в UI:** показывает **канонический номер** из БД
(`FoundOne.query`), а не сырой текст Vosk. Если нашли — `NV1366`,
если не нашли — сырой текст.

## Префиксы

**TTS:** произносит по буквам раздельно — «ка пэ дэ», «эн вэ», «даблю».

**Vosk:** звуки букв добавлены в грамматику (`VoiceLetterSounds.sounds.keys`).
Слышит «ка», «пэ», «дэ» отдельными словами. `VoicePrefixResolver`
собирает в буквы.

**Единый набор в `VoiceLetterSounds`:**
- K → «ка», P → «пэ», D → «дэ», N → «эн», V → «вэ», W → «даблю».
- Для каждой буквы — несколько форм.

**Мимикрия префиксов одной фразой** (спорно, отложено до `e4d`):
- `KPD1090031` → сейчас «ка пэ дэ сто девять ноль ноль тридцать один»,
  можно распознать снова.

## Приоритеты поиска

**В выбранном наряде:**
1. Строгий по всей БД (attention при другом наряде).
2. Префикс скважины (левые цифры).
3. Номер пробы (`sampleNumber`, слева).

**При совпадении 2 и 3 — приоритет скважине.**

**Реализация:** `e4-search-context` — запланировано (следующая серия).

## SORT-режим

**Отличие от SEARCH:**
- Нет статистики.
- Нет отметок.
- Короткий ответ: «Скважина X. Наряд Y.»
- **Attention работает так же:** если выбранный участок/наряд не
  совпадает — «Другой участок/наряд. Выберите на экране.»

**Переключение:** чип «ПОИСК / СОРТ» рядом с кнопкой 🎤 или голосом:
«поиск» / «сортировка».

**В SORT pin не работает** — каждый номер идёт через `voiceSort`,
а не через `parseForPinned`.

## Anti-echo и кулдаун

- Vosk глушится во время TTS, возобновляется через **250 мс** (`e4-fix-2`).
- Окно подавления = `600 мс + 70 мс × длина_фразы`.
- Duplicate window = 600 мс.

## Озвучка

- Скорость TTS: `1.10`.
- Склонения: `1 проба / 2 пробы / 5 проб`.
- Вес: `два и шесть`, `два с половиной`, `полтора`.
- Номера: `spellMimicry(text)` — как человек.
- Префиксы: по буквам раздельно («ка пэ дэ»).
- **Слово «Распознано» убрано из голоса** (`e4-pin-7`) — TTS ставил
  неверное ударение.

## Запрещённые преобразования

- `семья` → НЕ `MarkOrdinal(7)`.
- `это первая проба` → НЕ `MarkOrdinal(1)`.
- `два и шесть` → НЕ `Sort(["2","6"])`.
- Одиночные количественные (`семь`, `двадцать`) в `LISTENING` → НЕ `MarkOrdinal`.
- Фразы со служебными словами → `Unknown`.
- `четвертью` → НЕ `MarkOrdinal(4)`.
- «двадцать три» (одно слово) → НЕ `MarkByNumbers([2, 3])`.
- «семь утра было холодно» в pin → `Unknown` (проверка `isOnlyNumbersPhrase`).

## Файлы
data/util/
VoiceController.kt Vosk + TTS обёртка
VoiceFeedback.kt звуковая карта

data/voice/
VoiceCommand.kt sealed class команд
VoiceCommandParser.kt текст → VoiceCommand
VoiceNumberParser.kt русские числительные → кандидаты
VoiceGrammar.kt JSON-грамматика для Vosk
VoiceSpeaker.kt TTS-нормализатор + spellMimicry + splitLikeHuman
VoiceSession.kt состояние сессии + PinnedScope + queue + weightQueue
VoiceState.kt enum состояний
VoiceDictionary.kt словари числительных
VoiceOrdinals.kt порядковые
VoiceMarkOrdinalFallback.kt подсказки при промахе
VoiceLetterSounds.kt звук → буква
QueryNormalizer.kt нормализация строки
QueryTokenizer.kt строка → токены
QueryToken.kt типы токенов
DigitGroup.kt группа цифр
DigitGrouper.kt токены → группы
GroupToCandidates.kt группы → кандидаты
SearchService.kt единый сервис поиска
SearchResult.kt результат поиска
Response.kt модель ответа
Presenter.kt рендер Response
UnifiedSearch.kt базовый поиск

ui/screens/
VoiceDialog.kt Compose-панель ГП
VoicePanel.kt визуальная панель
ReconciliationViewModel.kt выполнение команд
ReconciliationWeightQueue.kt построение очереди веса

## Следующие заходы

- **`e4-dicts`** — динамические словари Vosk для 5 узких состояний.
- **`e4-search-context`** — три параллельные проверки.
- **`e4d`** — динамическая грамматика Vosk + пересоздание `Recognizer`.
  Решает И-35 и И-24.