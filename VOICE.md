# VOICE.md — спецификация голосового помощника

## Текущий статус

ГП работает через Vosk (маленькая модель `vosk-model-small-ru-0.22`) + Android TTS.
Грамматика — одна на все состояния (`VoiceGrammar.kt`, ~350 слов).
TTS-нормализация через `VoiceSpeaker`.

**Unit-тесты:** зелёные для `5.8.11-e4a/b/g/g3/fix-1/fix-2`.
**Device-check:** 🟡 серия `5.8.11-e4` частично пройдена.
**В работе:** `5.8.11-e4e` — свести пути поиска в один.

## Состояния ГП

| Состояние | Что означает | Что принимает |
|---|---|---|
| `IDLE` | ГП выключен | — |
| `LISTENING` | Слушает, полный словарь | Все команды + поиск |
| `FOUND_PINNED` | Скважина закреплена | Число 1..N → отметка; «следующая» → выход |
| `AWAITING_WEIGHT` | Ждёт вес | Весовые формы + управление |
| `AWAITING_CHOICE` | Ждёт выбор (снять/отложить/пропустить) | 3 команды + управление |
| `AWAITING_CONTINUE` | «Найден в нескольких нарядах» | «продолжить» / «стоп» / поиск |
| `AWAITING_NOT_FOUND_CHOICE` | 0 попаданий | «повторить» / «вернуться» |
| `PAUSED` | Пауза | «продолжить» / «стоп» / Undo / Redo |

**Реализовано:** `IDLE`, `LISTENING`, `AWAITING_WEIGHT`, `AWAITING_CHOICE`, `AWAITING_CONTINUE`, `PAUSED`.
**Планируется:** `FOUND_PINNED`, `AWAITING_NOT_FOUND_CHOICE`, `AWAITING_MARK`, `AWAITING_CLEAR`, `AWAITING_POSTPONE`.

**`VoiceSessionMode` (SEARCH/SORT)** ортогонален состоянию.

## Команды

### Управление сессией

| Фраза | Команда | Озвучка |
|---|---|---|
| `стоп`, `хватит` | `Stop` | (закрывает) |
| `пауза`, `паузу` | `Pause` | «Пауза» |
| `продолжить`, `продолжай` | `Resume` | «Продолжаю» |
| `отмена`, `отменить`, `верни`, `назад` | `Undo` | «Отменено.» |
| `вперёд`, `вперед` | `Redo` | «Повторено.» |
| `следующая`, `далее`, `следующую`, `следующий` | `Next` | (варьируется) |

### Информация

| Фраза | Команда | Озвучка |
|---|---|---|
| `помощь`, `команда`, `команды` | `Help` | Список команд **для текущего состояния** |
| `сколько осталось` | `HowManyLeft` | Список оставшихся |
| `отложенные` | `ShowPostponed` | Фильтр |
| `найденные` | `ShowFound` | Фильтр |

**`Help` — актуальные команды состояния, не всё подряд.**

### Режимы

| Фраза | Команда |
|---|---|
| `сортировка`, `режим сортировка` | `SetMode(SORT)` |
| `поиск`, `режим поиск` | `SetMode(SEARCH)` |

### Поиск

| Фраза | Команда |
|---|---|
| `1524`, `15 24 01`, `KPD1090031` | `Search(query)` |
| `найди`, `найти`, `ищи`, `искать`, `поищи` | `Find(null)` — переключение в ПОИСК |
| `найди 1524` | `Find("1524")` — переключение + поиск |

**FIX `e4g3`.**

### Отметка

| Фраза | Команда | Озвучка |
|---|---|---|
| `первая` … `тридцатая` | `MarkOrdinal(n)` | «Первая.» |
| `отметь 7`, `отметить седьмую` | `MarkOrdinal(7)` | «Седьмая.» |
| `первая вторая третья` | `MarkByNumbers([1,2,3])` | «Отмечено три пробы.» |
| `все`, `отметь все` | `MarkAll` | «Отмечено N проб.» |
| `отметь`, `отметь эту`, `эту`, `её` | `MarkCurrent` | (как обычная) |

**Планируется:** маркер `отметь` переводит в `AWAITING_MARK` — словарь 1..N, где N — число проб в скважине.

### Снятие

| Фраза | Команда |
|---|---|
| `снять первую`, `убрать первую` | `ClearOrdinal(1)` |
| `снять последнюю` | `ClearLast` |
| `снять все`, `сбросить все` | `ClearAll` |
| `снять отложенную` | `Unpostpone` |
| `отложить первую` | `Postpone` *(планируется)* |

### Вес (в `AWAITING_WEIGHT`)

| Фраза | Значение |
|---|---|
| `два` | 2.0 |
| `два и шесть` | 2.6 |
| `2,6` / `2.6` | 2.6 |
| `две целых шесть десятых` | 2.6 |
| `два целых шесть сотых` | 2.06 |
| `шесть десятых` | 0.6 |
| `два с половиной` | 2.5 |
| `полтора`, `полторы` | 1.5 |
| `полкило` | 0.5 |
| `два кг`, `два кило` | 2.0 |

**Убрано в `e4-fix-1`:** `два с четвертью` (2.25). Vosk путал «четвертью» с «четвёртая».

**Мусорный фильтр `e4b`:** фраза целиком должна состоять из весовых слов. Иначе → `Unknown`.

### Выбор действия (в `AWAITING_CHOICE`)

| Фраза | Команда |
|---|---|
| `снять`, `сними`, `убрать` | `ChoiceRemove` |
| `отложить`, `отложи`, `перенести` | `ChoicePostpone` |
| `пропустить`, `пропусти`, `дальше`, `потом` | `ChoiceSkip` |

### Сортировка (2+ номера)

Разделители: `и`, `запятая`, `тире`, `;`, `,`.

## Маркеры намерения (архитектура)

**Идея:** слова-маркеры переводят ГП в узкое состояние с маленьким словарём.

| Маркер | Ведёт в | Словарь | Эффект |
|---|---|---|---|
| «отметь» | `AWAITING_MARK` | порядковые 1..N | Убирает путаницу 4 ↔ 14 |
| «снять» | `AWAITING_CLEAR` | порядковые 1..N | Однозначно |
| «вес» | `AWAITING_WEIGHT` | весовые | Убирает мусор |
| «найди» | `AWAITING_SEARCH` | числа, префиксы | Однозначно |
| «отложить» | `AWAITING_POSTPONE` | порядковые 1..N | — |

**Ключевой эффект:** Vosk не может «подогнать» звук под слово, которого нет в словаре. Если N=5, «четырнадцать» отсутствует — Vosk выберет «четыре».

**Реализация:** `e4-markers` + `e4-dicts`.

## Динамические словари Vosk (архитектура)

**Идея:** у каждого состояния — свой словарь. Vosk пересоздаётся с новым `Recognizer` при смене.

**Эффективность по состояниям:**

| Состояние | Словарь | Эффект |
|---|---|---|
| `LISTENING` | полный (~350) | ❌ не сужать |
| `FOUND_PINNED` | полный (~340) | ❌ не сужать |
| `AWAITING_MARK` | 1..N | ✅✅ сильный |
| `AWAITING_WEIGHT` | 50 | ✅ |
| `AWAITING_CHOICE` | 7 | ✅✅ |
| `AWAITING_CONTINUE` | 30 | ✅ |
| `PAUSED` | 3 | ✅✅ |
| `AWAITING_NOT_FOUND_CHOICE` | 5 | ✅✅ |

**Механизм пересоздания:** во время TTS. Vosk стоит на `setPause(true)` — момент безопасный. Новый `Recognizer` создаётся за 200–400 мс, пока ГП говорит.

**В `LISTENING` и `FOUND_PINNED` — НЕ сужать.** Потеряются длинные фразы, имена участков, нестандартные номера.

**Реализация:** `e4d` — последний в очереди.

## Мимикрия TTS (архитектура)

**Идея:** ГП произносит номер так же, как это сделал пользователь — по группам, без склеивания в пары цифр.

**Пример:**
- Пользователь: «капэдэ сто девять ноль ноль тридцать один».
- ГП отвечает: «капэдэ сто девять ноль ноль тридцать один».
- На экране: `KPD1090031`.

**Правила озвучки (`VoiceSpeaker.spellOut(groups)`):**
- Группы без запятых, через пробел.
- Префикс — одним словом: `KPD` → «капэдэ» (не «ка пэ дэ»).
- Источник произношения префикса — `customPrefixPronunciations` (настройки пользователя). Если нет — каноническое.

**Канонический маппинг букв:**

A→а, B→бэ, C→цэ, D→дэ, E→е, F→эф, G→гэ, H→аш, I→и, J→жэ,
K→ка, L→эль, M→эм, N→эн, O→о, P→пэ, Q→ку, R→эр, S→эс, T→тэ,
U→у, V→вэ, W→**даблю**, X→икс, Y→игрек, Z→зэт.

**Реализация:** `e4e-a` + `e4e-c` + `e4e-b` (нужны `groups` из нового пути).

## Приоритеты поиска (архитектура)

**В выбранном наряде:**

1. **Строгий поиск по всей БД.** Нашлось в другом наряде/участке → «Внимание».
2. **Префикс по номеру скважины** (левые цифры после префикса участка).
3. **По номеру пробы** (`sampleNumber`, тоже с левого края).

**При совпадении в 2 и 3 — приоритет скважине.**

**`numberInWell` не ограничен 1..30.** Вычисляется как `sampleNumber − wellNumber`.

**Пример:** запрос «109» в наряде.
- `10900` → ✓ (префикс скважины).
- `1524 10109` → ✗ (109 в середине, не слева).

**Реализация:** `e4-search-context`.

## Pin скважины (архитектура)

**Идея:** после находки скважины голос работает с ней. Голое число 1..N → отметка пробы, не поиск.

**Правила:**
- `FOUND_PINNED` устанавливается после `FoundOne` / `FoundMany`.
- Выход: «следующая», «отмена», «найди X», ручной ввод в UI.
- Ручной ввод в UI — приоритет: сбрасывает pin.
- Визуально: пометка в панели ГП «Закреплено: Наряд 12 / KPD1090031».

**Реализация:** `e4-pin-1` + `e4-pin-2` + `e4-pin-3`.

## Очередь мультизапроса (архитектура)

**Идея:** если поиск дал несколько попаданий — они в очереди. «Следующая» листает.

**Правила:**
- Очередь заполняется при `FoundMany`.
- «Следующая» → к следующему попаданию.
- «Пропустить» / «дальше» → тоже.
- Конец очереди → «Все запросы обработаны. Жду следующий» + возврат в `LISTENING`.
- Pin текущего — сохраняется, пока не переключились.

**Реализация:** `e4-pin-2` + `e4-pin-3`.

## Anti-echo и кулдаун

- **Echo suppression:** Vosk глушится во время TTS, возобновляется через **250 мс** (`e4-fix-2`).
- Окно подавления = `600 мс + 70 мс × длина_фразы`.
- **Duplicate window:** 600 мс — одинаковые фразы в окне игнорируются.

## Озвучка

- Скорость TTS: `1.10`.
- Склонения: `1 проба / 2 пробы / 5 проб`.
- Вес: `два и шесть`, `два с половиной`, `полтора`.
- Номера: **по группам** (`spellOut(groups)`).

## Важные команды во всех состояниях

**Всегда работают:** Stop, Pause (кроме `PAUSED`), Undo, Redo, Отмена, Помощь.
**В `PAUSED`:** только Resume, Stop, Undo, Redo.

## Запрещённые преобразования (строгий шлюз)

- `семья` → НЕ `MarkOrdinal(7)`.
- `это первая проба` → НЕ `MarkOrdinal(1)`.
- `два и шесть` → НЕ `Sort(["2", "6"])`.
- Одиночные количественные (`семь`, `двадцать`) → НЕ `MarkOrdinal`.
- Фразы со служебными словами → `Unknown`, НЕ `Search`.
- **`четвертью` → НЕ `MarkOrdinal(4)`** (убрано из грамматики в `e4-fix-1`).

## Файлы
data/util/
VoiceController.kt Vosk + TTS обёртка

data/voice/
VoiceCommand.kt sealed class команд
VoiceCommandParser.kt текст → VoiceCommand
VoiceNumberParser.kt русские числительные → кандидаты
VoiceGrammar.kt JSON-грамматика для Vosk
VoiceSpeaker.kt TTS-нормализатор
VoiceSession.kt состояние сессии
VoiceState.kt enum состояний
VoiceDictionary.kt словари числительных
VoiceOrdinals.kt порядковые числительные
VoiceLetterSounds.kt звук → буква (для парсинга префиксов)
QueryNormalizer.kt нормализация строки
QueryTokenizer.kt строка → токены
QueryToken.kt типы токенов
DigitGroup.kt модель группы цифр
DigitGrouper.kt токены → группы
GroupToCandidates.kt группы → кандидаты поиска
SearchService.kt единый сервис поиска
SearchResult.kt результат поиска
Response.kt единая модель ответа
Presenter.kt рендер Response
UnifiedSearch.kt базовый поиск

ui/screens/
VoiceDialog.kt Compose-панель ГП
ReconciliationViewModel.kt выполнение команд
