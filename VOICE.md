# Голосовой помощник — полная спецификация

> Единый источник правды по логике ГП. Обновляется при каждом решении.
> Связано: `DECISIONS.md` §13, `NEXT_STEPS.md`, `ROADMAP.md`.

---

## 0. Назначение

ГП понимает голосовые команды рабочего в поле и выполняет их **без**
необходимости тапать по экрану руками (руки заняты — перчатки, мешки).

**Принцип:** грамматика собирает кандидатов — база выбирает победителя.

---

## 1. Уровни обработки
Аудио
└→ Vosk → текст
└→ [1] Нормализация (регистр, ё→е)
└→ [2] NULLS-детекция («два нуля» → [[NULLS:2]])
└→ [3] Предобработка слов (слипшиеся, многословные K, M)
└→ [4] Классификация токенов
└→ [5] Разбивка на блоки
└→ [6] Кандидаты
└→ [7] Поиск в БД (5 уровней)
└→ [8] Результат: AnswerState + TTS + звук

text

**NULLS — до токенизации**, иначе «два нуля» распадётся на D(2) и мусор.

---

## 2. Типы токенов

| Тип | Что | Значения |
|---|---|---|
| **D** | одиночная цифра | 0–9 |
| **T** | подросток | 10–19 |
| **X** | круглые десятки | 20–90 |
| **H** | круглые сотни | 100–900 |
| **K** | тысячи (в т.ч. многословные) | 1000–999000 |
| **M** | миллионы | 1 000 000+ |
| **NULLS** | «N нулей» | строка "0" × N |
| **PREFIX** | префикс участка | капэдэ → KPD и т.п. |
| **SEP** | разделитель | «и», «номер», «проба» |
| **STOP** | мусор | «эээ», «ну», «вот» |

---

## 3. Словари

### 3.1. Числа
- D: `ноль, нуль, один, одна, раз, два, две, три, четыре, пять, шесть, семь, восемь, девять`
- T: `десять … девятнадцать`
- X: `двадцать … девяносто`
- H: `сто … девятьсот`
- K: `тысяча, две тысячи, … девять тысяч`
- Дополнительно: `двойка, тройка, пятёрка` → 2, 3, 5

### 3.2. Порядковые
`первая … тридцатая`. Формы: `-ая`, `-ый`, `-ое`, `-ий`.

### 3.3. Префиксы
Из `ImportSettings.areaPrefixes` — динамически.
Пользовательские произношения — в `VoiceSettings`.

### 3.4. STOP
`эээ, ээ, ммм, мм, ну, вот, так, как-то, короче, типа, значит, это, блин, ёлки, ага, угу`

### 3.5. Управляющие (CMD)
`стоп, хватит, отмена, верни, повтори, следующая, другая, сначала, подтверждаю, нет`

### 3.6. SEP
`и, номер, проба, скважина`

---

## 4. Разбивка на блоки

### 4.1. Разрядный блок (R)
R = (K | H)? X? (T | D_неноль)?

### 4.2. Поцифровой блок (G)
G = D D+

### 4.3. NULLS-блок
Один блок = строка нулей.

### 4.4. Таблица границ

| Текущий ↓ / След. → | K | H | X | T | D≠0 | D=0 | NULLS | SEP | STOP |
|---|---|---|---|---|---|---|---|---|---|
| **K** | граница | продолж. | продолж. | продолж. | продолж. | граница | граница | граница | skip |
| **H** | граница | граница | продолж. | продолж. | продолж. | граница | граница | граница | skip |
| **X** | граница | граница | граница | граница | продолж. | граница | граница | граница | skip |
| **T** | граница | граница | граница | граница | граница | граница | граница | граница | skip |
| **D≠0** | граница | граница | граница | граница | → G | граница | граница | граница | skip |
| **D=0** | граница | граница | граница | граница | → G | → G | граница | граница | skip |
| **NULLS** | граница | граница | граница | граница | граница | граница | граница | граница | skip |
| **SEP** | граница | граница | граница | граница | граница | граница | граница | граница | skip |

---

## 5. Приоритеты кандидатов

| # | Кандидат |
|---|---|
| 1 | `concat(all_blocks)` |
| 2 | `each_block_as_number` |
| 3 | `partial_concat` |
| 4 | `prefix + concat(all_blocks)` |

Поиск по порядку. **Первый найденный — победитель.**

---

## 6. Уровни поиска в БД

| Уровень | Что | Условие |
|---|---|---|
| 1 | `sample_number` = кандидат | точное |
| 2 | `well_number` = кандидат | все пробы скважины |
| 3 | нормализация нулей | «10900031» ≈ «1090031» |
| 4 | well + суффикс (2–3 цифры) | структурное |
| 5 | fuzzy | см. §7 |

---

## 7. Fuzzy

**Правила отбора:**
- Ненулевые цифры **точно совпадают**.
- Разница длин **≤ 1**.
- Левенштейн полной строки **≤ 1**.

Если кандидат один — принимаем. Если ≥2 — уточняем. Если 0 — «Не нашёл».

---

## 8. Сегментация по паузам

- Тишина > **N мс** (500 / 800 / 1200, настройка) → конец фразы.
- Фраза обрабатывается **отдельно**.
- Буфер сбрасывается.

---

## 9. Команды

### 9.1. Поиск
- `<номер>` / `скважина <номер>` / `проба <номер>` / `найди <номер>` / `покажи <номер>`
- Работает через **поле поиска**, как вручную.

### 9.2. Авто-режим отметок
- Включается **автоматически**, когда ответ **единственный** (`OK_SINGLE`).
- Звук-сигнал: `soundOk` (см. §13).
- Если ответ — `ATTENTION` или `ERROR`, авто-режим **не включается**;
  после ответа ГП ставит паузу для ATTENTION.

### 9.3. Внутри режима отметок
- `первая` … `тридцатая` — отметка.
- `<суффикс>` — отметка по суффиксу.
- `вес <значение>` — вес.
- `снять первую` / `снять последнюю` / `снять все` (с подтверждением).
- `отложить` / `снять отложенную`.
- `следующая` / `далее` — **завершить скважину, очистить запрос**.

### 9.4. Вне режима
- `снять первую` — работает, если контекст есть.
- `отмена` / `верни` — undo.
- `повтори` — redo.
- `пауза` / `продолжить`.
- `стоп` / `хватит`.

### 9.5. Информация
- `сколько осталось` → «Осталось N проб.»
- `показать отложенные` → «Отложенные на экране.»
- `показать найденные` → «Найденные на экране.»
- `помощь` / `команды` → «Открываю справку.»

### 9.6. Сортировка
- Два и более номера подряд: `<номер1> и <номер2>`.
- **Без отметок.**
- До **5** номеров за раз.

---

## 10. Сценарий работы

### 10.1. Основной цикл
1. Рабочий: `1524`.
2. ГП: «Скважина 15 24. Наряд 7. Проб 3, отмечено 0.»
3. Ответ единственный → авто-режим (без звука).
4. Рабочий: `первая` → ГП: «Первая.» + `soundOk`.
5. Рабочий: `вторая` → ГП: «Вторая.» + `soundOk`.
6. Рабочий: `третья` (пропустил вторую — уже было) → ГП:
   «Третья. Отмечено 2 пробы.» + `soundOk`.
7. Рабочий: `следующая` → ГП: «Слушаю следующую скважину.» + `soundOk`.
8. Рабочий: `1525` → новый цикл.

### 10.2. Проба уже отмечена
`третья` (при 2 отмеченных) → ГП: «Третья уже отмечена. Снять,
отложить или пропустить?» + `soundAttention` + автопауза.

Ответы:
- `снять` → снять отметку.
- `отложить` → пометить отложенной.
- `пропустить` → ничего.
- `отмена` → остаться.

### 10.3. Проба из категории внимания
`пятая` (холостая без веса) → ГП: «Пятая — холостая. Вес?» +
`soundAttention` + автопауза.

### 10.4. Порядковая > размера скважины
`сороковая` при 15 пробах → ГП: «Проба 40 не существует. Всего 15.»
+ `soundError`.

### 10.5. Сортировка
`1524 и 1525`:
- Все однозначны → «15 24, наряд 7. 15 25, наряд 7.»
- Есть проблемные → «15 24 — другой наряд. 15 25, наряд 7.
  Выберите на экране.» + `soundAttention` + пауза.

### 10.6. Другой участок
`асдэ 1460162` при выбранном Коптеловском → «Другой участок —
Актайский, наряд 7. Выберите на экране.» + `soundAttention` + пауза.
Без отметок.

### 10.7. Молчание
Если рабочий молчит после нажатия «Слушаю» — ГП **не напоминает**.
Просто ждёт до авто-стопа.

---

## 11. Правила именования и счётчика

### 11.1. Именование проб
- **Существующие пробы** → порядковый: «первая», «вторая», «третья».
- **Несуществующие** (при ошибке) → номер: «проба 40».
- **Смена скважины** → снова с «первой».

### 11.2. Подтверждение отметки
- Всегда называем **порядковый**.
- **Счётчик** — общий по скважине (включая ручные отметки).
- Если порядковый == счётчик → только порядковый: «Вторая.»
- Если порядковый != счётчик → добавляем счётчик:
  «Третья. Отмечено 2 пробы.»

### 11.3. Снятие отметок
- Одиночное: «Первая снята.»
- Все: «Снято 3 отметки.»
- Счётчик всегда произносим.

### 11.4. Общая статистика
- ГП называет **только ненулевые** категории.
- Пример: «Проб 3, отмечено 0. Холостых 1.»
- Если всё по нулям — только «Проб 3, отмечено 0.»

---

## 12. UI

### 12.1. Индикатор ответа (бывший «фонарик»)

Три строки:
1. Значок состояния.
2. Краткий статус.
3. Причина (если есть).

| Причина | Строка 2 | Строка 3 |
|---|---|---|
| `IDLE_WAITING` | Жду | — |
| `OK_SINGLE` | Найдено | — |
| `FOUND_OTHER_ORDER` | Внимание | Другой наряд |
| `FOUND_OTHER_AREA` | Внимание | Другой участок |
| `FOUND_MULTIPLE` | Внимание | Несколько нарядов |
| `FOUND_MULTIPLE_AREA` | Внимание | Несколько участков |
| `NOT_FOUND` | Не найдено | — |
| `SEARCH_FAILED` | Ошибка | — |

### 12.2. Заголовок группы
Строка 1: Участок / Наряд №X
Строка 2: Проблема (если есть)
Строка 3: Найдено X/Y · Холостые · ВК · Отложено

text

- **Фон** — полупрозрачный цвет состояния (alpha 0.35).
- **Текст** — обычного цвета (`onSurface`).
- **Иконка и полоска слева** — цвет состояния.

### 12.3. Статус-бар внизу экрана

Тонкая полоса (~40 dp). См. `VoiceStatusBar.kt`.

### 12.4. Лог
- Файл `filesDir/voice_log.txt`.
- Формат: `timestamp | Vosk | parse | result`.
- Ротация: 10 сессий.
- Доступ: настройки ГП → «Показать лог».
- **Не в статус-баре.**

### 12.5. Онбординг
- 5 экранов при первом запуске.
- Кнопка «Больше не показывать».
- Повторно доступен из справки.

---

## 13. Звуковая карта

Три звука, единый источник — `VoiceFeedback.kt`.

| Имя | Ассоциация | Частота | Длительность |
|---|---|---|---|
| `soundOk` | Подтверждение | 1400 Гц | 80 мс |
| `soundAttention` | Внимание | 900 Гц × 2 | 100 + 50 + 100 мс |
| `soundError` | Ошибка | 400 Гц | 300 мс |

**Правила конфликтов (К1–К6)** — см. `DECISIONS.md` §13.5.

**Принцип:** звук звучит первым, TTS — через 200 мс. Рабочий сначала
слышит «что-то произошло», потом — «что именно».

---

## 14. Сессия

### 14.1. Начало
Тап по «Слушаю».

### 14.2. Завершение
- `стоп` / `хватит`, **или**
- тап по «Стоп», **или**
- смена вкладки приложения, **или**
- авто-стоп по тишине.

### 14.3. Пауза
- `пауза` → невидимый таймер 5 мин.
- За минуту до конца — видимый таймер + предупреждение.
- Истёк — авто-стоп.

### 14.4. Молчание
- Если рабочий молчит — **ГП не напоминает**.
- Ждёт до авто-стопа.

### 14.5. Новая сессия
- Чистый контекст.
- Показ последней скважины в статус-баре.

---

## 15. Настройки ГП (VoiceSettings)

| Параметр | Значения | По умолч. |
|---|---|---|
| Пауза сегментации | 500 / 800 / 1200 мс | 800 |
| Авто-стоп | 1 / 3 / 5 / 10 мин / выкл | 5 |
| Громкость TTS | выкл / тихо / норма / громко | норма |
| Режим | новичок / опытный | новичок |
| Пользовательские произношения префиксов | список | пусто |
| Онбординг | показывать / не показывать | показывать |

---

## 16. Модули

| Модуль | Отвечает за |
|---|---|
| `VoiceController` | Vosk, TTS, разрешения, шумоподавление |
| `VoiceNumberParser` | NULLS, предобработка, токены, блоки, кандидаты |
| `VoicePrefixResolver` | Префиксы из БД + пользовательские |
| `VoiceSearch` | 5 уровней поиска, fuzzy, уточнения |
| `VoiceSegmenter` | Сегментация по паузам |
| `VoiceSettings` | Настройки ГП |
| `VoiceSession` | Контекст, undo/redo, стоп |
| `VoiceFeedback` | Три звука + TTS-обёртки |
| `AnswerState` | Состояние ответа + маппер |
| `ReconciliationViewModel` | Выполнение действий |

---

## 17. Ограничения

- Диалекты и сильный акцент — вне Vosk-small-ru.
- Числа > 10 000 000 — не поддерживаем.
- Смешанный рус-англ в номере — только через словарь префиксов.
- Порядковые > 300 — не поддерживаем.

---

## 18. Обязательные тесты

**NULLS:**
- «два нуля» → `00`
- «сто два нуля три» → `100` + `00` + `3` → `100003`

**K-многословные:**
- «две тысячи пятьсот» → `2500`
- «сто тысяч двадцать» → `100020`

**Парсер:**
- «сто двадцать четыре» → `124`
- «ноль ноль три» → `003`
- «тысяча пятьсот шестьдесят два» → `1562`
- «тысяча пятьсот сто шестьдесят два» → `1500162`
- «пятьдесят один» → `51`
- «пятьдесят и один» → `50|1`

**STOP:**
- «сто эээ двадцать» → `120`

**Полный пайплайн:**
- «капэдэ сто девять два нуля тридцать один» → `KPD1090031`
- «энвэ тысяча пятьсот сто шестьдесят два» → `NV1500162`
- «энвэ ноль один» при активной `NV1500162` → `NV150016201`

**AnswerState:**
- Единственный ответ в выбранном наряде → `OK_SINGLE`.
- Единственный ответ в другом наряде → `FOUND_OTHER_ORDER`.
- Единственный ответ в другом участке → `FOUND_OTHER_AREA`.
- Несколько нарядов → `FOUND_MULTIPLE`.
- Не найдено → `NOT_FOUND`.
- Пустой запрос → `IDLE_WAITING`.
- Сортировка с разными причинами → худшее состояние побеждает.

**Звуки:**
- `soundOk` при отметке.
- `soundAttention` при уже отмеченной пробе.
- `soundError` при несуществующей пробе.
- Конфликт К1: сортировка с 🟡+🔴 → `soundError`.
