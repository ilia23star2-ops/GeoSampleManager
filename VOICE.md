# VOICE.md — спецификация голосового помощника

## Текущий статус

ГП работает через Vosk (`vosk-model-small-ru-0.22`) + Android TTS.
Грамматика — одна на все состояния (`VoiceGrammar.kt`, ~350 слов).
TTS-нормализация через `VoiceSpeaker`.

**Unit-тесты:** зелёные (206).
**Device-check:** серия `5.8.11-e4` частично пройдена.
**Реализовано в фиче:** `e4e-bundle`, `e4-tests`, `e4-pin-1`, `e4-pin-2`, `e4-pin-3`, `e4-pin-4`.
**В работе:** `e4-pin-5` — fallback 400→4.

## Состояния ГП

| Состояние | Что означает | Что принимает |
|---|---|---|
| `IDLE` | ГП выключен | — |
| `LISTENING` | Слушает, полный словарь | Все команды + поиск |
| `FOUND_PINNED` | Скважина закреплена | Голое число → отметка; «дальше» → очередь; «следующая X» → Find |
| `AWAITING_WEIGHT` | Ждёт вес | Весовые формы + управление |
| `AWAITING_CHOICE` | Ждёт выбор | 3 команды + управление |
| `AWAITING_CONTINUE` | «Найден в нескольких нарядах» | «продолжить» / «стоп» / поиск |
| `AWAITING_NOT_FOUND_CHOICE` | 0 попаданий | «повторить» / «вернуться» — **не реализовано** |
| `PAUSED` | Пауза | «продолжить» / «стоп» / Undo / Redo |

**`VoiceSessionMode` (SEARCH/SORT)** ортогонален состоянию.

## Pin скважины

**Правила:**
- После `FoundOne` скважина закрепляется (`VoiceSession.pin`).
- В `FOUND_PINNED` **любое голое число** → `MarkOrdinal`. Search запрещён.
- Фраза из числительных склеивается: «тринадцать шестьдесят семь» → `MarkOrdinal(1367)`.
- «Дальше» → `NextInQueue` — переключение в очереди.
- «Следующая» / «Далее» → `Next` — выход из очереди + из pin.
- «Следующая X» → `Find(X)` — выход + сразу поиск.
- «Отмена» → `Undo` + сброс pin и очереди.
- **Ручной ввод UI** — сбрасывает pin и очередь.

**Fallback в `voiceMarkOrdinal`:**
- Проба 14 не найдена → пробуем 4.
- Проба 4 не найдена → пробуем 14.
- **Открыто (`e4-pin-5`):** 400→4, 500→5, …, 900→9.

## Очередь мультизапроса

**Правила:**
- `voiceSort` при двух и более запросах: если **все** дали **единственное**
  попадание — строим очередь (`VoiceSession.enqueue`).
- Первый — `pinned`, остальные в `queue`.
- `VoiceExecResult.FoundOne.queueSize = N` — VoiceDialog показывает
  «Найдено N скважин. Слушайте первую.»
- «Дальше» → следующий в очереди.
- **Если хоть один запрос дал много попаданий / не нашёлся** — очередь
  не строится, старое поведение (описательный Message).
- Конец очереди → «Очередь пуста. Скажите следующая для нового запроса.»

## Команды

### Управление сессией

| Фраза | Команда | Озвучка |
|---|---|---|
| `стоп`, `хватит` | `Stop` | (закрывает) |
| `пауза`, `паузу` | `Pause` | «Пауза» |
| `продолжить`, `продолжай` | `Resume` | «Продолжаю» |
| `отмена`, `отменить`, `верни`, `назад` | `Undo` | «Отменено.» |
| `вперёд`, `вперед` | `Redo` | «Повторено.» |
| `следующая`, `далее`, `следующую`, `следующий` | `Next` | (варьируется) |
| `следующая X`, `далее X` | `Find(X)` | поиск |
| `дальше` (в pin с очередью) | `NextInQueue` | следующий в очереди |

### Поиск

| Фраза | Команда |
|---|---|
| `1524`, `15 24 01`, `KPD1090031` | `Search(query)` |
| `найди`, `найти`, `ищи`, `искать`, `поищи` | `Find(null)` |
| `найди 1524` | `Find("1524")` |

### Отметка

| Фраза | Команда | Озвучка |
|---|---|---|
| `первая` … `тридцатая` | `MarkOrdinal(n)` | «Первая.» |
| `отметь 7`, `отметить седьмую` | `MarkOrdinal(7)` | «Седьмая.» |
| `первая вторая третья` | `MarkByNumbers([1,2,3])` | «Отмечено три пробы.» |
| `все`, `отметь все` | `MarkAll` | «Отмечено N проб.» |
| `отметь`, `эту`, `её` | `MarkCurrent` | (как обычная) |
| в `FOUND_PINNED` — любое число | `MarkOrdinal` | «Седьмая.» |

**Планируется:** маркер `отметь` переводит в `AWAITING_MARK`.

### Снятие

| Фраза | Команда |
|---|---|
| `снять первую`, `убрать первую` | `ClearOrdinal(1)` |
| `снять последнюю` | `ClearLast` |
| `снять все`, `сбросить все` | `ClearAll` |
| `снять отложенную` | `Unpostpone` |

### Вес (в `AWAITING_WEIGHT`)

| Фраза | Значение |
|---|---|
| `два` | 2.0 |
| `два и шесть` | 2.6 |
| `два семьсот` | 2.7 |
| `три пятьсот` | 3.5 |
| `2,6` / `2.6` | 2.6 |
| `две целых шесть десятых` | 2.6 |
| `два целых шесть сотых` | 2.06 |
| `шесть десятых` | 0.6 |
| `два с половиной` | 2.5 |
| `полтора`, `полторы` | 1.5 |
| `полкило` | 0.5 |

**Убрано в `e4-fix-1`:** «два с четвертью» (2.25).

**Мусорный фильтр `e4b`:** фраза целиком из весовых слов. Иначе → `Unknown`.

### Выбор действия (в `AWAITING_CHOICE`)

| Фраза | Команда |
|---|---|
| `снять`, `сними`, `убрать` | `ChoiceRemove` |
| `отложить`, `отложи`, `перенести` | `ChoicePostpone` |
| `пропустить`, `пропусти`, `дальше`, `потом` | `ChoiceSkip` |

## Мимикрия TTS

**Идея:** ГП произносит номер по группам, без запятых, префикс одним словом.

**Пример:**
- Пользователь: «капэдэ сто девять ноль ноль тридцать один».
- ГП отвечает: «капэдэ сто девять ноль ноль тридцать один».
- На экране: `KPD1090031`.

**Правила `spellOut(groups)`:**
- Между группами — пробел, **без запятых**.
- Префикс — одним словом: `KPD` → «капэдэ».
- W → «даблю».

**Маппинг букв:** A→а, B→бэ, …, K→ка, P→пэ, D→дэ, W→даблю.

**Реализовано в `e4e-bundle`.**

## Приоритеты поиска

**В выбранном наряде:**
1. Строгий по всей БД (attention при другом наряде).
2. Префикс скважины (левые цифры).
3. Номер пробы (`sampleNumber`, слева).

**При совпадении 2 и 3 — приоритет скважине.**

**Реализация:** `e4-search-context` — запланировано.

## Маркеры намерения

**Планируется в `e4-markers`:**

| Маркер | Ведёт в | Словарь |
|---|---|---|
| «отметь» | `AWAITING_MARK` | порядковые 1..N |
| «снять» | `AWAITING_CLEAR` | 1..N |
| «вес» | `AWAITING_WEIGHT` | весовые |
| «найди» | `AWAITING_SEARCH` | числа, префиксы |
| «отложить» | `AWAITING_POSTPONE` | 1..N |

## Динамические словари Vosk

**Планируется в `e4d`:**

| Состояние | Словарь |
|---|---|
| `LISTENING` | полный (~350) — **не сужать** |
| `FOUND_PINNED` | полный — **не сужать** |
| `AWAITING_WEIGHT` | ~50 |
| `AWAITING_CHOICE` | 7 |
| `AWAITING_CONTINUE` | ~30 |
| `PAUSED` | 3 |
| `AWAITING_NOT_FOUND_CHOICE` | 5 |

**Пересоздание `Recognizer`** — во время TTS (`setPause(true)`).

## Anti-echo и кулдаун

- Vosk глушится во время TTS, возобновляется через **250 мс** (`e4-fix-2`).
- Окно подавления = `600 мс + 70 мс × длина_фразы`.
- Duplicate window = 600 мс.

## Озвучка

- Скорость TTS: `1.10`.
- Склонения: `1 проба / 2 пробы / 5 проб`.
- Вес: `два и шесть`, `два с половиной`, `полтора`.
- Номера: по группам (`spellOut(groups)`).

## Запрещённые преобразования

- `семья` → НЕ `MarkOrdinal(7)`.
- `это первая проба` → НЕ `MarkOrdinal(1)`.
- `два и шесть` → НЕ `Sort(["2","6"])`.
- Одиночные количественные (`семь`, `двадцать`) в `LISTENING` → НЕ `MarkOrdinal`.
- Фразы со служебными словами → `Unknown`.
- `четвертью` → НЕ `MarkOrdinal(4)`.

## Файлы
data/util/
VoiceController.kt Vosk + TTS обёртка

data/voice/
VoiceCommand.kt sealed class команд
VoiceCommandParser.kt текст → VoiceCommand
VoiceNumberParser.kt русские числительные → кандидаты
VoiceGrammar.kt JSON-грамматика для Vosk
VoiceSpeaker.kt TTS-нормализатор
VoiceSession.kt состояние сессии + PinnedScope + queue
VoiceState.kt enum состояний
VoiceDictionary.kt словари числительных
VoiceOrdinals.kt порядковые
VoiceLetterSounds.kt звук → буква
QueryNormalizer.kt нормализация строки
QueryTokenizer.kt строка → токены
QueryToken.kt типы токенов
DigitGroup.kt группа цифр
DigitGrouper.kt токены → группы
GroupToCandidates.kt группы → кандидаты
SearchService.kt единый сервис поиска
SearchResult.kt результат поиска
Response.kt модель ответа
Presenter.kt рендер Response
UnifiedSearch.kt базовый поиск

ui/screens/
VoiceDialog.kt Compose-панель ГП
ReconciliationViewModel.kt выполнение команд