# ISSUES.md — открытые и закрытые проблемы

> Обновляется по ходу работы. Открытые — сверху.
> Формат: `# И-NNN | проблема | где | приоритет`.

---

## Открытые

| # | Проблема | Где | Приоритет | Заход |
|---|---|---|---|---|
| И-1 | Авто-режим не включается после поиска единственного ответа | `VoiceSession` | 🔴 | 5.8.5 |
| И-2 | Статус-бар не отображается внизу сверки | `SearchScreen.kt` | 🔴 | 5.8.9 |
| И-3 | Голосовой диалог блокирует интерфейс (AlertDialog) | `VoiceDialog.kt` | 🔴 | 5.8.9 |
| И-21 | UI и голосовой поиск используют **разные** алгоритмы — расходятся при выбранных селекторах | `VoiceSearch.kt` / `ReconciliationModels.kt` | 🔴 | 5.8.9h |
| И-4 | Порядковые не работают, пока не включён авто-режим | `VoiceCommandParser.kt` | 🟡 | 5.8.5 |
| И-22 | Экран гаснет при активном ГП — микрофон отключается | `SearchScreen.kt` | 🟡 | 5.8.9e-3 |
| И-23 | Долгая озвучка (сложения, статистика, длинные номера) | `VoiceDialog.kt` | 🟡 | 5.8.9d |
| И-24 | Нет мимикрии произношения (пользователь сказал «два ноля», ГП говорит «двадцать») | `VoiceSession.kt` / `VoiceSpeaker.kt` | 🟡 | 5.8.9g |
| И-25 | Весовой контроль в настройках наряда не применяется к пробам | `ReconciliationState.kt` | 🟡 | 5.8.9bug-3 |
| И-7 | Множественный поиск не группирует по участкам | `ReconciliationState.kt` | 🟢 | 5.11-fix |
| И-8 | Нет звукового сигнала единственного ответа | `SearchScreen.kt` | 🟢 | 5.8.9 |
| И-9 | Онбординг не показывается при первом запуске | `SearchScreen.kt` | 🟢 | 5.8.9 |
| И-10 | `showCharacteristic` сбрасывается после перезапуска | `ReconciliationState.kt` | 🟢 | 5.10 |

---

## В работе

(Пусто)

---

## Закрытые

| # | Проблема | Как решено | Заход |
|---|---|---|---|
| И-0 | Миграция 1→2 в Room | `Migration` + `addMigrations` | 5.5.1 |
| И-00 | Фото не удаляются при удалении пробы | Собираем пути до DELETE | 5.5.1 |
| И-000 | Зависание UI при декодировании фото | `produceState` + `Dispatchers.IO` | 5.5.2 |
| И-5 | Вес не запрашивается для холостых при MANUAL | `VoiceSession.awaitingWeight` + `parseWeightAnswer` | 5.8.8d |
| И-6 | TTS не проговаривает длинные номера по цифрам | `VoiceSpeaker.spellNumber` → пары цифр | 5.8.8d/j |
| И-11 | «1524 и 1525» парсится как `Search`, а не `Sort` | `VoiceCommandParser`: явная проверка по «и» | 5.8.8e |
| И-12 | `VoiceNumberParser` дублирует кандидата: `[1524, 1524]` | `buildCandidates`: `distinct()` | 5.8.8e |
| И-13 | `VoiceSearch` возвращает `FoundOne` при разных нарядах | `FoundMany` при ≥2 уникальных `(orderId, wellNumber)` | 5.8.8f |
| И-14 | Неоднозначный ответ не выделен в UI | Красный ⚠ фонарик, баннер, подписи групп | 5.8.8g |
| И-15 | После неоднозначного ответа ГП продолжает слушать | `VoiceSession.awaitingContinue` | 5.8.8h |
| И-16 | Сортировка не сообщает о неоднозначных номерах | `voiceSort`: перечисляет + автопауза | 5.8.8i |
| И-17 | Поле поиска не заполняется при `FoundMany` | `voiceSearch`: `setQuery(candidate)` | 5.8.8j |
| И-18 | Один результат из другого участка отображается как «несколько нарядов» | `analyzeMatch`: 1 группа → всегда `Unique` | 5.8.8k |
| И-19 | «Один» → 251 наряд. Суффиксный поиск на коротких кандидатах | `VoiceSearch`: суффикс только ≥3 символов; порог 50 хитов | 5.8.9bug-1 |
| И-20 | Голосовой поиск перезаписывает селекторы участка и наряда | `voiceSearch`: НЕ трогаем `selectedArea/Order` | 5.8.9bug-2 |

---

## Подробнее про открытые

### И-21. Рассинхрон UI и голосового поиска

**Суть:** два независимых алгоритма поиска.

- **UI:** `filterByQuery` + `analyzeMatch`. В `filterMode` (выбран участок И наряд) — совпадение по `sample.startsWith(q)`.
- **ГП:** `VoiceSearch` — 5 уровней + fuzzy. Только точное совпадение или суффикс.

**Пример расхождения:** «152» при выбранных селекторах.
- UI: найдёт все пробы, начинающиеся на 152.
- ГП: не найдёт (ни суффикс, ни точное).

**Решение:** унифицировать в одну функцию. Отдельный заход 5.8.9h.

**Сейчас:** фикс 5.8.9bug-2 убрал **главную** причину (перезапись селекторов). Но когда пользователь **сам** выберет селекторы — расхождение вернётся.

### И-22. Экран гаснет при активном ГП

**Суть:** Android гасит экран через ~30 сек. Приложение уходит в фон, микрофон отключается.

**Решение:** `FLAG_KEEP_SCREEN_ON` через `view.keepScreenOn = voiceStatus != Idle`. Пока сессия активна — экран горит.

### И-23. Долгая озвучка

**Суть:** фразы вроде «Скважина пятнадцать двадцать четыре. Наряд семь. Проб три, отмечено ноль. Холостых один.» — 7+ секунд.

**Решение:**
- Режим SORT — короткие фразы без статистики («15 24, наряд 7»).
- Режим SEARCH — только главное.
- `setSpeechRate(1.15f)`.
- Мимикрия произношения (И-24).

### И-24. Мимикрия произношения

**Суть:** пользователь сказал «стодевять два ноля тридцать один» — ГП отвечает «один ноль девять ноль ноль три один».

**Решение:** в `VoiceSession` хранить `Map<normalized, userText>`. При озвучке — если номер в TTS-строке совпадает с сохранённым, использовать вариант пользователя.

**Сброс:** при новой сессии.

### И-25. Весовой контроль в настройках наряда

**Суть:** в `OrderSettingsDialog` параметр `weightControlStep` сохраняется, но не применяется. В `ReconciliationState.applyBlankSettingsForOrder` нет логики простановки `weightControl=true`.

**Правило:** каждая N-я проба в наряде, начиная с первой по п/п, только `normal` (холостые не считаем).

---

## Как добавлять

1. Новая проблема → строка в «Открытые».
2. Присвоить номер `И-NNN` (следующий).
3. Указать где (файл), приоритет, заход.
4. При закрытии → перенести в «Закрытые» с описанием решения.