# ISSUES.md — открытые и закрытые проблемы

> Обновляется по ходу работы. Открытые — сверху.
> Формат: `# И-NNN | проблема | где | приоритет`.

---

## Открытые (баги)

| # | Проблема | Где | Приоритет | Заход |
|---|---|---|---|---|
| И-3 | Голосовой диалог блокирует интерфейс (AlertDialog) | `VoiceDialog.kt` | 🟡 | 5.8.9 |
| И-9 | Онбординг не показывается при первом запуске | `SearchScreen.kt` | 🟢 | 5.8.9 |
| И-10 | `showCharacteristic` сбрасывается после перезапуска | `ReconciliationState.kt` | 🟢 | 5.10 |
| И-21 | UI и голосовой поиск используют разные алгоритмы — расходятся в filterMode | `VoiceSearch.kt` / `ReconciliationModels.kt` | 🔴 | 5.8.9h |
| И-22 | Экран гаснет при активной сессии ГП — микрофон отключается | `SearchScreen.kt` | 🟡 | 5.8.9e-3 |

---

## В планах (фичи / улучшения)

| # | Что | Где | Заход |
|---|---|---|---|
| И-23 | Долгая озвучка (сокращение фраз + скорость) | `VoiceDialog.kt` | 5.8.9d-2, d-3 |
| И-24 | Мимикрия произношения (пользователь сказал «два ноля», ГП — «двадцать») | `VoiceSession.kt` | 5.8.9i |
| И-26 | Индикатор мультипоиска — строки со статусом по каждому запросу | `SearchScreen.kt` | 5.8.9e-2 |
| И-27 | Режимы SORT / SEARCH с раздельной логикой TTS | `VoiceSession.kt` | 5.8.9f |
| И-28 | Отметка короче: «Третья. Отмечено 2 пробы.» | `VoiceDialog.kt` | 5.8.9d-2 |
| И-29 | Переспросы ВК / отложенных («2.5, верно?») | `VoiceSession.kt` | 5.8.9d-3 |

---

## В работе

(Пусто)

---

## Закрытые

| # | Проблема | Как решено | Заход |
|---|---|---|---|
| И-0 | Миграция 1→2 в Room | `Migration` + `addMigrations` | 5.5.1 |
| И-00 | Фото не удаляются при удалении пробы | Собираем пути до DELETE | 5.5.1 |
| И-000 | Зависание UI при декодировании фото | `produceState` + `Dispatchers.IO` | 5.5.2 |
| И-1 | Авто-режим не включался после поиска | Проверено на устройстве — работает | 5.8.5 |
| И-2 | Статус-бар не отображался | Проверено — работает | 5.8.5 |
| И-4 | Порядковые не работали | Проверено — работают | 5.8.5 |
| И-5 | Вес не запрашивался для холостых при MANUAL | `awaitingWeight` + `parseWeightAnswer` | 5.8.8d |
| И-6 | TTS не проговаривал длинные номера по цифрам | `VoiceSpeaker.spellNumber` → пары цифр | 5.8.8d |
| И-7 | Множественный поиск не группировал по участкам | Работает | 5.11 |
| И-8 | Нет звука единственного ответа | Решено: при OK_SINGLE звука нет | 5.8.9c |
| И-11 | «1524 и 1525» парсилось как `Search`, не `Sort` | Явная проверка по «и» | 5.8.8e |
| И-12 | `VoiceNumberParser` дублировал кандидата | `buildCandidates`: `distinct()` | 5.8.8e |
| И-13 | `VoiceSearch` возвращал `FoundOne` при разных нарядах | `FoundMany` при ≥2 уникальных `(orderId, wellNumber)` | 5.8.8f |
| И-14 | Неоднозначный ответ не выделялся в UI | ⚠ баннер, подписи групп | 5.8.8g |
| И-15 | После неоднозначного ответа ГП продолжал слушать | `awaitingContinue` | 5.8.8h |
| И-16 | Сортировка не сообщала о неоднозначных | `voiceSort` + автопауза | 5.8.8i |
| И-17 | Поле поиска не заполнялось при `FoundMany` | `setQuery(candidate)` | 5.8.8j |
| И-18 | Один результат из другого участка — как «несколько нарядов» | `analyzeMatch`: 1 группа → `Unique` | 5.8.8k |
| И-19 | «Один» → 251 наряд. Суффикс на коротких кандидатах | Суффикс ≥ 3; порог 50 | 5.8.9bug-1 |
| И-20 | Голосовой поиск перезаписывал селекторы | `voiceSearch`: не трогаем селекторы | 5.8.9bug-2 |
| И-25 | ВК в настройках наряда не применялся | `applyWeightControlForOrder` | 5.8.9bug-3-fix-1 |
| И-30 | П/п показывался как индекс в списке | `serialNumber` в `SampleRow` из БД | 5.8.9bug-3-fix-2 |
| И-31 | Кнопка сброса ВК в UI отсутствовала | Кнопка + диалог | 5.8.9bug-3-fix-3 |
| И-32 | Undo для настроек наряда не работал | `UndoAction.BulkRowsChange` | 5.8.9bug-3-fix-4 |
| И-33 | «Применить» для холостых и ВК — один | Раздельные кнопки | 5.8.9bug-3-fix-5 |
| И-34 | «Третья» не парсилась (форма «третьая») | Явные формы в `VoiceOrdinals` | 5.8.9e-5 |
| И-35 | `Message` с полным номером уходило в TTS как есть | `spellOut` в `Message` | 5.8.9e-5 |
| И-36 | Сессия не сбрасывалась при `NotFound` | `voiceSession.clear()` | 5.8.9g-3 |
| И-37 | «Все / отметь все» подменялись Vosk («семь» / «отменить») | Явные слова в грамматике | 5.8.9g-3 |

---

## Подробнее про открытые

### И-3. Блокирующий диалог

`VoiceDialog` — `AlertDialog`. Перекрывает таблицу проб. Заменить на
неблокирующий `VoiceOverlay`.

### И-21. Рассинхрон UI и голосового поиска

**Суть:** два независимых алгоритма поиска.

- UI: `filterByQuery` + `analyzeMatch`. В `filterMode` (выбран
  участок И наряд) — `sample.startsWith(q)`.
- ГП: `VoiceSearch` — 5 уровней + fuzzy. Только точное совпадение
  или суффикс.

**Пример расхождения:** «152» при выбранных селекторах.
- UI: найдёт все пробы, начинающиеся на 152.
- ГП: не найдёт (ни суффикс, ни точное).

**Решение:** унифицировать. Отдельный заход 5.8.9h.

### И-22. Экран гаснет при активном ГП

Android гасит экран через ~30 сек. Приложение уходит в фон, микрофон
отключается.

**Решение:** `view.keepScreenOn = voiceStatus != Idle`.

### И-23. Долгая озвучка (остаток)

Частично закрыто в 5.8.8d/j (TTS по парам). Осталось:
- Режим SORT — короткие фразы без статистики.
- Скорость TTS — `setSpeechRate(1.15f)`.
- Мимикрия (И-24).

### И-24. Мимикрия произношения

**Суть:** пользователь сказал «стодевять два ноля тридцать один» —
ГП отвечает «один ноль девять ноль ноль три один».

**Решение:** в `VoiceSession` хранить `Map<normalized, userText>`.
При озвучке — если номер совпадает, использовать вариант
пользователя. Сброс при новой сессии.

---

## Как добавлять

1. Новая проблема → строка в «Открытые».
2. Присвоить номер `И-NNN`.
3. Указать где, приоритет, заход.
4. При закрытии → перенести в «Закрытые» с решением.