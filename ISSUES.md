# ISSUES.md — проблемы

## Открытые

### И-35. Vosk путает «четвёртая» → «четырнадцатая»
**Статус:** 🟡 открыт, ждёт `e4d`.
**Симптом:** в `FOUND_PINNED` на слово «четвёртая» Vosk стабильно выдаёт
«четырнадцатая». Логи: `text=«четырнадцатая»` → `MarkOrdinal(14)`.
**Причина:** у порядковых «четвёрт-ая» и «четырнадцат-ая» общий корень
«четыр» + общий суффикс «-ая». Модель `vosk-model-small-ru-0.22` их не
различает фонетически.
**Что делать:** `e4d` — динамическая грамматика Vosk. В `FOUND_PINNED`
убрать порядковые из словаря → Vosk выдаст количественное или ближайшее.
**Временно:** честная ошибка + подсказка (см. `e4-pin-7`). Пользователь
уточняет числом: «Пробы №14 нет. Если нужна №4 — произнесите „четыре"».
**Файлы:** `data/voice/VoiceGrammar.kt`, `data/util/VoiceController.kt`.

### И-24. Vosk обрывает длинные номера
**Статус:** 🟡 открыт, нужна разведка API.
**Симптом:** `KPD1090031`, `15 24 01 2 ноля 5` обрываются после первых цифр.
**Причина:** endpoint Vosk срабатывает по короткой паузе.
**Стоп-сигнал:** в `vosk-android:0.3.47` нет `setEndpointerMode`.
**Что делать:** разведка в AS, варианты — обновление до 0.3.50+,
пост-обработка в `VoiceCallback`.
**Файл:** `data/util/VoiceController.kt`.

### И-23. Долгая озвучка, мимикрия
**Статус:** 🟢 частично решено в `5.8.9i` и `e4e-bundle`. Скорость TTS
1.10, склонения, `spokenWeight()`, `spellOut(groups)`.

### И-36. Приоритеты поиска в выбранном наряде
**Статус:** 🟡 открыт, ждёт `e4-search-context`.
**Симптом:** при выбранном наряде сначала ищется точное по всей БД,
потом префикс скважины, потом номер пробы. При совпадении 2 и 3 —
приоритет скважине.
**Что делать:** три параллельные проверки в `SearchService`.
**Файлы:** `data/voice/SearchService.kt`, `data/voice/UnifiedSearch.kt`.

### И-37. Confidence threshold Vosk
**Статус:** 🟡 открыт, нужно проверить `build.gradle`.
**Симптом:** галлюцинации Vosk в `LISTENING` на шум.
**Что делать:** проверить, доступен ли `setWords` / `SetLogLevel`
через `vosk-android`.

---

## Решённые в этой сессии

### И-34. Наряд не находится по orderTitle при коллизии
**Статус:** 🟢 решено в `5.8.10-e`, ✅ проверено на устройстве.

### И-33. Все листы файла → один наряд
**Статус:** 🟢 решено в `5.8.10-f`, ✅ проверено на устройстве.

### И-32. Импорт находит «несуществующий» наряд
**Статус:** 🟢 решено в `5.8.10-d`, 🟡 требует device-check.

### И-25. `назад` не обновляет UI
**Статус:** 🟢 решено в `5.8.6-5f`, 🟡 требует device-check.

### И-31. «Пять четвертых» → Search("5")
**Статус:** 🟢 решено в `5.8.6-5g`, 🟡 требует device-check.

### И-10. `showCharacteristic` сбрасывается после перезапуска
**Статус:** 🟢 решено в `5.8.10-a`, 🟡 требует device-check.

### И-9. Онбординг не показывается
**Статус:** 🟢 решено в `5.8.10-b`, 🟡 требует device-check.

### И-26. Ложные отметки по похожим словам
**Статус:** 🟢 решено unit-тестами в `5.8.6-5a`, 🟡 требует device-check.

### И-27. Команды уходят в поиск
**Статус:** 🟢 решено unit-тестами в `5.8.6-3`, 🟡 требует device-check.

### И-28. `отмена` не работала несколько раз подряд
**Статус:** 🟢 решено в `5.8.6-5c`.

### И-29. Нет звука при снятии пробы
**Статус:** 🟢 решено в `5.8.6-5c`.

### И-30. «15 24 01» → «15 20 01»
**Статус:** 🟢 решено в `5.8.6-2`.

### И-3. Голосовой диалог блокирует UI
**Статус:** 🟢 решено в `5.8.10-g1` + `5.8.10-g2`, ✅ проверено на устройстве.