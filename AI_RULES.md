# Правила работы ИИ над GeoSample Manager

> **ЧИТАЙ ПЕРВЫМ.** Этот файл описывает, как ИИ должен отвечать, форматировать
> код, что запрашивать у пользователя и куда смотреть за контекстом.
>
> Соблюдение обязательно. Единый формат — чтобы при смене чата новый ИИ
> сразу понял правила и продолжил работу без переобъяснений.

**Репозиторий:** https://github.com/ilia23star2-ops/GeoSampleManager

---

## 0. Быстрый старт в новом чате

Если ты — ИИ, открывший проект впервые:

1. **Прочитай эти файлы** (в порядке приоритета):
    - `AI_RULES.md` (этот) — как работать.
    - `CONTEXT_BRIEF.md` — **где мы сейчас** (обязательно).
    - `README.md` — где что лежит.
    - `PROGRESS.md` — что сделано и что в работе.
    - `NEXT_STEPS.md` — текущий заход и план.
    - `DECISIONS.md` — все решения по UI и логике.
    - `DATABASE.md` — схема БД.
    - `ROADMAP.md` — план до релиза.
    - `VOICE.md` — спецификация ГП (если работаешь над голосом).
    - `ISSUES.md` — открытые/закрытые проблемы.
    - `GLOSSARY.md` — термины проекта.
    - `TESTING.md` — что и как тестировать.

2. **Спроси пользователя: где он сейчас — дома или на работе?**
    От ответа зависит формат раздела «Как проверить» (§5) и то, что можно
    просить у пользователя (§20).
    - **Дома** — можно просить сборку, тесты, эмулятор, реальное устройство,
      логи. Режим А, §20.2.
    - **На работе** — только правки, коммит, CI. Режимы Б и В, §20.3–20.4.

3. **Дождись команды от пользователя**: обычно это «продолжаем с этапа N»,
   «делаем заход X.Y» или «приступаем».

4. **Перескажи понимание** в 3–5 строках: где мы, что делаем, что уже есть.
   Только после этого начинай код.

---

## 1. Стиль общения

- **Язык — русский.** Комментарии в коде — русские. UI — русский.
- **Без воды.** Не повторяй, что сказал пользователь. Не пиши «отличный
  вопрос» и «давайте разберёмся».
- **Коротко и по делу.** Абзацы — 2–4 строки. Списки — там, где они уместны.
- **Уважительно и по-рабочему.** Пользователь — разработчик, не нуждается в
  разжёвывании очевидного.
- **Не извиняйся без причины.** Ошибся — исправил, коротко отметил.
- **Не спрашивай разрешения на очевидное.** Если нужно уточнить — задай
  конкретный вопрос с вариантами А/Б/В.

---

## 2. Формат ответа на заход кода

### 2.1. Структура ответа

**Ответ состоит из четырёх частей, строго в этом порядке:**

1. **Блок «Как начать заход»** — что делать с веткой (см. §2.5).
2. **Файлы** — по одному блоку на файл (§2.2).
3. **Раздел «Как проверить»** — обязателен (§5).
4. **Напоминание про репозиторий и коммит** (§7.3).

### 2.2. Формат файла

Каждый файл — **отдельным блоком**. Строгий шаблон:
X/Y. <Заменить полностью> или <Новый файл> — <ИмяФайла.kt>
<если это не первый файл в заходе, короткий комментарий — зачем он>

<Заменить полностью> или <Новый файл>

text
полный/путь/к/файлу/ИмяФайла.kt
kotlin
// полный код файла, без сокращений
text

### 2.3. Обязательные элементы файла

1. **Заголовок файла в формате `X/Y`:** `X` — номер, `Y` — всего.
2. **Пометка «Заменить полностью»** или **«Новый файл»** — **жирным**.
3. **Путь в текстовом блоке** — полный путь от корня проекта.
4. **Полный код** в блоке ` ```kotlin `. **Никаких «…» и «остальное как
   было»**.
5. **После всех файлов — раздел «Как проверить»**.

### 2.4. Правило «X/Y» в шапке файла

**Всегда** пиши `X/Y`, даже если файл один: `1/1. Замена — ROADMAP.md`.

### 2.5. Блок «Как начать заход» — обязателен

**Перед первым файлом** — отдельный блок. Формат:
Как начать заход X.Y
Ветка: <feature|fix|docs>/X.Y-краткое
От: main
Тип: <фича | фикс | документация>
Коммит (AS / github.dev): X.Y — краткое описание
Коммиты (github.com): X.Y/1 — Имя1, X.Y/2 — Имя2, ...

text

**Ниже — пошаговая инструкция по режиму (§20):**

**Для дома (Android Studio):**
git fetch --all

git checkout main

git pull

git checkout -b <имя ветки>

text

**Для работы `github.dev`:**
Открыть репозиторий на github.com → нажать ..

Внизу слева кнопка main → Create new branch → ввести имя.

text

**Для работы `github.com`:**
Репозиторий → кнопка main ▾ → поле ввода → Create branch.

text

**Выбор имени ветки:**

| Заход | Префикс | Пример |
|---|---|---|
| Новая функциональность | `feature/` | `feature/5.8.9h-2-unified-search` |
| Исправление бага | `fix/` | `fix/5.8.9-infra-2a-ci-sdk` |
| Документация / правила | `docs/` | `docs/5.8.9-infra-2b-rules` |

**Правила:**
- Одна задача — одна ветка.
- От `main`, если не сказано иное.
- После PR и merge — ветку удалить.

### 2.6. Коммит

**Один заход = один коммит.** Формат строки:
<номер захода> — <краткое описание>

text

Примеры:
- `5.8.9h-2 — UnifiedSearch в UI и ГП`
- `5.8.9-infra-2b — правила: три режима работы`

**Разные режимы — разный способ коммита:**

| Режим | Способ |
|---|---|
| Дома (Android Studio) | Все файлы захода — один коммит. |
| На работе `github.dev` | Все файлы захода — один коммит (приоритет). |
| На работе `github.com` | Коммит под каждым файлом. |

**Способ коммита под каждый файл (для `github.com`):**
<номер захода>/<N> — <ИмяФайла>

text

Пример для захода из трёх файлов:
- `5.8.9-infra-2b/1 — AI_RULES.md`
- `5.8.9-infra-2b/2 — README.md`
- `5.8.9-infra-2b/3 — CONTEXT_BRIEF.md`

---

## 3. Что запрашивать у пользователя

**Если для работы не хватает контекста — попроси файлы.** Не догадывайся.

### Формат запроса — группировка по папкам
Нужны файлы:

ui/screens/
app/src/main/java/com/example/geosamplemanager/ui/screens/SearchScreen.kt
app/src/main/java/com/example/geosamplemanager/ui/screens/ReconciliationViewModel.kt

data/voice/
app/src/main/java/com/example/geosamplemanager/data/voice/VoiceController.kt

корень проекта
app/build.gradle
app/src/main/AndroidManifest.xml

text

**Правила:**
- Группируй по папке.
- Полный путь от корня.
- Только нужные файлы.
- Если файл уже был прислан в этом чате — не проси повторно.
- Пометь, зачем каждый файл — таблицей.

---

## 4. Правила кода

### 4.1. Стиль
- Kotlin, Jetpack Compose, Room.
- Комментарии — на русском.
- Имена классов/функций/переменных — английские.
- Отступ — 4 пробела.
- Максимум 100 символов в строке.

### 4.2. Структура
- Пакеты — `com.example.geosamplemanager.<слой>`.
- `data/`, `data/entity/`, `data/dao/`, `data/util/`, `data/voice/`.
- `ui/screens/`, `ui/navigation/`, `ui/theme/`.
- Никаких файлов «всё в одном».

### 4.3. Работа с БД
- Все изменения схемы — только через миграции.
- Перед изменением сущности — сверить с `DATABASE.md`.
- После изменения — предупредить и обновить `DATABASE.md`.

### 4.4. Асинхронность
- Все I/O — в `Dispatchers.IO`.
- Обновление UI — через `withContext(Dispatchers.Main)`.

### 4.5. Работа с файлами
- Фото — `filesDir/sample_photos/`.
- БД — `getDatabasePath()`.
- Настройки — `filesDir/*.json`.

### 4.6. Обработка ошибок
- Ловить исключения в каждом I/O-блоке.
- Сообщение — в `_message.value`.
- Не «глотать» ошибки молча.

---

## 5. Формат раздела «Как проверить»

**Формат зависит от режима (§20).**

### 5.1. Дома (IDE) — стандартный
✅ Как проверить заход X.Y

Сборка
Что нажать, что должно быть.

Запуск
Что должно открыться.

<Сценарий 1>
Пошаговые действия.
Ожидаемое поведение.

...

Если что-то падает
Куда смотреть, что прислать.

text

### 5.2. На работе — `github.dev` (приоритет)
✅ Как проверить заход X.Y (github.dev)

Создание ветки
Ветка: feature/xxx.

Правки
Файлы: N штук, перечислены.

Коммит
Один коммит: X.Y — описание.

Пуш
Commit & Push в github.dev.

PR
Открыть PR в main.

CI
Вкладка Actions → запуск Build & Test → зелёный.

Merge
Кнопка Merge pull request, потом Delete branch.

Если что-то падает
Полный текст ошибки — в чат.

text

### 5.3. На работе — `github.com` (fallback)
✅ Как проверить заход X.Y (github.com)

Создание ветки
На главной репозитория: main ▾ → Create branch: feature/xxx.

Правки
Файл 1: открыть, ✏️, изменить, Commit changes → «X.Y/1 — ИмяФайла».
Файл 2: ... → «X.Y/2 — ИмяФайла».

PR
Compare & pull request → Create pull request.

CI
Вкладка Actions → запуск Build & Test → зелёный.

Merge
Кнопка Merge pull request → Confirm merge → Delete branch.

Если что-то падает
Полный текст ошибки — в чат.

text

**Правила:**
- Конкретные действия и ожидания.
- Простой язык.
- Без adb и SQL, если не критично.

---

## 6. Что делать при ошибках

### 6.1. Сборка упала
- Попросить **полный текст ошибки** (не скриншот).
- Найти причину, исправить, выдать **полный файл** заново.

### 6.2. Логика не работает
- Попросить что происходит vs что ожидалось.
- Уточнить: что нажал, что увидел, что в логах.

### 6.3. Миграция БД упала
- Сказать снести приложение и запустить заново.
- Исправить `Migration`.

### 6.4. CI упал
- Попросить **полный лог** (не скриншот).
- Разобрать по шагам: `Setup Android SDK`, `Run unit tests`,
  `Build Debug APK`. Указать, где именно упало.

---

## 7. Правила по документации

### 7.1. Когда обновлять
- После каждого закрытого захода — `PROGRESS.md`, `NEXT_STEPS.md`,
  `CONTEXT_BRIEF.md`.
- Решения — в `DECISIONS.md`.
- Изменения БД — в `DATABASE.md`.
- Изменения ГП — в `VOICE.md`.
- Изменения правил работы — в `AI_RULES.md`, `README.md`.

### 7.2. Как обновлять
- Файл целиком, не фрагмент.
- Пометка «Заменить полностью» — жирным.
- Путь в текстовом блоке.
- Шапка `X/Y`.

### 7.3. Напоминание про репозиторий

**После каждого закрытого захода**, когда код прошёл и документация
обновлена, добавь **в самом конце ответа** напоминание:
Заход X.Y закрыт. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Коммит: X.Y — краткое описание

text

Для режима `github.com` — перечислить все коммиты по файлам.

---

## 8. Принципы обсуждения

### 8.1. Перед кодом — обсуждение
Если задача сложная: сначала спецификация, потом код.

### 8.2. Не пиши код, если:
- Нет полного понимания задачи.
- Пользователь задал вопрос «как лучше?».
- Есть открытые вопросы по архитектуре.

### 8.3. Аудит
Перед крупным блоком — аудит: каталог команд, конфликты, вопросы,
проблемы.

---

## 9. Приоритеты

1. Работающий код > красивый.
2. Простое > сложное.
3. Понятный > лаконичный.
4. Документация > код без документации.

---

## 10. Что категорически нельзя

- Обрезать код («…», «остальное как было»).
- Угадывать содержимое файлов.
- Менять схему БД без миграции.
- Использовать Apache POI.
- Писать комментарии на английском.
- Забывать раздел «Как проверить».
- Отвечать без номера файла.
- Забывать шапку `X/Y`.
- Забывать ссылку на репозиторий.
- **Забывать блок «Как начать заход»** (имя ветки, коммит).
- **Работать напрямую в `main` без ветки.**

---

## 11. Шаблон ответа на заход кода
Файлов: 3

Как начать заход 5.8.9-infra-2b
Ветка: docs/5.8.9-infra-2b-rules
От: main
Тип: документация
Коммит (AS / github.dev): 5.8.9-infra-2b — правила: три режима работы
Коммиты (github.com): 5.8.9-infra-2b/1 — AI_RULES.md,
5.8.9-infra-2b/2 — README.md, 5.8.9-infra-2b/3 — CONTEXT_BRIEF.md

Если ты дома (Android Studio):

git fetch --all

git checkout main

git pull

git checkout -b docs/5.8.9-infra-2b-rules

Если ты на работе (github.dev):

Открыть репозиторий → нажать ..

Внизу слева кнопка main → Create new branch →
ввести docs/5.8.9-infra-2b-rules.

Если ты на работе (github.com):

Репозиторий → main ▾ → поле ввода → Create branch.

1/3. Заменить полностью — AI_RULES.md
<Короткий комментарий>

Заменить полностью

text
AI_RULES.md
markdown
<полный код>
...

✅ Как проверить заход 5.8.9-infra-2b

...

Заход 5.8.9-infra-2b закрыт. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Коммит: 5.8.9-infra-2b — правила: три режима работы

text

---

## 12. Шаблон ответа на вопрос (без кода)

1. Короткий ответ — 1–2 строки.
2. Разбор по пунктам.
3. Рекомендация.
4. Вопросы.

Не пиши код в ответ на вопрос. Сначала обсудите.

---

## 13. Что делать при смене чата

Пользователь приложит файлы из §0. Ты:

1. Прочитай все.
2. Перескажи контекст — 5–8 строк.
3. **Спроси: где работает пользователь — дома или на работе?**
4. Ждать команду. Не начинай код сам.

---

## 14. Чек-лист перед отправкой ответа

- [ ] Первая строка — «Файлов: X/Y» (для кода).
- [ ] **Блок «Как начать заход» — есть.**
- [ ] **Имя ветки и коммит указаны.**
- [ ] У каждого файла шапка `X/Y`.
- [ ] Полный путь в текстовом блоке.
- [ ] Код полный, без «…».
- [ ] Комментарии на русском.
- [ ] Раздел «Как проверить» — есть.
- [ ] Формат «Как проверить» соответствует режиму (§5).
- [ ] Связанные документы обновлены.
- [ ] `CONTEXT_BRIEF.md` обновлён.
- [ ] Напоминание про репозиторий и строка коммита.
- [ ] Если просил файлы — сгруппированы по папкам.
- [ ] Если режим `github.com` — указано, как коммитить по файлам.

---

## 15. Полезные файлы проекта

| Файл | Когда нужен |
|---|---|
| `README.md` | Структура проекта |
| `CONTEXT_BRIEF.md` | Где мы сейчас |
| `PROGRESS.md` | История |
| `NEXT_STEPS.md` | Текущий заход |
| `DECISIONS.md` | Логика UI |
| `DATABASE.md` | Схема БД |
| `ROADMAP.md` | Приоритеты |
| `VOICE.md` | ГП |
| `ISSUES.md` | Проблемы |
| `GLOSSARY.md` | Термины |
| `TESTING.md` | Тесты |
| `AI_RULES.md` | Этот файл |

---

## 16. Финальное правило

**Пользователь — не робот.** Он устаёт, отвлекается, ошибается.
Твоя задача — **сокращать его работу**.

Каждый твой ответ должен:
- Не заставлять переспрашивать.
- Не заставлять копировать по частям.
- Не заставлять догадываться, что проверять.
- Экономить время.

Если сомневаешься между «быстро» и «понятно» — **делай понятно**.

---

## 17. Стоп-сигналы — когда ИИ обязан остановиться

**Если задача попадает хотя бы в один пункт — ИИ не пишет код.
Сначала спрашивает.**

1. **Задача затрагивает >3 файлов.** Спросить: «Разбить на части?».
2. **Задача меняет схему БД.** Спросить: «Какая миграция?».
3. **Задача меняет публичную сигнатуру.** Спросить: «Кто ещё
   использует?».
4. **Задача противоречит `CONTEXT_BRIEF.md`.** Сказать прямо.
5. **Задача сформулирована словами** «улучшить», «оптимизировать»,
   «починить». Спросить: «Что конкретно?».
6. **Нужно удалить код.** Спросить подтверждение.
7. **Правка каскадит** — тянет ещё 2+ файла. Остановиться.
8. **Задача похожа на уже сделанную.** Спросить: «Повторяем?».
9. **В чате >30 сообщений.** Предложить обновить `CONTEXT_BRIEF.md`.
10. **Не понятно, что надо.** Задать уточняющий вопрос.

### Формат вопроса при стоп-сигнале
Стоп-сигнал: <причина>.

Варианты:
А) <сделать как есть>
Б) <разбить на части>
В) <отложить>
Г) <свой вариант>

text

---

## 18. Атомарность заходов

**Правило:** один заход — **1–3 файла**.

- Если файлов больше 3 — разбить.
- Если один файл >500 строк — разбить на классы.
- Если заход смешивает UI и БД — разбить.

**Почему:** меньше файлов = меньше потерь, легко откатить, легко
проверить.

---

## 19. Откат по умолчанию

**Если после правки что-то сломалось — откатить, не чинить.**

### Как
1. Перед заходом — коммит: `before X.Y`.
2. Если сломалось — откат через VS Code / AS / `github.dev` (Discard
   Changes или Revert).
3. Вернуться к обсуждению.

### Что НЕ считается поломкой
- Мелкая ошибка в строке.
- Опечатка в комментарии.
- Отсутствующая зависимость в gradle.

### Поломка
- Приложение не запускается.
- Рабочий сценарий сломался.
- БД падает при миграции.
- Новая ошибка в логах.

---

## 20. Работа с двух машин — три режима

**Контекст:** проект разрабатывается на **двух машинах**:

- **Дома** — Android Studio, полный цикл (сборка, тесты, эмулятор).
- **На работе** — без IDE. **Приоритет:** `github.dev`. **Fallback:**
  `github.com`. Git на машине не установлен, сборка APK локально
  невозможна.

### 20.1. Как ИИ определяет режим

1. При старте сессии — **спрашивает: «где ты — дома или на работе?»**.
2. Если пользователь уже сказал — ИИ сохраняет это до конца сессии.
3. Если пользователь **перешёл с работы домой** — говорит «я дома».
   ИИ запускает **чек-лист §22** и работает в Режиме А.

### 20.2. Режим А — дома (Android Studio)

1. **Открыть Android Studio**, открыть проект.
2. **`git pull`** — подтянуть изменения с GitHub.
3. **Создать feature-ветку**: `git checkout -b feature/X.Y-...`.
4. **Правки** в IDE.
5. **Локальная проверка**: `Build → Make Project`, юнит-тесты
   (`Ctrl+Shift+F10` на тест-классе), эмулятор, реальное устройство,
   логи.
6. **Коммит + пуш** — все файлы захода одним коммитом.
7. **CI** — на GitHub, вкладка Actions.
8. **PR + merge** — через браузер (github.com) или Android Studio.
9. **Удалить ветку** после merge.

**Что можно просить у пользователя:** сборку, тесты, эмулятор,
устройство, логи, скриншоты.

### 20.3. Режим Б — на работе, `github.dev` (приоритет)

1. Открыть `https://github.com/ilia23star2-ops/GeoSampleManager`.
2. Нажать `.` (точка). Откроется `github.dev`.
3. **Создать feature-ветку**: внизу слева — имя ветки → Create new
   branch → `feature/X.Y-...`.
4. **Правки** в редакторе.
5. **Коммит + пуш**: левая панель → Source Control (`Ctrl+Shift+G`) →
   сообщение коммита → **Commit & Push**. **Один коммит на все файлы
   захода.**
6. **PR** — GitHub предложит баннер `Compare & pull request` или через
   вкладку Pull requests.
7. **CI** — вкладка Actions.
8. **Merge** — кнопка Merge pull request → Confirm merge.
9. **Удалить ветку** — Delete branch.

**Что нельзя просить у пользователя:** сборку, тесты, эмулятор,
устройство.

### 20.4. Режим В — на работе, `github.com` (fallback)

Используется, если `github.dev` недоступен (корпоративные блокировки,
медленный браузер, лимит по памяти).

1. Открыть репозиторий на github.com.
2. **Создать feature-ветку**: слева кнопка `main ▾` → поле ввода →
   `feature/X.Y-...` → Create branch.
3. **Правки по файлам:**
    - Файл 1: открыть → ✏️ → правки → Commit changes →
      `<X.Y>/1 — ИмяФайла.kt`.
    - Файл 2: ... → `<X.Y>/2 — ИмяФайла.kt`.
    - И так для каждого файла. **Коммит под каждым файлом.**
4. **PR** — Compare & pull request → Create pull request.
5. **CI** — вкладка Actions.
6. **Merge** — Merge pull request → Confirm merge.
7. **Удалить ветку** — Delete branch.

**Что нельзя просить у пользователя:** сборку, тесты, эмулятор,
устройство.

### 20.5. Правила гигиены

- **Всегда `pull` перед началом.** Иначе — конфликт.
- **Никогда не работать напрямую в `main`.** Только через feature-ветку.
- **Одна задача = одна ветка.** Не мешать.
- **Коммит после каждого захода.**
- **Пуш в конце сессии.** Даже если задача не закончена.
- **После merge — удалить ветку.**

### 20.6. Что ИИ должен учитывать

- **В рабочем чате (без IDE):** не просить «собери и пришли лог».
  Только правки, коммит, CI. Если CI упал — попросить полный лог из
  Actions.
- **В домашнем чате (с IDE):** можно просить сборку, тесты, логи,
  скриншоты.
- **Формат ответа — единый.** Файлы целиком, шапка `X/Y`, «Как проверить»
  по §5.
- **Уточнять, где работает пользователь**, если из контекста неясно.

### 20.7. Ветки

**Схема:**
- `main` — стабильная ветка. Всё, что в ней — считается рабочим.
- `feature/<заход>-<краткое>` — отдельная ветка под задачу.
- `fix/<заход>-<краткое>` — отдельная ветка под баг.
- `docs/<заход>-<краткое>` — отдельная ветка под документацию.

**Примеры имён:**
- `feature/5.8.9h-2-unified-search`
- `fix/5.8.9-infra-2a-ci-sdk`
- `docs/5.8.9-infra-2b-rules`

После PR и merge — ветка удаляется.

### 20.8. CI как вторая проверка

**Во всех режимах** (А, Б, В) после пуша запускается GitHub Actions:
- **Setup Android SDK.**
- **Run unit tests** (`./gradlew testDebugUnitTest`).
- **Build Debug APK** (`./gradlew assembleDebug`).
- Артефакты: `app-debug` (APK), `test-report` (HTML с результатами
  тестов).

**Что делать при красном CI:**
1. Открыть вкладку Actions.
2. Нажать на упавший запуск.
3. Найти шаг с красным крестом.
4. Скопировать полный текст ошибки → в чат.
5. ИИ разбирает и даёт правку.

**Merge блокируется**, пока CI красный.

**Vosk-модель** не коммитится в Git. В CI она выкачивается из
релиза GitHub (`models-v1`) отдельным шагом в `build.yml`.

---

## 21. Коммит и ветки — сводка

| Режим | Формат коммита | Всего коммитов на заход |
|---|---|---|
| Дома (AS) | `X.Y — краткое описание` | 1 |
| github.dev | `X.Y — краткое описание` | 1 |
| github.com | `X.Y/<N> — ИмяФайла` | по числу файлов |

**Напоминание о коммите** — в конце ответа (см. §7.3). Для `github.com`
— перечислить все коммиты по файлам.

---

## 22. Чек-лист перехода «работа → дом»

**Когда:** пользователь говорит «я дома» после сессии на работе.

**Что делает ИИ:**

1. **Подтверждает смену режима:** «Понял, ты дома. Режим А — IDE.
   Дальше можно просить сборку, тесты, логи.»
2. **Даёт пошаговый чек-лист для пользователя:**

    **Шаг 1. Открыть Android Studio.**
    Дождаться загрузки проекта.

    **Шаг 2. Синхронизировать с GitHub.**
git fetch --all
git checkout main
git pull

text
Если работа шла в feature-ветке — переключиться на неё:
git checkout feature/X.Y-...
git pull

text

**Шаг 3. Собрать проект.**
`Build → Make Project`.
Ожидаемо: `BUILD SUCCESSFUL`.

**Шаг 4. Прогнать юнит-тесты.**
ПКМ по `app/src/test/` → `Run 'Tests in …'`.
Ожидаемо: все зелёные.

**Шаг 5. Запустить на эмуляторе или устройстве.**
`Run → Run 'app'` → выбрать эмулятор или устройство.

**Шаг 6. Прогнать сценарии.**
ИИ даёт список: что открыть, что нажать, что ожидать.
Пользователь проходит, отвечает «ОК» / «не работает».

**Шаг 7. Если всё ок — merge PR.**
Через браузер: PR → Merge pull request → Delete branch.
Или через Android Studio: Git → Pull, потом Merge.

**Шаг 8. Если что-то не так — разбор.**
Прислать скриншот / лог → ИИ правит → правки → повторить.

3. **Составляет список проверки** на основе того, что делалось на
работе:
- Какие файлы менялись.
- Какие сценарии должны быть проверены.
- Что ожидать.

4. **Ждёт отчёта** пользователя. Если всё зелёное — merge PR.
Если нет — разбираем.

---

## 23. Финальное правило (повтор)

**Пользователь — не робот.** Он устаёт, отвлекается, ошибается.
Твоя задача — **сокращать его работу**.

Каждый твой ответ должен:
- Не заставлять переспрашивать.
- Не заставлять копировать по частям.
- Не заставлять догадываться, что проверять.
- Экономить время.

Если сомневаешься между «быстро» и «понятно» — **делай понятно**.