# Правила работы ИИ над GeoSample Manager

> **ЧИТАЙ ПЕРВЫМ.** Этот файл описывает, как ИИ должен отвечать, форматировать
> код, что запрашивать у пользователя и куда смотреть за контекстом.
>
> Соблюдение обязательно. Единый формат — чтобы при смене чата новый ИИ
> сразу понял правила и продолжил работу без переобъяснений.

**Репозиторий:** https://github.com/ilia23star2-ops/GeoSampleManager

---

## 0. Быстрый старт в новом чате

Если ты — ИИ, открывший проект впервые:

1. **Прочитай эти файлы** (в порядке приоритета):
   - `AI_RULES.md` (этот) — как работать.
   - `CONTEXT_BRIEF.md` — **где мы сейчас**.
   - `README.md` — где что лежит.
   - `PROGRESS.md` — что сделано.
   - `NEXT_STEPS.md` — текущий заход.
   - `DECISIONS.md` — решения.
   - `DATABASE.md` — схема БД.
   - `ROADMAP.md` — план до релиза.
   - `VOICE.md` — спецификация ГП.
   - `ISSUES.md` — проблемы.
   - `GLOSSARY.md` — термины.
   - `TESTING.md` — тесты.

2. **Задай два вопроса — до начала работы:**

   **Вопрос 1.** «Ты сейчас дома или на работе?»
   - **Дома** → Режим А (§20.1). Android Studio.
   - **На работе** → вопрос 2.

   **Вопрос 2 (только если на работе).** «Через `github.dev` или
   `github.com`?»
   - **`github.dev`** → Режим Б (§20.2).
   - **`github.com`** → Режим В (§20.3). Fallback.

3. **Дождись команды:** «продолжаем с этапа N», «делаем пачку X», «начинаем фичу Y».

4. **Перескажи понимание** в 3–5 строках: где мы, что делаем, что уже
   есть. Только после этого начинай код.

**Режим сохраняется до конца чата.** Если пользователь перешёл с работы
домой — он говорит «я дома», ИИ переходит в Режим А.

---

## 1. Стиль общения

### 1.1. Общие правила

- **Язык — русский.** Комментарии в коде — русские. UI — русский.
- **Без воды.** Не повторяй, что сказал пользователь. Не пиши «отличный
  вопрос», «давайте разберёмся».
- **Коротко и по делу.** Абзацы — 2–4 строки.
- **Уважительно и по-рабочему.**
- **Не извиняйся без причины.** Ошибся — исправил, коротко отметил.
- **Не спрашивай разрешения на очевидное.** Если уточняешь — задай
  конкретный вопрос с вариантами А/Б/В.

### 1.2. Комментарии к действиям — разделение

| Тип действия | Комментарий |
|---|---|
| **Замена файла в IDE** | **Не писать.** Пользователь знает: `Ctrl+A` → `Delete` → `Ctrl+V` → `Ctrl+S`. |
| **Сборка / запуск** | **Коротко.** «`Build → Make Project`. Ожидаемо: `BUILD SUCCESSFUL`.» |
| **Git-операции в AS** | **Подробно, пошагово.** Куда нажать, что выбрать, что ожидать. См. §2.4. |
| **Git-операции в Terminal** | **Пошагово.** Одна команда — одно действие. |
| **Что-то новое в интерфейсе** | **Подробно.** |

---

## 2. Формат ответа на заход кода

### 2.1. Структура ответа — пять частей

1. **Шапка захода** (§2.2).
2. **Файлы кода** — по одному блоку на файл (§2.3).
3. **Файлы тестов** — по одному блоку на файл (§2.3).
4. **Раздел «Что проверить вручную»** (§5).
5. **Напоминание про репозиторий и коммит** (§7.3).

**Структура ответа — с git-шагами по ходу.** Git-подсказки встроены
в шапку и в раздел «Что проверить вручную» — в тех местах, где они
логически нужны. См. §2.2.

### 2.2. Шапка захода — короткая + git-шаги по ходу

**Формат шапки:**

```
**Файлов кода:** N
**Файлов тестов:** M

**Пачка:** X.Y-bundle — <краткое название>     ← или «Заход: X.Y — ...»
**Фича:** feature/X.Y-xxx                       ← или «нет (docs)»
**Режим:** дома (Android Studio)
```

**Дальше — git-шаги**, каждый с подсказкой `>` в одну строку. Подсказки
**только для текущего режима**, второй режим **не показывается**.

**Порядок шагов — по логике процесса:**

1. 🐙 **Шаг 1. Создать ветку** — подсказка «где нажать / что вписать».
2. 📁 **Шаг 2. Файлы** — блоки файлов (§2.3).
3. ✅ **Шаг 3. Собрать / тесты** — команды для режима.
4. 💾 **Шаг 4. Коммит + Push** — формат сообщения для режима.
5. 📤 **Шаг 5. PR в фичу** — как открыть PR и с какой base.
6. 🎉 **Шаг 6. Merge** — как смержить и удалить ветку.

**Пример (дома, AS):**

```
**Файлов кода:** 3
**Файлов тестов:** 2

**Пачка:** 5.8.11-e4e-bundle — мимикрия + единый путь
**Фича:** feature/5.8.11-e4-voice-v2
**Режим:** дома (Android Studio)

---

🐙 **Шаг 1. Создать ветку пачки**
> правый нижний угол AS → клик по `5.8.11-e4-voice-v2` → **New Branch** →
> имя `fix/5.8.11-e4e-bundle` → Checkout → **Create**.

📁 **Шаг 2. Файлы**
<файлы>

✅ **Шаг 3. Собрать и прогнать тесты**
> `Ctrl+F9` → `BUILD SUCCESSFUL`. `Alt+F12` → `./gradlew testDebugUnitTest`.

💾 **Шаг 4. Коммит + Push**
> `Ctrl+K` → галочка `All` → сообщение `5.8.11-e4e-bundle — мимикрия` → **Commit** → **Push** → **Push**.

📤 **Шаг 5. PR в фичу**
> `Git → GitHub → Create Pull Request` → base выбрать `feature/5.8.11-e4-voice-v2` → Title → **Create**.

🎉 **Шаг 6. Merge**
> В браузере на PR → **Merge pull request** → **Confirm merge** → **Delete branch** → в AS `Ctrl+T`.
```

**Пример (на работе, github.com):**

```
**Файлов кода:** 3
**Файлов тестов:** 2

**Пачка:** 5.8.11-e4e-bundle — мимикрия + единый путь
**Фича:** feature/5.8.11-e4-voice-v2
**Режим:** на работе (github.com)

---

🐙 **Шаг 1. Создать ветку пачки**
> `main ▾` → `feature/5.8.11-e4-voice-v2` → поле ввода → `fix/5.8.11-e4e-bundle` → **Create branch**.

📁 **Шаг 2. Файлы**
<файлы>

✅ **Шаг 3. Проверить**
> Сборки нет. Тесты — авто на PR в шаге 5.

💾 **Шаг 4. Коммит**
> Каждый файл через ✏️ → Commit changes → `5.8.11-e4e-bundle/N — ИмяФайла.kt`.

📤 **Шаг 5. PR в фичу**
> **Compare & pull request** → Base: `feature/5.8.11-e4-voice-v2` → **Create pull request**.

🎉 **Шаг 6. Merge**
> **Merge pull request** → **Confirm merge** → **Delete branch**.
```

**Правила шапки:**
- Шапка короткая: только «Файлов кода / тестов / Пачка-Заход / Фича / Режим».
- Всё git-специфичное — в шагах ниже.
- В подсказке `>` — одна строка: где нажать + что вписать.
- Подсказки — только для **текущего** режима. Второй не упоминать.
- Если в шаге нет действия — пропустить.

### 2.3. Формат файла

Каждый файл — отдельным блоком:

```
X/Y. **Заменить полностью** или **Новый файл** — <ИмяФайла.kt>
<комментарий, зачем>

**Заменить полностью** или **Новый файл**

```text
полный/путь/к/файлу/ИмяФайла.kt
```

```kotlin
// полный код файла, без сокращений
```
```

**Комментарии в коде — на русском. Код полный, без «…».**

**Для `github.com`** — после каждого файла строка коммита в подсказке:
> ✏️ → Commit changes → `X.Y/N — ИмяФайла.kt`.

### 2.4. Git-операции в ответе — пошагово

Когда работаем в **Android Studio**, git-шаги — подробно:

```
🐙 **Шаг N. Закоммитить и запушить**

> `Ctrl+K` (Commit) → галочка `All` → поле Commit Message:
> `5.8.11-e4e-bundle — мимикрия` → **Commit** → сверху **Push** → снова **Push**.
```

**Правило.** Пользователь сам сказал: «По гиту надо — я им в AS не
пользовался».

### 2.5. Обязательные элементы

1. **Заголовок `X/Y`** — `X` номер, `Y` всего.
2. **Пометка «Заменить полностью»** или **«Новый файл»** — **жирным**.
3. **Полный путь в текстовом блоке** — от корня проекта.
4. **Полный код** в блоке ` ```kotlin `. **Никаких «…»**.
5. **Раздел «Что проверить вручную»** (§5) — с командами для текущего режима.

### 2.6. Имя ветки

| Тип ветки | Префикс | Пример |
|---|---|---|
| Фича (долгоживущая) | `feature/` | `feature/5.8.11-e4-voice-v2` |
| Пачка (ответвление от фичи) | `fix/` | `fix/5.8.11-e4e-bundle` |
| Одиночный заход | `fix/` / `feature/` / `docs/` | `fix/5.8.9-infra-2a-ci-sdk` |
| Документация | `docs/` | `docs/5.8.11-rules-header` |

**Правила:**
- Одна задача — одна ветка.
- **Пачки — ответвление от фичи**, не от `main`.
- **Пачки мержатся в фичу** (не в `main`).
- **Фича мержится в `main`** — когда вся серия готова.
- После merge **пачки** — удалить.
- После merge **фичи** — **не удалять** (архив, §2.9).
- **Дома** — одиночные заходы можно в `main`. Пачки — всегда в ветке.

### 2.7. Сообщение коммита

| Режим | Способ коммита | Формат |
|---|---|---|
| Дома (AS) | **Один общий** на пачку | `X.Y-bundle — описание` |
| github.dev | Один общий | `X.Y-bundle — описание` |
| github.com | **По одному на файл** | `X.Y-bundle/N — ИмяФайла` |

**Почему так:** на github.com веб-редактор коммитит только текущий файл.
Дома и в github.dev можно выбрать все файлы за раз.

### 2.8. Пачки (bundles)

**Пачка** — группа связанных заходов в одной ветке с одним PR и одним
device-check.

**Зачем:**
- Меньше сборок (одна на пачку вместо N).
- Меньше CI-прогонов.
- Логически связанные изменения идут вместе.

**Правила:**
- Пачка — **3–7 файлов кода** + тесты.
- Пачка — **одна логическая задача.**
- Внутри пачки — подзаходы `X.Y-bundle/1, /2, /3…`.
- **Один device-check** на пачку.
- **Один PR** из пачки в фичу.

**Откат:**
- Упал один подзаход — `git revert <коммит>`.
- Упала вся пачка — не мержить в фичу.

### 2.9. Жизненный цикл веток

```
main                                    ← стабильный
└── feature/X.Y-xxx                   ← фича (долгоживущая)
├── fix/X.Y-xxx-bundle1          ← пачка
├── fix/X.Y-xxx-bundle2
└── fix/X.Y-xxx-bundle3
└── feature/X.Y-yyy                    ← архив (закрыта)
└── feature/X.Y-yyy-v2           ← возврат через время
```

**Фича.** Долгоживущая. Не мержится в `main` до готовности всей серии.
**После merge в `main` — НЕ удаляется.** Остаётся как архив.

**Пачка.** Короткая. Живёт до merge в фичу. **После merge — удаляется.**

**Архивная фича.** Закрыта, не удалена. Возврат — новое ответвление
`-v2`, `-v3`. Старая ветка не трогается.

**`main`.** Трогается редко. Только merge готовой фичи.

---

## 3. Что запрашивать у пользователя

**Если не хватает контекста — попроси файлы.** Не догадывайся.

### Формат запроса — группировка по папкам

```
Нужны файлы:

ui/screens/
app/src/main/java/com/example/geosamplemanager/ui/screens/SearchScreen.kt

data/voice/
app/src/main/java/com/example/geosamplemanager/data/voice/VoiceController.kt

корень проекта
app/build.gradle
```

**Правила:**
- Группируй по папке.
- Полный путь от корня.
- Только нужные файлы.
- Если файл уже прислан в этом чате — не проси повторно.
- Пометь, зачем каждый файл — таблицей.

---

## 4. Правила кода

### 4.1. Стиль
- Kotlin, Jetpack Compose, Room.
- Комментарии — на русском.
- Имена классов/функций/переменных — английские.
- Отступ — 4 пробела.
- Максимум 100 символов в строке.

### 4.2. Структура
- Пакеты — `com.example.geosamplemanager.<слой>`.
- `data/`, `data/entity/`, `data/dao/`, `data/util/`, `data/voice/`.
- `ui/screens/`, `ui/navigation/`, `ui/theme/`.
- Никаких файлов «всё в одном».

### 4.3. Работа с БД
- Все изменения схемы — только через миграции.
- Перед изменением сущности — сверить с `DATABASE.md`.
- После изменения — предупредить и обновить `DATABASE.md`.

### 4.4. Асинхронность
- Все I/O — в `Dispatchers.IO`.
- Обновление UI — через `withContext(Dispatchers.Main)`.

### 4.5. Работа с файлами
- Фото — `filesDir/sample_photos/`.
- БД — `getDatabasePath()`.
- Настройки — `filesDir/*.json`.

### 4.6. Обработка ошибок
- Ловить исключения в каждом I/O-блоке.
- Сообщение — в `_message.value`.
- Не «глотать» ошибки молча.

---

## 5. Формат раздела «Что проверить вручную»

**Unit-тесты — автоматически на PR.** Отдельный раздел только для того,
что тестами не покрывается: Vosk, TTS, UI, БД-миграции, реальное
устройство.

### 5.1. Дома (Android Studio)

```
✅ Что проверить вручную — пачка X.Y-bundle

**Сборка**
> `Ctrl+F9` → ожидаемо `BUILD SUCCESSFUL`.

**Локальные тесты**
> `Alt+F12` → `./gradlew testDebugUnitTest` → `BUILD SUCCESSFUL`.

**Unit-тесты на PR**
> Job `unit-tests` зелёный (автоматически).

**Device-check (если требуется)**
> Сценарий 1: <пошагово> → ожидаемое поведение.
> Сценарий 2: ...

**Если что-то падает**
> Куда смотреть (logcat, тег), что прислать (полный текст).
```

### 5.2. На работе (github.com)

```
✅ Что проверить вручную — пачка X.Y-bundle

**Unit-тесты (авто)**
> На PR — job `unit-tests` (зелёный = можно мержить).

**APK (если нужен device-check)**
> Повесить лейбл `build-apk` на PR → job `build-apk` → артефакт
> `app-debug` внизу страницы запуска.

**Device-check (если требуется)**
> Сценарий 1: <пошагово> → ожидаемое поведение.

**Если что-то падает**
> Полный текст ошибки CI или logcat — в чат.
```

**Правила:**
- Команды в подсказке `>` — простые, по одной строке.
- Если device-check не нужен — писать прямо «не нужен».
- Не дублировать то, что покрыто тестами.

---

## 6. Что делать при ошибках

### 6.1. Сборка упала
- Попросить **полный текст ошибки** (не скриншот).
- Найти причину, исправить, выдать **полный файл** заново.

### 6.2. Логика не работает
- Что происходит vs что ожидалось.
- Что нажал, что увидел, что в логах.

### 6.3. Миграция БД упала
- Снести приложение и запустить заново.
- Исправить `Migration`.

### 6.4. CI упал
- Попросить **полный лог** (не скриншот).
- Разобрать по шагам: `Setup Android SDK`, `Run unit tests`,
  `Build Debug APK`. Указать, где именно упало.

### 6.5. Юнит-тест упал
- Тест — это контракт. **Не удалять, не «подкручивать».**
- Сначала понять: тест прав или код прав.
- Если код сломан — откатить правку.
- Если тест устарел — обновить тест с пояснением в комментарии.

### 6.6. Git-конфликт
- Попросить имя ветки и имена файлов из конфликта.
- Объяснить пошагово, что оставить.

---

## 7. Правила по документации

### 7.1. Когда обновлять
- После каждого закрытого захода — `PROGRESS.md`, `NEXT_STEPS.md`,
  `CONTEXT_BRIEF.md`.
- Решения — в `DECISIONS.md`.
- Изменения БД — в `DATABASE.md`.
- Изменения ГП — в `VOICE.md`.
- Изменения правил работы — в `AI_RULES.md`, `README.md`.
- Изменения тестов — в `TESTING.md`.

### 7.2. Как обновлять
- Файл целиком, не фрагмент.
- Пометка «Заменить полностью» — жирным.
- Путь в текстовом блоке.
- Шапка `X/Y`.

### 7.3. Напоминание про репозиторий и коммит

**В самом конце ответа** — напоминание.

**Для AS и github.dev:**

```
Пачка X.Y-bundle закрыта. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Ветка: fix/X.Y-bundle → PR в feature/X.Y-xxx

Коммит: X.Y-bundle — краткое описание
```

**Для github.com:**

```
Пачка X.Y-bundle закрыта. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Ветка: fix/X.Y-bundle → PR в feature/X.Y-xxx

Коммиты:
- X.Y-bundle/1 — Имя1
- X.Y-bundle/2 — Имя2
```

**Unit-тесты запускаются автоматически на PR.** APK — по лейблу `build-apk`.

---

## 8. Принципы обсуждения

### 8.1. Перед кодом — обсуждение
Если задача сложная: сначала спецификация, потом код.

### 8.2. Не пиши код, если:
- Нет полного понимания задачи.
- Пользователь задал вопрос «как лучше?».
- Есть открытые вопросы по архитектуре.

### 8.3. Аудит
Перед крупным блоком — аудит: каталог команд, конфликты, вопросы.

---

## 9. Приоритеты

1. Работающий код > красивый.
2. Простое > сложное.
3. Понятный > лаконичный.
4. Документация > код без документации.
5. **Тесты > отсутствие тестов.**

---

## 10. Что категорически нельзя

- Обрезать код («…», «остальное как было»).
- Угадывать содержимое файлов.
- Менять схему БД без миграции.
- Использовать Apache POI.
- Писать комментарии на английском.
- Забывать раздел «Что проверить вручную».
- Отвечать без номера файла.
- Забывать шапку `X/Y`.
- Забывать ссылку на репозиторий.
- **Забывать git-шаги в шапке захода** (§2.2).
- **Мержить пачку в `main`** — пачки идут в фичу.
- **Удалять ветку фичи** после merge — она архив.
- **Писать новый код без юнит-тестов** (§23).
- **Забывать про device-check** — если он нужен, писать в §5.

---

## 11. Шаблон ответа на пачку

```
**Файлов кода:** 3
**Файлов тестов:** 2

**Пачка:** 5.8.11-e4e-bundle — мимикрия + единый путь
**Фича:** feature/5.8.11-e4-voice-v2
**Режим:** дома (Android Studio)

---

🐙 **Шаг 1. Создать ветку пачки**
> правый нижний угол AS → клик по `5.8.11-e4-voice-v2` → New Branch →
> имя `fix/5.8.11-e4e-bundle` → Checkout → **Create**.

📁 **Шаг 2. Файлы**

1/5. **Заменить полностью** — VoiceSpeaker.kt

```text
app/src/main/java/com/example/geosamplemanager/data/voice/VoiceSpeaker.kt
```

```kotlin
<полный код>
```

2/5. **Заменить полностью** — VoiceModels.kt
<полный код>

3/5. **Заменить полностью** — ReconciliationViewModel.kt
<полный код>

4/5. **Новый файл** — VoiceSpeakerTest.kt

```text
app/src/test/java/com/example/geosamplemanager/data/voice/VoiceSpeakerTest.kt
```

```kotlin
<полный код теста>
```

5/5. **Новый файл** — VoiceModelsTest.kt
<полный код>

✅ **Шаг 3. Собрать и прогнать тесты**
> `Ctrl+F9` → `BUILD SUCCESSFUL`. `Alt+F12` → `./gradlew testDebugUnitTest` → `BUILD SUCCESSFUL`.

💾 **Шаг 4. Коммит + Push**
> `Ctrl+K` → галочка `All` → сообщение `5.8.11-e4e-bundle — мимикрия` → **Commit** → **Push** → **Push**.

📤 **Шаг 5. PR в фичу**
> `Git → GitHub → Create Pull Request` → base выбрать `feature/5.8.11-e4-voice-v2` → Title `5.8.11-e4e-bundle — мимикрия` → **Create Pull Request**.

🎉 **Шаг 6. Merge**
> В браузере на PR → дождаться зелёного `unit-tests` → **Merge pull request** → **Confirm merge** → **Delete branch** → в AS `Ctrl+T`.

---

✅ Что проверить вручную — пачка 5.8.11-e4e-bundle

**Сборка**
> `Ctrl+F9` → `BUILD SUCCESSFUL`.

**Локальные тесты**
> `Alt+F12` → `./gradlew testDebugUnitTest` → `BUILD SUCCESSFUL`.

**Unit-тесты на PR**
> Job `unit-tests` зелёный (автоматически).

**Device-check**
> Сценарий 1: голосом «KPD 109 00 31» → ответ «капэдэ сто девять ноль ноль тридцать один».
> Сценарий 2: на экране `KPD1090031` (цифры).

**Если что-то падает**
> Полный stacktrace или лог CI — в чат.

---

Пачка 5.8.11-e4e-bundle закрыта. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Ветка: fix/5.8.11-e4e-bundle → PR в feature/5.8.11-e4-voice-v2

Коммит: 5.8.11-e4e-bundle — мимикрия
```

---

## 12. Шаблон ответа на вопрос (без кода)

1. Короткий ответ — 1–2 строки.
2. Разбор по пунктам.
3. Рекомендация.
4. Вопросы.

Не пиши код в ответ на вопрос. Сначала обсудите.

---

## 13. Что делать при смене чата

Пользователь приложит файлы из §0. Ты:

1. Прочитай все.
2. Перескажи контекст — 5–8 строк.
3. **Задай вопросы по режиму (§0, п. 2).**
4. Ждать команду. Не начинай код сам.

---

## 14. Чек-лист перед отправкой ответа

- [ ] **Задал вопрос про режим.**
- [ ] Первая строка — «Файлов кода: N» + «Файлов тестов: M».
- [ ] **Шапка захода — короткая** (Пачка-Заход / Фича / Режим).
- [ ] **Git-шаги встроены по ходу** (создание ветки, коммит, PR, merge).
- [ ] **Подсказки `>` — только для текущего режима.**
- [ ] У каждого файла шапка `X/Y`.
- [ ] Полный путь в текстовом блоке.
- [ ] Код полный, без «…».
- [ ] Комментарии на русском.
- [ ] **Новый код — с тестами.**
- [ ] **Раздел «Что проверить вручную» — есть, с командами режима.**
- [ ] **Device-check указан** — или явно «не нужен».
- [ ] `CONTEXT_BRIEF.md` обновлён (если заход закрыт).
- [ ] `TESTING.md` обновлён (если добавлены/изменены тесты).
- [ ] **Напоминание про репозиторий и коммит — в конце.**
- [ ] **Указано: PR идёт в фичу, не в main.**
- [ ] **Указано: unit-тесты — авто на PR, APK — по лейблу.**
- [ ] Если просил файлы — сгруппированы по папкам.

---

## 15. Полезные файлы проекта

| Файл | Когда нужен |
|---|---|
| `README.md` | Структура проекта |
| `CONTEXT_BRIEF.md` | Где мы сейчас |
| `PROGRESS.md` | История |
| `NEXT_STEPS.md` | Текущий заход |
| `DECISIONS.md` | Логика UI |
| `DATABASE.md` | Схема БД |
| `ROADMAP.md` | Приоритеты |
| `VOICE.md` | ГП |
| `ISSUES.md` | Проблемы |
| `GLOSSARY.md` | Термины |
| `TESTING.md` | Тесты |
| `AI_RULES.md` | Этот файл |

---

## 16. Финальное правило

**Пользователь — не робот.** Он устаёт, отвлекается, ошибается.
Твоя задача — **сокращать его работу**.

Каждый твой ответ должен:
- Не заставлять переспрашивать.
- Не заставлять копировать по частям.
- Не заставлять догадываться, что проверять.
- Экономить время.

Если сомневаешься между «быстро» и «понятно» — **делай понятно**.

---

## 17. Стоп-сигналы — когда ИИ обязан остановиться

**Если задача попадает хотя бы в один пункт — ИИ не пишет код.
Сначала спрашивает.**

1. **Задача затрагивает >7 файлов.** Спросить: «Разбить на пачки?».
2. **Задача меняет схему БД.** Спросить: «Какая миграция?».
3. **Задача меняет публичную сигнатуру.** Спросить: «Кто ещё
   использует?».
4. **Задача противоречит `CONTEXT_BRIEF.md`.** Сказать прямо.
5. **Задача сформулирована словами** «улучшить», «оптимизировать»,
   «починить». Спросить: «Что конкретно?».
6. **Нужно удалить код.** Спросить подтверждение.
7. **Правка каскадит** — тянет ещё 2+ файла. Остановиться.
8. **Задача похожа на уже сделанную.** Спросить: «Повторяем?».
9. **В чате >30 сообщений.** Предложить обновить `CONTEXT_BRIEF.md`.
10. **Не понятно, что надо.** Задать уточняющий вопрос.

### Формат вопроса при стоп-сигнале

```
Стоп-сигнал: <причина>.

Варианты:
А) <сделать как есть>
Б) <разбить на пачки>
В) <отложить>
Г) <свой вариант>
```

---

## 18. Атомарность заходов и пачек

**Одиночный заход:** 1–3 файла кода + тесты.

**Пачка:** 3–7 файлов кода + тесты, **один PR и один device-check**.

**Правила:**
- Если задача **>7 файлов** — разбить на пачки.
- Если пачка смешивает разные задачи — разбить.
- Если файл >500 строк — разбить на классы.
- Если правка смешивает UI и БД — разбить.

---

## 19. Откат по умолчанию

**Если после правки что-то сломалось — откатить, не чинить.**

### Как (дома, AS)

1. Terminal (`Alt+F12`).
2. Откат одного файла:
   ```
git checkout main -- путь/к/файлу.kt
   ```
3. Откат всех незакоммиченных правок:
   ```
git checkout -- .
   ```
4. Откат последнего коммита:
   ```
git reset --hard HEAD~1
   ```
5. Откат подзахода внутри пачки:
   ```
git revert <hash>
   ```

### Что НЕ считается поломкой
- Мелкая ошибка в строке.
- Опечатка в комментарии.
- Отсутствующая зависимость в gradle.

### Поломка
- Приложение не запускается.
- Рабочий сценарий сломался.
- БД падает при миграции.
- Новая ошибка в логах.
- Красный `unit-tests` в PR.

---

## 20. Работа с двух машин

**Контекст:** проект разрабатывается на **двух машинах**:

- **Дома** — Android Studio, полный цикл + git в AS.
- **На работе** — без IDE. Через `github.com`. Правки + CI + APK + device-check.
- **Git на рабочей машине не установлен.**

### 20.1. Модель веток — общая

**7 шагов git-цикла одинаковы для всех режимов.** Отличается только
интерфейс — где нажимать.

```
1. Подтянуть свежее из GitHub
2. Переключиться на фичу
3. Создать пачку от фичи
4. Правки (и локальные тесты дома)
5. Коммит + Push
6. PR пачки → в фичу
7. Merge PR → удалить пачку
```

**Правила:**
- **Пачка → в фичу**, не в `main`.
- **Фича → в `main`** — только когда вся серия готова.
- После merge **пачки** — удалить.
- После merge **фичи** — **не удалять** (архив, §2.9).

### 20.2. Режим А — дома (Android Studio)

Каждый шаг — с указанием **куда нажать** и **что заполнить**.

#### Шаг 1. Подтянуть свежее из GitHub

**Действия:**
1. Открой AS. Открой проект.
2. Меню **Git → Update Project** (или `Ctrl+T`).
3. В окне подтверждения — **OK** (ветка Merge).

**Ожидаемо:** снизу мелькнёт «All files are up-to-date» или список
обновлённых файлов.

#### Шаг 2. Переключиться на фичу

**Действия:**
1. Правый **нижний угол AS** — название текущей ветки. Клик.
2. В выпадающем списке — раздел **Local** или **Remote**.
   - Если фича локально — клик → **Checkout**.
   - Если нет — **Remote** → `origin/feature/X.Y-xxx` → **Checkout**.
3. Если фича устарела — `Ctrl+T` (Update Project).

**Проверка:** правый нижний угол показывает `feature/...`.

#### Шаг 3. Создать пачку от фичи

**Действия:**
1. Правый нижний угол → **New Branch**.
2. Поле **Name** — имя пачки: `fix/5.8.11-e4e-bundle`.
3. Галочка **Checkout branch** — оставь включённой.
4. **Create**.

**Проверка:** правый нижний угол показывает `fix/...`.

#### Шаг 4. Правки и локальные тесты

**Действия:**
1. Замени/создай файлы по инструкции ИИ (§2.3).
2. **Build → Make Project** (`Ctrl+F9`). Ожидаемо: `BUILD SUCCESSFUL`.
3. Терминал (`Alt+F12`):
   ```
./gradlew testDebugUnitTest
   ```
   Ожидаемо: `BUILD SUCCESSFUL`.
4. Если есть device-check — `Run → Run 'app'` → прогнать сценарии.

#### Шаг 5. Коммит + Push

**Действия:**
1. Слева иконка **Commit** (третья сверху) или `Ctrl+K`.
2. В панели Commit — галочка **`All`**.
3. Поле **Commit Message** — ОДНО сообщение для всей пачки:
   ```
5.8.11-e4e-bundle — мимикрия
   ```
4. Кнопка **`Commit`**.
5. Сверху синяя полоса **`Push`** → нажми.
6. В окне подтверждения — **`Push`**.

**Первый push новой ветки:** AS спросит — согласиться.

#### Шаг 6. PR пачки → в фичу

**Действия:**
1. Меню **Git → GitHub → Create Pull Request**.
2. Откроется окно. **Слева вверху — строка `GeoSampleManager: main ← GeoSampleManager: fix/...`**.
3. **Кликни по надписи `main` (слева от стрелки ←).**
4. В выпадающем списке выбери **`feature/X.Y-xxx`**.
5. **Проверь:** строка теперь `feature/X.Y-xxx ← fix/...`.
6. **Title** — заголовок.
7. **Description** — пусто.
8. Кнопка **`Create Pull Request`**.

**Что произойдёт:**
- Через 5 сек. job **`unit-tests`** запустится автоматически.
- Через 2–3 минуты — зелёная галочка.

#### Шаг 7. Merge PR

**Действия:**
1. Дождись **зелёного `unit-tests`** в PR (открой ссылку в браузере).
2. Если **хочешь APK** — правая панель PR → **Labels** → `build-apk`.
   Через 5–7 минут — артефакт `app-debug` внизу.
3. Прогони device-check.
4. **Merge pull request** → **Confirm merge**.
5. **Delete branch**.
6. В AS: `Ctrl+T` — подтянуть фичу.

#### Если PR показывает `out-of-date`

**Что это:** фича ушла вперёд. Пачка отстала.

**Что делать:** кнопка **`Update branch`** внизу PR → **`Update with merge commit`**. `unit-tests` перезапустится.

**Чтобы не возникало:** прежде чем создавать PR — обновляй фичу от main.

### 20.3. Режим В — на работе, `github.com`

#### Шаг 1. Подтянуть свежее

Обновить страницу репозитория (F5).

#### Шаг 2. Переключиться на фичу

Слева вверху — `main ▾` → выбрать `feature/X.Y-xxx`.

#### Шаг 3. Создать пачку от фичи

Слева вверху — `feature/... ▾` → поле ввода → `fix/X.Y-bundle` → **Create branch**.

#### Шаг 4. Правки

Каждый файл — через ✏️ Edit → Commit changes → `X.Y-bundle/N — ИмяФайла.kt`.

**Новый файл:** **Add file** → **Create new file** → путь + содержимое → Commit.

#### Шаг 5. PR пачки → в фичу

**Compare & pull request** → **Base:** `feature/X.Y-xxx` → Title → **Create pull request**.

#### Шаг 6. Проверка CI

Ждать 2–3 минуты. `unit-tests` зелёный. Если APK нужен — повесить лейбл `build-apk`.

#### Шаг 7. Merge PR

**Merge pull request** → **Confirm merge** → **Delete branch**.

### 20.4. CI — что автоматически, что вручную

**Автоматически:**

- **`unit-tests`** — на каждый PR в `main` и `feature/*`.
- **`build-apk`** — если на PR висит лейбл `build-apk`.

**Вручную (Actions → Build & Test → Run workflow):**
- Прогнать тесты без PR.
- Собрать APK без лейбла.

**Артефакты:**
- `app-debug` — APK.
- `test-report` — HTML-отчёт.

**Что делать при красном CI:** скопировать полный лог шага с ошибкой → в чат.

### 20.5. Правила гигиены

- **Дома — `Ctrl+T` перед началом.**
- **На работе — только через ветку.**
- **Одна задача = одна ветка.**
- **Коммит после каждого подзахода.**
- **Пуш в конце сессии.**
- **Пачка → в фичу.** `main` не трогаем.
- **После merge пачки — удалить.**
- **После merge фичи в `main` — НЕ удалять.**

### 20.6. Что ИИ должен учитывать

- **На работе:** правки + CI + APK + device-check.
- **Дома:** правки + AS + локальные тесты + device-check.
- **Формат ответа — единый.**
- **При работе с пачками** — явно указывать: PR в фичу, не в `main`.
- **Дома** — давать git-шаги под AS.
- **На работе** — давать шаги под github.com.

### 20.7. Vosk-модель

**Не коммитится в Git.** В CI выкачивается из релиза GitHub
(`models-v1`) в `build.yml`. Дома — локально в
`app/src/main/assets/vosk-model-small-ru-0.22/`.

### 20.8. Ветки — сводка

| Режим | Одиночный заход | Пачка | Фича |
|---|---|---|---|
| Дома (AS) | Можно в `main` | Ветка от фичи | `feature/X.Y-xxx` |
| github.dev | Ветка от фичи | Ветка от фичи, PR в фичу | `feature/X.Y-xxx` |
| github.com | Ветка от фичи | Ветка от фичи, PR в фичу | `feature/X.Y-xxx` |

---

## 21. Коммит и ветки — сводка

| Режим | Ветка | Коммит |
|---|---|---|
| Дома (AS), одиночный | `main` | `X.Y — описание` |
| Дома (AS), пачка | `fix/X.Y-bundle` | `X.Y-bundle — описание` (общий) |
| github.dev, пачка | `fix/X.Y-bundle` | `X.Y-bundle — описание` |
| github.com, пачка | `fix/X.Y-bundle` | `X.Y-bundle/N — ИмяФайла` |

**Unit-тесты — авто на PR.** APK — по лейблу `build-apk`.

**PR пачки → в фичу, не в main.**

---

## 22. Модель веток — сводка

```
main
│
├── feature/X.Y-xxx                ← активная фича
│     ├── fix/X.Y-xxx-bundle1      ← пачка
│     ├── fix/X.Y-xxx-bundle2
│     └── fix/X.Y-xxx-bundle3
│
├── feature/X.Y-yyy                ← архив (закрыта)
│     └── feature/X.Y-yyy-v2       ← возврат
│
└── feature/X.Y-zzz                ← архив
```

**Жизненный цикл:**

1. **Фича** — от `main`, до конца серии.
2. **Пачка** — от фичи, до merge в фичу.
3. **Merge фичи в `main`** — когда вся серия готова.
4. **Фича остаётся архивом.** Возврат — `-v2`.

---

## 23. Тесты для нововведений

### 23.1. Правило

**Каждый заход или пачка с новым кодом — сопровождается юнит-тестами.**

- Есть код → есть тест.
- Тест — часть захода. Не отдельным коммитом «потом».
- Заход **не закрыт**, пока тесты не написаны и не зелёные.

### 23.2. Что тестируем

**Обязательно (чистые функции, парсеры, логика):**
- Парсеры (`VoiceCommandParser`, `VoiceNumberParser`, `QueryTokenizer`).
- Конвертеры (`VoiceSpeaker`, `DigitGrouper`, `GroupToCandidates`).
- Мапперы (`ResponseMapper`, `analyzeMark`).
- Доменные функции (`UnifiedSearch`, `validateWeight`).
- Расчёты (веса, номера, скважины).

**По возможности (ViewModel):**
- Логика `voiceExecute` — если можно оторвать от Android.
- `ReconciliationState` — undo/redo, мутации.

**Только device-check (не юнит-тестами):**
- Поведение Vosk (галлюцинации, распознавание).
- TTS (произношение, кулдаун).
- UI (Compose, экраны, диалоги).
- Реальная БД и миграции — только на устройстве.

### 23.3. Исключения (тесты не нужны)

- Чистая **документация** (`.md`).
- **Конфиг CI** (`.yml`), если не меняется логика.
- **Константы, ресурсы** — цвета, строки, размеры.
- **Рефакторинг без изменения поведения** — если тесты уже покрывают.
- **Одноразовый экспериментальный заход** — по согласованию.

### 23.4. Где лежат тесты

`app/src/test/java/com/example/geosamplemanager/`.

Правила (см. `TESTING.md`):
- Имя теста — **латиница** (backtick-кириллица ломает Gradle).
- Один тест — одна проверка.
- Тест <100 мс.
- Простые `assertEquals(expected, actual)`.

### 23.5. Как запускать

**Локально (дома, AS):**
- ПКМ по `app/src/test/` → Run 'Tests in …'.

**Терминал:**
```
./gradlew testDebugUnitTest
```

**Автоматически (на PR):**
- Job `unit-tests` в GitHub Actions.
- Запускается на каждый PR в `main` и `feature/*`.
- Блокирует merge при красном (branch protection).

**Вручную (Actions):**
- Actions → Build & Test → Run workflow → ✅ `Run unit tests`.

### 23.6. Что делать при падении теста

1. **Не удалять тест.** Найти причину.
2. Тест — контракт. Если он упал — сломалось поведение.
3. Если тест прав, а код неправ — откатить правку (§19).
4. Если тест устарел — обновить тест с пояснением в комментарии.

### 23.7. Долг по тестам

Если в пачке появился код без тестов — **не закрываем заход.**
Пишем тесты отдельной пачкой `docs/…-tests` или включаем в ту же
пачку, если это разумно.

**Текущий долг** (серия `5.8.11-e4`) — покрыть отдельной пачкой
`e4-tests`. Список в `TESTING.md`.

---

## 24. Что осталось на ручную проверку

После того как тесты зелёные и всё скомпилировано — **на устройстве
проверяем только то, что тестами не покрывается**:

- Vosk — распознавание, галлюцинации, реакция на шум.
- TTS — произношение, скорость, кулдаун.
- UI — расположение, реакции на тапы, диалоги.
- Взаимодействие ГП ↔ UI — синхронизация.
- БД — миграции, реальные данные, сохранение.
- Сценарии, включающие всё вместе.

**Формат блока «Что проверить вручную» (§5):**
- **Unit-тесты (авто)** — короткая строка.
- **Device-check** — сценарии, что нажать, что ожидать.
- **Если падает** — что прислать.

**Не пиши в ручную проверку то, что уже покрыто тестами.**

---

## 25. Финальное правило (повтор)

**Пользователь — не робот.** Он устаёт, отвлекается, ошибается.
Твоя задача — **сокращать его работу**.

Каждый твой ответ должен:
- Не заставлять переспрашивать.
- Не заставлять копировать по частям.
- Не заставлять догадываться, что проверять.
- Экономить время.

Если сомневаешься между «быстро» и «понятно» — **делай понятно**.