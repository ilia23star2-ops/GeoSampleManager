# Правила работы ИИ над GeoSample Manager

> **ЧИТАЙ ПЕРВЫМ.** Этот файл описывает, как ИИ должен отвечать, форматировать
> код, что запрашивать у пользователя и куда смотреть за контекстом.
>
> Соблюдение обязательно. Единый формат — чтобы при смене чата новый ИИ
> сразу понял правила и продолжил работу без переобъяснений.

**Репозиторий:** https://github.com/ilia23star2-ops/GeoSampleManager

---

## 0. Быстрый старт в новом чате

Если ты — ИИ, открывший проект впервые:

1. **Прочитай эти файлы** (в порядке приоритета):
    - `AI_RULES.md` (этот) — как работать.
    - `CONTEXT_BRIEF.md` — **где мы сейчас**.
    - `README.md` — где что лежит.
    - `PROGRESS.md` — что сделано.
    - `NEXT_STEPS.md` — текущий заход.
    - `DECISIONS.md` — решения.
    - `DATABASE.md` — схема БД.
    - `ROADMAP.md` — план до релиза.
    - `VOICE.md` — спецификация ГП.
    - `ISSUES.md` — проблемы.
    - `GLOSSARY.md` — термины.
    - `TESTING.md` — тесты.

2. **Задай два вопроса — до начала работы:**

   **Вопрос 1.** «Ты сейчас дома или на работе?»
    - **Дома** → Режим А (§20.1). Android Studio, полный цикл.
    - **На работе** → вопрос 2.

   **Вопрос 2 (только если на работе).** «Через `github.dev` или
   `github.com`?»
    - **`github.dev`** → Режим Б (§20.2). Приоритет.
    - **`github.com`** → Режим В (§20.3). Fallback.

3. **Дождись команды:** «продолжаем с этапа N», «делаем пачку X», «начинаем фичу Y».

4. **Перескажи понимание** в 3–5 строках: где мы, что делаем, что уже
   есть. Только после этого начинай код.

**Режим сохраняется до конца чата.** Если пользователь перешёл с работы
домой — он говорит «я дома», ИИ переходит в Режим А.

---

## 1. Стиль общения

### 1.1. Общие правила

- **Язык — русский.** Комментарии в коде — русские. UI — русский.
- **Без воды.** Не повторяй, что сказал пользователь. Не пиши «отличный
  вопрос», «давайте разберёмся».
- **Коротко и по делу.** Абзацы — 2–4 строки.
- **Уважительно и по-рабочему.**
- **Не извиняйся без причины.** Ошибся — исправил, коротко отметил.
- **Не спрашивай разрешения на очевидное.** Если уточняешь — задай
  конкретный вопрос с вариантами А/Б/В.

### 1.2. Комментарии к действиям — разделение

**Правило.** Комментарии в ответе зависят от типа действия.

| Тип действия | Комментарий |
|---|---|
| **Замена файла в IDE** | **Не писать.** Пользователь знает: `Ctrl+A` → `Delete` → `Ctrl+V` → `Ctrl+S`. Достаточно строки «**Действие:** открой файл X, замени содержимое». |
| **Сборка / запуск** | **Коротко.** «`Build → Make Project`. Ожидаемо: `BUILD SUCCESSFUL`.» |
| **Git-операции в AS** | **Подробно, пошагово.** Куда нажать, что выбрать, что ожидать. См. §2.4. |
| **Git-операции в Terminal** | **Пошагово.** Одна команда — одно действие. Ожидаемый вывод. |
| **Что-то новое в интерфейсе** | **Подробно.** Пользователь может не знать, где кнопка. |

**Почему так.** Пользователь сам сказал: «По элементарным заменам — не
надо комментариев. А по гиту — надо». Придерживаться.

---

## 2. Формат ответа на заход кода

### 2.1. Структура ответа — пять частей

1. **Шапка захода** (§2.2).
2. **Файлы кода** — по одному блоку на файл (§2.3).
3. **Файлы тестов** — по одному блоку на файл (§2.3).
4. **Раздел «Как проверить»** (§5): unit-тесты (авто) + ручная проверка.
5. **Напоминание про репозиторий и PR** (§7.3).

**Никаких пошаговых инструкций по веткам внутри ответа.** Всё это
описано в §20 — один раз для всех.

### 2.2. Шапка захода — компактная

**Одиночный заход (1–3 файла):**
Файлов: N (код: K, тесты: T)

Заход: X.Y — <краткое название>
Режим: <дома (Android Studio) | github.dev | github.com>
Ветка: <feature|fix|docs>/X.Y-краткое
Коммит: X.Y — <краткое описание>

text

**Для `github.com` — вместо одной строки коммита:**
Коммиты:

X.Y/1 — <ИмяФайла1>

X.Y/2 — <ИмяФайла2>

text

**Пачка (несколько подзаходов) — шапка расширенная:**
Файлов: N (пачка)

Пачка: X.Y-bundle — <краткое название>
Фича: feature/X.Y-<название-фичи> — базовая ветка
Ветка: fix/X.Y-bundle — ответвление от фичи
Подзаходы:

X.Y-bundle/1 — ИмяФайла1

X.Y-bundle/2 — ИмяФайла2

X.Y-bundle/3 — ИмяФайла1Test

text

### 2.3. Формат файла

Каждый файл — отдельным блоком:
X/Y. Заменить полностью или Новый файл — <ИмяФайла.kt>
<если это не первый файл, короткий комментарий — зачем он>

Заменить полностью или Новый файл

text
полный/путь/к/файлу/ИмяФайла.kt
kotlin
// полный код файла, без сокращений
text

**Для `github.com` — после кода файла добавить строку коммита:**
Коммит: X.Y/N — ИмяФайла.kt

text

**Правило.** Тесты идут **после** файлов кода, в том же ответе, отдельными
блоками. Порядок: сначала код, потом тесты.

### 2.4. Git-операции в ответе — пошагово

Когда пользователь работает в **Android Studio** и в ответе есть
git-шаги, **расписывать подробно**:

**Пример — шаг «Коммит и пуш»:**
Шаг N. Закоммитить и запушить
Куда смотреть: слева в AS вертикальная полоса с иконками. Третья
сверху — иконка с ветками (Commit). Нажми её.

Действия:

В панели Commit сверху поставь галочку All — отметятся все файлы.

Поле «Commit Message» — введи:

text
5.8.9h-2b-i — UI на UnifiedMatchKind
Кнопка Commit внизу панели. Нажми. Изменения зафиксируются
локально, но на GitHub ещё не уйдут.

Сверху появится синяя полоска Push — нажми её.

В окне подтверждения снова Push.

text

**Правило.** Пользователь сам сказал: «По гиту надо — я им в AS не
пользовался».

### 2.5. Обязательные элементы

1. **Заголовок `X/Y`** — `X` номер, `Y` всего. Даже если файл один:
   `1/1. Замена — ROADMAP.md`.
2. **Пометка «Заменить полностью»** или **«Новый файл»** — **жирным**.
3. **Полный путь в текстовом блоке** — от корня проекта.
4. **Полный код** в блоке ` ```kotlin `. **Никаких «…»**.
5. **После всех файлов** — раздел «Как проверить».

### 2.6. Имя ветки

| Тип ветки | Префикс | Пример |
|---|---|---|
| Фича (долгоживущая) | `feature/` | `feature/5.8.11-e4-voice-v2` |
| Пачка (ответвление от фичи) | `fix/` или `feature/` | `fix/5.8.11-e4e-bundle` |
| Одиночный заход без фичи | `fix/` или `feature/` | `fix/5.8.9-infra-2a-ci-sdk` |
| Документация / правила | `docs/` | `docs/5.8.11-tests-rules` |

**Правила:**
- Одна задача — одна ветка.
- **Пачки — ответвление от фичи**, не от `main`.
- **Пачки мержатся в фичу** (не в `main`).
- **Фича мержится в `main`** только когда вся серия готова.
- После merge **пачки** — удалить.
- После merge **фичи** — **не удалять**, оставить как архив (§2.9).
- **Дома** — одиночные заходы можно в `main`. Пачки — всегда в ветке.

### 2.7. Сообщение коммита

Формат строки:
<номер захода> — <краткое описание>

text

Примеры:
- `5.8.9h-2 — UnifiedSearch в UI и ГП`
- `5.8.11-e4e-bundle/3 — ReconciliationViewModel.kt`

**Разные режимы — разный способ коммита:**

| Режим | Способ коммита | Формат |
|---|---|---|
| Дома (Android Studio) | Общий, в конце работы | `X.Y — описание` |
| На работе `github.dev` | Общий, в конце работы | `X.Y — описание` |
| На работе `github.com` | По одному на файл | `X.Y/N — ИмяФайла` |

### 2.8. Пачки (bundles)

**Пачка** — группа связанных заходов в одной ветке с одним PR и одним
device-check.

**Зачем:**
- Меньше сборок (одна на пачку вместо N).
- Меньше CI-прогонов.
- Логически связанные изменения идут вместе.

**Правила:**
- Пачка — **3–7 файлов** (включая тесты).
- Пачка — **одна логическая задача.** «Мимикрия», «Pin», «Маркеры».
- Внутри пачки — подзаходы `X.Y-bundle/1, /2, /3…` — каждый атомарный
  коммит.
- **Один device-check** на пачку.
- **Один PR** из пачки в фичу.
- **Один CI-прогон** на пачку — `unit-tests` запускается автоматически.

### 2.9. Жизненный цикл веток

**Три уровня:**
main ← стабильный
└── feature/X.Y-xxx ← фича (долгоживущая)
├── fix/X.Y-xxx-bundle1 ← пачка
├── fix/X.Y-xxx-bundle2
└── fix/X.Y-xxx-bundle3
└── feature/X.Y-yyy ← архив (закрыта)
└── feature/X.Y-yyy-v2 ← возврат через время

text

**Фича.**
- Долгоживущая. Не мержится в `main` до готовности всей серии.
- Пачки ответвляются от неё.
- **После merge в `main` — НЕ удаляется.** Остаётся как архив.

**Пачка.**
- Короткая. Живёт от старта до merge в фичу.
- **После merge — удаляется.**

**Архивная фича.**
- Закрыта, но не удалена.
- Возврат — новое ответвление с суффиксом `-v2`, `-v3`.
- Старая ветка **не трогается**.

**`main`.**
- Трогается редко. Только merge готовой фичи.

---

## 3. Что запрашивать у пользователя

**Если не хватает контекста — попроси файлы.** Не догадывайся.

### Формат запроса — группировка по папкам
Нужны файлы:

ui/screens/
app/src/main/java/com/example/geosamplemanager/ui/screens/SearchScreen.kt

data/voice/
app/src/main/java/com/example/geosamplemanager/data/voice/VoiceController.kt

корень проекта
app/build.gradle

text

**Правила:**
- Группируй по папке.
- Полный путь от корня.
- Только нужные файлы.
- Если файл уже прислан в этом чате — не проси повторно.
- Пометь, зачем каждый файл — таблицей.

---

## 4. Правила кода

### 4.1. Стиль
- Kotlin, Jetpack Compose, Room.
- Комментарии — на русском.
- Имена классов/функций/переменных — английские.
- Отступ — 4 пробела.
- Максимум 100 символов в строке.

### 4.2. Структура
- Пакеты — `com.example.geosamplemanager.<слой>`.
- `data/`, `data/entity/`, `data/dao/`, `data/util/`, `data/voice/`.
- `ui/screens/`, `ui/navigation/`, `ui/theme/`.
- Никаких файлов «всё в одном».

### 4.3. Работа с БД
- Все изменения схемы — только через миграции.
- Перед изменением сущности — сверить с `DATABASE.md`.
- После изменения — предупредить и обновить `DATABASE.md`.

### 4.4. Асинхронность
- Все I/O — в `Dispatchers.IO`.
- Обновление UI — через `withContext(Dispatchers.Main)`.

### 4.5. Работа с файлами
- Фото — `filesDir/sample_photos/`.
- БД — `getDatabasePath()`.
- Настройки — `filesDir/*.json`.

### 4.6. Обработка ошибок
- Ловить исключения в каждом I/O-блоке.
- Сообщение — в `_message.value`.
- Не «глотать» ошибки молча.

---

## 5. Формат раздела «Как проверить»

**Раздел состоит из двух частей:**

1. **Unit-тесты (авто).** Что запустится само в PR, что ожидать.
2. **Ручная проверка.** Что нельзя проверить тестами.

### 5.1. Дома (Android Studio)
✅ Как проверить пачку X.Y-bundle

Unit-тесты (авто через PR)
Список новых тестов.
Запуск: PR создан → CI → unit-tests → зелёный.

Сборка
Build → Make Project. Ожидаемо: BUILD SUCCESSFUL.

Запуск
Run → Run 'app'.

Ручная проверка (device-check)
Сценарии, которые не покрываются тестами.
Пошаговые действия.
Ожидаемое поведение.

Если что-то падает
Куда смотреть, что прислать.

text

### 5.2. На работе — `github.dev`
✅ Как проверить пачку X.Y-bundle (github.dev)

Создание ветки
Ветка: fix/X.Y-bundle от feature/X.Y-xxx. См. §20.2.

Правки
Файлы: N штук.

Коммит
Один коммит: X.Y — описание.

Пуш и PR
Commit & Push → PR в feature/X.Y-xxx (НЕ в main).

Unit-тесты (авто)
Открыл PR → GitHub сам запустил unit-tests.
Ждём зелёную галочку.

APK (по лейблу, если нужен device-check)
Повесь лейбл build-apk на PR.
Через 5–7 минут — APK в артефактах.

Ручная проверка (device-check)
Сценарии, которые нельзя покрыть тестами.

Merge
Merge pull request → Delete branch.

Если что-то падает
Полный текст ошибки CI или logcat — в чат.

text

### 5.3. На работе — `github.com`
✅ Как проверить пачку X.Y-bundle (github.com)

Создание ветки
Репозиторий → переключиться на feature/X.Y-xxx → Create branch
fix/X.Y-bundle.

Правки
Файл 1: открыть → ✏️ → Commit changes → «X.Y-bundle/1 — ИмяФайла».
Файл 2: ...
Файл K (тест): открыть → ✏️ → Commit changes → «X.Y-bundle/K — ИмяФайлаTest».

PR
Compare & pull request → base: feature/X.Y-xxx (НЕ main) → Create.

Unit-тесты (авто)
После создания PR CI запустится сам — unit-tests.

APK (по лейблу, если нужен device-check)
Повесь лейбл build-apk на PR.
Скачай APK из артефактов CI.

Ручная проверка (device-check)
Сценарии, которые нельзя покрыть тестами.

Merge
Merge pull request → Confirm merge → Delete branch.

Если что-то падает
Полный текст ошибки — в чат.

text

**Правила:**
- Конкретные действия и ожидания.
- Простой язык.
- Без adb и SQL, если не критично.
- **Unit-тесты — всегда в первом блоке.** Ручная проверка — отдельно.

---

## 6. Что делать при ошибках

### 6.1. Сборка упала
- Попросить **полный текст ошибки** (не скриншот).
- Найти причину, исправить, выдать **полный файл** заново.

### 6.2. Логика не работает
- Попросить что происходит vs что ожидалось.
- Уточнить: что нажал, что увидел, что в логах.

### 6.3. Миграция БД упала
- Сказать снести приложение и запустить заново.
- Исправить `Migration`.

### 6.4. CI упал
- Попросить **полный лог** (не скриншот).
- Разобрать по шагам: `Setup Android SDK`, `Run unit tests`,
  `Build Debug APK`. Указать, где именно упало.
- **Если упал `unit-tests`** — сначала проверить, не наш ли новый тест
  сломался. Если да — исправить код или тест.

### 6.5. Git-конфликт
- Попросить имя ветки и имена файлов из конфликта.
- Объяснить пошагово, что оставить.

---

## 7. Правила по документации

### 7.1. Когда обновлять
- После каждого закрытого захода — `PROGRESS.md`, `NEXT_STEPS.md`,
  `CONTEXT_BRIEF.md`.
- Решения — в `DECISIONS.md`.
- Изменения БД — в `DATABASE.md`.
- Изменения ГП — в `VOICE.md`.
- Изменения правил работы — в `AI_RULES.md`, `README.md`.
- Изменения правил тестов — в `TESTING.md`.

### 7.2. Как обновлять
- Файл целиком, не фрагмент.
- Пометка «Заменить полностью» — жирным.
- Путь в текстовом блоке.
- Шапка `X/Y`.

### 7.3. Напоминание про репозиторий и PR

**В самом конце ответа** — напоминание:

**Для AS и github.dev:**
Пачка X.Y закрыта. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Ветка: fix/X.Y-bundle → PR в feature/X.Y-xxx

Коммит: X.Y — краткое описание

После создания PR:

unit-tests — запустится сам, ждать зелёный.

build-apk — при необходимости повесить лейбл build-apk.

text

**Для github.com:**
Пачка X.Y закрыта. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Ветка: fix/X.Y-bundle → PR в feature/X.Y-xxx

Коммиты:

X.Y/1 — Имя1

X.Y/2 — Имя2

После создания PR:

unit-tests — запустится сам.

build-apk — по лейблу.

text

---

## 8. Принципы обсуждения

### 8.1. Перед кодом — обсуждение
Если задача сложная: сначала спецификация, потом код.

### 8.2. Не пиши код, если:
- Нет полного понимания задачи.
- Пользователь задал вопрос «как лучше?».
- Есть открытые вопросы по архитектуре.

### 8.3. Аудит
Перед крупным блоком — аудит: каталог команд, конфликты, вопросы.

---

## 9. Приоритеты

1. Работающий код > красивый.
2. Простое > сложное.
3. Понятный > лаконичный.
4. Документация > код без документации.
5. **Тесты > вера в то, что код работает.**

---

## 10. Что категорически нельзя

- Обрезать код («…», «остальное как было»).
- Угадывать содержимое файлов.
- Менять схему БД без миграции.
- Использовать Apache POI.
- Писать комментарии на английском.
- Забывать раздел «Как проверить».
- Отвечать без номера файла.
- Забывать шапку `X/Y`.
- Забывать ссылку на репозиторий.
- **Забывать компактную шапку захода** (§2.2).
- **Писать пошаговые инструкции по веткам внутри ответа** — они в §20.
- **Мержить пачку в `main`** — пачки идут в фичу.
- **Удалять ветку фичи** после merge — она архив.
- **Коммитить код без тестов** — исключение только документация, конфиги, константы (§23).
- **Удалять падающий тест**, чтобы прошёл CI. Найти причину.

---

## 11. Шаблон ответа на пачку

**Пример — пачка с кодом и тестами:**
Файлов: 5 (пачка)

Пачка: 5.8.11-e4e-bundle — мимикрия + единый путь поиска
Фича: feature/5.8.11-e4-voice-v2
Ветка: fix/5.8.11-e4e-bundle
Подзаходы:

5.8.11-e4e-bundle/1 — VoiceSpeaker.kt

5.8.11-e4e-bundle/2 — VoiceModels.kt

5.8.11-e4e-bundle/3 — ReconciliationViewModel.kt

5.8.11-e4e-bundle/4 — VoiceSpeakerTest.kt (тест)

5.8.11-e4e-bundle/5 — ReconciliationViewModelTest.kt (тест)

1/5. Заменить полностью — VoiceSpeaker.kt
<полный код>

Коммит: 5.8.11-e4e-bundle/1 — VoiceSpeaker.kt

2/5. Заменить полностью — VoiceModels.kt
<полный код>

Коммит: 5.8.11-e4e-bundle/2 — VoiceModels.kt

3/5. Заменить полностью — ReconciliationViewModel.kt
<полный код>

Коммит: 5.8.11-e4e-bundle/3 — ReconciliationViewModel.kt

4/5. Новый файл — VoiceSpeakerTest.kt
<полный код теста>

Коммит: 5.8.11-e4e-bundle/4 — VoiceSpeakerTest.kt

5/5. Новый файл — ReconciliationViewModelTest.kt
<полный код теста>

Коммит: 5.8.11-e4e-bundle/5 — ReconciliationViewModelTest.kt

✅ Как проверить пачку 5.8.11-e4e-bundle

Unit-тесты (авто)
Открой PR → CI сам запустит unit-tests.
Ждать зелёный.
Если красный — прислать лог.

Сборка (дома)
Build → Make Project. Ожидаемо: BUILD SUCCESSFUL.

Ручная проверка (device-check)
Только то, что не покрыто тестами:

Голосом «KPD 109 00 31» → мимикрия.

Голосом «семь утра было холодно» → отказ.

... (2–4 сценария)

Если что-то падает
Полный logcat с тегом VoiceDialog.

Пачка 5.8.11-e4e-bundle закрыта. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Ветка: fix/5.8.11-e4e-bundle → PR в feature/5.8.11-e4-voice-v2

После PR:

unit-tests — автоматически.

build-apk — по лейблу.

text

---

## 12. Шаблон ответа на вопрос (без кода)

1. Короткий ответ — 1–2 строки.
2. Разбор по пунктам.
3. Рекомендация.
4. Вопросы.

Не пиши код в ответ на вопрос. Сначала обсудите.

---

## 13. Что делать при смене чата

Пользователь приложит файлы из §0. Ты:

1. Прочитай все.
2. Перескажи контекст — 5–8 строк.
3. **Задай вопросы по режиму (§0, п. 2).**
4. Ждать команду. Не начинай код сам.

---

## 14. Чек-лист перед отправкой ответа

- [ ] **Задал вопрос про режим** (при старте чата).
- [ ] Первая строка — «Файлов: X/Y».
- [ ] **Шапка захода — компактная** (§2.2).
- [ ] **В шапке: заход/пачка, фича, режим, ветка, коммит/подзаходы.**
- [ ] **Никаких пошаговых инструкций по веткам внутри ответа.**
- [ ] **Комментарии к элементарным действиям — минимальны** (§1.2).
- [ ] **Комментарии к git-операциям — пошаговые** (§2.4).
- [ ] У каждого файла шапка `X/Y`.
- [ ] Полный путь в текстовом блоке.
- [ ] Код полный, без «…».
- [ ] Комментарии на русском.
- [ ] **Тесты — включены в пачку (§23).**
- [ ] Раздел «Как проверить» — есть, содержит **отдельно** unit-тесты и ручную проверку.
- [ ] `CONTEXT_BRIEF.md` обновлён (если заход закрыт).
- [ ] **Напоминание про репозиторий и PR — в конце.**
- [ ] **Указано: PR идёт в фичу, не в main.**
- [ ] **Указано: `unit-tests` — автоматически, `build-apk` — по лейблу.**
- [ ] Если просил файлы — сгруппированы по папкам.

---

## 15. Полезные файлы проекта

| Файл | Когда нужен |
|---|---|
| `README.md` | Структура проекта |
| `CONTEXT_BRIEF.md` | Где мы сейчас |
| `PROGRESS.md` | История |
| `NEXT_STEPS.md` | Текущий заход |
| `DECISIONS.md` | Логика UI |
| `DATABASE.md` | Схема БД |
| `ROADMAP.md` | Приоритеты |
| `VOICE.md` | ГП |
| `ISSUES.md` | Проблемы |
| `GLOSSARY.md` | Термины |
| `TESTING.md` | Тесты |
| `AI_RULES.md` | Этот файл |

---

## 16. Финальное правило

**Пользователь — не робот.** Он устаёт, отвлекается, ошибается.
Твоя задача — **сокращать его работу**.

Каждый твой ответ должен:
- Не заставлять переспрашивать.
- Не заставлять копировать по частям.
- Не заставлять догадываться, что проверять.
- Экономить время.

Если сомневаешься между «быстро» и «понятно» — **делай понятно**.

---

## 17. Стоп-сигналы — когда ИИ обязан остановиться

**Если задача попадает хотя бы в один пункт — ИИ не пишет код.
Сначала спрашивает.**

1. **Задача затрагивает >7 файлов.** Спросить: «Разбить на пачки?».
2. **Задача меняет схему БД.** Спросить: «Какая миграция?».
3. **Задача меняет публичную сигнатуру.** Спросить: «Кто ещё
   использует?».
4. **Задача противоречит `CONTEXT_BRIEF.md`.** Сказать прямо.
5. **Задача сформулирована словами** «улучшить», «оптимизировать»,
   «починить». Спросить: «Что конкретно?».
6. **Нужно удалить код.** Спросить подтверждение.
7. **Правка каскадит** — тянет ещё 2+ файла. Остановиться.
8. **Задача похожа на уже сделанную.** Спросить: «Повторяем?».
9. **В чате >30 сообщений.** Предложить обновить `CONTEXT_BRIEF.md`.
10. **Не понятно, что надо.** Задать уточняющий вопрос.
11. **Тесты не покрывают новую логику.** Предложить дописать (§23).

### Формат вопроса при стоп-сигнале
Стоп-сигнал: <причина>.

Варианты:
А) <сделать как есть>
Б) <разбить на пачки>
В) <отложить>
Г) <свой вариант>

text

---

## 18. Атомарность заходов и пачек

**Одиночный заход:** 1–3 файла (включая тесты).

**Пачка:** 3–7 файлов (включая тесты), один PR, один device-check.

**Правила:**
- Если задача **>7 файлов** — разбить на **пачки**.
- Если пачка смешивает разные задачи — разбить.
- Если файл >500 строк — разбить на классы.
- Если правка смешивает UI и БД — разбить.

**Почему:**
- Меньше файлов = меньше потерь.
- Пачки = меньше сборок, быстрее цикл.
- Один device-check на пачку = экономия времени.
- **Тесты — обязательная часть любой пачки с кодом.**

---

## 19. Откат по умолчанию

**Если после правки что-то сломалось — откатить, не чинить.**

### Как (дома, AS)

1. Открой Terminal (`Alt+F12`).
2. Откат одного файла:
git checkout main -- путь/к/файлу.kt

text
3. Откат всех незакоммиченных правок:
git checkout -- .

text
4. Откат последнего коммита (если уже закоммичен):
git reset --hard HEAD~1

text
5. Откат **подзахода** внутри пачки (коммит `/N`):
git revert <hash-коммита>

text

### Что НЕ считается поломкой
- Мелкая ошибка в строке.
- Опечатка в комментарии.
- Отсутствующая зависимость в gradle.

### Поломка
- Приложение не запускается.
- Рабочий сценарий сломался.
- БД падает при миграции.
- Новая ошибка в логах.
- **Тест красный на CI.**

---

## 20. Работа с двух машин — три режима

**Контекст:** проект разрабатывается на **двух машинах**:

- **Дома** — Android Studio, полный цикл: правки + сборка + device-check.
- **На работе** — без IDE. **Приоритет:** `github.dev`. **Fallback:**
`github.com`. Правки + CI + APK + device-check.
- **Git на рабочей машине не установлен.**

**Режим определяется один раз в начале чата (§0).**

**На работе можно:**
- Писать код через веб-редактор.
- Запускать CI автоматически через PR (`unit-tests`).
- Собирать APK через лейбл `build-apk` на PR.
- Скачивать APK из артефактов CI.
- Устанавливать APK на устройство.
- Прогонять device-check.

**На работе нельзя:**
- Компилировать локально.
- Использовать Android Studio.
- Работать с git через командную строку.

### 20.1. Режим А — дома (Android Studio)

**Главное:** дома — **одиночные заходы можно в `main`. Пачки — всегда
в ветке** `fix/X.Y-bundle` от фичи.

**Что можно просить у пользователя:** сборку, тесты, эмулятор,
устройство, логи, скриншоты.

**Цикл:**

1. **Открыть Android Studio.** Открыть проект.
2. **Подтянуть свежее.** Терминал (`Alt+F12`):
git fetch origin
git checkout main
git pull

text
3. **Переключиться на фичу** (если работаем с пачкой):
git checkout feature/X.Y-xxx
git pull

text
4. **Создать пачку** (если новая):
git checkout -b fix/X.Y-bundle

text
5. **Заменить файлы** — по инструкции из ответа ИИ.
6. **Собрать.** `Build → Make Project`. Ожидаемо: `BUILD SUCCESSFUL`.
7. **Запустить.** `Run → Run 'app'` → эмулятор или устройство.
8. **Прогнать тесты локально.** ПКМ по `app/src/test/` →
`Run 'Tests in …'`. Ожидаемо: зелёные.
9. **Device-check** — по сценариям из ответа ИИ.
10. **Закоммитить и запушить** — подробно см. §2.4.
11. **PR** — из пачки в фичу (не в main). Через github.com в браузере.
12. **CI** — на PR автоматически запустится `unit-tests`. Ждать зелёный.
13. **APK** (если нужен для повторного device-check) — повесить лейбл
 `build-apk` на PR, скачать артефакт.
14. **Merge** — в фичу. Пачку удалить.

**Одиночный заход без фичи:** можно прямо в `main`, коммит — в конце.
CI запустится автоматически? Нет — на push в main CI не запускается.
Проверка — локально (пункты 6–8).

### 20.2. Режим Б — на работе, `github.dev` (приоритет)

**Цикл:**

1. Открыть `https://github.com/ilia23star2-ops/GeoSampleManager`.
2. Нажать `.` (точка) — откроется `github.dev`.
3. **Переключиться на фичу:** внизу слева кнопка `main` → выбрать
`feature/X.Y-xxx`.
4. **Создать пачку:** `main` → **Create new branch** от фичи.
5. Правки файлов.
6. **Коммит + пуш:** Source Control (`Ctrl+Shift+G`) → сообщение →
**Commit & Push**.
7. **PR** — баннер `Compare & pull request` или вкладка Pull requests.
**Base: `feature/X.Y-xxx`**, не `main`.
8. **Unit-тесты** — запустятся автоматически. Ждать зелёный.
9. **APK** (для device-check) — повесить лейбл `build-apk` на PR.
10. **Скачать APK** из артефактов CI.
11. **Device-check** на устройстве.
12. **Merge** — в фичу. Пачку удалить.

### 20.3. Режим В — на работе, `github.com` (fallback)

**Цикл:**

1. Открыть репозиторий на github.com.
2. **Переключиться на фичу:** `main ▾` → выбрать `feature/X.Y-xxx`.
3. **Создать пачку:** `main ▾` → поле ввода → `fix/X.Y-bundle` →
Create branch.
4. **Правки по файлам**, коммит под каждым:
 - Файл 1: открыть → ✏️ → Commit changes → `X.Y-bundle/1 — ИмяФайла.kt`.
 - Файл 2: ... → `X.Y-bundle/2 — ИмяФайла.kt`.
5. **PR** — Compare & pull request → **base: фича**, не main.
6. **Unit-тесты** — запустятся автоматически.
7. **APK** (для device-check) — повесить лейбл `build-apk` на PR.
8. **Скачать APK** из артефактов.
9. **Device-check** на устройстве.
10. **Merge** — в фичу. Пачку удалить.

### 20.4. CI — настройка

**Автоматически:**
- **`unit-tests`** — при создании / обновлении PR в `main` или `feature/*`.
- **`build-apk`** — при наличии лейбла `build-apk` на PR (в любой момент).
- **Документация-онли PR** — `unit-tests` запускается, но шаги пропущены,
job зелёный (для branch protection).

**Вручную (при необходимости):**
1. GitHub → вкладка **Actions**.
2. Слева — **Build & Test**.
3. Справа сверху — **Run workflow**.
4. Выбрать ветку.
5. Чек-боксы: `Run unit tests`, `Build Debug APK`.
6. **Run workflow**.

**Артефакты:**
- `app-debug` — APK (если `build_apk`).
- `test-report` — HTML-отчёт (если `run_tests`).

**Что делать при красном CI:**
1. Открыть упавший запуск.
2. Нажать на шаг с красным крестом.
3. Скопировать полный текст ошибки → в чат.
4. ИИ разбирает и даёт правку.

### 20.5. Правила гигиены

- **Дома — `git pull` перед началом.**
- **На работе — только через ветку.**
- **Дома — можно в `main`** (одиночные заходы). Пачки — в ветке.
- **Одна задача = одна ветка.**
- **Коммит после каждого подзахода.**
- **Пуш в конце сессии.** Даже если задача не закончена.
- **Пачка → в фичу.** `main` не трогаем.
- **После merge пачки — удалить.**
- **После merge фичи в `main` — НЕ удалять.** Архив (§2.9).
- **Тесты — обязательны.** Каждый заход с новым кодом → +тест (§23).

### 20.6. Что ИИ должен учитывать

- **На работе:** правки + CI + device-check. Локальной сборки нет, но
проверка на устройстве — есть.
- **Дома:** правки + AS + device-check.
- **Формат ответа — единый.** Файлы целиком, компактная шапка, «Как
проверить».
- **Уточнять, где работает пользователь**, если из контекста неясно.
- **При работе с пачками** — явно указывать, что PR идёт в фичу,
не в `main`.

### 20.7. Vosk-модель

**Не коммитится в Git.** В CI выкачивается из релиза GitHub
(`models-v1`) отдельным шагом в `build.yml`. Дома лежит локально в
`app/src/main/assets/vosk-model-small-ru-0.22/`.

### 20.8. Ветки — сводка

| Режим | Одиночный заход | Пачка | Фича |
|---|---|---|---|
| Дома (AS) | Можно в `main` | Ветка `fix/X.Y-bundle` от фичи | Ветка `feature/X.Y-xxx` |
| github.dev | Ветка `fix/X.Y-xxx` | Ветка `fix/X.Y-bundle` от фичи | Ветка `feature/X.Y-xxx` |
| github.com | Ветка `fix/X.Y-xxx` | Ветка `fix/X.Y-bundle` от фичи | Ветка `feature/X.Y-xxx` |

---

## 21. Коммит и ветки — сводка

| Режим | Ветка | Коммит |
|---|---|---|
| Дома (AS), одиночный | `main` | `X.Y — описание` |
| Дома (AS), пачка | `fix/X.Y-bundle` | `X.Y-bundle/N — ИмяФайла` |
| github.dev, пачка | `fix/X.Y-bundle` | `X.Y-bundle — описание` |
| github.com, пачка | `fix/X.Y-bundle` | `X.Y-bundle/N — ИмяФайла` |

**CI не запускается от коммита.** `unit-tests` — на PR, `build-apk` —
по лейблу (§20.4).

**PR пачки → в фичу, не в main.** Merge фичи в main — после всей серии.

---

## 22. Модель веток — сводка
main
│
├── feature/X.Y-xxx ← активная фича (долгоживущая)
│ ├── fix/X.Y-xxx-bundle1 ← пачка (мерж в фичу, удалить)
│ ├── fix/X.Y-xxx-bundle2
│ └── fix/X.Y-xxx-bundle3
│
├── feature/X.Y-yyy ← архив (закрыта, не удалять)
│ └── feature/X.Y-yyy-v2 ← возврат через время
│
└── feature/X.Y-zzz ← архив (закрыта)

text

**Жизненный цикл:**

1. **Фича** — от `main`, живёт до конца серии.
2. **Пачка** — от фичи, живёт до merge в фичу.
3. **Merge фичи в `main`** — когда вся серия готова.
4. **Фича остаётся архивом.** Возврат — `-v2` от архива.

---

## 23. Тесты для нововведений

**Правило.** Каждый заход с новым кодом сопровождается тестами.
**Без тестов заход не закрыт.**

### Что покрывать

**Обязательно (unit-тесты):**
- Парсеры (чистые функции) — `VoiceCommandParser`, `VoiceNumberParser`,
  `VoiceOrdinal`, `QueryTokenizer`, `QueryNormalizer`.
- Алгоритмы поиска — `UnifiedSearch`, `SearchService`.
- Голосовые данные — `VoiceSpeaker.spellOut`, `VoiceDictionary`.
- Решения — `MarkDecision`, `validateWeight`.

**Желательно:**
- `ReconciliationState` — логика undo/redo, bulk-действия.
- Вспомогательные утилиты.

**Не тестируем unit-тестами:**
- UI (Compose) — device-check.
- `AndroidViewModel` с `Application` — сложно, дорого.
- Vosk, TTS — только device-check.
- БД целиком (кроме миграций).

### Где лежат тесты
app/src/test/java/com/example/geosamplemanager/
data/
reconciliation/
MarkDecisionTest.kt
voice/
VoiceCommandParserTest.kt
VoiceNumberParserTest.kt
VoiceSpeakerTest.kt
UnifiedSearchTest.kt
...
ui/screens/
AnalyzeMatchTest.kt

text

**Один класс — один тестовый файл.** `VoiceSpeaker.kt` →
`VoiceSpeakerTest.kt` в том же пакете.

### Как писать

1. **Имя теста — латиница.** `parseWeightClean`, не `парситВес`.
2. **Ассерты — простые.** `assertEquals`, `assertTrue`, `assertNull`.
3. **Один тест — одна проверка.** Не смешивать.
4. **Тест — <100 мс.**
5. **Комментарии — на русском.**
6. **`@Test` из `org.junit.Test`** (JUnit 4.13.2).

### Как запускать

**Дома (AS):** ПКМ по `app/src/test/` → `Run 'Tests in …'`.

**В CI (авто):** открыл PR → `unit-tests` стартует сам. Через 1–3 мин —
зелёный/красный. Branch protection блокирует merge красного.

**Вручную (Actions):** `Run workflow` → чек-бокс `Run unit tests`.

### Что делать при падении

1. **Не удалять тест.**
2. Открыть лог: полный текст ошибки.
3. Понять: проблема в коде или в тесте?
   - **Код сломан** — исправить код.
   - **Тест устарел** — обновить тест с пояснением.
4. Если непонятно — прислать лог в чат.

### Когда НЕ обязателен тест

- Только документация (`.md`).
- Только конфиги (`build.gradle`, `.yml`, `.json` в `filesDir/…`).
- Только константы (`250L`, `"Пауза"`).
- Косметика в UI (текст, отступы).

**Во всех остальных случаях — тест нужен.**

---

## 24. Финальное правило (повтор)

**Пользователь — не робот.** Он устаёт, отвлекается, ошибается.
Твоя задача — **сокращать его работу**.

Каждый твой ответ должен:
- Не заставлять переспрашивать.
- Не заставлять копировать по частям.
- Не заставлять догадываться, что проверять.
- Экономить время.

Если сомневаешься между «быстро» и «понятно» — **делай понятно**.
