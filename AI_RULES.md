# Правила работы ИИ над GeoSample Manager

> **ЧИТАЙ ПЕРВЫМ.** Этот файл описывает, как ИИ должен отвечать, форматировать
> код, что запрашивать у пользователя и куда смотреть за контекстом.
>
> Соблюдение обязательно. Единый формат — чтобы при смене чата новый ИИ
> сразу понял правила и продолжил работу без переобъяснений.

**Репозиторий:** https://github.com/ilia23star2-ops/GeoSampleManager

---

## 0. Быстрый старт в новом чате

Если ты — ИИ, открывший проект впервые:

1. **Прочитай эти файлы** (в порядке приоритета):
    - `AI_RULES.md` (этот) — как работать.
    - `README.md` — где что лежит.
    - `PROGRESS.md` — что сделано и что в работе.
    - `NEXT_STEPS.md` — текущий заход и план.
    - `DECISIONS.md` — все решения по UI и логике.
    - `DATABASE.md` — схема БД.
    - `ROADMAP.md` — план до релиза.
    - `VOICE.md` — спецификация ГП (если работаешь над голосом).

2. **Дождись команды от пользователя**: обычно это «продолжаем с этапа N»,
   «делаем заход X.Y» или «приступаем».

3. **Перескажи понимание** в 3–5 строках: где мы, что делаем, что уже есть.
   Только после этого начинай код.

---

## 1. Стиль общения

- **Язык — русский.** Комментарии в коде — русские. UI — русский.
- **Без воды.** Не повторяй, что сказал пользователь. Не пиши «отличный
  вопрос» и «давайте разберёмся».
- **Коротко и по делу.** Абзацы — 2–4 строки. Списки — там, где они уместны.
- **Уважительно и по-рабочему.** Пользователь — разработчик, не нуждается в
  разжёвывании очевидного.
- **Не извиняйся без причины.** Ошибся — исправил, коротко отметил.
- **Не спрашивай разрешения на очевидное.** Если нужно уточнить — задай
  конкретный вопрос с вариантами А/Б/В.

---

## 2. Формат ответа на заход кода

Каждый файл — **отдельным блоком**. Строгий шаблон:

```
## X/Y. <Заменить полностью> или <Новый файл> — <ИмяФайла.kt>

<если это не первый файл в заходе, короткий комментарий — зачем он>

**<Заменить полностью>** или **<Новый файл>**
```
полный/путь/к/файлу/ИмяФайла.kt
```

```kotlin
// полный код файла, без сокращений
```
```

### Обязательные элементы

1. **Заголовок файла в формате `X/Y`:**
   - `X` — номер текущего файла в заходе (1, 2, 3…).
   - `Y` — общее количество файлов в заходе.
   - Пример: `## 3/7. Замена — SampleDao.kt` (третий из семи).
2. **Пометка «Заменить полностью»** или **«Новый файл»** — **жирным**.
3. **Путь в текстовом блоке** (` ``` ` без указания языка) — чтобы удобно
   копировать. **Полный путь от корня проекта.**
4. **Полный код** в блоке ` ```kotlin `. **Никаких «…» и «остальное как
   было»**. Файл должен быть целиком.
5. **После всех файлов — раздел «Как проверить»** (см. §5).

### Если файлов несколько

Пиши **«Файлов: X/Y»** в самом начале ответа — с расшифровкой. Это помогает
пользователю оценить объём.

**Пример первой строки ответа:**
```
Файлов: 3
```
или
```
# Файлов: 3
```

### Пример заголовков в одном заходе

```
## 1/3. Замена — SampleEntity.kt
...
## 2/3. Новый файл — SampleImageEntity.kt
...
## 3/3. Замена — AppDatabase.kt
...
```

### Правило «X/Y» в шапке файла

**Всегда** пиши `X/Y`, даже если файл один:
```
## 1/1. Замена — ROADMAP.md
```
Пользователь по этой шапке сразу видит, сколько всего файлов впереди.

---

## 3. Что запрашивать у пользователя

**Если для работы не хватает контекста — попроси файлы.** Не догадывайся,
не предполагай.

### Формат запроса — группировка по папкам

Файлы **группируются по папкам**, где они лежат. Внутри группы — списком.
Между группами — пустая строка.

```
Нужны файлы:

**ui/screens/**
- app/src/main/java/com/example/geosamplemanager/ui/screens/SearchScreen.kt
- app/src/main/java/com/example/geosamplemanager/ui/screens/ReconciliationViewModel.kt

**data/voice/**
- app/src/main/java/com/example/geosamplemanager/data/voice/VoiceController.kt

**корень проекта**
- app/build.gradle
- app/src/main/AndroidManifest.xml
```

**Правила:**
- **Группируй по папке.** Если файлы из разных папок — отдельные группы.
- **Полный путь от корня проекта** — иначе пользователь будет искать.
- **Только нужные файлы.** Не заваливай списком из 30 пунктов.
- **Если файл уже был прислан в этом чате** — не проси повторно.
- **Пометь, зачем каждый файл** — таблицей или списком после запроса.

### Зачем каждый файл (обязательно)

После списка — короткая таблица:

| Файл | Зачем |
|---|---|
| `SearchScreen.kt` | Точка входа — куда врезать голосовой UI |
| `VoiceController.kt` | Обновить интерфейс под Vosk |
| `build.gradle` | Добавить зависимость Vosk |

---

## 4. Правила кода

### 4.1. Стиль
- **Kotlin, Jetpack Compose, Room.**
- **Комментарии — на русском.**
- **Имена классов, функций, переменных — английские.** Примеры:
  `PhotoStorage`, `ReconciliationState`, `voiceSettings`.
- **Отступ — 4 пробела.**
- **Максимум 100 символов в строке**, если возможно.

### 4.2. Структура
- Пакеты — `com.example.geosamplemanager.<слой>`.
- `data/`, `data/entity/`, `data/dao/`, `data/util/`, `data/voice/` (план).
- `ui/screens/`, `ui/navigation/`, `ui/theme/`.
- Никаких файлов «всё в одном». Разделяй по ответственности.

### 4.3. Работа с БД
- **Все изменения схемы — только через миграции.**
- Перед созданием / изменением сущности — сверить с `DATABASE.md`.
- После изменения — **предупредить** пользователя и обновить `DATABASE.md`.
- **Никогда не удалять колонку без миграции.**

### 4.4. Асинхронность
- **Все I/O — в `Dispatchers.IO`.**
- Обновление UI-состояния — через `withContext(Dispatchers.Main)`.
- Долгие операции — не в главном потоке.

### 4.5. Работа с файлами
- **Фото — только в `filesDir/sample_photos/`.**
- **Файлы БД — только в `getDatabasePath()`.**
- **Настройки — в `filesDir/*.json`.**

### 4.6. Обработка ошибок
- **Ловить исключения в каждом I/O-блоке.**
- Сообщение об ошибке — в `_message.value`.
- Никогда не «глотать» ошибки молча.

---

## 5. Формат раздела «Как проверить»

После каждого захода **обязателен** раздел:

```
## ✅ Как проверить заход X.Y

### 1. Сборка
- Что нажать, что должно быть.

### 2. Запуск
- Что должно открыться.

### 3. <Сценарий 1>
- Пошаговые действия.
- Ожидаемое поведение.

### 4. <Сценарий 2>
- ...

### 5. Если что-то падает
- Куда смотреть, что прислать.
```

**Правила:**
- **Конкретные действия:** «нажми молоток», «открой меню», «введи 1524».
- **Конкретные ожидания:** «должно появиться…», «снекбар: "Заметка
  сохранена"».
- **Простой язык.** Не «проверить в Logcat тег Room», а «приложение
  запускается без ошибок».
- **Если критично — без adb и SQL.** Пользователь — не разработчик ядра.

---

## 6. Что делать при ошибках

### 6.1. Сборка упала
- Попросить **полный текст ошибки** (не скриншот, а текст).
- Найти причину, исправить, выдать **полный файл** заново.

### 6.2. Логика не работает
- Попросить **что именно происходит** vs **что ожидалось**.
- Не угадывать. Уточнить:
    - Что нажал?
    - Что увидел?
    - Что было в логах?

### 6.3. Миграция БД упала
- Сказать пользователю снести приложение и запустить заново (данных
  рабочих нет).
- Исправить `Migration`.
- Выдать полный файл.

---

## 7. Правила по документации

### 7.1. Когда обновлять
- **После каждого закрытого захода** — обновить `PROGRESS.md`,
  `NEXT_STEPS.md`, при необходимости `ROADMAP.md` и `DECISIONS.md`.
- **Решения по ходу** — в `DECISIONS.md`.
- **Изменения БД** — в `DATABASE.md`.

### 7.2. Как обновлять
- **Файл целиком**, не фрагмент.
- **Пометка «Заменить полностью»** — жирным.
- **Путь в текстовом блоке.**
- **Шапка `X/Y`** — как у кода (см. §2).
- **Раздел «Как проверить»** — не нужен для документации, но можно
  попросить пользователя пролистать.

### 7.3. Напоминание про репозиторий

**После каждого закрытого захода**, когда код прошёл и документация
обновлена, добавь **в самом конце ответа** напоминание:

```
---

Заход X.Y закрыт. Файлы можно пушить:

**Репозиторий:** https://github.com/ilia23star2-ops/GeoSampleManager

**Коммит:** `5.8.3 — префиксы + поиск` (или актуальный номер захода).
```

Пользователь пушит вручную с подписью номера захода. Дай ему готовую
строку для коммита — это экономит время.

**Формат строки коммита:** `<номер захода> — <краткое описание>`.
Примеры:
- `5.8.3 — префиксы + поиск`
- `5.11 — множественный поиск UI`
- `5.8.2 — парсер ГП`

---

## 8. Принципы обсуждения

### 8.1. Перед кодом — обсуждение
Если задача сложная (новая архитектура, много конфликтов):
1. **Сначала спецификация** — что делаем, какие правила, какие конфликты.
2. **Проверка на конфликты** — пользователь отвечает, ИИ уточняет.
3. **Фиксация** — обновляем документ (например, `VOICE.md`).
4. **Только потом код.**

### 8.2. Не пиши код, если:
- Нет полного понимания задачи.
- Пользователь задал вопрос «как лучше?».
- Есть открытые вопросы, влияющие на архитектуру.

### 8.3. Аудит
Перед реализацией крупного блока — **аудит**:
- Каталог команд / функций.
- Проверка на конфликты.
- Открытые вопросы.
- Список проблем.

Только после ответов пользователя — код.

---

## 9. Приоритеты

1. **Работающий код > красивый код.** Не оптимизируй преждевременно.
2. **Простое решение > сложное.** Если можно сделать без библиотеки —
   сделай без.
3. **Понятный код > лаконичный.** Пользователю важно читать, не
   разгадывать.
4. **Документация > код без документации.** Будущий ИИ должен понять
   контекст без тебя.

---

## 10. Что категорически нельзя

- **Обрезать код** («…», «остальное как было»). Всегда целиком.
- **Угадывать** содержимое файлов, которые пользователь не присылал.
- **Менять схему БД** без предупреждения и миграции.
- **Использовать Apache POI.** Проект его избегает.
- **Писать комментарии на английском.** Только русский.
- **Забывать раздел «Как проверить».**
- **Отвечать без номера файла** («вот файл:» без пути).
- **Давать фрагменты пути.** Только полный путь от корня проекта.
- **Забывать шапку `X/Y`.** Даже для одного файла пиши `1/1`.
- **Забывать ссылку на репозиторий** в конце закрытого захода (см. §7.3).

---

## 11. Шаблон ответа на заход кода

```
Файлов: 3

## 1/3. Замена — SampleEntity.kt

<Короткий комментарий — зачем файл, что изменилось>

**Заменить полностью**
```
app/src/main/java/com/example/geosamplemanager/data/entity/SampleEntity.kt
```

```kotlin
<полный код>
```

## 2/3. Новый файл — SampleImageEntity.kt

<Короткий комментарий>

**Новый файл**
```
app/src/main/java/com/example/geosamplemanager/data/entity/SampleImageEntity.kt
```

```kotlin
<полный код>
```

## 3/3. Замена — AppDatabase.kt

<Короткий комментарий>

**Заменить полностью**
```
app/src/main/java/com/example/geosamplemanager/data/AppDatabase.kt
```

```kotlin
<полный код>
```

---

## ✅ Как проверить заход X.Y

### 1. Сборка
...

### 2. Запуск
...

### 3. Проверка <чего-то>
...

---

Заход X.Y закрыт. Файлы можно пушить:

**Репозиторий:** https://github.com/ilia23star2-ops/GeoSampleManager

**Коммит:** `X.Y — краткое описание`
```

---

## 12. Шаблон ответа на вопрос (без кода)

Если пользователь задал вопрос, а не попросил код:

1. **Короткий ответ** — 1–2 строки.
2. **Разбор по пунктам** — если нужно.
3. **Рекомендация** — конкретная.
4. **Вопросы** — если есть что уточнить.

Не пиши код в ответ на вопрос. Сначала обсудите, потом код.

---

## 13. Что делать при смене чата

Пользователь откроет новый чат и приложит все файлы из §0. Ты:

1. **Прочитай все** (быстро, но внимательно).
2. **Перескажи контекст** — 5–8 строк:
    - Где мы (этап).
    - Что сделано.
    - Что в работе.
    - Что дальше.
3. **Спроси подтверждение**: «Продолжаем с этапа N?».
4. **Ждать команду.** Не начинай код сам.

---

## 14. Чек-лист перед отправкой ответа

- [ ] Первая строка — **«Файлов: X/Y»** (если это заход кода).
- [ ] У каждого файла шапка **`X/Y. <Заменить полностью>/<Новый файл>
      — Имя.kt`**.
- [ ] Полный путь от корня проекта в текстовом блоке.
- [ ] Код полный, без «…».
- [ ] Комментарии на русском.
- [ ] Раздел «Как проверить» — есть.
- [ ] Если это документация — файл целиком.
- [ ] Не забыл обновить связанные документы, если код затрагивает
      архитектуру / БД / UI.
- [ ] **После закрытого захода** — напоминание про репозиторий и готовая
      строка коммита (см. §7.3).
- [ ] Если просил файлы — **сгруппированы по папкам** с полными путями
      и пояснением, зачем каждый (см. §3).

---

## 15. Полезные файлы проекта (для быстрого ориентира)

| Файл | Когда нужен |
|---|---|
| `README.md` | Понять структуру проекта |
| `PROGRESS.md` | Узнать текущее состояние |
| `NEXT_STEPS.md` | Понять текущий заход |
| `DECISIONS.md` | Сверить логику UI |
| `DATABASE.md` | Работа со схемой БД |
| `ROADMAP.md` | Прикинуть приоритеты |
| `VOICE.md` | Работа над голосовым помощником |
| `AI_RULES.md` | Этот файл |

---

## 16. Финальное правило

**Пользователь — не робот.** Он устаёт, отвлекается, ошибается.
Твоя задача — **сокращать его работу**, а не добавлять.

Каждый твой ответ должен:
- Не заставлять переспрашивать.
- Не заставлять копировать по частям.
- Не заставлять догадываться, что проверять.
- Экономить его время.

Если сомневаешься между «сделать быстро» и «сделать понятно» —
**делай понятно**.
```

---

## Что изменилось

| Раздел | Что добавил |
|---|---|
| Шапка файла | Ссылка на репозиторий вверху |
| §2 | Формат `X/Y` в шапке файла + примеры. Требование писать `1/1` даже для одного файла |
| §3 | Группировка файлов **по папкам**, полные пути, обязательная таблица «Зачем каждый файл» |
| §7.3 (новый) | Напоминание про репозиторий в конце захода + готовая строка коммита (`<номер захода> — <описание>`) |
| §10 | Запрет: забывать `X/Y`, забывать ссылку на репозиторий |
| §11 | Обновлён шаблон под новые правила |
| §14 | Обновлён чек-лист под новые правила |

---
