# Правила работы ИИ над GeoSample Manager

> **ЧИТАЙ ПЕРВЫМ.** Этот файл описывает, как ИИ должен отвечать, форматировать
> код, что запрашивать у пользователя и куда смотреть за контекстом.
>
> Соблюдение обязательно. Единый формат — чтобы при смене чата новый ИИ
> сразу понял правила и продолжил работу без переобъяснений.

**Репозиторий:** https://github.com/ilia23star2-ops/GeoSampleManager

---

## 0. Быстрый старт в новом чате

Если ты — ИИ, открывший проект впервые:

1. **Прочитай эти файлы** (в порядке приоритета):
    - `AI_RULES.md` (этот) — как работать.
    - `CONTEXT_BRIEF.md` — **где мы сейчас**.
    - `README.md` — где что лежит.
    - `PROGRESS.md` — что сделано.
    - `NEXT_STEPS.md` — текущий заход.
    - `DECISIONS.md` — решения.
    - `DATABASE.md` — схема БД.
    - `ROADMAP.md` — план до релиза.
    - `VOICE.md` — спецификация ГП.
    - `ISSUES.md` — проблемы.
    - `GLOSSARY.md` — термины.
    - `TESTING.md` — тесты.

2. **Задай два вопроса — до начала работы:**

   **Вопрос 1.** «Ты сейчас дома или на работе?»
    - **Дома** → Режим А (§20.1). Можно просить сборку, тесты,
      эмулятор, устройство, логи.
    - **На работе** → вопрос 2.

   **Вопрос 2 (только если на работе).** «Через `github.dev` или
   `github.com`?»
    - **`github.dev`** → Режим Б (§20.2). Приоритет.
    - **`github.com`** → Режим В (§20.3). Fallback.

3. **Дождись команды:** «продолжаем с этапа N», «делаем заход X.Y»,
   «приступаем».

4. **Перескажи понимание** в 3–5 строках: где мы, что делаем, что уже
   есть. Только после этого начинай код.

**Режим сохраняется до конца чата.** Если пользователь перешёл с работы
домой — он говорит «я дома», ИИ переходит в Режим А.

---

## 1. Стиль общения

### 1.1. Общие правила

- **Язык — русский.** Комментарии в коде — русские. UI — русский.
- **Без воды.** Не повторяй, что сказал пользователь. Не пиши «отличный
  вопрос», «давайте разберёмся».
- **Коротко и по делу.** Абзацы — 2–4 строки.
- **Уважительно и по-рабочему.**
- **Не извиняйся без причины.** Ошибся — исправил, коротко отметил.
- **Не спрашивай разрешения на очевидное.** Если уточняешь — задай
  конкретный вопрос с вариантами А/Б/В.

### 1.2. Комментарии к действиям — разделение

**Правило.** Комментарии в ответе зависят от типа действия.

| Тип действия | Комментарий |
|---|---|
| **Замена файла в IDE** | **Не писать.** Пользователь знает: `Ctrl+A` → `Delete` → `Ctrl+V` → `Ctrl+S`. Достаточно строки «**Действие:** открой файл X, замени содержимое». |
| **Сборка / запуск** | **Коротко.** «`Build → Make Project`. Ожидаемо: `BUILD SUCCESSFUL`.» |
| **Git-операции в AS** | **Подробно, пошагово.** Куда нажать, что выбрать, что ожидать. См. §2.4. |
| **Git-операции в Terminal** | **Пошагово.** Одна команда — одно действие. Ожидаемый вывод. |
| **Что-то новое в интерфейсе** | **Подробно.** Пользователь может не знать, где кнопка. |

**Почему так.** Пользователь сам сказал: «По элементарным заменам — не
надо комментариев. А по гиту — надо». Придерживаться.

---

## 2. Формат ответа на заход кода

### 2.1. Структура ответа — четыре части

1. **Шапка захода** (§2.2).
2. **Файлы** — по одному блоку на файл (§2.3).
3. **Раздел «Как проверить»** (§5).
4. **Напоминание про репозиторий и коммит** (§7.3).

**Никаких пошаговых инструкций по веткам внутри ответа.** Всё это
описано в §20 — один раз для всех. В шапке — только имя ветки и коммита.

### 2.2. Шапка захода — компактная

**Формат:**

```
**Файлов:** N

**Заход:** X.Y — <краткое название>
**Режим:** <дома (Android Studio) | github.dev | github.com>
**Ветка:** `<feature|fix|docs>/X.Y-краткое` — только для работы
**Коммит:** `X.Y — <краткое описание>` — для AS и github.dev
```

**Для `github.com` — вместо одной строки коммита:**

```
**Коммиты:**
- `X.Y/1 — <ИмяФайла1>`
- `X.Y/2 — <ИмяФайла2>`
```

### 2.3. Формат файла

Каждый файл — отдельным блоком:

```
X/Y. **Заменить полностью** или **Новый файл** — <ИмяФайла.kt>
<если это не первый файл, короткий комментарий — зачем он>

**Заменить полностью** или **Новый файл**

```text
полный/путь/к/файлу/ИмяФайла.kt
```

```kotlin
// полный код файла, без сокращений
```
```

**Для `github.com` — после кода файла добавить строку коммита:**

```
**Коммит:** `X.Y/1 — ИмяФайла.kt`
```

### 2.4. Git-операции в ответе — пошагово

Когда пользователь работает в **Android Studio** и в ответе есть
git-шаги, **расписывать подробно**:

**Пример — шаг «Коммит и пуш»:**

```
## Шаг N. Закоммитить и запушить

**Куда смотреть:** слева в AS вертикальная полоса с иконками. Третья
сверху — иконка с ветками (Commit). Нажми её.

**Действия:**

1. В панели Commit сверху поставь галочку `All` — отметятся все файлы.
2. Поле «Commit Message» — введи:
   ```
   5.8.9h-2b-i — UI на UnifiedMatchKind
   ```
3. Кнопка `Commit` внизу панели. Нажми. Изменения зафиксируются
   локально, но на GitHub ещё не уйдут.
4. Сверху появится синяя полоска `Push` — нажми её.
5. В окне подтверждения снова `Push`.
```

**Правило.** Пользователь сам сказал: «По гиту надо — я им в AS не
пользовался».

### 2.5. Обязательные элементы

1. **Заголовок `X/Y`** — `X` номер, `Y` всего. Даже если файл один:
   `1/1. Замена — ROADMAP.md`.
2. **Пометка «Заменить полностью»** или **«Новый файл»** — **жирным**.
3. **Полный путь в текстовом блоке** — от корня проекта.
4. **Полный код** в блоке ` ```kotlin `. **Никаких «…»**.
5. **После всех файлов** — раздел «Как проверить».

### 2.6. Имя ветки

| Тип захода | Префикс | Пример |
|---|---|---|
| Новая функциональность | `feature/` | `feature/5.8.9h-2-unified-search` |
| Исправление бага | `fix/` | `fix/5.8.9-infra-2a-ci-sdk` |
| Документация / правила | `docs/` | `docs/5.8.9-infra-2b-rules` |

**Правила:**
- Одна задача — одна ветка.
- От `main`, если не сказано иное.
- После PR и merge — ветку удалить.
- **Дома — можно работать без веток.** Работаем прямо в `main`. Ветки —
  только для работы. См. §20.

### 2.7. Сообщение коммита

Формат строки:

```
<номер захода> — <краткое описание>
```

Примеры:
- `5.8.9h-2 — UnifiedSearch в UI и ГП`
- `5.8.9-infra-2d — CI вручную: тесты и/или APK`

**Разные режимы — разный способ коммита:**

| Режим | Способ коммита | Формат |
|---|---|---|
| Дома (Android Studio) | Общий, в конце работы | `X.Y — описание` |
| На работе `github.dev` | Общий, в конце работы | `X.Y — описание` |
| На работе `github.com` | По одному на файл | `X.Y/N — ИмяФайла` |

---

## 3. Что запрашивать у пользователя

**Если не хватает контекста — попроси файлы.** Не догадывайся.

### Формат запроса — группировка по папкам

```
Нужны файлы:

ui/screens/
app/src/main/java/com/example/geosamplemanager/ui/screens/SearchScreen.kt
app/src/main/java/com/example/geosamplemanager/ui/screens/ReconciliationViewModel.kt

data/voice/
app/src/main/java/com/example/geosamplemanager/data/voice/VoiceController.kt

корень проекта
app/build.gradle
app/src/main/AndroidManifest.xml
```

**Правила:**
- Группируй по папке.
- Полный путь от корня.
- Только нужные файлы.
- Если файл уже прислан в этом чате — не проси повторно.
- Пометь, зачем каждый файл — таблицей.

---

## 4. Правила кода

### 4.1. Стиль
- Kotlin, Jetpack Compose, Room.
- Комментарии — на русском.
- Имена классов/функций/переменных — английские.
- Отступ — 4 пробела.
- Максимум 100 символов в строке.

### 4.2. Структура
- Пакеты — `com.example.geosamplemanager.<слой>`.
- `data/`, `data/entity/`, `data/dao/`, `data/util/`, `data/voice/`.
- `ui/screens/`, `ui/navigation/`, `ui/theme/`.
- Никаких файлов «всё в одном».

### 4.3. Работа с БД
- Все изменения схемы — только через миграции.
- Перед изменением сущности — сверить с `DATABASE.md`.
- После изменения — предупредить и обновить `DATABASE.md`.

### 4.4. Асинхронность
- Все I/O — в `Dispatchers.IO`.
- Обновление UI — через `withContext(Dispatchers.Main)`.

### 4.5. Работа с файлами
- Фото — `filesDir/sample_photos/`.
- БД — `getDatabasePath()`.
- Настройки — `filesDir/*.json`.

### 4.6. Обработка ошибок
- Ловить исключения в каждом I/O-блоке.
- Сообщение — в `_message.value`.
- Не «глотать» ошибки молча.

---

## 5. Формат раздела «Как проверить»

**Формат зависит от режима.**

### 5.1. Дома (Android Studio)

```
✅ Как проверить заход X.Y

**Сборка**
Что нажать, что должно быть.

**Запуск**
Что должно открыться.

**<Сценарий 1>**
Пошаговые действия.
Ожидаемое поведение.

**Если что-то падает**
Куда смотреть, что прислать.
```

### 5.2. На работе — `github.dev`

```
✅ Как проверить заход X.Y (github.dev)

**Создание ветки**
Ветка: feature/xxx. См. §20.2.

**Правки**
Файлы: N штук.

**Коммит**
Один коммит: X.Y — описание.

**Пуш и PR**
Commit & Push → PR в main.

**CI (вручную)**
Actions → Build & Test → Run workflow → чек-боксы.

**Merge**
Merge pull request → Delete branch.

**Если что-то падает**
Полный текст ошибки — в чат.
```

### 5.3. На работе — `github.com`

```
✅ Как проверить заход X.Y (github.com)

**Создание ветки**
Репозиторий → main ▾ → Create branch. См. §20.3.

**Правки**
Файл 1: открыть → ✏️ → Commit changes → «X.Y/1 — ИмяФайла».
Файл 2: открыть → ✏️ → Commit changes → «X.Y/2 — ИмяФайла».

**PR**
Compare & pull request → Create pull request.

**CI (вручную)**
Actions → Build & Test → Run workflow.

**Merge**
Merge pull request → Confirm merge → Delete branch.

**Если что-то падает**
Полный текст ошибки — в чат.
```

**Правила:**
- Конкретные действия и ожидания.
- Простой язык.
- Без adb и SQL, если не критично.

---

## 6. Что делать при ошибках

### 6.1. Сборка упала
- Попросить **полный текст ошибки** (не скриншот).
- Найти причину, исправить, выдать **полный файл** заново.

### 6.2. Логика не работает
- Попросить что происходит vs что ожидалось.
- Уточнить: что нажал, что увидел, что в логах.

### 6.3. Миграция БД упала
- Сказать снести приложение и запустить заново.
- Исправить `Migration`.

### 6.4. CI упал
- Попросить **полный лог** (не скриншот).
- Разобрать по шагам: `Setup Android SDK`, `Run unit tests`,
  `Build Debug APK`. Указать, где именно упало.

### 6.5. Git-конфликт
- Попросить имя ветки и имена файлов из конфликта.
- Объяснить пошагово, что оставить.

---

## 7. Правила по документации

### 7.1. Когда обновлять
- После каждого закрытого захода — `PROGRESS.md`, `NEXT_STEPS.md`,
  `CONTEXT_BRIEF.md`.
- Решения — в `DECISIONS.md`.
- Изменения БД — в `DATABASE.md`.
- Изменения ГП — в `VOICE.md`.
- Изменения правил работы — в `AI_RULES.md`, `README.md`.

### 7.2. Как обновлять
- Файл целиком, не фрагмент.
- Пометка «Заменить полностью» — жирным.
- Путь в текстовом блоке.
- Шапка `X/Y`.

### 7.3. Напоминание про репозиторий и коммит

**В самом конце ответа** — напоминание:

**Для AS и github.dev:**

```
Заход X.Y закрыт. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Коммит: X.Y — краткое описание
```

**Для github.com:**

```
Заход X.Y закрыт. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Коммиты:
- X.Y/1 — Имя1
- X.Y/2 — Имя2
```

**Важно:** коммит **не запускает** CI автоматически. CI — вручную
(§20.5).

---

## 8. Принципы обсуждения

### 8.1. Перед кодом — обсуждение
Если задача сложная: сначала спецификация, потом код.

### 8.2. Не пиши код, если:
- Нет полного понимания задачи.
- Пользователь задал вопрос «как лучше?».
- Есть открытые вопросы по архитектуре.

### 8.3. Аудит
Перед крупным блоком — аудит: каталог команд, конфликты, вопросы.

---

## 9. Приоритеты

1. Работающий код > красивый.
2. Простое > сложное.
3. Понятный > лаконичный.
4. Документация > код без документации.

---

## 10. Что категорически нельзя

- Обрезать код («…», «остальное как было»).
- Угадывать содержимое файлов.
- Менять схему БД без миграции.
- Использовать Apache POI.
- Писать комментарии на английском.
- Забывать раздел «Как проверить».
- Отвечать без номера файла.
- Забывать шапку `X/Y`.
- Забывать ссылку на репозиторий.
- **Забывать компактную шапку захода** (§2.2).
- **Писать пошаговые инструкции по веткам внутри ответа** — они в §20.
- **Работать в ветке дома** — дома работаем в `main`.

---

## 11. Шаблон ответа на заход кода

**Пример — дома, два файла:**

```
**Файлов:** 2

**Заход:** 5.8.9h-2 — UnifiedSearch в UI и ГП
**Режим:** дома (Android Studio)
**Ветка:** нет, работаем в main
**Коммит:** `5.8.9h-2 — UnifiedSearch в UI и ГП`

---

1/2. **Заменить полностью** — UnifiedSearch.kt
Новый единый алгоритм поиска.

**Заменить полностью**

```text
app/src/main/java/com/example/geosamplemanager/data/voice/UnifiedSearch.kt
```

```kotlin
<полный код>
```

---

2/2. **Заменить полностью** — VoiceSearch.kt
Обёртка над UnifiedSearch.

**Заменить полностью**

```text
app/src/main/java/com/example/geosamplemanager/data/voice/VoiceSearch.kt
```

```kotlin
<полный код>
```

---

## Шаг 1. Собрать и запустить

**Действие:** `Build → Make Project`. Ожидаемо: `BUILD SUCCESSFUL`.
Затем `Run → Run 'app'`.

## Шаг 2. Закоммитить и запушить

**Действие:** иконка Commit слева (третья сверху). Галочка `All`.
Сообщение `5.8.9h-2 — UnifiedSearch в UI и ГП`. Кнопка `Commit`.
Сверху появится `Push` — нажми, затем снова `Push`.

## Шаг 3. Проверка

**Действие:** Terminal (`Alt+F12`) → `git status`.
Ожидаемо: `nothing to commit, working tree clean`.

---

✅ Как проверить заход 5.8.9h-2

<сценарии>

---

Заход 5.8.9h-2 закрыт. Файлы можно пушить:

Репозиторий: https://github.com/ilia23star2-ops/GeoSampleManager

Коммит: 5.8.9h-2 — UnifiedSearch в UI и ГП
```

---

## 12. Шаблон ответа на вопрос (без кода)

1. Короткий ответ — 1–2 строки.
2. Разбор по пунктам.
3. Рекомендация.
4. Вопросы.

Не пиши код в ответ на вопрос. Сначала обсудите.

---

## 13. Что делать при смене чата

Пользователь приложит файлы из §0. Ты:

1. Прочитай все.
2. Перескажи контекст — 5–8 строк.
3. **Задай вопросы по режиму (§0, п. 2).**
4. Ждать команду. Не начинай код сам.

---

## 14. Чек-лист перед отправкой ответа

- [ ] **Задал вопрос про режим** (при старте чата).
- [ ] Первая строка — «Файлов: X/Y».
- [ ] **Шапка захода — компактная** (§2.2).
- [ ] **В шапке: заход, режим, ветка, коммит.**
- [ ] **Никаких пошаговых инструкций по веткам внутри ответа.**
- [ ] **Комментарии к элементарным действиям — минимальны** (§1.2).
- [ ] **Комментарии к git-операциям — пошаговые** (§2.4).
- [ ] У каждого файла шапка `X/Y`.
- [ ] Полный путь в текстовом блоке.
- [ ] Код полный, без «…».
- [ ] Комментарии на русском.
- [ ] Раздел «Как проверить» — есть, соответствует режиму.
- [ ] `CONTEXT_BRIEF.md` обновлён (если заход закрыт).
- [ ] **Напоминание про репозиторий и коммит — в конце.**
- [ ] **Указано, что CI — вручную.**
- [ ] Если просил файлы — сгруппированы по папкам.

---

## 15. Полезные файлы проекта

| Файл | Когда нужен |
|---|---|
| `README.md` | Структура проекта |
| `CONTEXT_BRIEF.md` | Где мы сейчас |
| `PROGRESS.md` | История |
| `NEXT_STEPS.md` | Текущий заход |
| `DECISIONS.md` | Логика UI |
| `DATABASE.md` | Схема БД |
| `ROADMAP.md` | Приоритеты |
| `VOICE.md` | ГП |
| `ISSUES.md` | Проблемы |
| `GLOSSARY.md` | Термины |
| `TESTING.md` | Тесты |
| `AI_RULES.md` | Этот файл |

---

## 16. Финальное правило

**Пользователь — не робот.** Он устаёт, отвлекается, ошибается.
Твоя задача — **сокращать его работу**.

Каждый твой ответ должен:
- Не заставлять переспрашивать.
- Не заставлять копировать по частям.
- Не заставлять догадываться, что проверять.
- Экономить время.

Если сомневаешься между «быстро» и «понятно» — **делай понятно**.

---

## 17. Стоп-сигналы — когда ИИ обязан остановиться

**Если задача попадает хотя бы в один пункт — ИИ не пишет код.
Сначала спрашивает.**

1. **Задача затрагивает >3 файлов.** Спросить: «Разбить на части?».
2. **Задача меняет схему БД.** Спросить: «Какая миграция?».
3. **Задача меняет публичную сигнатуру.** Спросить: «Кто ещё
   использует?».
4. **Задача противоречит `CONTEXT_BRIEF.md`.** Сказать прямо.
5. **Задача сформулирована словами** «улучшить», «оптимизировать»,
   «починить». Спросить: «Что конкретно?».
6. **Нужно удалить код.** Спросить подтверждение.
7. **Правка каскадит** — тянет ещё 2+ файла. Остановиться.
8. **Задача похожа на уже сделанную.** Спросить: «Повторяем?».
9. **В чате >30 сообщений.** Предложить обновить `CONTEXT_BRIEF.md`.
10. **Не понятно, что надо.** Задать уточняющий вопрос.

### Формат вопроса при стоп-сигнале

```
Стоп-сигнал: <причина>.

Варианты:
А) <сделать как есть>
Б) <разбить на части>
В) <отложить>
Г) <свой вариант>
```

---

## 18. Атомарность заходов

**Правило:** один заход — **1–3 файла**.

- Если файлов больше 3 — разбить.
- Если один файл >500 строк — разбить на классы.
- Если заход смешивает UI и БД — разбить.

**Почему:** меньше файлов = меньше потерь, легко откатить, легко
проверить.

---

## 19. Откат по умолчанию

**Если после правки что-то сломалось — откатить, не чинить.**

### Как (дома, AS)

1. Открой Terminal (`Alt+F12`).
2. Откат одного файла:
   ```
git checkout main -- путь/к/файлу.kt
   ```
3. Откат всех незакоммиченных правок:
   ```
git checkout -- .
   ```
4. Откат последнего коммита (если уже закоммичен):
   ```
git reset --hard HEAD~1
   ```

### Что НЕ считается поломкой
- Мелкая ошибка в строке.
- Опечатка в комментарии.
- Отсутствующая зависимость в gradle.

### Поломка
- Приложение не запускается.
- Рабочий сценарий сломался.
- БД падает при миграции.
- Новая ошибка в логах.

---

## 20. Работа с двух машин — три режима

**Контекст:** проект разрабатывается на **двух машинах**:

- **Дома** — Android Studio, полный цикл (сборка, тесты, эмулятор).
- **На работе** — без IDE. **Приоритет:** `github.dev`. **Fallback:**
  `github.com`. Git на машине не установлен.

**Режим определяется один раз в начале чата (§0).**

### 20.1. Режим А — дома (Android Studio)

**Главное:** дома **не работаем в ветках**. Всё — прямо в `main`. Git-цикл
простой.

**Что можно просить у пользователя:** сборку, тесты, эмулятор,
устройство, логи, скриншоты.

**Цикл:**

1. **Открыть Android Studio.** Открыть проект.
2. **Подтянуть свежее.** Терминал (`Alt+F12`):
   ```
git checkout main
git pull
   ```
3. **Заменить файлы** — по инструкции из ответа ИИ.
4. **Собрать.** `Build → Make Project`. Ожидаемо: `BUILD SUCCESSFUL`.
5. **Запустить.** `Run → Run 'app'` → эмулятор или устройство.
6. **Прогнать тесты** (если есть). ПКМ по `app/src/test/` → `Run 'Tests in …'`.
7. **Закоммитить и запушить** — подробно см. §2.4:
    - Иконка **Commit** слева (третья сверху).
    - Галочка `All`.
    - Сообщение: `X.Y — краткое описание`.
    - Кнопка `Commit`.
    - Сверху `Push` → снова `Push`.
8. **Проверить.** Terminal → `git status`. Ожидаемо:
   `nothing to commit, working tree clean`.

**Опционально:** можно работать в feature-ветке — если задача крупная
или экспериментальная. Но по умолчанию — в `main`.

### 20.2. Режим Б — на работе, `github.dev` (приоритет)

**Что нельзя просить:** сборку, тесты, эмулятор, устройство.

**Цикл:**

1. Открыть `https://github.com/ilia23star2-ops/GeoSampleManager`.
2. Нажать `.` (точка) — откроется `github.dev`.
3. **Создать ветку:** внизу слева кнопка `main` → **Create new branch**
   → ввести имя.
4. Правки файлов.
5. **Коммит + пуш:** левая панель → Source Control (`Ctrl+Shift+G`) →
   сообщение → **Commit & Push**. Один коммит на все файлы.
6. **PR** — баннер `Compare & pull request` или вкладка Pull requests.
7. **CI — вручную** (§20.4).
8. **Merge** → удалить ветку.

### 20.3. Режим В — на работе, `github.com` (fallback)

Используется, если `github.dev` недоступен.

**Что нельзя просить:** сборку, тесты, эмулятор, устройство.

**Цикл:**

1. Открыть репозиторий на github.com.
2. **Создать ветку:** `main ▾` → поле ввода → `feature/X.Y-...` →
   Create branch.
3. **Правки по файлам**, коммит под каждым:
    - Файл 1: открыть → ✏️ → Commit changes → `X.Y/1 — ИмяФайла.kt`.
    - Файл 2: ... → `X.Y/2 — ИмяФайла.kt`.
4. **PR** — Compare & pull request → Create pull request.
5. **CI — вручную** (§20.4).
6. **Merge** → удалить ветку.

### 20.4. CI — только вручную

**Автозапуска по push / PR нет.** CI запускается **по команде**.

**Шаги:**

1. GitHub → вкладка **Actions**.
2. Слева — **Build & Test**.
3. Справа сверху — **Run workflow**.
4. Выбрать ветку.
5. Чек-боксы:
    - **Run unit tests** — по умолчанию ✅.
    - **Build Debug APK** — по умолчанию ⬜.
6. Зелёная кнопка **Run workflow**.
7. Через 2–3 секунды — запуск в списке.
8. По окончании — артефакты внизу страницы запуска.

**Что когда выбирать:**

| Ситуация | run_tests | build_apk |
|---|---|---|
| Быстрая проверка (только тесты) | ✅ | ⬜ |
| Полная сборка | ✅ | ✅ |
| Только APK (тесты уже прогонялись) | ⬜ | ✅ |

**Артефакты:**
- `app-debug` — APK (если `build_apk`).
- `test-report` — HTML-отчёт (если `run_tests`).

**Что делать при красном CI:**
1. Открыть упавший запуск.
2. Нажать на шаг с красным крестом.
3. Скопировать полный текст ошибки → в чат.
4. ИИ разбирает и даёт правку.

### 20.5. Правила гигиены

- **Дома — `git pull` перед началом.**
- **На работе — только через ветку.**
- **Дома — можно в `main`.**
- **Одна задача = одна ветка** (для работы).
- **Коммит после каждого захода.**
- **Пуш в конце сессии.** Даже если задача не закончена.
- **После merge — удалить ветку.**

### 20.6. Что ИИ должен учитывать

- **В рабочем чате:** не просить «собери и пришли лог». Только правки,
  коммит, CI.
- **В домашнем чате:** можно просить сборку, тесты, логи, скриншоты.
- **Формат ответа — единый.** Файлы целиком, компактная шапка, «Как
  проверить».
- **Уточнять, где работает пользователь**, если из контекста неясно.

### 20.7. Vosk-модель

**Не коммитится в Git.** В CI выкачивается из релиза GitHub
(`models-v1`) отдельным шагом в `build.yml`. Дома лежит локально в
`app/src/main/assets/vosk-model-small-ru-0.22/`.

### 20.8. Ветки — сводка

| Режим | Ветки |
|---|---|
| Дома (AS) | Не создаём. Работаем в `main`. |
| github.dev | `feature/X.Y-...` или `fix/X.Y-...`. |
| github.com | `feature/X.Y-...` или `fix/X.Y-...`. |

---

## 21. Коммит и ветки — сводка

| Режим | Ветка | Коммит |
|---|---|---|
| Дома (AS) | Нет | `X.Y — описание` |
| github.dev | `feature/X.Y-...` | `X.Y — описание` |
| github.com | `feature/X.Y-...` | `X.Y/N — ИмяФайла` |

**CI не запускается от коммита.** Запуск — вручную (§20.4).

---

## 22. Финальное правило (повтор)

**Пользователь — не робот.** Он устаёт, отвлекается, ошибается.
Твоя задача — **сокращать его работу**.

Каждый твой ответ должен:
- Не заставлять переспрашивать.
- Не заставлять копировать по частям.
- Не заставлять догадываться, что проверять.
- Экономить время.

Если сомневаешься между «быстро» и «понятно» — **делай понятно**.