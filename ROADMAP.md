# Roadmap — GeoSample Manager

> План развития проекта от текущего состояния до релиза.
> Обновляется по мере продвижения. Детали каждого этапа — в `NEXT_STEPS.md`.
>
> Легенда:
> - ✅ — сделано
> - 🟡 — в работе
> - ⏸ — приостановлено (ждёт решения или зависимостей)
> - ⬜ — запланировано
> - 🔮 — отложено на потом

---

## Статус одной строкой

**Этапы 1–4 завершены. Этап 5.1 завершён. Этап 5.9 (Room) частично закрыт.
В работе — 5.5 (заметки и фото).**

---

## Ближайший план (до релиза MVP)

| # | Этап | Статус | Заходов | Зависит от |
|---|---|---|---|---|
| 5.9 | Room — доводка | 🟡 | 1 | — |
| 5.5 | Заметки и фото | ⬜ | 3 | 5.9 |
| 5.8 | Голосовой помощник | ⬜ | 4–5 | 5.5, 5.9 |
| 5.10 | Полировка и адаптивность | ⬜ | 3 | 5.5, 5.8 |
| 6 | Релиз MVP | ⬜ | 2 | 5.9, 5.5, 5.8, 5.10 |

**Итого от текущего момента: ~13–14 заходов.**

---

## Этапы

### ✅ Этапы 1–4 — База проекта

**Что сделано:**
1. Скелет приложения, Drawer, 7 вкладок, навигация.
2. Room-БД: 5 таблиц, DAO, Repository.
3. Настройки импорта, дерево категорий, экспорт/импорт JSON,
   пользовательские префиксы участков.
4. Полный Excel-импорт: свой парсер `.xlsx`, `ExcelAnalyzer`,
   `AreaResolver`, `OrderNumberExtractor`, `SampleFilter`,
   `MappingEditorDialog`, словари, история импортов.

**Детали:** `PROGRESS.md`, `DATABASE.md`.

---

### ✅ Этап 5.1 — Каркас сверки

**Что сделано:**
- Полный UI-экран «Сверка и поиск» на заглушках.
- Модели UI, состояние, undo/redo (20 шагов), плоский `LazyColumn`.
- Все диалоги: вес, характеристика, заметка, редактор, удаление,
  настройки наряда, массовые решения.
- Логика чек-боксов: найдена / ВК / холостая / отложена / ошибка.
- Поиск по скважине и пробе, фонарик, группировка.
- Счётчики групп и статистика на лету.
- Динамический заголовок «Скважина / Выработка».
- Легенда символики, цветовая индикация.

**Детали:** `PROGRESS.md`, `DECISIONS.md`.

---

### 🟡 Этап 5.9 — Room (доводка)

**Что уже работает:**
- `ReconciliationViewModel` загружает данные из БД.
- Атомарные UPDATE для одиночных действий.
- Батч-обновления для групповых.
- Удаление с пересчётом номеров в транзакции.
- Undo/redo синхронизируется с БД.

**Что осталось:**
- ⬜ Проверить производительность на больших нарядах (300+ проб).
  Если тормозит — перевести статистику на SQL (`COUNT` с фильтрами).
- ⬜ Заменить `persistAll` после undo/redo на diff-подход (сохранять
  только изменённые строки).
- ⬜ Финальная проверка синхронизации `has_note` (после 5.5).
- ⬜ Обработка `order_wells` — сейчас заполняется, но не используется
  в сверке. Возможно, пригодится для группировки по скважинам.

**Заходов:** 1.

**Зависимости:** нет. Можно делать параллельно с 5.5.

---

### ⬜ Этап 5.5 — Заметки и фото

**Решения приняты (см. `DECISIONS.md`):**
- Несколько фото на пробу.
- Заметка и фото независимы.
- Файлы — во внутреннем хранилище.
- Сжатие до 1024 px, JPEG 80%.
- Съёмка — `TakePicture()` + `FileProvider`.
- Галерея — `PickVisualMedia()`.
- Иконки в строке + пункты в меню 3 точки.
- Один диалог на заметку и фото.

**План заходов:**

**5.5.1 — Инфраструктура** ⬜
- `FileProvider` в манифесте.
- `res/xml/file_paths.xml`.
- Разрешение `CAMERA`.
- Утилита `PhotoStorage` (создание, сохранение, сжатие, удаление).
- Методы в репозитории: `addPhoto`, `deletePhoto`, `getPhotosForSample`.
- Изменения БД (миграция 1 → 2):
    - `SampleEntity.has_photo`.
    - Убрать `SampleNoteEntity.image_path`.
    - Новая таблица `sample_images`.

**5.5.2 — Диалог** ⬜
- `NotePhotoDialog`.
- `TakePicture` и `PickVisualMedia` через `rememberLauncherForActivityResult`.
- Превью, полноэкранный просмотр.

**5.5.3 — Отображение** ⬜
- Поля `hasNote`, `hasPhoto`, `photos` в UI-модели.
- Синхронизация флагов.
- Иконки в строке как точки входа.

**Заходов:** 3.

**Зависимости:** 5.9 (нужна работающая БД).

---

### ⬜ Этап 5.8 — Голосовой помощник

**Решение:** Вариант А — микрофон на экране сверки.

**Компоненты:**
- `SpeechRecognizer` (распознавание).
- `TextToSpeech` (озвучка, короткая, без задержек).
- Bluetooth-гарнитура (роутинг аудио, микрофон).
- Разрешения: `RECORD_AUDIO`, `BLUETOOTH_CONNECT`.

**Сценарии:**
1. «Скважина 123» → TTS: «Скважина 123, наряд 5, N проб».
2. «Первая» / «Один» / «1» → отметка пробы 12301.
3. ВК: «Взвесьте» → диалог веса (голосом или руками).
4. Отложенная → TTS: «Отложена. Заметка?».
5. Все пробы отмечены → TTS: «Всё отмечено».
6. Кнопка «стоп» всегда доступна.

**План заходов:**

**5.8.1 — Каркас голоса** ⬜
- Кнопка микрофона на экране (уже есть в поле поиска, заменить
  на реальный запуск).
- `SpeechRecognizer` + разрешения.
- TTS + настройка русского голоса.
- Bluetooth-роутинг.

**5.8.2 — Сценарий «скважина → список проб»** ⬜
- Распознать номер скважины.
- Найти в БД → озвучить наряд + количество проб.
- Ожидание команды выбора пробы.

**5.8.3 — Отметка проб голосом** ⬜
- «Первая», «Вторая», «Один», «Два» → отметка.
- Проверка на ВК / холостую / отложенную.
- Голосовой диалог веса.
- Отчёт при полной отметке скважины.

**5.8.4 — Расширенные команды** ⬜
- «Отложить с заметкой», «Пропустить», «Отмена».
- Навигация по списку.
- Стоп-слова.

**5.8.5 — Полировка** ⬜
- Быстрые ответы TTS (без пауз).
- Обработка шумной среды.
- Fallback на ручной ввод.

**Заходов:** 4–5.

**Зависимости:** 5.5, 5.9.

---

### ⬜ Этап 5.10 — Полировка

**План заходов:**

**5.10.1 — Темы** ⬜
- Тёмная тема.
- Системная / ручное переключение в настройках.

**5.10.2 — Адаптивность под телефон** ⬜
- Сжатые таблицы.
- Альтернативные раскладки для узких экранов.

**5.10.3 — UX-полировка** ⬜
- Haptic feedback на чек-боксы.
- Крупные элементы под перчатки.
- Сохранение позиции скролла.
- Обработка «нет прав на камеру/микрофон/микрофон BT».
- Анимации переходов.

**Заходов:** 3.

**Зависимости:** 5.5, 5.8.

---

### ⬜ Этап 6 — Релиз MVP

**Что входит:**

**6.1 — Стабилизация** ⬜
- Прогон сценариев на реальных данных (2–3 файла из практики).
- Баг-фикс.
- Проверка миграций БД.

**6.2 — Подготовка к публикации** ⬜
- Иконка приложения.
- Скриншоты для внутреннего распространения.
- Инструкция для рабочих (кратко, с картинками).
- Сборка подписанного APK.

**Заходов:** 2.

**Зависимости:** 5.9, 5.5, 5.8, 5.10.

---

## Технический долг

Задачи, которые не блокируют, но стоит закрыть до релиза.

| # | Задача | Приоритет | Когда |
|---|---|---|---|
| ТД-1 | SQL-статистика (вместо подсчёта в UI) | Средний | Если 5.9 покажет тормоза |
| ТД-2 | Diff-persist после undo/redo | Средний | В 5.9 |
| ТД-3 | Поле `number_in_well` в БД вместо вычисления в UI | Низкий | Опционально |
| ТД-4 | Флаг `is_import_error` вместо эвристики «проба == скважина» | Средний | При следующей миграции |
| ТД-5 | Хранение настроек холостых в `OrderEntity` | Средний | При следующей миграции |
| ТД-6 | Хранение шага ВК в `OrderEntity` / `AreaEntity` | Средний | При следующей миграции |
| ТД-7 | Автоматическая простановка флага ВК при импорте | Высокий | Отдельный этап после 5.8 |
| ТД-8 | Использование `order_wells` в сверке (группировка по скважинам) | Низкий | Опционально |

---

## Отложено на потом (🔮)

Не входит в MVP. Возможные направления развития.

- **Экспорт результатов сверки** (Excel / CSV).
- **Синхронизация между устройствами** (через облако или обмен файлами).
- **Роли и права** (геолог / рабочий / администратор).
- **История изменений пробы** (кто, когда, что).
- **Оффлайн-карта** с привязкой проб к координатам.
- **Bluetooth-весы** (прямая передача веса).
- **Штрих-код-сканер** для номеров проб.
- **Камера внутри приложения** (CameraX) вместо системной.
- **Печать сопроводительных листов** через Bluetooth-принтер.

---

## Оценка по заходам (сводная)

| Этап | Заходов | Статус |
|---|---|---|
| 5.9 — доводка Room | 1 | 🟡 |
| 5.5 — заметки и фото | 3 | ⬜ |
| 5.8 — голосовой помощник | 4–5 | ⬜ |
| 5.10 — полировка | 3 | ⬜ |
| 6 — релиз MVP | 2 | ⬜ |
| **Итого** | **13–14** | |

---

## Правила работы над этапами

1. **Один заход = один законченный кусок.** Не смешивать «инфраструктуру»
   и «UI», «логику» и «полировку».
2. **После каждого захода — коммит** с описанием.
3. **Перед сменой чата** — обновить `PROGRESS.md` и `NEXT_STEPS.md`.
4. **Если что-то решено по ходу** — дописать в `DECISIONS.md`.
5. **Не менять Room-сущности без предупреждения** — сначала согласовать,
   потом писать миграцию.

---

## Что считать «готовым к релизу MVP»

**Критерии:**

1. Импорт Excel работает на 3+ разных файлах из практики без падений.
2. Сверка наряда проходит end-to-end: загрузка → поиск → отметка →
   ВК → холостые → отложенные → удаление → undo.
3. Заметки и фото сохраняются, переживают перезапуск, читаются обратно.
4. Голосовой помощник уверенно распознаёт номера и отметки
   в тихой комнате, с Bluetooth-гарнитурой.
5. Тёмная тема и адаптивность под телефон работают.
6. Нет падений на сценариях из списка 2–3 часов работы подряд.

Если всё зелёное — MVP готов. Дальше — отложенные фичи.