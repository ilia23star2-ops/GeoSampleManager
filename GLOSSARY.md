# GLOSSARY.md — термины проекта

> Единый словарь. Если в задаче встретилось слово — оно должно быть здесь.
> Если слова нет — добавь, потом используй.

---

## Данные

| Термин | Что значит |
|---|---|
| **Участок** | `AreaEntity`. Объект разведки. Пример: «Коптеловский». |
| **Наряд** | `OrderEntity`. Поручение на отбор проб. Пример: «наряд 27». |
| **Проба** | `SampleEntity`. Единица опробования. Имеет `sample_number`. |
| **Скважина** | Строка `well_number` в пробе. Может быть «выработка» (для бороздовых). |
| **Заметка** | `SampleNoteEntity`. Одна на пробу. |
| **Фото** | `SampleImageEntity`. 0..N на пробу. |
| **Группа** | `SampleGroup` — UI-модель наряда с его пробами. |
| **Строка** | `SampleRow` — UI-модель одной пробы. |

## Поля пробы

| Термин | Что значит |
|---|---|
| **found** | Проба найдена (галка). |
| **postponed** | Отложена (разобраться позже). |
| **weight_control** | Флаг ВК. |
| **control_weight** | Вес ВК. |
| **has_note** / **has_photo** | Есть заметка / есть фото. |
| **status** | `normal` / `blank` / `control`. |
| **sample_type** | `auger` / `channel` / `cobra` / `duplicate`. |

## Excel-импорт

| Термин | Что значит |
|---|---|
| **Лист** | Sheet в Excel. Один лист = один наряд (обычно). |
| **Шапка** | Строки заголовков колонок в Excel. |
| **Маппинг** | Сопоставление колонок Excel с ролями (`well`, `sample`, …). |
| **Роль** | `serial`, `well`, `sample`, `int_from`, `int_to`, `weight`, `type`, `material`. |
| **Общий список** | Лист с пробами из нескольких нарядов сразу. Не импортируется. |

## Голосовой помощник

| Термин | Что значит |
|---|---|
| **ГП** | Голосовой помощник. |
| **Сессия** | Один цикл слушания — от «Слушаю» до «Стоп». |
| **Авто-режим** | Режим отметок. Включается, когда ответ единственный. |
| **Сортировка** | Справочный режим. ≥2 номеров подряд, без отметок. |
| **Запрос** | Один токен в поле поиска. Через пробел = несколько запросов. |
| **Порядковый** | «Первая», «вторая», …, «тридцатая». Работает только в авто-режиме. |
| **Суффикс** | Последние 2–3 цифры номера пробы. Пример: `01` в `NV152401`. |
| **Префикс** | Буквенная часть номера. Пример: `KPD`, `NV`, `ACD`. |
| **Фонарик** | Индикатор единственного ответа (💡). |
| **Статус-бар** | Тонкая полоса внизу экрана сверки с текущим состоянием ГП. |

## UI

| Термин | Что значит |
|---|---|
| **Легенда** | Кнопка «?» с пояснениями цветов и иконок. |
| **Фильтр** | Чип-фильтр: Найдены / ВК / Отложены / Ошибки. |
| **Развернуть все** | Кнопка раскрытия всех групп в множественном поиске. |
| **Характеристика** | Столбец с `material_desc`. Может быть скрыт фильтром. |

## Процесс

| Термин | Что значит |
|---|---|
| **Заход** | Законченный кусок работы. Пример: «5.8.5 — авто-режим». |
| **Аудит** | Разбор логики перед реализацией. Без кода. |
| **Спецификация** | Документ с правилами. Пример: `VOICE.md`. |
| **Стоп-сигнал** | Ситуация, когда ИИ обязан спросить. См. `AI_RULES.md` §17. |

---

## Что НЕ значат эти термины

- «**Проба**» — не «попытка». Это `SampleEntity`.
- «**Наряд**» — не «одежда». Это `OrderEntity`.
- «**Группа**» — не группа проб в UI-списке. Это `SampleGroup` = наряд + его пробы.
- «**Запрос**» — не «SQL-запрос». Это токен в поле поиска.

---

## Как добавлять

Если в обсуждении встретилось слово, которого нет:
1. Добавить в таблицу.
2. Использовать в коде и документации одинаково.
