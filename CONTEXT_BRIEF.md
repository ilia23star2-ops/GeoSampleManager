# CONTEXT_BRIEF.md — где мы сейчас

> **Одна страница «где мы сейчас».** Обновляется в конце каждой сессии.
> Новый ИИ читает **вторым** после `AI_RULES.md`.
>
> **Дата обновления:** 2026-09-16

---

## Где мы

- **Текущий заход:** 5.8.8 — ГП: голосовые команды и сортировка (**закрыт**)
- **Последний закрытый:** 5.8.8k — неоднозначность, чужой участок, TTS
- **Следующий:** 5.8.9 — UI без диалога (волны + статус-бар)

## Что было в последней сессии

- Закрыли серию **5.8.8d–k** — работа над голосовым помощником:
    - 5.8.8d — `spellNumber`, `awaitingWeight`, вес для холостых.
    - 5.8.8e — фикс `Sort` по «и» в `VoiceCommandParser`.
    - 5.8.8f — `VoiceSearch`: `FoundMany` при разных нарядах.
    - 5.8.8g — UI: красный ⚠ фонарик, баннер, подписи на заголовках.
    - 5.8.8h — автопауза после «Найден в нескольких нарядах».
    - 5.8.8i — сортировка: цифры без парсера, автопауза при неоднозначности.
    - 5.8.8j — UI обновляется при `FoundMany`; TTS говорит **парами**.
    - 5.8.8k — чужой участок ≠ «несколько нарядов»; TTS без участка.
- Проверили 7 сценариев — все работают.
- Обновили `ISSUES.md`, `PROGRESS.md`, `NEXT_STEPS.md`.

## Что делать дальше

1. **Заход 5.8.9 — UI без диалога:**
    - Убрать блокирующий `VoiceDialog` (AlertDialog).
    - Добавить неблокирующий `VoiceOverlay` (волны, статус-бар).
    - Статус-бар под кнопками «Отмена / Вперёд».
    - Звук единственного ответа (двойной восходящий бип).

## Открытые вопросы

1. **Пауза:** 5 мин тишины — норм? Или 3?
2. **Где рисовать волны:** снизу или в шапке?
3. **Статус-бар:** видим постоянно или только когда ГП слушает?

## Известные проблемы

См. `ISSUES.md`. Ключевые:

- **И-1.** Авто-режим не включается после поиска (5.8.5).
- **И-2.** Статус-бар не отображается (5.8.9).
- **И-3.** Голосовой диалог блокирует UI (5.8.9).
- **И-4.** Порядковые не работают, пока не включён авто-режим (5.8.5).
- **И-7.** Множественный поиск не группирует по участкам (5.11-fix).
- **И-8.** Нет звукового сигнала единственного ответа (5.8.9).
- **И-9.** Онбординг не показывается при первом запуске (5.8.9).
- **И-10.** `showCharacteristic` сбрасывается после перезапуска (5.10).

## Не трогать в этой сессии

- Excel-импорт (работает).
- Заметки и фото (закрыто в 5.5).
- Схема БД (version = 2).
- `VoiceSearch`, `VoiceNumberParser`, `VoiceCommandParser` (закрыты в 5.8.8).

## Файлы, которые понадобятся

**ui/screens/**
- `SearchScreen.kt`
- `VoiceDialog.kt`
- `ReconciliationViewModel.kt`
- `ReconciliationState.kt`

**data/voice/**
- `VoiceSession.kt`
- `VoiceController.kt`
- `VoiceFeedback.kt`

**новые:**
- `ui/screens/VoiceOverlay.kt`
- `ui/screens/VoiceStatusBar.kt`

## Правила текущей сессии

- **Только эти файлы.**
- **Не менять схему БД.**
- **Не менять `VoiceSearch`, `VoiceNumberParser`, `VoiceCommandParser`.**
- **После захода** — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`,
  `NEXT_STEPS.md`, `ISSUES.md`.

---

## Как обновлять этот файл

В конце каждой сессии:

1. Обновить «Дата обновления».
2. Обновить «Где мы» — текущий заход, статус, следующий.
3. Обновить «Что было в последней сессии» — 3–5 пунктов.
4. Обновить «Что делать дальше».
5. Обновить «Открытые вопросы».
6. Обновить «Известные проблемы» (синхронизировать с `ISSUES.md`).
7. Обновить «Не трогать в этой сессии».
8. Обновить «Файлы, которые понадобятся».
9. Обновить «Правила текущей сессии».

**Максимум одна страница.** Если больше — режем.