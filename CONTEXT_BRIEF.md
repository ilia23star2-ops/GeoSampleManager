# CONTEXT_BRIEF.md — где мы сейчас

Одна страница «где мы сейчас». Обновляется в конце каждой сессии.
Новый ИИ читает вторым после `AI_RULES.md`.

**Дата обновления:** 2026-09-25 (вечер)

## Где мы

**Дома:** работаем в `main`, Android Studio.
**На работе:** правки через `github.com`.
**Фича в работе:** `feature/5.8.11-e4-voice-v2` — серия `e4` (готова к PR в main).
**Последнее закрытое:** `e4-fix-voice-1` — 4 фикса голоса.
**Следующее:** docs-заход + PR фичи `e4` в `main`.

## Что было в последних сессиях

**Серия `5.8.11-e4` — рефакторинг голосового помощника.**

**Закрыто (в фиче `feature/5.8.11-e4-voice-v2`):**

- **`e4e-bundle`** — мимикрия TTS + единый путь поиска через `SearchService`.
- **`e4-tests`** — покрытие парсера (3 тест-файла, 31 тест).
- **`e4-pin-1`** — состояние `FOUND_PINNED`, `PinnedScope`, `pin/unpin`.
- **`e4-pin-2`** — очередь мультизапроса (`queue`, `enqueue`, `nextInQueue`).
- **`e4-pin-3`** — вес «X сотни», «следующая X», `FoundOne.queueSize`.
- **`e4-pin-4`** — числительные в pin, fallback 14↔4.
- **`e4-pin-5`** — fallback расширен (40↔4, 400↔4, 4000↔4).
- **`e4-pin-6`** — показ подмены «Распознано X → Y» (откачен в pin-7).
- **`e4-pin-7`** — отказ от fallback. Честная ошибка + подсказка числом.
  Acсent fix — «Распознано» убрано из голоса.
- **`e4-markers`** — маркеры намерения (`AWAITING_MARK/CLEAR/POSTPONE`),
  подтверждение массовых (`AWAITING_CONFIRM`). Тайм-аут 30 сек,
  предупреждение на 20-й.
- **`e4-markers-2`** — отложение одной фразой («отложи вторую»),
  `spellMimicry` везде (мимикрия номера из БД).
- **`e4-speak-1`** — `splitLikeHuman`: разбиение длинных номеров «как человек».
- **`e4-prefix-1`** — префиксы произносятся по буквам раздельно
  («ка пэ дэ»); буквы добавлены в грамматику Vosk.
- **`e4-weight-queue`** — очередь веса после «отметь все»: отметить
  всё, что можно, потом по одной спросить вес.
- **`e4-ui-1`** — кнопка «Наверх» в списке проб (появление при скролле >10).
- **`e4-fix-voice-1`** — 4 фикса: «Распознано» = канонический номер;
  подсказка в `voiceClearOrdinal`; фраза «Все пробы отмечены»;
  SORT+attention (участок/наряд).

**Закрыто (в `main`):**

- `e4-hotfix`, `e4-fix-1`, `e4-fix-2` — точечные фиксы.
- `docs/5.8.11-rules-fix-merge`, `docs/5.8.11-git-rules`, `docs/5.8.11-tests-rules`.
- `docs/5.8.11-e4-pin-close` — фиксация серии `e4-pin`.
- `chore/5.8.11-ci-automation` — CI на PR, APK по лейблу.

## Что делать дальше

**Сейчас:** PR фичи `feature/5.8.11-e4-voice-v2` в `main`. После merge — фича
остаётся архивом (§2.9). Возврат — `feature/5.8.11-e4-voice-v3`.

**Очередь заходов (следующая серия):**

1. **`e4-dicts`** — динамические словари Vosk для узких состояний. 3 файла.
2. **`e4-search-context`** — три параллельные проверки в выбранном наряде. 2 файла.
3. **`e4d`** — динамическая грамматика Vosk + пересоздание `Recognizer`
   во время TTS. 3–4 файла. Решает И-35.

**После — общая полировка, тёмная тема, релиз MVP.**

## Открытые вопросы

- **И-35.** Vosk стабильно путает «четвёртая» ↔ «четырнадцатая».
  Сейчас — честная ошибка + подсказка. Лечится в `e4d`.
- **D — команды выбора наряда/участка голосом.** Отложено до `e4d`.
- **Confidence threshold Vosk** — нужно проверить `build.gradle`.
- **Визуальная пометка pin в UI.** Панель ГП.

## Известные проблемы

- **И-24.** Vosk обрывает длинные номера. Открыт, отложен до `e4d`.
- **И-35.** Vosk путает «четвёртая» ↔ «четырнадцатая». Открыт, ждёт `e4d`.
- **И-25, И-31, И-9, И-10, И-32…И-34** — код-фиксы выпущены, ждут device-check.

## Не трогать

- Схема БД (version = 2).
- `AI_RULES.md` — актуален (обновить правилом «правки только там, где нужно»).

## Правила текущей сессии

- Сначала спроси: дома или на работе? Если на работе — `github.dev` или `github.com`?
- **Ветки — левый нижний угол AS.**
- Дома пачка → merge локально в фичу.
- Docs → PR в `main`.
- После каждого захода — обновлять `CONTEXT_BRIEF`, `PROGRESS`, `NEXT_STEPS`, `VOICE`, `DECISIONS`.
- **После docs-пачки — догнать фичу (`git merge origin/main`).**
- **Правки в файлах — точечные.** Полный файл — да, но не переписывать то, что не требует изменения.