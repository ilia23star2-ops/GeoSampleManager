# CONTEXT_BRIEF.md — где мы сейчас

Одна страница «где мы сейчас». Обновляется в конце каждой сессии.
Новый ИИ читает вторым после `AI_RULES.md`.

**Дата обновления:** 2026-09-24

## Где мы

**Дома:** работаем в `main`, Android Studio.
**На работе:** правки через `github.com`.
**Последнее закрытое:** серия `5.8.11-e4` — аудит ГП и точечные фиксы.
**Следующее:** `5.8.11-e4e` — свести пути поиска (UI и голос) в один.

## Что было в последней сессии

**Аудит голосового пути `5.8.11-e`** — нашли 5 проблем:

1. Вес голосом не принимался в `AWAITING_WEIGHT`.
2. Мусор («семь утра было холодно») принимался за вес.
3. «Пауза» в `AWAITING_WEIGHT` не работала.
4. Vosk «четвёртая» → «четвертью».
5. Голосовой поиск не учитывал контекст наряда.

**Закрыто:**

- **`e4a`** — вес в `AWAITING_WEIGHT` + починка `voiceSetWeight` для холостых.
- **`e4b`** — фильтр мусора в весе (`isCleanWeightPhrase`).
- **`e4g`** — Pause в `AWAITING_WEIGHT` + фикс опечатки «хатит» → «хватит».
- **`e4g3`** — команда «Найди» (`VoiceCommand.Find`).
- **`e4-fix-1`** — убрано «четвертью» из грамматики Vosk.
- **`e4-fix-2`** — кулдаун TTS 800 → 250 мс.
- **`e4c`** — **откатили** (не нужен, менял UI-путь).

**Обсуждено (архитектура):**

- **Pin скважины** — после находки голос работает с ней, а не сбрасывает контекст.
- **Очередь мультизапроса** — «следующая» листает.
- **Маркеры намерения** — «отметь» → `AWAITING_MARK`, «вес» → `AWAITING_WEIGHT` и т.д.
- **Динамические словари Vosk** — сужение только в узких состояниях.
- **Мимикрия TTS** — озвучка по группам, без запятых, KPD → «капэдэ».
- **Приоритет левой части** — запрос → префикс скважины важнее номера пробы.

## Что делать дальше

**Очередь заходов:**

1. **`e4e`** — свести пути поиска (UI и голос) в один через `SearchService`. **Ключевой.**
2. **`e4-pin-1`** — состояние `FOUND_PINNED`, голое число → отметка.
3. **`e4-pin-2`** — очередь мультизапроса + attention-переключение.
4. **`e4-pin-3`** — конец очереди + `AWAITING_NOT_FOUND_CHOICE`.
5. **`e4-markers`** — маркеры намерения (`AWAITING_MARK`, `AWAITING_CLEAR`, `AWAITING_POSTPONE`).
6. **`e4-dicts`** — динамические словари для 5 узких состояний.
7. **`e4d`** — динамическая грамматика Vosk + пересоздание `Recognizer` во время TTS.
8. **`e4-search-context`** — три параллельные проверки в выбранном наряде.

**`e4e` — дома.** Требует device-check.

## Открытые вопросы

- **Галлюцинации Vosk в `LISTENING`.** Лечится confidence threshold или двойным Vosk.
  **Нужно проверить `build.gradle`** — есть ли у `vosk-android` доступ к `confidence`.
- **D (команды выбора наряда/участка голосом).** Отложено до `e4d` (нужны имена участков в грамматике).
- **`Postpone` (отложить голосом).** Запланировано в `e4-pin-4`.

## Известные проблемы

См. `ISSUES.md`. Ключевые:
- **И-24.** Vosk обрывает длинные номера. Открыт, отложен.
- **И-25, И-31, И-9, И-10, И-32…И-34** — код-фиксы выпущены, ждут device-check.

## Не трогать в этой сессии

- Схема БД (version = 2).
- `UnifiedSearch`, `VoiceSampleHit`, `VoiceModels.kt`.
- `AI_RULES.md`.

## Правила текущей сессии

- Сначала спроси: дома или на работе? Если на работе — `github.dev` или
  `github.com`? См. `AI_RULES.md` §0, §20.
- Компактная шапка захода.
- Дома — работаем в `main`, ветки не создаём.
- На работе — ветка + PR + CI вручную.
- После захода — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`, `NEXT_STEPS.md`.
- Голосовые заходы явно помечать: unit OK / device-check нужен / пройден.
- **Серия `5.8.11-e` — по одному заходу. Не объединять.**
