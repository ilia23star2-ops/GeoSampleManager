# CONTEXT_BRIEF.md — где мы сейчас

Одна страница «где мы сейчас». Обновляется в конце каждой сессии.
Новый ИИ читает вторым после `AI_RULES.md`.

**Дата обновления:** 2026-09-23

## Где мы

**Дома:** интеграция `5.8.11-e1/e2/e3` (унификация поиска) + device-check.
**На github.com:** свободных задач нет.
**Последнее закрытое:** серия `5.8.11-a…d2` — закладки единого поиска.
**Следующее:** `5.8.11-e1` — `setQuery` на новый путь.

## Что было в последней сессии

Закрыта серия закладок `5.8.11-a…d2` — это **подготовка** единого пути
поиска (спецификация — `SEARCH_MODEL.md`):

1. **`5.8.11-a`** — `VoiceState` (enum, 6 состояний) + `VoiceSession.state`
   как вычисляемое свойство.

2. **`5.8.11-b`** — `QueryToken`, `QueryNormalizer`, `QueryTokenizer` —
   единый ввод для ручного и голосового.

3. **`5.8.11-c1/c2`** — `DigitGroup`, `DigitGrouper`, `GroupToCandidates`,
   `VoiceSpeaker.spellOut(groups)` — модель числа, сохраняющая структуру
   ввода («109 00 31»).

4. **`5.8.11-d1/d2`** — `SearchResult`, `SearchService`, `Response`,
   `ResponseMapper`, `Presenter` — единый ответ, из которого строятся
   и UI, и TTS.

**Закладки не используются.** Поведение ГП не менялось. Интеграция —
в `5.8.11-e1/e2/e3`.

## Что делать дальше

### Дома (нужно устройство)

**Серия `5.8.11-e` — интеграция. Строго по одному заходу:**

1. **`5.8.11-e1`** — `ReconciliationViewModel.setQuery` на новый путь.
   Device-check: ручной ввод ищет как раньше.

2. **`5.8.11-e2`** — `VoiceCommandParser.parse(text, state, mode)`.
   Device-check: «семь утра» → молчание; «семь» в `AWAITING_WEIGHT` → вес.

3. **`5.8.11-e3`** — `VoiceDialog` через `Response` + `Presenter`.
   Device-check: ГП говорит те же фразы.

**После каждого — откат возможен одним файлом.** Старые методы не удаляем
до полного успеха серии.

**Device-check серии `5.8.6`:** `5f`, `5g`.
**Device-check серии `5.8.10`:** `a`, `b`, `c`.

## Открытые вопросы

- **И-24 — Vosk endpoint.** Требует апгрейда Vosk или пост-обработки.
  Отложено.
- **Порядок `5.8.11-e1/e2/e3`.** Если `e1` сломает ручной ввод —
  откатываем и разбираемся до `e2`.
- **Синхронизация `VoiceCommandParser` с `VoiceState`.** Может быть
  много неочевидных мест, где парсер ждёт флаги. Проверять на устройстве.

## Известные проблемы

См. `ISSUES.md`. Ключевые:
- **И-24.** Vosk обрывает длинные номера. **Открыт, отложен.**
- **И-25, И-31, И-9, И-10, И-32, И-33, И-34** — код-фиксы выпущены,
  ждут device-check.
- **И-3** — решён в `5.8.10-g1/g2`, проверен.
- **И-26…И-30** — решены в `5.8.6`.

## Не трогать в этой сессии

- Схема БД (version = 2).
- `UnifiedSearch`, `VoiceSampleHit`, `VoiceModels.kt`.
- `AI_RULES.md`.
- Существующий `VoiceExecResult` и `MatchInfo` — пока не сломаются все
  device-check-и серии `e`.

## Файлы, которые понадобятся для следующего захода

ui/screens/
ReconciliationViewModel.kt

data/voice/
VoiceState.kt
VoiceMode.kt (нет — VoiceSessionMode уже есть)
SearchService.kt
SearchResult.kt
QueryNormalizer.kt
QueryTokenizer.kt
QueryToken.kt
DigitGroup.kt
DigitGrouper.kt
GroupToCandidates.kt
Response.kt
Presenter.kt

корень проекта
SEARCH_MODEL.md
NEXT_STEPS.md

## Правила текущей сессии

- Сначала спроси: дома или на работе? Если на работе — `github.dev` или
  `github.com`? См. `AI_RULES.md` §0, §20.
- Компактная шапка захода: заход, режим, ветка, коммит.
- Дома — работаем в `main`, ветки не создаём.
- Git-инструкции в AS — подробно, пошагово.
- Замены файлов — без пояснений.
- CI — вручную (Actions → Build & Test → Run workflow).
- После захода — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`,
  `NEXT_STEPS.md`, `ISSUES.md`.
- Для голосовых заходов явно помечать: unit OK / device-check нужен /
  device-check пройден.
- **Серия `5.8.11-e` — по одному заходу. Не объединять.**

## Как обновлять этот файл

В конце каждой сессии:
- Обновить «Дата обновления».
- Обновить «Где мы».
- Обновить «Что было в последней сессии» — 3–5 пунктов.
- Обновить «Что делать дальше».
- Обновить «Открытые вопросы».
- Обновить «Известные проблемы».
- Обновить «Не трогать в этой сессии».
- Обновить «Файлы, которые понадобятся».
- Обновить «Правила текущей сессии».

Максимум одна страница. Если больше — режем.
