# CONTEXT_BRIEF.md — где мы сейчас

> **Одна страница «где мы сейчас».** Обновляется в конце каждой сессии.
> Новый ИИ читает **вторым** после `AI_RULES.md`.
>
> **Дата обновления:** 2026-09-17

---

## Где мы

- **Текущий заход:** 5.8.9e-2 — индикатор мультипоиска (🟡 в работе)
- **Последний закрытый:** 5.8.9g-3 — грамматика «все/отметь» + сброс сессии
- **Следующий:** 5.8.9e-3 — `keepScreenOn`

## Что было в последней сессии

- Закрыли серию **5.8.9a–g-3** — UI индикатора ответа и голосовые фичи:
  - `AnswerState` / `AnswerReason` (модель).
  - `AnswerStateColors` + UI индикатора и заголовков.
  - `VoiceFeedback` — три звука + гвардия.
  - Раздельные «Применить» для холостых и ВК.
  - `UndoAction.BulkRowsChange` — undo для настроек наряда.
  - `serialNumber` в `SampleRow` — п/п из БД, не зависит от фильтров.
  - Свёрнутые группы при ATTENTION.
  - Участок в заголовке запроса мультипоиска.
  - `FoundOne.attentionReason` — другой участок / наряд в ГП.
  - `VoiceOrdinals` — «третья/третий/третье/третью».
  - `MarkByNumbers`, `MarkAll`, `matchAll`.
  - «Search → отметка» в активной сессии («восемь» = проба №8).
  - Грамматика Vosk: «все / отметь / отметьте / отметить».
  - Сброс `voiceSession` при `NotFound`.
- Пропушили **5.8.9-infra** — правила работы с двух машин.
- Проверили на устройстве — работает.

## Что делать дальше

1. **5.8.9e-2** — индикатор мультипоиска:
  - строки со статусом по каждому запросу;
  - строка с ответом в индикаторе.
2. **5.8.9e-3** — `keepScreenOn`.
3. **5.8.9h** — унификация UI и ГП.
4. **5.8.9f** — режимы SORT / SEARCH.
5. **5.8.9d-2** — отметка короче.
6. **5.8.9d-3** — переспросы.
7. **5.8.9i** — мимикрия.
8. **5.8.6** — Vosk-полировка.

## Открытые вопросы

1. **`keepScreenOn`** — экран горит всё время сессии или только
   пока идёт распознавание? Сейчас решение: пока активна сессия.
2. **Индикатор мультипоиска** — что писать в строке с ответом:
   наряд или пробу?
3. **Режимы SORT/SEARCH** — где показывать текущий режим:
   у микрофона, в индикаторе или в шапке?
4. **Множественная отметка** — озвучивать список проб или «Отмечено
   N проб»?

## Известные проблемы

См. `ISSUES.md`. Ключевые:

- **И-3.** Голосовой диалог блокирует UI (5.8.9, отложено).
- **И-9.** Онбординг не показывается.
- **И-10.** `showCharacteristic` сбрасывается после перезапуска.
- **И-21.** UI и ГП используют разные алгоритмы поиска (5.8.9h).
- **И-22.** Экран гаснет при активной сессии (5.8.9e-3).
- **И-23.** Долгая озвучка (частично), осталось мимикрия (5.8.9i).

## Не трогать в этой сессии

- Excel-импорт (работает).
- Заметки и фото (закрыто в 5.5).
- Схема БД (version = 2).
- `VoiceSearch`, `VoiceNumberParser`, `VoiceCommandParser`
  (закрыты в 5.8.8–5.8.9).
- Модель `AnswerState` (закрыта в 5.8.9a).
- Звуки `VoiceFeedback` (закрыты в 5.8.9c).

## Файлы, которые понадобятся

**ui/screens/**
- `SearchScreen.kt`
- `ReconciliationViewModel.kt`

**data/voice/**
- `VoiceModels.kt`
- `VoiceSession.kt`

## Правила текущей сессии

- **Только эти файлы.**
- **Не менять схему БД.**
- **Не менять `VoiceSearch`, `VoiceNumberParser`, `VoiceCommandParser`.**
- **После захода** — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`,
  `NEXT_STEPS.md`, `ISSUES.md`.

---

## Как обновлять этот файл

В конце каждой сессии:

1. Обновить «Дата обновления».
2. Обновить «Где мы».
3. Обновить «Что было в последней сессии» — 3–5 пунктов.
4. Обновить «Что делать дальше».
5. Обновить «Открытые вопросы».
6. Обновить «Известные проблемы».
7. Обновить «Не трогать в этой сессии».
8. Обновить «Файлы, которые понадобятся».
9. Обновить «Правила текущей сессии».

**Максимум одна страница.** Если больше — режем.