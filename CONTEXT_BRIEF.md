# CONTEXT_BRIEF.md — где мы сейчас

Одна страница «где мы сейчас». Обновляется в конце каждой сессии.
Новый ИИ читает вторым после `AI_RULES.md`.

**Дата обновления:** 2026-09-23

## Где мы

**Текущий фокус (github.com):** разведка API Vosk (И-24).
**Текущий фокус (дома):** device-check серий `5.8.6` и `5.8.10`.
**Последнее закрытое:** `5.8.10-g1` + `5.8.10-g2` — панель ГП вместо
модального диалога (И-3). **Проверено на устройстве — ОК.**
**Следующее:** разведка Vosk-API (github.com), device-check (дома).

## Что было в последней сессии

Закрыт **И-3 — голосовой диалог блокирует UI** (`5.8.10-g1` + `5.8.10-g2`):

1. **`VoicePanel.kt`** — новый компонент. Немодальная панель внизу экрана:
   - цветная полоска слева по `VoiceStatus`;
   - строка статуса (всегда видна);
   - кнопки сворачивания `[▲]/[▼]` и закрытия `[✕]`;
   - развёрнутый блок: «Слышу», «Распознано», «Результат», «Ещё раз».

2. **`VoiceDialog.kt`** — `AlertDialog` заменён на `VoicePanel`:
   - вся логика `VoiceController`, колбэки, `voiceExecute` сохранены;
   - добавлено состояние `expanded` для сворачивания.

3. **`SearchScreen.kt`** — интеграция:
   - `VoiceDialog` перенесён внутрь основного `Box`, прижат к низу;
   - удалён `VoiceStatusBar` (панель делает его работу);
   - `SnackbarHost` поднят на 100 dp.

**Результат:** тапы по списку проб, чек-боксы, меню работают, пока ГП
слушает микрофон. Проверено на устройстве.

Ранее в этой же сессии `5.8.10` закрыты:
- `5.8.10-a` (И-10), `5.8.10-b` (И-9), `5.8.10-c` (озвучка списка),
  `5.8.10-d` (И-32), `5.8.10-e` (И-34), `5.8.10-f` (И-33).

Серия `5.8.6` (🟡 device):
`5.8.6-5f`, `5.8.6-5g`, `5.8.6-5c`, `5.8.6-5a`, `5.8.6-4`, `5.8.6-3`, `5.8.6-2`.

## Что делать дальше

### На github.com (без устройства)

1. **И-24 — разведка API Vosk.**
   Открыть `alphacephei/vosk-android`, посмотреть, какие методы
   endpoint-tuning реально доступны в `Recognizer` версии 0.3.47.
   Варианты после разведки: обновление до 0.3.50+, пост-обработка
   в `VoiceCallback`, комбинированное решение.

### Дома (нужно устройство)

1. **Device-check серии `5.8.6`:**
   - числа: `15 24 01`, `1524`, `KPD1090031`;
   - команды: `первая`, `отметь`, `снять первая`, `следующая`, `стоп`, `пауза`;
   - вес: `два`, `два и шесть`, `две целых шесть десятых`, `полтора`, `полкило`;
   - **5f:** отмена голосом и кнопкой мгновенно снимает отметку;
   - **5g:** «пять четвертых» → Unknown; «четвертая» → MarkOrdinal(4).

2. **Device-check серии `5.8.10`:**
   - **5.8.10-a (И-10):** свернуть «Характеристика» → перезапуск → скрыто.
   - **5.8.10-b (И-9):** снести → свежий APK → онбординг появился.
   - **5.8.10-c:** «первая вторая третья» → «Отмечено: первая, вторая, третья».
   - **5.8.10-f:** 6 листов → 6 нарядов. **Проверено — OK.**
   - **5.8.10-g1/g2 (И-3):** тапы по списку работают при живой панели ГП.
     **Проверено — OK.**

3. **`5.8.6-5e` — Vosk endpoint tuning.**
   Требует разведки API. Отложено.

## Открытые вопросы

- **API Vosk 0.3.47.** Что реально доступно — после разведки.
- **Эхо TTS на слабом планшете.** 800 мс может быть мало.
- **Undo UI — 5f.** Работает ли на устройстве.
- **«Четвертых» — 5g.** Не отсекаются ли лишние фразы.

## Известные проблемы

См. `ISSUES.md`. Ключевые:
- **И-24.** Vosk обрывает длинные номера. **Открыт.**
- **И-25, И-31, И-9, И-10, И-32, И-33, И-34** — код-фиксы выпущены,
  ждут device-check.
- **И-3** — решён в `5.8.10-g1/g2`, проверен на устройстве.
- **И-26, И-27, И-28, И-29, И-30** — решены в серии `5.8.6`.

## Не трогать в этой сессии

- Схема БД (version = 2).
- `UnifiedSearch`, `VoiceSampleHit`, `VoiceModels.kt`.
- `AI_RULES.md`.

## Файлы, которые понадобятся для следующего захода

data/util/
VoiceController.kt

data/voice/
VoiceCallback.kt
VoiceGrammar.kt

корень проекта
app/build.gradle

(для разведки API Vosk)

## Правила текущей сессии

- Сначала спроси: дома или на работе? Если на работе — `github.dev` или
  `github.com`? См. `AI_RULES.md` §0, §20.
- Компактная шапка захода: заход, режим, ветка, коммит.
- Дома — работаем в `main`, ветки не создаём.
- Git-инструкции в AS — подробно, пошагово.
- Замены файлов — без пояснений.
- CI — вручную (Actions → Build & Test → Run workflow).
- После захода — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`,
  `NEXT_STEPS.md`, `ISSUES.md`.
- Для голосовых заходов явно помечать: unit OK / device-check нужен /
  device-check пройден.

## Как обновлять этот файл

В конце каждой сессии:
- Обновить «Дата обновления».
- Обновить «Где мы».
- Обновить «Что было в последней сессии» — 3–5 пунктов.
- Обновить «Что делать дальше».
- Обновить «Открытые вопросы».
- Обновить «Известные проблемы».
- Обновить «Не трогать в этой сессии».
- Обновить «Файлы, которые понадобятся».
- Обновить «Правила текущей сессии».

Максимум одна страница. Если больше — режем.
