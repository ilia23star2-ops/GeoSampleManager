# CONTEXT_BRIEF.md — где мы сейчас

> **Одна страница «где мы сейчас».** Обновляется в конце каждой сессии.
> Новый ИИ читает **вторым** после `AI_RULES.md`.
>
> **Дата обновления:** 2026-09-18

---

## Где мы

- **Текущий заход:** проверка `5.8.9f-2a-fix-5` на устройстве (🟡)
- **Последний закрытый:** `5.8.9f-2a-fix-5` — SORT: номер из БД
- **Следующий:** `5.8.9f-2b` — индикатор режима у микрофона

## Что было в последней сессии

- Серия **5.8.9f** — режимы SORT / SEARCH:
  - `VoiceSessionMode` (SEARCH / SORT) в `VoiceSession`.
  - `VoiceCommand.SetMode`, распознавание «сортировка» / «поиск» / «режим …».
  - Грамматика Vosk: слова «сортировка», «поиск», «режим».
  - `VoiceExecResult.ModeChanged` — озвучка «Сортировка.» / «Поиск.»
  - В SORT: `markGuard` блокирует отметки, `handleSearchInSession`
    направляет в `voiceSort`, статистики нет.
  - На паузе обрабатываются только `Resume` / `Pause` / `Stop`.
  - `Next` сбрасывает режим в SEARCH.
  - Стоп закрывает диалог.
- Много итераций по озвучке SORT — остановились на «номер из БД, как в
  SEARCH, без статистики».

## Что делать дальше

1. **Проверить fix-5** — SORT и SEARCH должны давать одинаковый ответ
   (озвучка + карточка), разница только в отсутствии статистики и
   запрете отметок.
2. **5.8.9f-2b** — UI: индикатор режима у микрофона, справа.
3. **5.8.9d-2** — отметка короче («Третья. Отмечено 2 пробы.»).
4. **5.8.9d-3** — переспросы ВК / отложенных.
5. **5.8.9i** — мимикрия произношения.
6. **5.8.6** — Vosk-полировка.

## Открытые вопросы

1. **Индикатор режима.** Где именно у микрофона справа: маленькая
   надпись под кнопкой, значок-переключатель или бейдж на самой
   кнопке? Решим в `5.8.9f-2b`.
2. **Множественная отметка** — озвучивать список проб или
   «Отмечено N проб»?
3. **UI без диалога** (волны + статус-бар) — заход **5.8.9**, вне
   текущей серии. Не трогаем.

## Известные проблемы

См. `ISSUES.md`. Ключевые:

- **И-3.** Голосовой диалог блокирует UI (5.8.9).
- **И-9.** Онбординг не показывается.
- **И-10.** `showCharacteristic` сбрасывается после перезапуска.
- **И-23.** Долгая озвучка (частично), мимикрия — 5.8.9i.

## Не трогать в этой сессии

- Excel-импорт, заметки и фото.
- Схема БД (version = 2).
- `UnifiedSearch`, `VoiceSampleHit`, `VoiceModels.kt`.
- `AI_RULES.md` — правила уже актуальны.

## Файлы, которые понадобятся

**ui/screens/**
- `SearchScreen.kt` — UI индикатора режима.
- `ReconciliationViewModel.kt` — источник режима.
- `VoiceDialog.kt` — отображение режима.

**data/voice/**
- `VoiceSession.kt` — `VoiceSessionMode`.

## Правила текущей сессии

- **Сначала спроси: дома или на работе? Если на работе —
  `github.dev` или `github.com`?** См. `AI_RULES.md` §0, §20.
- **Компактная шапка захода:** заход, режим, ветка, коммит.
- **Дома — работаем в `main`, ветки не создаём.**
- **Git-инструкции в AS — подробно, пошагово.**
- **Замены файлов — без пояснений.**
- **CI — вручную** (Actions → Build & Test → Run workflow).
- **После захода** — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`,
  `NEXT_STEPS.md`, `ISSUES.md`.

---

## Как обновлять этот файл

В конце каждой сессии:

1. Обновить «Дата обновления».
2. Обновить «Где мы».
3. Обновить «Что было в последней сессии» — 3–5 пунктов.
4. Обновить «Что делать дальше».
5. Обновить «Открытые вопросы».
6. Обновить «Известные проблемы».
7. Обновить «Не трогать в этой сессии».
8. Обновить «Файлы, которые понадобятся».
9. Обновить «Правила текущей сессии».

**Максимум одна страница.** Если больше — режем.
