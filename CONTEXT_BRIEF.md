# CONTEXT_BRIEF.md — где мы сейчас

> **Одна страница «где мы сейчас».** Обновляется в конце каждой сессии.
> Новый ИИ читает **вторым** после `AI_RULES.md`.
>
> **Дата обновления:** 2026-09-17

---

## Где мы

- **Текущий заход:** 5.8.9-infra-2b — правила (три режима работы) — 🟡
- **Последний закрытый:** 5.8.9e-3 — `keepScreenOn`
- **Следующий:** 5.8.9-infra-2d — CI: тесты + APK (с выкачкой Vosk)

## Что было в последней сессии

- Закрыли **5.8.9e-2** (+fix-1/2/3) — индикатор мультипоиска, показ
  скв./пробы, фикс отмены корутин.
- Закрыли **5.8.9e-3** — `keepScreenOn` в `SearchScreen`.
- Запустили **5.8.9h-1** — `UnifiedSearch` + 18 тестов (в ветке
  `feature/5.8.9h-1-unified-search`).
- Настроили **CI (GitHub Actions)**: build.yml — тесты + APK.
- Починили CI-фиксы: `Setup Android SDK`, `testOptions.returnDefaultValues`,
  обновление `VoiceSearchTest`.
- Создали правила **5.8.9-infra-2b**: три режима работы (дома, работа
  `github.dev`, работа `github.com`).

## Что делать дальше

1. **5.8.9-infra-2d** — доработать `build.yml`: сборка APK с моделью Vosk
   (выкачка из релиза GitHub).
2. **Смержить 5.8.9h-1** — `UnifiedSearch` (дождаться зелёного CI).
3. **5.8.9h-2** — переключение UI и ГП на `UnifiedSearch`.
4. **5.8.9f** — режимы SORT / SEARCH.
5. **5.8.9d-2** — отметка короче.
6. **5.8.9d-3** — переспросы.
7. **5.8.9i** — мимикрия.
8. **5.8.6** — Vosk-полировка.

## Открытые вопросы

1. **Vosk в CI** — модель выкачивается из релиза GitHub (`models-v1`).
   Детали — в `build.yml`, заход 2d.
2. **Режимы SORT/SEARCH** — где показывать текущий режим:
   у микрофона, в индикаторе или в шапке?
3. **Множественная отметка** — озвучивать список проб или «Отмечено
   N проб»?

## Известные проблемы

См. `ISSUES.md`. Ключевые:

- **И-3.** Голосовой диалог блокирует UI (5.8.9, отложено).
- **И-9.** Онбординг не показывается.
- **И-10.** `showCharacteristic` сбрасывается после перезапуска.
- **И-21.** UI и ГП используют разные алгоритмы поиска (5.8.9h).
- **И-22.** Экран гаснет при активной сессии — закрыто 5.8.9e-3.
- **И-23.** Долгая озвучка (частично), осталось мимикрия (5.8.9i).

## Не трогать в этой сессии

- Excel-импорт (работает).
- Заметки и фото (закрыто в 5.5).
- Схема БД (version = 2).
- `VoiceSearch`, `VoiceNumberParser`, `VoiceCommandParser`
  (закрыты в 5.8.8–5.8.9).
- Модель `AnswerState` (закрыта в 5.8.9a).
- Звуки `VoiceFeedback` (закрыты в 5.8.9c).
- `MatchedKind`, `MatchInfo`, `QuickAnswer` (закрыты в 5.8.9e-2-fix-2).
- `UnifiedSearch` (закрыт в 5.8.9h-1, ждёт merge).

## Файлы, которые понадобятся

**ui/screens/**
- `SearchScreen.kt`
- `ReconciliationViewModel.kt`

**data/voice/**
- `VoiceModels.kt`
- `VoiceSession.kt`

**.github/workflows/**
- `build.yml`

## Правила текущей сессии

- **Сначала спроси пользователя: где он — дома или на работе?**
  См. `AI_RULES.md` §0, §20.
- **Только эти файлы.**
- **Не менять схему БД.**
- **Не менять `VoiceSearch`, `VoiceNumberParser`, `VoiceCommandParser`.**
- **После захода** — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`,
  `NEXT_STEPS.md`, `ISSUES.md`.

---

## Как обновлять этот файл

В конце каждой сессии:

1. Обновить «Дата обновления».
2. Обновить «Где мы».
3. Обновить «Что было в последней сессии» — 3–5 пунктов.
4. Обновить «Что делать дальше».
5. Обновить «Открытые вопросы».
6. Обновить «Известные проблемы».
7. Обновить «Не трогать в этой сессии».
8. Обновить «Файлы, которые понадобятся».
9. Обновить «Правила текущей сессии».

**Максимум одна страница.** Если больше — режем.
