# CONTEXT_BRIEF.md — где мы сейчас

> **Одна страница «где мы сейчас».** Обновляется в конце каждой сессии.
> Новый ИИ читает **вторым** после `AI_RULES.md`.
>
> **Дата обновления:** 2025-09-14

---

## Где мы

- **Текущий заход:** 5.8.8d — финальные фиксы ГП (в работе)
- **Последний закрытый заход:** 5.8.4 — команды отметок (парсер)
- **Следующий после 5.8.8d:** 5.8.9 — UI без диалога (волны + статус-бар)

## Что было в последней сессии

- Разобрали логику авто-режима отметок.
- Договорились о поведении при неоднозначном ответе (пауза + «посмотрите на экране»).
- Определили формат TTS для поиска скважины.
- Согласовали принцип «ГП = помощник, логика одна с ручным, отличается только ответом и защитой».

## Что делать дальше

1. **Заход 5.8.8d** — доработать:
   - `VoiceSpeaker.spellNumber` (короткие числа словами).
   - `VoiceSession.awaitingWeight` (состояние «жду вес»).
   - Вес для холостых, если MANUAL.
2. **Заход 5.8.9** — UI без диалога:
   - Убрать блокирующий `VoiceDialog`.
   - Добавить неблокирующий оверлей с волнами.
   - Статус-бар под кнопками «Отмена / Вперёд».

## Открытые вопросы

1. **Пауза:** 5 мин — норм? Или 3?
2. **Смена вкладки:** завершать сессию — уже решено, да.
3. **ГП и удаление:** не поддерживаем (безопасность).

## Известные проблемы

См. `ISSUES.md` — там открытые и закрытые.

Ключевые открытые:
- **И-1.** Авто-режим не включается после поиска.
- **И-2.** Статус-бар не отображается.
- **И-3.** Голосовой диалог блокирует UI.
- **И-4.** Порядковые не работают, пока не включён авто-режим.

## Не трогать в этой сессии

- Excel-импорт (работает).
- Заметки и фото (закрыто в 5.5).
- Множественный поиск (закрыто в 5.11).
- `SampleDao`, `SampleEntity` (стабильно).
- Схема БД (version = 2).

## Файлы, которые понадобятся

**data/voice/**
- `VoiceSession.kt`
- `VoiceCommandParser.kt`
- `VoiceSpeaker.kt`

**ui/screens/**
- `ReconciliationViewModel.kt`
- `VoiceDialog.kt` (или новый `VoiceOverlay.kt`)
- `SearchScreen.kt`

## Правила текущей сессии

- **Только эти файлы.** Не трогать UI поиска, кроме статус-бара.
- **Не менять схему БД.**
- **Не менять `VoiceSearch`** (работает).
- **После захода** — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`, `NEXT_STEPS.md`, `ISSUES.md`.

---

## Как обновлять этот файл

В конце каждой сессии:

1. Обновить «Дата обновления».
2. Обновить «Где мы» — текущий заход, статус, следующий.
3. Обновить «Что было в последней сессии» — 3–5 пунктов.
4. Обновить «Что делать дальше».
5. Обновить «Открытые вопросы».
6. Обновить «Известные проблемы» (синхронизировать с `ISSUES.md`).
7. Обновить «Не трогать в этой сессии».
8. Обновить «Файлы, которые понадобятся».
9. Обновить «Правила текущей сессии».

**Максимум одна страница.** Если больше — режем.
