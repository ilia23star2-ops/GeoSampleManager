# CONTEXT_BRIEF.md — где мы сейчас

Одна страница «где мы сейчас». Обновляется в конце каждой сессии.
Новый ИИ читает вторым после `AI_RULES.md`.

**Дата обновления:** 2026-09-25 (день)

## Где мы

**Дома:** работаем в `main`, Android Studio.
**На работе:** правки через `github.com`.
**Фича в работе:** `feature/5.8.11-e4-voice-v2` — серия `e4`.
**Последнее закрытое:** `e4-pin-7` — отказ от fallback, честная ошибка + подсказка числом.
**Следующее:** `e4-markers` — маркеры намерения + подтверждение массовых.

## Что было в последних сессиях

**Серия `5.8.11-e4` — рефакторинг голосового помощника.**

**Закрыто (в фиче `feature/5.8.11-e4-voice-v2`):**

- **`e4e-bundle`** — мимикрия TTS + единый путь поиска.
- **`e4-tests`** — покрытие парсера (3 тест-файла).
- **`e4-pin-1`** — состояние `FOUND_PINNED` (`PinnedScope`, `pin/unpin`).
- **`e4-pin-2`** — очередь мультизапроса (`queue`, `enqueue`, `nextInQueue`).
- **`e4-pin-3`** — вес «X сотни», «следующая X», `FoundOne.queueSize`.
- **`e4-pin-4`** — числительные в pin (склейка в одно число), fallback 14↔4.
- **`e4-pin-5`** — fallback расширен (400→4, 40→4, 4000→4 и обратно).
- **`e4-pin-6`** — показ подмены при fallback («Распознано X → Y»).
- **`e4-pin-7`** — **отказ от fallback**. Вместо молчаливой подмены — честная ошибка + подсказка числом: «Пробы №14 нет. Если нужна №4 — произнесите „четыре"». Плюс accent fix: убрано «Распознано» из голоса. Тесты: `VoiceMarkOrdinalFallbackTest` (26 тестов).

**Закрыто (в `main`):**

- `e4-hotfix`, `e4-fix-1`, `e4-fix-2` — точечные фиксы.
- `docs/5.8.11-rules-fix-merge` — чистка AI_RULES.
- `docs/5.8.11-git-rules`, `docs/5.8.11-tests-rules` — правила git и тестов.
- `chore/5.8.11-ci-automation` — CI на PR, APK по лейблу.

## Что делать дальше

**Очередь заходов — все в фиче `feature/5.8.11-e4-voice-v2`:**

1. **`e4-markers`** — маркеры намерения + подтверждение массовых. 5–6 файлов.
   - `AWAITING_MARK` (словарь 1..N).
   - `AWAITING_CLEAR` (1..N).
   - `AWAITING_POSTPONE` (1..N).
   - `AWAITING_CONFIRM` — для массовых действий («отметь все», «снять все»).
2. **`e4-dicts`** — динамические словари Vosk для 5 узких состояний. 3 файла.
3. **`e4-search-context`** — три параллельные проверки в выбранном наряде. 2 файла.
4. **`e4d`** — динамическая грамматика Vosk + пересоздание `Recognizer` во время TTS. 3–4 файла. Решает И-35.

**После всей серии `e4` — PR фичи в `main`.**

## Открытые вопросы

- **И-35.** Vosk стабильно путает «четвёртая» → «четырнадцатая». Лечится в `e4d` (динамическая грамматика). Сейчас — честная ошибка + подсказка.
- **TTS не даёт отменить переключение pin.** Обсудим.
- **Галлюцинации Vosk в `LISTENING`.** Confidence threshold — нужно проверить `build.gradle`.
- **D (команды выбора наряда/участка голосом).** Отложено до `e4d`.
- **Визуальная пометка pin в UI.** Панель ГП.

## Известные проблемы

- **И-24.** Vosk обрывает длинные номера. Открыт.
- **И-35.** Vosk путает «четвёртая» → «четырнадцатая». Открыт, ждёт `e4d`.
- **И-25, И-31, И-9, И-10, И-32…И-34** — код-фиксы выпущены, ждут device-check.

## Не трогать

- Схема БД (version = 2).
- `AI_RULES.md` — актуален.

## Правила текущей сессии

- Сначала спроси: дома или на работе? Если на работе — `github.dev` или `github.com`?
- **Ветки — левый нижний угол AS.**
- Дома пачка → merge локально в фичу.
- Docs → PR в `main`.
- После каждого захода — обновлять `CONTEXT_BRIEF`, `PROGRESS`, `NEXT_STEPS`, `VOICE`, `DECISIONS`.
- **После docs-пачки — догнать фичу (`git merge origin/main`).**
