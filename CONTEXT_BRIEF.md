# CONTEXT_BRIEF.md — где мы сейчас

Одна страница «где мы сейчас». Обновляется в конце каждой сессии.
Новый ИИ читает вторым после `AI_RULES.md`.

**Дата обновления:** 2026-09-26 (вечер)

## Где мы

**Дома:** работаем в `main`, Android Studio.
**На работе:** правки через `github.com`.
**Фича в работе:** `feature/5.8.11-e4-voice-v2` — серия `e4` + серия
`sort-fix`, готова к PR в main.
**Последнее закрытое:** серия `5.8.11-sort-fix` (5 пачек) — SORT,
унификация Next, каноника в Result. Device-check ✅.
**Следующее:** И-24 (Vosk обрыв) или PR фичи `e4` в main.

## Что было в последних сессиях

**Серия `5.8.11-sort-fix` — SORT и голосовая навигация.**

**Закрыто (в фиче `feature/5.8.11-e4-voice-v2`):**

- **`sort-fix`** (bab0a01) — SORT flat, `trySplitByNumberBlocks`
  (позже удалён), `advanceToNext` не сбрасывает mode, `spellMimicry`
  не сыпет буквами на кириллице. Убран мусор `.github/app/`.
- **`sort-fix-3`** (8769657) — `trySplitByNumberBlocks` удалён
  (авто-split без «и» — ограничение Vosk). `Message.spoken`.
  `voiceNext()` в SORT → «не используется». Тесты обновлены.
- **`sort-ui`** (405d6f1) — `voiceSortFlat` пишет канонику в
  `state.query`. UI отрисовывает мультизапрос как при ручном вводе.
- **`sort-fix-4`** (ad26841) — фикс задвоения в `QueryTokenizer`
  (raw у Prefix и Number — свои части). `Message.display`.
- **`sort-fix-5`** (eb4d1f5) — `NextInQueue` удалён. Синонимы
  «дальше/далее/следующая» → одна `Next`. `voiceNext()` проверяет
  `hasQueue`. `voiceSortFlat` формирует канонику (`text`) и фонетику
  (`spoken`) параллельно.

**Серия `5.8.11-e4`** (предыдущая, до `sort-fix`):

- `e4e-bundle`, `e4-tests`, `e4-pin-1…7`, `e4-markers`,
  `e4-markers-2`, `e4-speak-1`, `e4-prefix-1`, `e4-weight-queue`,
  `e4-ui-1`, `e4-fix-voice-1` — 15 пачек.

**Закрыто (в `main`):**

- `e4-hotfix`, `e4-fix-1`, `e4-fix-2` — точечные фиксы.
- `docs/5.8.11-rules-fix-merge`, `docs/5.8.11-git-rules`,
  `docs/5.8.11-tests-rules`, `docs/5.8.11-rules-cleanup`.
- `docs/5.8.11-e4-pin-close` — фиксация серии `e4-pin`.
- `chore/5.8.11-ci-automation` — CI на PR, APK по лейблу.

## Что делать дальше

**Сейчас:** серия `sort-fix` полностью закрыта в фиче. Осталось:

1. **PR фичи `feature/5.8.11-e4-voice-v2` в `main`** — через браузер
   github.com. Base `main`, compare фича. После merge — фича остаётся
   архивом (§2.9). Возврат — `feature/5.8.11-e4-voice-v3`.
2. **И-24** — Vosk обрывает по короткой паузе. Пользователь не успевает
   договорить длинную фразу. Debounce `onResult` в `VoiceDialog` /
   `VoiceController`. Пачка 2–3 файла.

**Очередь заходов (следующая серия):**

1. **И-24** — Vosk endpoint tuning. 2–3 файла.
2. **`e4-dicts`** — динамические словари Vosk для узких состояний. 3 файла.
3. **`e4-search-context`** — три параллельные проверки в выбранном наряде. 2 файла.
4. **`e4d`** — динамическая грамматика Vosk + пересоздание `Recognizer`
   во время TTS. 3–4 файла. Решает И-35.

**После — общая полировка, тёмная тема, релиз MVP.**

## Открытые вопросы

- **И-24.** Vosk обрывает длинные номера, промежуточный `onResult`.
  Нужен debounce. Ждёт отдельного захода.
- **И-35.** Vosk стабильно путает «четвёртая» ↔ «четырнадцатая».
  Сейчас — честная ошибка + подсказка. Лечится в `e4d`.
- **D — команды выбора наряда/участка голосом.** Отложено до `e4d`.
- **Confidence threshold Vosk** — нужно проверить `build.gradle`.
- **Визуальная пометка pin в UI.** Панель ГП.

## Известные проблемы

- **И-24.** Vosk обрывает длинные номера. Открыт.
- **И-35.** Vosk путает «четвёртая» ↔ «четырнадцатая». Открыт, ждёт `e4d`.
- **И-25, И-31, И-9, И-10, И-32…И-34** — код-фиксы выпущены, ждут
  device-check (некоторые ✅).

## Не трогать

- Схема БД (version = 2).
- `AI_RULES.md` — актуален.

## Правила текущей сессии

- Сначала спроси: дома или на работе? Если на работе — `github.dev` или `github.com`?
- **Ветки — левый нижний угол AS.**
- Дома пачка → merge локально в фичу.
- Docs → PR в `main`.
- После каждого захода — обновлять `CONTEXT_BRIEF`, `PROGRESS`, `NEXT_STEPS`, `VOICE`, `DECISIONS`.
- **После docs-пачки — догнать фичу (`git merge origin/main`).**
- **Правки в файлах — точечные.** Полный файл — да, но не переписывать то,
  что не требует изменения.