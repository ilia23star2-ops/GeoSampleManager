# CONTEXT_BRIEF.md — где мы сейчас

> **Одна страница «где мы сейчас».** Обновляется в конце каждой сессии.
> Новый ИИ читает **вторым** после `AI_RULES.md`.
>
> **Дата обновления:** 2025-09-14

---

## Где мы

- **Текущий заход:** 5.8.8d — финальные фиксы ГП (🟡 в работе)
- **Последний закрытый:** 5.8.8 — Vosk + грамматика + TTS
- **Следующий:** 5.8.9 — UI без диалога (волны + статус-бар)

## Что было в последней сессии

- Создали минимальный набор документации: `AI_RULES.md` (§17–19),
  `CONTEXT_BRIEF.md`, `GLOSSARY.md`, `ISSUES.md`, `TESTING.md`, обновили
  `README.md`.
- Обсудили логику авто-режима отметок и поведение при неоднозначном ответе.
- Согласовали принцип «ГП = помощник, логика одна с ручным».
- Подготовили фиксы 5.8.8d: `spellNumber`, `awaitingWeight`, вес для холостых.

## Что делать дальше

1. **Заход 5.8.8d** — проверить на устройстве:
   - `VoiceSpeaker.spellNumber` для коротких чисел.
   - `VoiceSession.awaitingWeight`.
   - Вес для холостых (при MANUAL).
2. **Заход 5.8.9** — UI без диалога:
   - Убрать блокирующий `VoiceDialog`.
   - Добавить неблокирующий `VoiceOverlay` (волны).
   - Статус-бар под кнопками «Отмена / Вперёд».
   - Звук единственного ответа (двойной восходящий бип).

## Открытые вопросы

1. **Пауза:** 5 мин тишины — норм? Или 3?
2. **Смена вкладки:** сессия завершается — уже решено, да.
3. **ГП и удаление:** не поддерживаем (безопасность).
4. **Где рисовать волны:** снизу или в шапке?
5. **Статус-бар:** видим постоянно или только когда ГП слушает?

## Известные проблемы

См. `ISSUES.md`. Ключевые:

- **И-1.** Авто-режим не включается после поиска (5.8.5).
- **И-2.** Статус-бар не отображается (5.8.9).
- **И-3.** Голосовой диалог блокирует UI (5.8.9).
- **И-4.** Порядковые не работают, пока не включён авто-режим (5.8.5).
- **И-5.** Вес не запрашивается для холостых при MANUAL (5.8.8d).
- **И-6.** TTS не проговаривает длинные номера по цифрам (5.8.8d).

## Не трогать в этой сессии

- Excel-импорт (работает).
- Заметки и фото (закрыто в 5.5).
- Множественный поиск (закрыто в 5.11).
- `SampleDao`, `SampleEntity` (стабильно).
- Схема БД (version = 2).
- `VoiceSearch`, `VoiceNumberParser`, `VoicePrefixResolver` (работают,
  26 тестов зелёные).

## Файлы, которые понадобятся

**data/voice/**
- `VoiceSpeaker.kt`
- `VoiceSession.kt`
- `VoiceCommandParser.kt`

**ui/screens/**
- `ReconciliationViewModel.kt`
- `VoiceDialog.kt`
- `SearchScreen.kt`

## Правила текущей сессии

- **Только эти файлы.**
- **Не менять схему БД.**
- **Не менять `VoiceSearch`** (работает).
- **После захода** — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`,
  `NEXT_STEPS.md`, `ISSUES.md`.

---

## Как обновлять этот файл

В конце каждой сессии:

1. Обновить «Дата обновления».
2. Обновить «Где мы» — текущий заход, статус, следующий.
3. Обновить «Что было в последней сессии» — 3–5 пунктов.
4. Обновить «Что делать дальше».
5. Обновить «Открытые вопросы».
6. Обновить «Известные проблемы» (синхронизировать с `ISSUES.md`).
7. Обновить «Не трогать в этой сессии».
8. Обновить «Файлы, которые понадобятся».
9. Обновить «Правила текущей сессии».

**Максимум одна страница.** Если больше — режем.
