# CONTEXT_BRIEF.md — где мы сейчас

> **Одна страница «где мы сейчас».** Обновляется в конце каждой сессии.
> Новый ИИ читает **вторым** после `AI_RULES.md`.
>
> **Дата обновления:** 2026-09-17

---

## Где мы

- **Текущий заход:** 5.8.9h-2 — переключение UI и ГП на `UnifiedSearch` (🟡)
- **Последний закрытый:** 5.8.9-infra-2d — CI вручную
- **Следующий:** 5.8.9-infra-2e — Vosk в CI (выкачка модели из релиза)

## Что было в последней сессии

- Закрыли **5.8.9e-2** (+fix-1/2/3) — индикатор мультипоиска, показ
  скв./пробы, фикс отмены корутин.
- Закрыли **5.8.9e-3** — `keepScreenOn`.
- Запустили **5.8.9h-1** — `UnifiedSearch` + 18 тестов (в ветке,
  ждёт merge).
- Настроили **CI**: `build.yml` — Setup Android SDK, тесты, APK.
  Починили: `returnDefaultValues`, устаревшие тесты.
- Переписали **правила** (`AI_RULES.md`) — три режима работы, компактная
  шапка захода, коммит по режиму.
- **CI — вручную** (`5.8.9-infra-2d`): `workflow_dispatch` с чек-боксами
  `run_tests` и `build_apk`.

## Что делать дальше

1. **Смержить `feature/5.8.9h-1-unified-search`** — если CI зелёный.
2. **5.8.9h-2** — переключение UI и ГП на `UnifiedSearch`:
   - 5.8.9h-2a — ГП.
   - 5.8.9h-2b — UI.
3. **5.8.9-infra-2e** — Vosk в CI (нужна ссылка на релиз модели).
4. **5.8.9f** — режимы SORT / SEARCH.
5. **5.8.9d-2**, **5.8.9d-3** — отметка короче, переспросы.
6. **5.8.9i** — мимикрия.
7. **5.8.6** — Vosk-полировка.

## Открытые вопросы

1. **Vosk в CI** — нужна ссылка на релиз с моделью `vosk-small-ru`.
2. **Режимы SORT/SEARCH** — где показывать текущий режим:
   у микрофона, в индикаторе или в шапке?
3. **Множественная отметка** — озвучивать список проб или «Отмечено
   N проб»?

## Известные проблемы

См. `ISSUES.md`. Ключевые:

- **И-3.** Голосовой диалог блокирует UI (5.8.9, отложено).
- **И-9.** Онбординг не показывается.
- **И-10.** `showCharacteristic` сбрасывается после перезапуска.
- **И-21.** UI и ГП используют разные алгоритмы поиска (5.8.9h-2).
- **И-23.** Долгая озвучка (частично), осталось мимикрия (5.8.9i).

## Не трогать в этой сессии

- Excel-импорт.
- Заметки и фото.
- Схема БД (version = 2).
- `VoiceNumberParser`, `VoiceCommandParser`.
- `AnswerState`.
- `VoiceFeedback`.
- `UnifiedSearch` (закрыт в 5.8.9h-1).
- `build.yml` — закрыт в 5.8.9-infra-2d.

## Файлы, которые понадобятся

**ui/screens/**
- `ReconciliationViewModel.kt`
- `ReconciliationModels.kt`

**data/voice/**
- `VoiceSearch.kt`
- `UnifiedSearch.kt`

## Правила текущей сессии

- **Сначала спроси пользователя: где он — дома или на работе?
  Если на работе — через `github.dev` или `github.com`?**
  См. `AI_RULES.md` §0, §20.
- **Компактная шапка захода** (`§2.2`):
  заход, режим, ветка, коммит.
- **Коммит:**
    - AS / `github.dev` — общий, в конце.
    - `github.com` — под каждым файлом.
- **CI — вручную** (Actions → Build & Test → Run workflow).
- **После захода** — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`,
  `NEXT_STEPS.md`.

---

## Как обновлять этот файл

В конце каждой сессии:

1. Обновить «Дата обновления».
2. Обновить «Где мы».
3. Обновить «Что было в последней сессии» — 3–5 пунктов.
4. Обновить «Что делать дальше».
5. Обновить «Открытые вопросы».
6. Обновить «Известные проблемы».
7. Обновить «Не трогать в этой сессии».
8. Обновить «Файлы, которые понадобятся».
9. Обновить «Правила текущей сессии».

**Максимум одна страница.** Если больше — режем.