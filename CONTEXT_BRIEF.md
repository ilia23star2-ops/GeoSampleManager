# CONTEXT_BRIEF.md — где мы сейчас

Одна страница «где мы сейчас». Обновляется в конце каждой сессии.
Новый ИИ читает вторым после `AI_RULES.md`.

**Дата обновления:** 2026-09-23

## Где мы

**Текущий фокус:** закрытие задач без устройства на github.com; дома — device-check серий `5.8.6` и `5.8.10`.
**Последнее закрытое:** `5.8.10-a` — И-10 (showCharacteristic); `5.8.10-b` — И-9 (showOnboarding); `5.8.9f-2b` — чип режима у микрофона (проверено, реализовано ранее).
**Следующий (github.com):** `5.8.9d-3` — переспросы ВК / отложенных (UX).
**Следующий (дома):** device-check всей серии `5.8.6` + `5.8.10` на планшете.

## Что было в последней сессии

Закрыты две задачи по настройкам UI и одна проверка:

1. **`5.8.10-a` (И-10)** — `showCharacteristic` переживает перезапуск:
   - `VoiceSettings.kt`: новое поле `showCharacteristic: Boolean = true`; защита дефолта в `load()`;
   - `ReconciliationViewModel.kt`: `loadVoiceUiSettings()` читает и `showOnboarding`, и `showCharacteristic`; метод `setShowCharacteristic(value)` — единая точка изменения + запись в файл;
   - `SearchScreen.kt`: `onShowCharacteristicChange` теперь идёт через ViewModel.

2. **`5.8.10-b` (И-9)** — онбординг не показывался:
   - `VoiceSettings.kt`: в `load()` добавлена защита `showOnboarding = true`, если поля нет в JSON;
   - причина: Gson игнорирует Kotlin-дефолты, старый `voice_settings.json` без ключа → `false`.

3. **`5.8.9f-2b` — проверено:** чип режима уже реализован в `SearchScreen.kt` как `VoiceModeChip` (ПОИСК/СОРТ). Работает: тап переключает `VoiceSession.mode`. Отдельного кода не требуется.

Ранее в серии `5.8.6` (🟡 device):
- `5.8.6-5f` (Undo UI), `5.8.6-5g` (четвертых → Unknown), `5.8.6-5c` (debounce + звуки), `5.8.6-5a` (строгий голосовой шлюз), `5.8.6-4` (грамматика Vosk), `5.8.6-3` (защита команд от поиска), `5.8.6-2` (ноль + anti-echo).

## Что делать дальше

### На github.com (без устройства)

1. **`5.8.9d-3`** — переспросы ВК / отложенных (UX).
   Логика `pendingMarkChoice` есть в `VoiceSession.kt`, `ReconciliationViewModel.kt`, `VoiceDialog.kt`. Проверить UX-сценарии по `VOICE.md §Выбор действия` и `DECISIONS.md §13.7`.

2. **Множественная отметка** — озвучивать список проб.
   Сейчас после «отметь 1 и 2 и 3» — только «Отмечено N проб». Файл: `ui/screens/VoiceDialog.kt` (`handleFeedback`).

3. **И-24 — разведка API Vosk** (можно начать на github.com, читая исходники `alphacephei/vosk-android`).

### Дома (нужно устройство)

1. **Device-check серии `5.8.6`**:
   - числа: `15 24 01`, `1524`, `KPD1090031`;
   - команды: `первая`, `отметь`, `снять первая`, `следующая`, `стоп`, `пауза`, `продолжить`, `помощь`, `сколько осталось`;
   - выбор действия после «Уже отмечена»: `снять`, `отложить`, `пропустить`;
   - вес: `два`, `два и шесть`, `две целых шесть десятых`, `два целых шесть сотых`, `полтора`, `полкило`, `два с половиной`, `вес два и шесть`;
   - команды не должны попадать в поиск: `назад`, `отмена`, `стоп`, `пауза`, `продолжить`, `помощь`;
   - **5f:** отмена голосом и кнопкой мгновенно снимает отметку;
   - **5g:** «пять четвертых» → Unknown; «четвертая» → MarkOrdinal(4).

2. **Device-check серии `5.8.10`**:
   - **5.8.10-a (И-10):** свернуть «Характеристика» → перезапустить → колонка скрыта;
   - **5.8.10-b (И-9):** снести приложение → установить свежий APK → должен появиться онбординг (5 экранов).

3. **Разведка API Vosk** (перед `5.8.6-5e`):
   - в AS открыть любой файл с `Recognizer` → курсор на `Recognizer` → `Ctrl+B`;
   - понять, какие методы endpoint-tuning вообще доступны в `vosk-android:0.3.47`;
   - решение: обновление до 0.3.50+, пост-обработка в `VoiceCallback`, либо комбинированное решение.

## Открытые вопросы

- **Эхо TTS на слабом планшете.** 800 мс может быть мало. Нужен device-check с разной длиной фраз.
- **API Vosk 0.3.47.** `setEndpointerMode` / `setEndpointerDelays` отсутствуют. Что доступно — разведка в AS.
- **Undo UI — 5f.** Работает ли на устройстве: чекбокс снимается мгновенно, счётчик «Отмена (N)» обновляется.
- **«Четвертых» — 5g.** Не отсекаются ли лишние фразы.
- **5.8.9d-3.** Что именно осталось по переспросам ВК / отложенных — сверить с `VOICE.md`.

## Известные проблемы

См. `ISSUES.md`. Ключевые:
- **И-24.** Vosk обрывает длинные номера. **Открыт.**
- **И-25.** `назад` не обновляет UI. **Код-фикс 5f, ждёт device-check.**
- **И-31.** «Пять четвертых» → Search("5"). **Код-фикс 5g, ждёт device-check.**
- **И-9.** Онбординг не показывается. **Код-фикс 5.8.10-b, ждёт device-check.**
- **И-10.** `showCharacteristic` сбрасывается. **Код-фикс 5.8.10-a, ждёт device-check.**
- **И-3.** Голосовой диалог блокирует UI. **Отложен (5.8.9).**
- И-26, И-27, И-28, И-29, И-30 — решены в серии `5.8.6`, ждут device-check.

## Не трогать в этой сессии

- Excel-импорт, заметки и фото.
- Схема БД (version = 2).
- `UnifiedSearch`, `VoiceSampleHit`, `VoiceModels.kt`.
- `AI_RULES.md` — правила актуальны.

## Файлы, которые понадобятся для следующего захода

ui/screens/
VoiceDialog.kt
ReconciliationViewModel.kt

data/voice/
VoiceSession.kt
VoiceCommand.kt

корень проекта
VOICE.md
DECISIONS.md

## Правила текущей сессии

- Сначала спроси: дома или на работе? Если на работе — `github.dev` или `github.com`? См. `AI_RULES.md` §0, §20.
- Компактная шапка захода: заход, режим, ветка, коммит.
- Дома — работаем в `main`, ветки не создаём.
- Git-инструкции в AS — подробно, пошагово.
- Замены файлов — без пояснений.
- CI — вручную (Actions → Build & Test → Run workflow).
- После захода — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`, `NEXT_STEPS.md`, `ISSUES.md`.
- Для голосовых заходов явно помечать: unit OK / device-check нужен / device-check пройден.

## Как обновлять этот файл

В конце каждой сессии:
- Обновить «Дата обновления».
- Обновить «Где мы».
- Обновить «Что было в последней сессии» — 3–5 пунктов.
- Обновить «Что делать дальше».
- Обновить «Открытые вопросы».
- Обновить «Известные проблемы».
- Обновить «Не трогать в этой сессии».
- Обновить «Файлы, которые понадобятся».
- Обновить «Правила текущей сессии».

Максимум одна страница. Если больше — режем.
