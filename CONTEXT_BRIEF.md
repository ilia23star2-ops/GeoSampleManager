# CONTEXT_BRIEF.md — где мы сейчас

> **Одна страница «где мы сейчас».** Обновляется в конце каждой сессии.
> Новый ИИ читает **вторым** после `AI_RULES.md`.
>
> **Дата обновления:** 2026-09-17

---

## Где мы

- **Текущий заход:** 5.8.9f — режимы SORT / SEARCH (🟡)
- **Последний закрытый:** 5.8.9h-2b-ii-fix-1 — восстановление VoiceModels.kt
- **Следующий:** 5.8.9d-2 — отметка короче

## Что было в последней сессии

- Серия **5.8.9h-2b** — UI и ГП переведены на `UnifiedSearch`:
    - `typealias MatchedKind = UnifiedMatchKind`;
    - правило filterMode: точное по базе, префикс — только в выбранном наряде;
    - порог `MAX_AMBIGUOUS` из `UnifiedSearch` убран;
    - класс `VoiceSearch` удалён, `VoiceSampleHit` вынесен в отдельный файл.
- Починен `VoiceModels.kt` (`VoiceStatus`, `VoiceExecResult`).
- **Правила** (§20 в `AI_RULES.md`):
    - дома — работаем в `main`, ветки не создаём;
    - git-инструкции в AS — пошаговые, замены — без пояснений;
    - компактная шапка захода.

## Что делать дальше

1. **5.8.9f** — режимы SORT / SEARCH:
    - **5.8.9f-1** — `VoiceMode` в сессии, переключение голосом.
    - **5.8.9f-2** — UI: индикатор режима, кнопка переключения.
2. **5.8.9d-2** — отметка короче.
3. **5.8.9d-3** — переспросы.
4. **5.8.9i** — мимикрия.
5. **5.8.6** — Vosk-полировка.

## Открытые вопросы

1. **Режимы SORT/SEARCH** — где показывать текущий режим:
   у микрофона, в индикаторе или в шапке?
2. **Множественная отметка** — озвучивать список проб или «Отмечено
   N проб»?
3. **Vosk в CI** — работает. Модель в релизе `models-v1`.

## Известные проблемы

См. `ISSUES.md`. Ключевые:

- **И-3.** Голосовой диалог блокирует UI (5.8.9, отложено).
- **И-9.** Онбординг не показывается.
- **И-10.** `showCharacteristic` сбрасывается после перезапуска.
- **И-21.** UI и ГП используют разные алгоритмы — **закрыто** в
  `5.8.9h-2b` (оба на `UnifiedSearch`).
- **И-23.** Долгая озвучка (частично), осталось мимикрия (5.8.9i).

## Не трогать в этой сессии

- Excel-импорт.
- Заметки и фото.
- Схема БД (version = 2).
- `UnifiedSearch` — закрыт, стабилен.
- `VoiceSampleHit`, `VoiceModels.kt` — восстановлены в 5.8.9h-2b-ii.
- `AI_RULES.md` — обновлён в 5.8.9-infra-2b-fix-3.

## Файлы, которые понадобятся

**data/voice/**
- `VoiceSession.kt`
- `VoiceModels.kt`

**ui/screens/**
- `ReconciliationViewModel.kt`
- `SearchScreen.kt`
- `VoiceDialog.kt`

## Правила текущей сессии

- **Сначала спроси: дома или на работе? Если на работе — `github.dev`
  или `github.com`?** См. `AI_RULES.md` §0, §20.
- **Компактная шапка захода:** заход, режим, ветка, коммит.
- **Git-инструкции в AS — подробно, пошагово.**
- **Замены файлов — без пояснений.**
- **Дома — работаем в `main`, ветки не создаём.**
- **CI — вручную** (Actions → Build & Test → Run workflow).
- **После захода** — обновить `CONTEXT_BRIEF.md`, `PROGRESS.md`,
  `NEXT_STEPS.md`, `ISSUES.md`.

---

## Как обновлять этот файл

В конце каждой сессии:

1. Обновить «Дата обновления».
2. Обновить «Где мы».
3. Обновить «Что было в последней сессии» — 3–5 пунктов.
4. Обновить «Что делать дальше».
5. Обновить «Открытые вопросы».
6. Обновить «Известные проблемы».
7. Обновить «Не трогать в этой сессии».
8. Обновить «Файлы, которые понадобятся».
9. Обновить «Правила текущей сессии».

**Максимум одна страница.** Если больше — режем.