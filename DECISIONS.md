# Решения по UI и логике GeoSample Manager

> Фиксирует договорённости. Обновляется по ходу.
> **Читать перед любым изменением UI и логики.**

---

## 1. Экран «Сверка и поиск»

### Общее
- Одна вкладка. Два неявных режима: **Поиск** и **Сверка**.

### Селекторы
- **Участок**, **Наряд** — опциональны.
- **Наряд нельзя выбрать без участка.** Поле заблокировано с подписью
  «Сначала выберите участок».
- **Шестерёнка** — настройки наряда.

### Поле поиска
- Принимает **один или несколько** номеров через пробел (см. §14).
- Крестик — очищает.
- Ищет по всей БД.
- Дебаунс 500 мс — поиск запускается после паузы ввода.
- Лишние пробелы по краям обрезаются.

### Режимы поиска
1. **Ничего не выбрано** → строгое совпадение по номеру скважины или
   пробы.
2. **Выбран только участок** → строгое совпадение; участок задаёт
   приоритет в результатах.
3. **Выбраны участок И наряд** → строгое совпадение **или** префикс по
   номеру пробы (строка работает и как фильтр).

### Логика поиска (пустой запрос)
1. Пусто + ничего не выбрано → подсказка.
2. Пусто + участок без наряда → подсказка «Выберите наряд».
3. Пусто + выбран наряд → все пробы наряда.

### Индикатор ответа
См. §13.2. Три цвета + серый IDLE. Название в коде и UI —
**«Индикатор ответа»** (бывший «фонарик»).

### Приоритеты групп
См. §13.3. Сначала по состоянию (🟢 → 🟡 → 🔴), внутри — по близости
(выбранный наряд, участок, другое).

---

## 2. Строка пробы

Колонки: чек-бокс, п/п, № скважины, № пробы, интервал, вес,
характеристика, тип, иконки, меню.

### Цвет фона
1. Найдена → зелёный.
2. Ошибка → красный.
3. Отложена → голубой.
4. Холостая → жёлтый.
5. ВК → фиолетовый.

### Характеристика
Отдельный **фильтр-чип** в блоке фильтров. По умолчанию включён.
Снятие — скрывает столбец.

---

## 3. Логика чек-бокса
(см. текущую реализацию)

---

## 4. Вес, ВК, холостые
(см. текущую реализацию)

---

## 5. Массовые действия
(см. текущую реализацию)

---

## 6. Undo / Redo — 20 шагов.

---

## 7. Удаление пробы — с чек-боксом «Пересчитать №».

---

## 8. Типы и статусы
(auger / channel / cobra / duplicate; normal / blank / control)

---

## 9. Настройки наряда
Холостые (FIXED/AVERAGE/MANUAL) + шаг ВК.

---

## 10. Легенда — кнопка «?». Контент скроллится.

---

## 11. Заметки и фото (ЗАКРЫТ)
(см. реализацию в `ReconciliationDialogs.kt`)

---

## 12. Платформенные ограничения
Камера только на реальном устройстве. Photo Picker — Android 13+.

---

## 13. Голосовой помощник (полная спецификация — в `VOICE.md`)

### 13.1. Общее

- Push-to-talk. **Не** wake-word.
- Активация: тап по «Слушаю».
- Завершение: «стоп» / «хватит» / тап / смена вкладки / авто-стоп.
- Авто-стоп: 1/3/5/10 мин (настройка). За минуту — предупреждение.
- Пауза — «пауза» / «продолжить». Невидимый таймер.
- Сегментация по паузам 500/800/1200 мс (настройка).
- Сортировка: два и более номера подряд → справка без отметок.
- Авто-режим отметок: включается, когда ответ единственный.
- Префиксы — из БД + пользовательские произношения в `VoiceSettings`.
- 20 шагов undo.
- TTS только на важное: вес, отмена, ошибка, «повторите».
- Fuzzy — сужен: ненулевые цифры совпадают, длина ±1, Левенштейн ≤1.
- Порядковые `первая … тридцатая` — только в режиме отметок.
- Снятие отметки — отдельные команды «снять первую» и т.д.
- Проба уже отмечена → «Снять / Отложить / Пропустить».
- Другой участок → TTS-сигнал, без отметки.
- Лог → `filesDir/voice_log.txt`, доступ из настроек.
- Онбординг → 5 экранов при первом запуске.
- Статус-бар внизу сверки — реальное время.
- **Молчание в сессии** — ГП не напоминает. Просто ждёт.

### 13.2. Индикатор ответа — модель

**Четыре состояния, три цвета, серый IDLE:**

| Состояние | Иконка | Цвет | Звук |
|---|---|---|---|
| `OK` | 🟢 лампа | зелёный | — |
| `ATTENTION` | 🟡 треугольник ⚠ | жёлтый | `soundAttention` |
| `ERROR` | 🔴 лампа | красный | `soundError` |
| `IDLE` | ⚪ | серый | — |

**Восемь причин:**

| Причина | Состояние | Когда |
|---|---|---|
| `IDLE_WAITING` | ⚪ | Пустой запрос |
| `OK_SINGLE` | 🟢 | Единственный ответ |
| `FOUND_OTHER_ORDER` | 🟡 | Выбран наряд, ответ в другом наряде |
| `FOUND_OTHER_AREA` | 🟡 | Ответ в другом участке |
| `FOUND_MULTIPLE` | 🟡 | Несколько нарядов с этим номером |
| `FOUND_MULTIPLE_AREA` | 🟡 | Несколько участков |
| `NOT_FOUND` | 🔴 | Номера нет в БД |
| `SEARCH_FAILED` | 🔴 | Ошибка парсера/поиска |

**Правила:**
- `FOUND_OTHER_ORDER` действует **только если выбран наряд**. Если выбран
  только участок — это не «другой наряд», это нейтрально.
- `FOUND_OTHER_AREA` действует **только если выбран участок**.
- **Строгий цвет:** три цвета, без оттенков. Все «внимания» — одного
  жёлтого. Причина отличается только **текстом**.

**Строки индикатора:**
1. Значок.
2. Краткий статус: «Найдено» / «Внимание» / «Не найдено» / «Жду».
3. Причина (если есть): «Другой наряд» / «Другой участок» /
   «Несколько нарядов» / «Несколько участков».

### 13.3. Заголовок группы — шаблон
Строка 1: [Участок / Наряд №X] ← обычный текст
Строка 2: [Описание проблемы, если есть] ← жирный
Строка 3: Найдено X/Y · Холостые · ВК · Отложено ← серый

text

- **Фон** = цвет состояния (полупрозрачный, alpha 0.35).
- **Текст** — обычного цвета (`onSurface`), НЕ цветной.
- **Иконка слева** = значок состояния.
- **Полоска слева** (4 dp) = цвет состояния.
- **Статистика** — всегда снизу, серым.
- Строка `X/Y` из шапки **убрана**.

**Приоритет в списке:** 🟢 → 🟡 → 🔴. Внутри — по близости (выбранный
наряд → тот же участок → другой участок).

### 13.4. Звуковая карта — 3 звука

| Имя | Тип | Когда |
|---|---|---|
| `soundOk` | подтверждение | Действие выполнено |
| `soundAttention` | внимание | Нужно решение / посмотреть |
| `soundError` | ошибка | Не получилось |

**`soundOk` — короткий высокий бип (1400 Гц, 80 мс):**

| Ситуация |
|---|
| Проба отмечена |
| Проба снята |
| Вес принят |
| Отмена / повтор |
| Следующая скважина |

**`soundAttention` — два ровных средних бипа (900 Гц, 100+100 мс):**

| Ситуация |
|---|
| Поиск: другой наряд / участок / несколько |
| Проба уже отмечена |
| Нужен вес (холостая / ВК) |
| Запрещённая команда в автопаузе *(только первый раз)* |

**`soundError` — низкий длинный бип (400 Гц, 300 мс):**

| Ситуация |
|---|
| Поиск: не найдено |
| Поиск: ошибка |
| Проба не существует |

**Без звука:** успешный одиночный поиск, IDLE, пауза, старт/конец сессии,
справка.

### 13.5. Конфликты (К1–К6)

**К1. Разные состояния в одной команде.** Худший побеждает:
- Есть ERROR → `soundError`.
- Иначе есть ATTENTION → `soundAttention`.
- Иначе — без звука.

**К2. Внимание и действие одновременно.** Если действие не выполнено
из-за спорной ситуации — `soundAttention`, не `soundOk`. `soundOk`
звучит только при реальном изменении данных.

**К3. Два звука подряд.** Один звук на событие. Второй звук в течение
300 мс после первого — заглушается.

**К4. Звук и TTS одновременно.** Звук — прелюдия. TTS начинается через
200 мс после начала звука.

**К5. Автопауза.** Первая запрещённая команда → `soundAttention` + TTS.
Последующие (пока не сказано «продолжить/стоп») → без звука, только TTS.

**К6. Пустой запрос (IDLE).** Без звука, только TTS «Не расслышал».

### 13.6. Правила сокращений и счётчика

**Именование проб:**
- Существующие пробы → **порядковый** («первая», «вторая»).
- Несуществующие (при ошибке) → **номер** («проба 40»).
- При смене скважины → снова с «первой».

**Подтверждение отметки:**
- Всегда называем **порядковый**.
- **Счётчик** — общий по скважине (включая ручные отметки).
- Если порядковый **== счётчик** → только порядковый: «Вторая.»
- Если порядковый **!= счётчик** → добавляем счётчик:
  «Третья. Отмечено 2 пробы.»

**Снятие отметок:**
- Одиночное: «Первая снята.»
- Все: «Снято 3 отметки.»
- Счётчик всегда произносим.

### 13.7. Фразы ГП — сводка

**Поиск:**

| Ситуация | ГП говорит | Звук | Пауза |
|---|---|---|---|
| ⚪ IDLE | «Не расслышал.» | — | нет |
| 🟢 OK_SINGLE | «Скважина 15 24. Наряд 7. Проб 3, отмечено 0.» + холостые/ВК/отложено (только ненулевые) | — | нет |
| 🟡 OTHER_ORDER | «Другой наряд номер два. Выберите на экране.» | `soundAttention` | да |
| 🟡 OTHER_AREA | «Другой участок — Коптеловский, наряд 27. Выберите на экране.» | `soundAttention` | да |
| 🟡 MULTIPLE | «Несколько нарядов — 1, 2, 7. Выберите на экране.» | `soundAttention` | да |
| 🟡 MULTIPLE_AREA | «Несколько участков — Тестовый и Коптеловский. Выберите на экране.» | `soundAttention` | да |
| 🔴 NOT_FOUND | «Не нашёл.» | `soundError` | нет |
| 🔴 SEARCH_FAILED | «Ошибка поиска. Повторите.» | `soundError` | нет |

**Отметки:**

| Ситуация | ГП говорит | Звук | Пауза |
|---|---|---|---|
| Порядковый == счётчик | «Первая.» / «Вторая.» | `soundOk` | нет |
| Порядковый != счётчик | «Третья. Отмечено 2 пробы.» | `soundOk` | нет |
| Проба уже отмечена | «Вторая уже отмечена. Снять, отложить или пропустить?» | `soundAttention` | да |
| Снятие одиночное | «Первая снята.» | `soundOk` | нет |
| Снятие всех | «Снято 3 отметки.» | `soundOk` | нет |
| Проба не существует | «Проба 40 не существует. Всего 15.» | `soundError` | нет |
| Холостая — нужен вес | «Пятая — холостая. Вес?» | `soundAttention` | да |
| ВК — нужен вес | «Пятая — весовой контроль. Вес?» | `soundAttention` | да |
| Вес принят | «Вес 2.5 килограмма.» | `soundOk` | нет |
| Следующая скважина | «Слушаю следующую скважину.» | `soundOk` | нет |

**Сортировка:**

| Ситуация | ГП говорит | Звук | Пауза |
|---|---|---|---|
| Все 🟢 | «15 24, наряд 7. 15 26, наряд 7.» | — | нет |
| Есть 🟡 | «15 24 — другой наряд. 15 26, наряд 7. Выберите на экране.» | `soundAttention` | да |
| Есть 🔴 | «15 26, наряд 7. 15 99 — не найдено.» | `soundError` | нет |

Участок в сортировке **не называем** — только номер наряда.

**Управление:**

| Команда | ГП говорит | Звук |
|---|---|---|
| «пауза» | «Пауза.» | — |
| «продолжить» | «Продолжаю.» | — |
| «стоп» | *(тишина)* | — |
| Запрещённая команда в автопаузе | «Скажите продолжить или стоп.» | `soundAttention` *(один раз)* |
| «отмена» / «верни» | «Отменил.» | `soundOk` |
| «повтори» / «вперёд» | «Повторил.» | `soundOk` |
| «помощь» | «Открываю справку.» | — |
| «сколько осталось» | «Осталось 5 проб.» | — |
| «показать отложенные» | «Отложенные на экране.» | — |
| «показать найденные» | «Найденные на экране.» | — |

**Молчание в сессии:** ГП не напоминает. Ждёт.

---

## 14. UI «Множественный поиск» (ЗАКРЫТ — 5.11)

### Ввод
- До **5 запросов** через пробел. Лишние пробелы обрезаются.
- Дебаунс 500 мс.
- Префиксы и номера — независимые запросы.

### Режимы поиска
- **Ничего не выбрано** → строгое совпадение.
- **Выбран участок** → строгое совпадение, участок = приоритет.
- **Выбран участок и наряд** → строгое совпадение + префикс по № пробы.

### Отображение групп
- Заголовки `QueryHeaderCard` — по модели §13.2.
- Цвет фона = `AnswerState`: зелёный / жёлтый / красный.

### Формат заголовка запроса
Запрос №N: «номер»
X вариантов | Наряд - Y

text
С причиной (если есть):
Запрос №N: «номер»
X вариантов | Наряд - 1, 2, 7
⚠ Несколько нарядов

text

### Свёртка / разворот
- Все группы свёрнуты по умолчанию.
- Кнопка «Развернуть все» ↔ «Свернуть все».

### Индикатор
- Берётся **худшее** состояние среди запросов.
- Все 🟢 → 🟢. Хоть один 🟡 → 🟡. Хоть один 🔴 → 🔴.

### Поведение
- Голосовой помощник не активируется при мультизапросе.
- Отметки — только вручную.

---

## 15. Открытые вопросы

1. `number_in_well` — в БД или в UI.
2. Настройки холостых — в памяти или в `OrderEntity`.
3. `is_import_error` — добавить ли флаг.
4. Авто-простановка ВК — при импорте.
