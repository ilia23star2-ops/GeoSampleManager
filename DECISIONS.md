# Решения по UI и логике GeoSample Manager

> Фиксирует договорённости. Обновляется по ходу.
> **Читать перед любым изменением UI и логики.**

---

## 0. Решения 24.09.2026 — архитектура ГП

### 0.1. Свод путей поиска

- **UI и голос — один путь.** `QueryNormalizer` → `QueryTokenizer` →
  `DigitGrouper` → `SearchService`.
- **Реализовано в `e4e-bundle`.** `voiceSearch` теперь использует
  `SearchService`, как и `setQuery`.

### 0.2. Pin скважины

- После `FoundOne` — скважина **закрепляется** (`VoiceSession.pin`).
- В `FOUND_PINNED` **любое голое число** → `MarkOrdinal`, **не** `Search`.
- Фраза из числительных склеивается: «тринадцать шестьдесят семь» →
  `MarkOrdinal(1367)`.
- Выходы: «следующая», «следующая X», «отмена», ручной ввод UI.
- Ручной ввод — **приоритет**: сбрасывает голосовой pin.
- **Реализовано в `e4-pin-1`, `e4-pin-3`, `e4-pin-4`.**

### 0.3. Очередь мультизапроса

- Два и более запроса, **все дали единственное попадание** → очередь.
- Первый — `pinned`, остальные в `queue`.
- «Дальше» → `NextInQueue`.
- «Следующая» → выход + сброс.
- Конец очереди → «Очередь пуста. Скажите следующая для нового запроса.»
- **Если хоть один не уникален** — очередь не строится, старое поведение.
- **Реализовано в `e4-pin-2`, `e4-pin-3`.**

### 0.4. Attention-переключение

- `FOUND_OTHER_AREA` / `FOUND_OTHER_ORDER` / `FOUND_MULTIPLE` →
  старое поведение (Message).
- **Не реализовано.** В будущем: «работать» / «выбери» → закрепить.

### 0.5. Маркеры намерения

- «Отметь» → `AWAITING_MARK` (словарь 1..N).
- «Снять» → `AWAITING_CLEAR` (1..N).
- «Вес» → `AWAITING_WEIGHT` (весовые).
- «Найди» → переключение в ПОИСК.
- «Отложить» → `AWAITING_POSTPONE` (1..N).

**Планируется в `e4-markers`.**

### 0.6. Динамические словари Vosk

- Сужение **только в узких состояниях.**
- `LISTENING`, `FOUND_PINNED` — **не сужать.**
- `AWAITING_MARK`, `AWAITING_CHOICE`, `PAUSED`, `AWAITING_NOT_FOUND_CHOICE` —
  сильно сужать.
- Пересоздание `Recognizer` — во время TTS.

**Планируется в `e4-dicts` и `e4d`.**

### 0.7. Мимикрия TTS

- Группы **без запятых**, через пробел.
- Префикс — одним словом: `KPD` → «капэдэ».
- W → «даблю».
- Источник — `customPrefixPronunciations`.

**Реализовано в `e4e-bundle`.**

### 0.8. Приоритеты поиска в выбранном наряде

1. Строгий по всей БД.
2. Префикс скважины (левые цифры).
3. Номер пробы (`sampleNumber`, слева).

**При совпадении 2 и 3 — приоритет скважине.**

**Планируется в `e4-search-context`.**

### 0.9. Поиск при 0 попаданий

- Состояние `AWAITING_NOT_FOUND_CHOICE`.
- ГП: «Не нашёл. Повторить или вернуться к прежней?»

**Не реализовано.**

### 0.10. Конец очереди

- «Очередь пуста. Скажите следующая для нового запроса.» → `LISTENING`.
- **Реализовано в `e4-pin-2`.**

### 0.11. Команды всегда

- Stop, Pause, Undo, Redo, Отмена, Помощь.
- В `PAUSED` — Resume, Stop, Undo, Redo.

### 0.12. Отменённые решения

- `e4c` — числительные в `QueryTokenizer`. Откачен.
- `e4f` — подтверждение веса > 30. Отменён.
- `e4h` — таблица команд. Отложен.

### 0.13. Кулдаун TTS

- 250 мс (`e4-fix-2`).
- Эхо TTS отсекается `suppressUntil`.

### 0.14. «Четвертью» в грамматике

- Убрано (`e4-fix-1`).

### 0.15. Fallback в `voiceMarkOrdinal`

- Vosk путает «четвёртая» ↔ «четырнадцатая» (4 ↔ 14).
- Fallback: если проба `ordinal` не найдена — пробуем `ordinal ± 10`.
- **Открыто (`e4-pin-5`):** расширить fallback на круглые сотни
  (400→4, 500→5, …, 900→9) и десятки (40→4, 50→5, …, 90→9).

### 0.16. Вес «X сотни»

- «Два семьсот» → 2.7. «Три пятьсот» → 3.5.
- Формат кг + граммы без слов.
- **Реализовано в `e4-pin-3`.**

### 0.17. «Следующая X» — одной командой

- «Следующая 1525» → `Find("1525")`.
- В любом состоянии: `LISTENING`, `FOUND_PINNED`.
- **Реализовано в `e4-pin-3`.**

### 0.18. Очередь — только если все уникальны

- Мультизапрос → если **все** запросы дали единственное попадание —
  очередь. Иначе — старое поведение (описательный Message).
- **Реализовано в `e4-pin-2`.**

### 0.19. Префикс «Найдено N скважин»

- `VoiceExecResult.FoundOne.queueSize > 1` → VoiceDialog озвучивает
  «Найдено N скважин. Слушайте первую.»
- **Реализовано в `e4-pin-3`.**

### 0.20. Очередь — только мультизапрос

- Одиночный поиск **не** строит очередь.
- «Дальше» в одиночном pin → «Очередь пуста.»
- **Реализовано в `e4-pin-2`.**

---

## 1. Экран «Сверка и поиск»

### Общее
- Одна вкладка. Два неявных режима: Поиск и Сверка.

### Селекторы
- Участок, Наряд — опциональны.
- Наряд нельзя выбрать без участка.
- Шестерёнка — настройки наряда.

### Поле поиска
- Один или несколько номеров через пробел.
- Крестик — очищает.
- Ищет по всей БД.
- Дебаунс 500 мс.
- Лишние пробелы обрезаются.

### Режимы поиска
1. Ничего не выбрано → строгое совпадение.
2. Выбран участок → строгое; участок — приоритет.
3. Выбраны участок И наряд → строгое или префикс по № пробы.

**Уточнение 24.09.2026:** при выбранном наряде сначала строгий по всей
БД, если не нашлось — префикс скважины, потом — номер пробы. При
совпадении — приоритет скважине.

### Логика поиска (пустой запрос)
1. Пусто + ничего не выбрано → подсказка.
2. Пусто + участок без наряда → «Выберите наряд».
3. Пусто + выбран наряд → все пробы наряда.

---

## 2. Строка пробы

Колонки: чек-бокс, п/п, № скважины, № пробы, интервал, вес,
характеристика, тип, иконки, меню.

### Цвет фона
1. Найдена → зелёный.
2. Ошибка → красный.
3. Отложена → голубой.
4. Холостая → жёлтый.
5. ВК → фиолетовый.

### Характеристика
Фильтр-чип.

---

## 3–5. Логика чек-бокса, вес, массовые действия
(см. текущую реализацию)

---

## 6. Undo / Redo — 20 шагов.
**Глобальные**, не локальные в pin.

---

## 7. Удаление пробы — с чек-боксом «Пересчитать №».

---

## 8. Типы и статусы
(auger / channel / cobra / duplicate; normal / blank / control)

---

## 9. Настройки наряда
Холостые (FIXED/AVERAGE/MANUAL) + шаг ВК.

---

## 10. Легенда — кнопка «?».

---

## 11. Заметки и фото (ЗАКРЫТ)

---

## 12. Платформенные ограничения
Камера только на реальном устройстве. Photo Picker — Android 13+.

---

## 13. Голосовой помощник (полная спецификация — в `VOICE.md`)

### 13.1. Общее

- Push-to-talk.
- Активация: тап по «Слушаю».
- Завершение: «стоп» / «хватит» / тап / смена вкладки / авто-стоп.
- Авто-стоп: 1/3/5/10 мин.
- Пауза — «пауза» / «продолжить».
- **Pin скважины.**
- **Очередь мультизапроса.**
- **Маркеры намерения** — планируется.
- **Динамические словари Vosk** — планируется.
- **Мимикрия TTS** — реализовано.

### 13.2. Индикатор ответа

Четыре состояния: `OK` (🟢), `ATTENTION` (🟡), `ERROR` (🔴), `IDLE` (⚪).

### 13.3. Заголовок группы — шаблон
Строка 1: [Участок / Наряд №X]
Строка 2: [Описание проблемы, если есть]
Строка 3: Найдено X/Y · Холостые · ВК · Отложено

### 13.4. Звуковая карта

| Имя | Тип |
|---|---|
| `soundOk` | 1400 Гц, 80 мс |
| `soundAttention` | 900 Гц × 2, 100+100 мс |
| `soundError` | 400 Гц, 300 мс |

### 13.5–13.6.
(без изменений)

### 13.7. Фразы ГП

**Поиск:**

| Ситуация | ГП говорит | Звук |
|---|---|---|
| ⚪ IDLE | «Не расслышал.» | — |
| 🟢 OK_SINGLE | «Скважина … Наряд … Проб N, отмечено M.» | — |
| 🟡 OTHER_ORDER | «Другой наряд номер … Выберите на экране.» | `soundAttention` |
| 🟡 OTHER_AREA | «Другой участок — … Выберите на экране.» | `soundAttention` |
| 🟡 MULTIPLE | «Несколько нарядов — … Выберите на экране.» | `soundAttention` |
| 🔴 NOT_FOUND | «Не нашёл. Повторить или вернуться?» | `soundError` |

**Очередь:**
| Ситуация | ГП говорит |
|---|---|
| Начало очереди | «Найдено N скважин. Слушайте первую.» + описание |
| «Дальше» | Описание следующей |
| Конец | «Очередь пуста. Скажите следующая для нового запроса.» |

**Отметки, снятия, вес, сортировка** — в `VOICE.md`.

**Управление:** см. `VOICE.md`.

---

## 14. UI «Множественный поиск»

### Ввод
- До **5 запросов** через пробел.
- Дебаунс 500 мс.

### Режимы поиска
- Ничего не выбрано → строгое.
- Выбран участок → строгое.
- Выбран участок и наряд → строгое + префикс.

### Отображение групп
- `QueryHeaderCard` — по модели §13.2.

### Свёртка / разворот
- Все группы свёрнуты по умолчанию.

### Поведение
- **Голосовой помощник в мультизапросе** — очередь (если все уникальны).

---

## 15. Открытые вопросы

1. `number_in_well` — в БД или в UI.
2. Настройки холостых — в памяти или в `OrderEntity`.
3. `is_import_error` — добавить ли флаг.
4. Авто-простановка ВК — при импорте.
5. **Confidence threshold Vosk** — доступен ли через `vosk-android`.
6. **D — команды выбора наряда/участка голосом** — после `e4d`.
7. **Визуальная пометка pin в UI** — панель ГП.
8. **`Postpone` голосом** — не реализовано.