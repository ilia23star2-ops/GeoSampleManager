# Решения по UI и логике GeoSample Manager

> Фиксирует договорённости. Обновляется по ходу.
> **Читать перед любым изменением UI и логики.**

---

## 0. Решения 24.09.2026 — архитектура ГП

> Приняты в сессии аудита и планирования `5.8.11-e4`.

### 0.1. Свод путей поиска

- **UI и голос — один путь.** `QueryNormalizer` → `QueryTokenizer` →
  `DigitGrouper` → `SearchService`.
- Сейчас: два пути (`setQuery` и `voiceSearch`).
- Будет: один. Заход `e4e`.

### 0.2. Pin скважины

- После находки скважины — она **закрепляется**.
- Голое число 1..N → отметка пробы, **не поиск**.
- Выход из pin: «следующая», «отмена», «найди X», ручной ввод UI.
- Ручной ввод — **приоритет**: сбрасывает голосовой pin.
- Визуально: пометка в панели ГП.

### 0.3. Очередь мультизапроса

- `FoundMany` → попадания в очередь.
- «Следующая» / «пропустить» → листает.
- Конец очереди → «Все запросы обработаны. Жду следующий» + `LISTENING`.

### 0.4. Attention-переключение

- `FOUND_OTHER_AREA` / `FOUND_OTHER_ORDER` / `FOUND_MULTIPLE` →
  пользователь может:
  - «работать» / «выбери» → закрепить текущую, работать как обычно;
  - «следующая» / «пропустить» → к следующей в очереди;
  - «отмена» → выход.

### 0.5. Маркеры намерения

- «Отметь» → `AWAITING_MARK` (словарь 1..N).
- «Снять» → `AWAITING_CLEAR` (1..N).
- «Вес» → `AWAITING_WEIGHT` (весовые).
- «Найди» → переключение в ПОИСК.
- «Отложить» → `AWAITING_POSTPONE` (1..N).

**Эффект:** Vosk не может ошибиться в числе, которого нет в словаре.

### 0.6. Динамические словари Vosk

- Сужение **только в узких состояниях.**
- `LISTENING`, `FOUND_PINNED` — **полный словарь, не сужать.**
- `AWAITING_MARK`, `AWAITING_CHOICE`, `PAUSED`, `AWAITING_NOT_FOUND_CHOICE` —
  сильно сужать.
- Пересоздание `Recognizer` — **во время TTS** (`setPause(true)`).

### 0.7. Мимикрия TTS

- Группы без запятых, через пробел.
- Префикс — одним словом: `KPD` → «капэдэ».
- W → «даблю».
- Источник — `customPrefixPronunciations`. Если нет — канонический маппинг.

### 0.8. Приоритеты поиска в выбранном наряде

1. **Строгий по всей БД.** Есть результат в другом наряде → «Внимание».
2. **Префикс скважины** (левые цифры после префикса участка).
3. **Номер пробы** (`sampleNumber`, с левого края).

**При совпадении 2 и 3 — приоритет скважине.**

**`numberInWell`** не ограничен 1..30. Вычисляется как `sampleNumber − wellNumber`.

### 0.9. Поиск при 0 попаданий

- Состояние `AWAITING_NOT_FOUND_CHOICE`.
- ГП: «Не нашёл. Повторить или вернуться к прежней?»
- Команды: «повторить» / «вернуться» / «отмена».

### 0.10. Конец очереди

- «Все запросы обработаны. Жду следующий» → `LISTENING`.

### 0.11. Команды всегда

- Stop, Pause, Undo, Redo, Отмена, Помощь.
- В `PAUSED` — только Resume, Stop, Undo, Redo.

### 0.12. Отменённые решения

- **`e4c`** (числительные в `QueryTokenizer`) — отменён. Менял UI-путь.
- **`e4f`** (подтверждение веса > 30) — отменён. `e4d` решает лучше.
- **`e4h`** (таблица команд `VoiceCommands.kt`) — отложен.

### 0.13. Кулдаун TTS

- Было 800 мс. Стало 250 мс (`e4-fix-2`).
- Эхо TTS отсекается `suppressUntil`.

### 0.14. «Четвертью» в грамматике

- Убрано (`e4-fix-1`).
- Vosk путал «четвёртая» ↔ «четвертью».
- Форма веса «с двумя четвертями» (2.25) больше не распознаётся.

---

## 1. Экран «Сверка и поиск»

### Общее
- Одна вкладка. Два неявных режима: **Поиск** и **Сверка**.

### Селекторы
- **Участок**, **Наряд** — опциональны.
- **Наряд нельзя выбрать без участка.**
- **Шестерёнка** — настройки наряда.

### Поле поиска
- Принимает **один или несколько** номеров через пробел.
- Крестик — очищает.
- Ищет по всей БД.
- Дебаунс 500 мс.
- Лишние пробелы обрезаются.

### Режимы поиска
1. **Ничего не выбрано** → строгое совпадение.
2. **Выбран только участок** → строгое совпадение; участок — приоритет.
3. **Выбраны участок И наряд** → строгое совпадение **или** префикс по
   номеру пробы (строка работает как фильтр).

**Уточнение (24.09.2026):** при выбранном наряде сначала строгий по всей
БД, если не нашлось — префикс по скважине (левые цифры), потом — номер
пробы. При совпадении — приоритет скважине.

### Логика поиска (пустой запрос)
1. Пусто + ничего не выбрано → подсказка.
2. Пусто + участок без наряда → подсказка «Выберите наряд».
3. Пусто + выбран наряд → все пробы наряда.

### Индикатор ответа
См. §13.2.

### Приоритеты групп
См. §13.3.

---

## 2. Строка пробы

Колонки: чек-бокс, п/п, № скважины, № пробы, интервал, вес,
характеристика, тип, иконки, меню.

### Цвет фона
1. Найдена → зелёный.
2. Ошибка → красный.
3. Отложена → голубой.
4. Холостая → жёлтый.
5. ВК → фиолетовый.

### Характеристика
Фильтр-чип. По умолчанию включён.

---

## 3. Логика чек-бокса
(см. текущую реализацию)

---

## 4. Вес, ВК, холостые
(см. текущую реализацию)

---

## 5. Массовые действия
(см. текущую реализацию)

---

## 6. Undo / Redo — 20 шагов.
**Глобальные**, не локальные в pin (решение 24.09.2026).

---

## 7. Удаление пробы — с чек-боксом «Пересчитать №».

---

## 8. Типы и статусы
(auger / channel / cobra / duplicate; normal / blank / control)

---

## 9. Настройки наряда
Холостые (FIXED/AVERAGE/MANUAL) + шаг ВК.

---

## 10. Легенда — кнопка «?». Контент скроллится.

---

## 11. Заметки и фото (ЗАКРЫТ)

---

## 12. Платформенные ограничения
Камера только на реальном устройстве. Photo Picker — Android 13+.

---

## 13. Голосовой помощник (полная спецификация — в `VOICE.md`)

### 13.1. Общее

- Push-to-talk. **Не** wake-word.
- Активация: тап по «Слушаю».
- Завершение: «стоп» / «хатит» / тап / смена вкладки / авто-стоп.
- Авто-стоп: 1/3/5/10 мин. За минуту — предупреждение.
- Пауза — «пауза» / «продолжить».
- Сегментация по паузам 500/800/1200 мс.
- **Pin скважины** — после находки, работа идёт с ней.
- **Очередь мультизапроса** — «следующая» листает.
- **Маркеры намерения** — «отметь», «вес», «найди».
- **Динамические словари Vosk** — для узких состояний.
- **Мимикрия TTS** — озвучка по группам.
- Порядковые `первая … тридцатая` — только в режиме отметок.
- Лог → `filesDir/voice_log.txt`.

### 13.2. Индикатор ответа — модель

**Четыре состояния, три цвета, серый IDLE:**

| Состояние | Иконка | Цвет | Звук |
|---|---|---|---|
| `OK` | 🟢 лампа | зелёный | — |
| `ATTENTION` | 🟡 треугольник ⚠ | жёлтый | `soundAttention` |
| `ERROR` | 🔴 лампа | красный | `soundError` |
| `IDLE` | ⚪ | серый | — |

**Восемь причин:**

| Причина | Состояние | Когда |
|---|---|---|
| `IDLE_WAITING` | ⚪ | Пустой запрос |
| `OK_SINGLE` | 🟢 | Единственный ответ |
| `FOUND_OTHER_ORDER` | 🟡 | Ответ в другом наряде |
| `FOUND_OTHER_AREA` | 🟡 | Ответ в другом участке |
| `FOUND_MULTIPLE` | 🟡 | Несколько нарядов |
| `FOUND_MULTIPLE_AREA` | 🟡 | Несколько участков |
| `NOT_FOUND` | 🔴 | Номера нет в БД |
| `SEARCH_FAILED` | 🔴 | Ошибка поиска |

### 13.3. Заголовок группы — шаблон
Строка 1: [Участок / Наряд №X]
Строка 2: [Описание проблемы, если есть] — жирный
Строка 3: Найдено X/Y · Холостые · ВК · Отложено — серый

### 13.4. Звуковая карта — 3 звука

| Имя | Тип | Когда |
|---|---|---|
| `soundOk` | 1400 Гц, 80 мс | Действие выполнено |
| `soundAttention` | 900 Гц × 2, 100+100 мс | Внимание |
| `soundError` | 400 Гц, 300 мс | Ошибка |

### 13.5. Конфликты (К1–К6)
(без изменений)

### 13.6. Правила сокращений и счётчика
(без изменений)

### 13.7. Фразы ГП

**Поиск:**

| Ситуация | ГП говорит | Звук | Пауза |
|---|---|---|---|
| ⚪ IDLE | «Не расслышал.» | — | нет |
| 🟢 OK_SINGLE | «Скважина … Наряд … Проб N, отмечено M.» | — | нет |
| 🟡 OTHER_ORDER | «Другой наряд номер … Выберите на экране.» | `soundAttention` | да |
| 🟡 OTHER_AREA | «Другой участок — … Выберите на экране.» | `soundAttention` | да |
| 🟡 MULTIPLE | «Несколько нарядов — … Выберите на экране.» | `soundAttention` | да |
| 🔴 NOT_FOUND | «Не нашёл. Повторить или вернуться?» | `soundError` | да |
| 🔴 SEARCH_FAILED | «Ошибка поиска. Повторите.» | `soundError` | нет |

**Отметки, снятия, вес, сортировка** — см. `VOICE.md`.

**Управление:**

| Команда | ГП говорит | Звук |
|---|---|---|
| «пауза» | «Пауза.» | — |
| «продолжить» | «Продолжаю.» | — |
| «стоп» | *(тишина)* | — |
| «отмена» | «Отменил.» | `soundOk` |
| «повтори» | «Повторил.» | `soundOk` |
| «найди X» | «Поиск: X.» + результат | — |
| «помощь» | Команды текущего состояния | — |

**Молчание в сессии:** ГП не напоминает. Ждёт.

**При 0 попаданий:** «Не нашёл. Повторить или вернуться к прежней?»

**При завершении очереди:** «Все запросы обработаны. Жду следующий.»

---

## 14. UI «Множественный поиск»

### Ввод
- До **5 запросов** через пробел.
- Дебаунс 500 мс.
- Префиксы и номера — независимые запросы.

### Режимы поиска
- **Ничего не выбрано** → строгое.
- **Выбран участок** → строгое; участок — приоритет.
- **Выбран участок и наряд** → строгое + префикс по № пробы.

### Отображение групп
- `QueryHeaderCard` — по модели §13.2.
- Цвет фона = `AnswerState`.

### Свёртка / разворот
- Все группы свёрнуты по умолчанию.

### Индикатор
- Худшее состояние среди запросов.

### Поведение
- **Голосовой помощник в мультизапросе** — работает через очередь
  (решение 24.09.2026).
- Отметки — возможны после выбора конкретного запроса.

---

## 15. Открытые вопросы

1. `number_in_well` — в БД или в UI.
2. Настройки холостых — в памяти или в `OrderEntity`.
3. `is_import_error` — добавить ли флаг.
4. Авто-простановка ВК — при импорте.
5. **Confidence threshold Vosk** — доступен ли через `vosk-android`.
6. **D — команды выбора наряда/участка голосом** — после `e4d`.
