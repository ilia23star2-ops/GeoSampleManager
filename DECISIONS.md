# Решения по UI и логике GeoSample Manager

> Фиксирует договорённости по интерфейсу, логике и правилам поведения.
> Обновляется по ходу.
>
> **Читать перед любым изменением UI и логики сверки.**

---

## 1. Экран «Сверка и поиск»

### Общее
- Одна вкладка. Два **неявных** режима: **Поиск** (наряд не выбран) и
  **Сверка** (наряд выбран). Переключателя нет.

### Селекторы
- **Участок** (опционально) — список из БД.
- **Наряд** (опционально) — зависит от участка. Пустые наряды не показываются.
- **Шестерёнка** рядом с нарядом — настройки наряда. Активна, только если
  наряд выбран.

### Поле поиска
- Принимает номер скважины или пробы, с префиксом или без.
- Префикс = участок. Если указан — сужает поиск.
- **Крестик** очищает поле, **клавиатуру не скрывает.**
- **«Поиск»** на клавиатуре — скрывает клавиатуру.
- Ищет **по всей БД**, не только по выбранному наряду.

### Логика поиска
1. Пусто + ничего не выбрано → «Введите номер скважины или пробы».
2. Пусто + участок без наряда → «Выберите наряд или введите номер».
3. Пусто + выбран наряд → все пробы наряда.
4. Запрос → поиск по всей БД:
   - точное совпадение с номером скважины → все пробы скважины;
   - совпадение с началом номера пробы → такие пробы.
5. Совпадение номера пробы с номером скважины или короче — **ошибка импорта**
   (иконка + фон).

### Фонарик
- Слева от поля.
- **Горит (жёлтый)** — ответ единственный.
- **Не горит** — совпадений несколько.
- Рядом — название наряда или «Несколько».

### Приоритеты результатов
1. Выбранный наряд — сверху, обычный вид.
2. Тот же участок, другой наряд — полоска + подпись.
3. Другой участок — красная полоска + подпись.

---

## 2. Строка пробы

Колонки слева направо:
1. Чек-бокс «найдена».
2. п/п.
3. № скважины / выработки (динамический заголовок).
4. № пробы + «№ в скв.: N».
5. Интервал (`от–до`) + «интервал, м».
6. Вес (основной + ВК в скобках).
7. Характеристика (2 строки, кликабельна).
8. Тип.
9. Иконки: ВК, отложена, заметка, **фото**, ошибка.
10. 3 точки → меню.

### Цвет фона — приоритет
1. Найдена → зелёный.
2. Ошибка → красный.
3. Отложена → голубой.
4. Холостая → жёлтый.
5. ВК → фиолетовый.
6. Не найдена → без фона.

**Все признаки дублируются иконками справа.**

---

## 3. Логика чек-бокса

| Ситуация | Что происходит |
|---|---|
| Уже найдена | Диалог: снять / отложить / заметка / редактировать |
| Ошибка импорта | Диалог: продолжить / редактировать / отмена |
| Отложена | Диалог: отметить / заметка / отмена |
| ВК без веса | Диалог ввода веса ВК |
| Холостая | Зависит от настроек (FIXED/AVERAGE/MANUAL) |
| Обычная | Отметить как найденную |

---

## 4. Вес, ВК, холостые

### Основной вес
- `weight: Double?`.
- Вводится вручную (тап по ячейке) или автоматически.
- Задаётся при импорте.

### Весовой контроль
- Флаг `weight_control`, вес `control_weight`.
- ВК **не может быть холостой**.
- Постановка ВК на найденную — **сбрасывает `found` и `control_weight`**.
- Тап по ячейке веса ВК → диалог.

### Холостая
- `status = "blank"`.
- Не может быть ВК и отложенной.
- Может иметь вес.
- Если нет веса при отметке → диалог.

### Настройки холостых
Хранятся **в памяти** `ReconciliationState` (см. ТД-5).
Режимы: **FIXED**, **AVERAGE**, **MANUAL**.
При сохранении FIXED/AVERAGE вес **проставляется СРАЗУ**, но `found` **не**
ставится. Снекбар: «Вес проставлен N пробам — отметьте их вручную».
Уже введённые веса **не перезаписываются**.
Кнопка «Сбросить вес холостых» — сбрасывает вес и `found` у всех холостых.

---

## 5. Массовые действия

### «Выделить все»
1. Собираем пробы, требующие решения (ВК без веса, отложенные).
2. Если таких нет — отмечаем всё сразу.
3. Если есть — диалог решений.
4. После «Применить» — отмечаем.

**Пропускаются:** ошибки, холостые без веса при MANUAL, ВК без веса.

### «Сбросить все»
С подтверждением.

---

## 6. Undo / Redo

- До **20** шагов.
- Кнопки «Отмена (N)» / «Вперёд (N)» в верхней панели.
- Статус-бар между кнопками описывает верхнее undo-действие.
- Новое действие **очищает** стек redo.
- Удаление пробы восстанавливается полностью (позиция + `sample_number`
  + `numberInWell` всех проб скважины).

---

## 7. Удаление пробы

Через 3 точки → «Удалить». Диалог с чек-боксом **«Пересчитать № проб
в скважине»** (по умолчанию **выкл**).

- **Вкл:** `sample_number` и `numberInWell` всех проб скважины пересчитываются
  подряд. Длина суффикса сохраняется.
- **Выкл:** только `numberInWell`.

Вся операция — в одной транзакции.

---

## 8. Типы проб

| Код | UI | Примечание |
|---|---|---|
| `auger` | Шнековая | По умолчанию |
| `channel` | Бороздовая | Заголовок → «Выработка» |
| `cobra` | Кобра | |
| `duplicate` | Дубликат | |

### Статусы
| Код | UI |
|---|---|
| `normal` | Обычная |
| `blank` | Холостая |
| `control` | Весовой контроль |

---

## 9. Настройки наряда

Диалог из шестерёнки. Две секции.

### Холостые
- FIXED / AVERAGE / MANUAL.
- Поле «Единый вес» (для FIXED).
- Пояснение: вес применяется только к холостым без веса, не отмечает.
- Кнопка «Сбросить вес холостых в наряде» (красная, с подтверждением).

### Весовой контроль
- Поле «Каждая N-я проба». По умолчанию 5.
- Холостые пропускаются.
- Применяется при следующем импорте (TODO, ТД-7).

### «Сбросить к глобальным»
Убирает переопределение.

---

## 10. Легенда

Кнопка «?» в верхней панели. Что содержит: фонарик, настройки наряда,
undo/redo, цвета фонов, значки.

---

## 11. Заметки и фото (этап 5.5, решения приняты)

### Общие принципы
1. **Несколько фото на пробу.**
2. **Заметка и фото независимы:** текст без фото, фото без текста, оба, ничего.
3. Файлы — **внутреннее хранилище**: `filesDir/sample_photos/`.
   Никто извне не подсунет файл.
4. **Сжатие:** до 1024 px по длинной стороне, JPEG 80%.
   Ориентир: 100–300 КБ на файл.
5. **Съёмка:** `ActivityResultContracts.TakePicture()` + `FileProvider`.
   Проще и надёжнее, чем CameraX. Пользователь уходит в системную камеру.
6. **Галерея:** `PickVisualMedia()` — современный Photo Picker (Android 13+).
   Не требует разрешений.
7. **UI:** иконки `EditNote` / `PhotoCamera` в строке + пункты в меню 3 точки.
8. **Манифест:** явные разрешения не нужны, но добавляем `CAMERA`
   (на всякий случай) и `FileProvider`.
9. **Один диалог** на заметку и фото: сверху текст (редактируемый),
   ниже превью фото, кнопки «Сделать фото», «Из галереи», «Удалить фото»,
   «Сохранить».
10. **Флаги `has_note` / `has_photo`** в `SampleEntity` синхронизируются
    при сохранении/удалении. Если у пробы есть хоть одна заметка/фото —
    флаг `true`.

### Что делать при удалении
При удалении пробы/наряда файлы фото **надо чистить вручную** — FK CASCADE
удалит только строки БД. См. `DATABASE.md` и `NEXT_STEPS.md` → открытый
вопрос 7.
