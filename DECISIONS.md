# Решения по UI и логике GeoSample Manager

> Фиксирует договорённости по интерфейсу, логике и правилам поведения.
> Обновляется по ходу.
>
> **Читать перед любым изменением UI и логики сверки.**

---

## 1. Экран «Сверка и поиск»

### Общее
- Одна вкладка. Два **неявных** режима: **Поиск** (наряд не выбран) и
  **Сверка** (наряд выбран). Переключателя нет.

### Селекторы
- **Участок** (опционально) — список из БД.
- **Наряд** (опционально) — зависит от участка. Пустые наряды не показываются.
- **Шестерёнка** рядом с нарядом — настройки наряда. Активна, только если
  наряд выбран.

### Поле поиска
- Принимает номер скважины или пробы, с префиксом или без.
- **Крестик** очищает поле, **клавиатуру не скрывает.**
- **«Поиск»** на клавиатуре — скрывает клавиатуру.
- Ищет **по всей БД**.

### Логика поиска
1. Пусто + ничего не выбрано → подсказка.
2. Пусто + участок без наряда → «Выберите наряд или введите номер».
3. Пусто + выбран наряд → все пробы наряда.
4. Запрос → поиск по всей БД.
5. Совпадение номера пробы с номером скважины или короче — **ошибка
   импорта** (иконка + фон).

### Фонарик
- Слева от поля.
- **Горит (жёлтый)** — ответ единственный.
- **Не горит** — совпадений несколько.

### Приоритеты результатов
1. Выбранный наряд — сверху.
2. Тот же участок, другой наряд — полоска.
3. Другой участок — красная полоска.

---

## 2. Строка пробы

Колонки слева направо:
1. Чек-бокс «найдена».
2. п/п.
3. № скважины / выработки.
4. № пробы + «№ в скв.: N».
5. Интервал (`от–до`).
6. Вес (основной + ВК в скобках).
7. Характеристика (2 строки).
8. Тип.
9. Иконки: ВК, отложена, **заметка**, **фото**, ошибка.
10. 3 точки → меню.

### Цвет фона — приоритет
1. Найдена → зелёный.
2. Ошибка → красный.
3. Отложена → голубой.
4. Холостая → жёлтый.
5. ВК → фиолетовый.
6. Не найдена → без фона.

---

## 3. Логика чек-бокса

| Ситуация | Что происходит |
|---|---|
| Уже найдена | Диалог: снять / отложить / заметка / редактировать |
| Ошибка импорта | Диалог: продолжить / редактировать / отмена |
| Отложена | Диалог: отметить / заметка / отмена |
| ВК без веса | Диалог ввода веса ВК |
| Холостая | FIXED/AVERAGE/MANUAL |
| Обычная | Отметить как найденную |

---

## 4. Вес, ВК, холостые

### Основной вес
- `weight: Double?`.
- Вводится вручную или автоматически.

### Весовой контроль
- Флаг `weight_control`, вес `control_weight`.
- ВК **не может быть холостой**.
- Постановка ВК на найденную — **сбрасывает `found` и `control_weight`**.

### Холостая
- `status = "blank"`.
- Не может быть ВК и отложенной.
- Может иметь вес.

### Настройки холостых
Хранятся **в памяти** `ReconciliationState`.
Режимы: **FIXED**, **AVERAGE**, **MANUAL**.
При сохранении вес **проставляется СРАЗУ**, но `found` **не** ставится.
Уже введённые веса **не перезаписываются**.

---

## 5. Массовые действия

### «Выделить все»
1. Собираем пробы, требующие решения.
2. Если таких нет — отмечаем всё сразу.
3. Если есть — диалог решений.
4. После «Применить» — отмечаем.

**Пропускаются:** ошибки, холостые без веса при MANUAL, ВК без веса.

### «Сбросить все»
С подтверждением.

---

## 6. Undo / Redo

- До **20** шагов.
- Кнопки «Отмена (N)» / «Вперёд (N)».
- Статус-бар описывает верхнее undo-действие.
- Новое действие **очищает** стек redo.
- Удаление восстанавливается полностью.

---

## 7. Удаление пробы

Через 3 точки → «Удалить». Чек-бокс **«Пересчитать № проб в скважине»**
(по умолчанию **выкл**).

- **Вкл:** `sample_number` и `numberInWell` пересчитываются, длина суффикса
  сохраняется.
- **Выкл:** только `numberInWell`.

---

## 8. Типы и статусы

### Типы
| Код | UI |
|---|---|
| `auger` | Шнековая |
| `channel` | Бороздовая |
| `cobra` | Кобра |
| `duplicate` | Дубликат |

### Статусы
| Код | UI |
|---|---|
| `normal` | Обычная |
| `blank` | Холостая |
| `control` | Весовой контроль |

---

## 9. Настройки наряда

Диалог из шестерёнки. Две секции.

### Холостые
- FIXED / AVERAGE / MANUAL.
- Кнопка «Сбросить вес холостых в наряде».

### Весовой контроль
- Поле «Каждая N-я проба». По умолчанию 5.
- Применяется при следующем импорте (TODO, ТД-7).

### «Сбросить к глобальным»

---

## 10. Легенда

Кнопка «?» в верхней панели.

---

## 11. Заметки и фото (этап 5.5 — ЗАКРЫТ)

### Общие принципы (реализовано)
1. **Несколько фото на пробу** — таблица `sample_images`.
2. **Заметка и фото независимы** — текст без фото, фото без текста, оба,
   ни одного.
3. Файлы — **внутреннее хранилище**: `filesDir/sample_photos/`.
4. **Сжатие:** до 1024 px по длинной стороне, JPEG 80%.
5. **Съёмка:** `ActivityResultContracts.TakePicture()` + `FileProvider`.
6. **Галерея:** `PickVisualMedia()`.
7. **UI:** иконки `EditNote` / `PhotoCamera` в строке + пункт меню
   «Заметка и фото».
8. **Манифест:** `CAMERA` + `FileProvider` с authority
   `${applicationId}.fileprovider`.
9. **Один диалог:** сверху текст, ниже превью фото, кнопки
   «Сделать фото», «Из галереи», «Сохранить», «Закрыть».
10. **Флаги `has_note` / `has_photo`** в `SampleEntity` синхронизируются
    в `DatabaseRepository`.

### Детали реализации

**Диалог `NotePhotoDialog`:**
- Текст заметки — по кнопке «Сохранить».
- Фото добавляются/удаляются **сразу**, не по «Сохранить».
- **Защита от закрытия:** если текст изменён и не сохранён, при попытке
  закрыть (тап мимо / «Закрыть») появляется подтверждение.
- Миниатюры декодируются в фоне (`Dispatchers.IO`), пока идёт загрузка —
  крутящийся индикатор. UI не виснет.
- Крестик на миниатюре — удаление фото с подтверждением.

**Производительность:**
- Все операции с заметкой/фото — в `Dispatchers.IO`:
    - `loadNoteWithPhotos`
    - `saveNoteText`
    - `addPhoto`, `addPhotoFromFile`
    - `deletePhoto`
- Обновление UI-состояния (флаги) — через `withContext(Dispatchers.Main)`.

**Файлы фото при удалении:**
- При `DELETE FROM samples` FK CASCADE снесёт строки `sample_images`,
  но файлы на диске — нет.
- Поэтому `clearOrder` и `deleteSampleWithRenumber` **собирают пути ДО**
  удаления строк и потом `PhotoStorage.deleteAll`.
- `deletePhoto` удаляет файл сразу.

---

## 12. Платформенные ограничения

- **Эмулятор:** камера обычно недоступна. Тестировать фото только на
  реальном устройстве.
- **Photo Picker** (`PickVisualMedia`) требует Android 13+ или
  backport-библиотеку. На Android 6–12 используется fallback системной
  галереи.
- **FileProvider authority** должен совпадать: в манифесте
  `${applicationId}.fileprovider`, в коде — `context.packageName + ".fileprovider"`.